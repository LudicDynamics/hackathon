# 五世界日语化迁移

> 2026-09-12 用户确认五个样板世界迁日语，先迁初雪为 `first-snow-jp`。2026-09-13 补充：日语世界的提示词、内容文件和目录也用日语；英语世界全部对应使用英文。只复用图形，内部设计文档保留中文。本轮同步规范，尚未执行路径迁移。

## 1. 当前批次

| 世界 | 当前状态 |
|---|---|
| 初雪 | `templates/first-snow-jp` 文本首轮已迁；内容路径仍英文，尚不符合新增的全语言要求；九张图可复用 |
| 雾坞镇 | 待迁；保留零级导入、地点主视图与雾外生长 |
| 福尔摩斯 | 待迁；保留 Holmes 身份、Watson 来信与地点导航 |
| 时间线 | 待迁；保留零级导入 → 时间 → 对应时间下的地点 |
| 魔法学院 | 待迁；先沿现有体验稿，不追加未讨论剧情 |

“待迁”不代表用 UI 切换就已经变成日文世界。旧世界和玩家存档不自动覆盖；每个新日文世界验收后再替换对应模板入口。

## 2. 初雪重写范围

- 世界名、描述、玩家身份、人物显示名；
- 根 README、独立 `language.md`、玩家文档；
- 所有场景 README、Chalk、信件、物件、选项；
- 七海与雪村澄各自的身份、性格、空白记忆、preset 名称与描述；
- 日语世界连续性 skill 和日文资产说明。

协议键、工具名及引擎保留名不翻译；**普通内容路径需要翻译**。角色目录、可配置文件名若变更，连同角色引用与 preset 文件槽一起迁，不只改显示名。复用九张 WebP 的图形字节，日语世界内资源文件名与引用按语言整理；不复制旧会话、数据库或生成结果。图中文字仍是图片复用边界，不能声称已重绘。

## 3. 主观流程与目录

玩家先在零级放送室听到七海叫自己，读到纸上消不干净的约定；进入一级“今夜の約束”，再选择放送室、咖啡店或屋顶。场景里的物件让玩家有具体话题可问，而不是只点角色名字选恋爱结局。

当前落盘结构（历史英文路径，待迁，不是新的命名样板）：

```text
world/                         零级：放送終了まで九十秒
  README.md                    场景第一眼
  01-opening.md                首次可行动 Chalk
  request-sheet.md             可携带的节目用纸
  tonight/                     一级：今夜の約束
    radio-studio/              二级：七海与台本
    amber-cafe/                 二级：澄与四十分钟
    campus-rooftop/             二级：初雪旧约
    first-snow/                 二级：短结算的场景骨架
```

当前前端以 `map` 对应 `world/`，因此零级直接放根目录；不是绕过导入进入一级。每个目录仍用自己的 README 描述场景，没有在 manifest 里重复声明 layers。

目标内容命名示例（尚未建立）：`world/今夜の約束/放送室/台本.md`、`world/今夜の約束/初雪/雪宿り/README.md`、`player/今夜の手紙.md`。英语版对应采用英文文件和目录，不混用日语内容名。

`README.md`、`world.json`、`SKILL.md` 与硬编码系统目录保留原名以兼容固定引擎；`language.md`、人物资料等可配置文件应按实际引用关系迁移，不能一概当系统文件。现有包标识 `first-snow-jp` 是历史入口，保留到明确执行迁移；不以此宣称内部内容路径已日语化。

## 4. 生成语言与显示接线

当前 Writer 通过世界 skill 加载 `skills/first-snow-jp-continuity/SKILL.md` 并读取 `language.md`。角色 preset 使用平台 `system-char`，其后读语言文件与人物文档。这是现状记录：全提示词语言化还要覆盖共享指令和动态 brief 的人类可读内容，按语言维护可复用版本，不按每个世界复制一套平台规则；工具字段与协议不翻译。本轮不更改引擎或运行提示词。

两个角色 preset 里的语言槽相同：

```json
{
  "kind": "slot",
  "id": "world-language",
  "slot": "file",
  "options": {
    "path": "language.md",
    "baseDir": "cwd",
    "onMissing": "skip"
  }
}
```

`world.json.locale = "ja"` 让 UI 在世界载入时切日语；`characters[].name` 是可选显示名，不改变 `characters[].id` 或文件路径。未声明的旧模板保持原回退行为。

## 5. 本轮不算完成的内容

初雪已有现场赴约选项、两份约定、信件 Gate 与可选避雪规则，但真实 Writer 首测漏写关键文件。最新目标是“玩家触发 ending → 既有 CG → 根据此前各层 RP 实时生成短后日谈”，还未按此完成实现。见 [初雪验收](初雪短闭环与AI生长验收.md)；不扩展长后日谈或完整重玩系统。

角色回复接线以 [Agent 前端接线](Agent前端接线.md) 的当前实现为准；它不代表全语言提示词、角色记忆与结局选图已经实玩通过。

## 6. 验收

- `node --test tools/first-snow-jp.test.mjs`：manifest、日文文本、图片一致性、场景层级、角色 preset 文件槽与无旧存档；
- `pnpm build && pnpm probe`：全仓构建与既有离线探针；
- 在用户入口 `http://localhost:5173/` 的世界列表选择“初雪ラジオ · 日本語”，检查导入、角色名、物品、一级导航与选项；
- 图片复用和静态验收不代表真实模型已完成一整局 RP。
- 先建立旧路径 → 新路径映射，再在新模板副本同步 README target、requires.items、entry、home、preset path/baseDir、skill 中的指令路径、图片引用与测试。旧存档不自动迁移或删除。
- 验证日文路径从文件扫描、HTTP 编码、Gate 进入、工具读写到持久化回访全部连通；不将字符串显示成功等同路径链已兼容。
- 采样最终运行提示词，确认世界 skill、动态 brief 与共用指令的人类可读部分均为对应语言；仅检验输出语言不足以验收“所有提示词”。
