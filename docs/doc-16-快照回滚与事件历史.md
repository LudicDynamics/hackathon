# doc-16 快照回滚与事件历史（待完善）

> 状态：**待设计（2026-09-11 立项）**。doc-05 §8.6（快照打点 + 多设备云端地图）+ doc-07 赛后 #1#2（快照回滚 UI、事件表前端可视化）展开为独立专题。
> 关联：**doc-21-事件表协议.md（事件记什么、怎么读、与回滚的关系——先读它）**、doc-05 §5.1（事件表）、§8.6（回滚机制定案）、§8.3（move 事件）；doc-07 §3（世界重载/恢复）；doc-01 §2.10（时间线回滚对照）。

## 背景与现状（已定案，不重谈）

- **不用 git 做回滚**（doc-01 §2.10 定案：AIRP 用快照打点 + 事件表回溯）；
- 快照打点机制已定（zip 打包 + 事件表，doc-05 §8.6）；
- 世界历史面板（事件表前端可视化）挂在 doc-07 赛后 #2；

> **实现进度（2026-09-14，B7）**：§3 快照打点与 §4 恢复**已落地**——`packages/shared/src/actions/world.ts` 的 `snapshotWorld` / `rollbackWorld`，HTTP 入口 `POST /api/snapshot` `POST /api/rollback`，切世界前自动打一枪。**回滚限于文件**（`world/` `characters/` `player/` `world.json` `.airpworld/assets`）；`canvas.db` 当前值仍按下文 §13 待设计 #1 的"仍待定"处理，未发明语义。验收：`packages/shared/test/world-snapshot.test.mjs`。§2 回滚 UI 与 §13 前端可视化仍赛后。
- 快照 zip 可以赛前写好，触发时机赛时定（doc-07 C4）。

## 待设计清单

| # | 项 | 说明 |
|---|---|---|
| 1 | ~~**回滚语义边界**~~（事件侧已答，见 doc-21 §6） | **事件表 append-only，回滚不删事件**；回滚只换文件，另落一条 `world_rolled_back`，并把所有读游标推到最新（否则作家会把回滚前那段再念一遍）。**仍待定**：背包、骰子结果、跟随状态这些 `canvas.db` 当前值怎么跟着回 |
| 2 | **前端可视化：世界历史面板** | 事件时间线（谁/何时/改了什么）的呈现；条目标签（玩家/作家/上帝/骰子）；点击回滚到某点后的 UX（确认 + 还原动画） |
| 3 | **快照打点触发时机** | 手动（玩家/上帝）/ 自动（章节节点 / writer turn 结束 / 骰子裁决后）的触发策略 |
| 4 | **恢复流程** | 进程重启后作家怎么"接上"。**2026-09-12 简化**：不再有 journal 要恢复（doc-12 文末撤销），重启后作家靠 doc-22 的状态块**全量重注**（首轮无指纹记忆 = 全量）+ doc-21 事件段从自己的游标续读。剩余待定只有：重启算不算一次"游标重置"，以及 pi-rp 会话是续用还是新开 |
| 5 | **多快照策略** | 保留几档快照（满/中/轻？）、自动清理、快照与 .airpworld.zip 的关系（同一机制？） |

## 边界

- 不用 git：快照 = zip + 事件表，不引入版本控制系统；
- 回滚不撤销"世界已亲眼发生"的叙事记忆——文档语义是"文件回到之前"，玩家记忆由叙事承担（不深究时间旅行的真相）。

> **标记：待办**。赛时只做打点（防空）+ 恢复（重载），UI 与可视化赛后偿还。
