# docs/tts 评审报告 B — 可实施性与闭环（维度 4/5/6）

> 评审人：TtsReviewerB（独立评审；与评审 A 分工：A 管跨文档一致性/契约形状，本报告只管**闭环性(4)/可实施性(5)/诚实性(6)**。触及跨文档一致的条目本报告只从「动作是否落到唯一文件与函数」角度提）。
> 被评审对象：`docs/tts/00`–`06`（**只写文档，无任何代码**）。厂商参考 `docs/tts/qwen3-tts-flash.md`。
> 评审方式：**只读**。所有「已核实」行号均在本机当前 HEAD 用 `read`/`grep`/`awk`/`node` 逐条核对；未核对的标 `[未核实]`。**未改任何文件、未跑项目级命令**（仅跑了 `node tools/check-request-bodies.mjs` 单命令与一个 node 小脚本核对门禁文本提取行为，属约束允许的单命令事实核对）。
> 基线快照（当前 HEAD，2026-09-13 03:25 CST，实测 `wc -l`）：
> `App.tsx`=670、`apps/server/src/routes/world.ts`=1076、`CharacterModal.tsx`=468、`lib/audio.ts`=981、`apps/server/src/index.ts`=226。
> 分级：`BLOCKER`（不修不能实现）/ `MAJOR`（会造成缺陷或返工）/ `MINOR`（措辞/美观）。

---

## ① 总评

**符号落点层面扎实度罕见**：`lib/audio.ts` 的 4 个新导出、`routes/tts.ts` 的 6 个函数、`dialogue-pages.ts` 的 6 个导出，每一个都有**唯一定义处**且用**锚定符号 + 相对位置**（不是裸行号），`02 §11` 甚至显式声明「行号对应写作时 HEAD，漂移时按符号重定位」。门禁的机械约束（`check:bodies` 逐行扫描 + 内联对象字面量）被 `00 §11`、`03 §5.1`、`05 §4` 三处实测复述，我本机复跑 `bodyKeysFor`/`compare` **逐条证实**（§5.3）。诚实性高于本仓同类文档：01/04 有 11 处 `[推断]`/`[未逐条核验]` 且方向正确。

**但可实施性有一类系统性缺陷**：所有对既有大文件的 `file:line` 锚点**都已漂移**，幅度大到会**改错块**——`world.ts` 的 `/characters` 编辑目标被标 `:545-555`（实测 `:660-687`）、`/api/audio` 被标 `:901`（实测 `:1046`）、`App.tsx` 遮罩挂载被标 `:597-612`（实测 `:639-652`）。文档自己写了「行号有保质期」，却仍有若干处只给行号或行号与符号不符，落进 `05 §8` 那种「精确到行」的施工表就会误导。

**闭环性有一处 `BLOCKER`**：`worldId === undefined` 时 mock 引导语的持久化，**冻结裁决 `00 §15.13`（不写 localStorage）与 `03 §2.2`（仍写）/`03 §6.1`（无条件写，且 key 形状又不同）三方冲突**——实现者照 `03` 写必违反契约，照 `00` 写与 `03` 伪代码不符。

**诚实性有一处要点名**：`00 §13` 把它自己在 `§6.2` 判为「未经真实模型证实」的「角色一句一行」，在「现状实测」表里写成**已证事实**（`00:462`），证据却只是提示词文本（`extensions/instructions.ts:178-186`），不是模型输出。

**用户四条需求**：分页 ✅、每页配音 ✅、点击切换 ✅、mock 不进上下文 ✅（`03 §6.1` 给了可 grep 的机械判据）；**「样式要改（像聊天框，丑）」只落成两行功能性 CSS**（`03 §8.1/§8.2`），未触及根因（§4.3）。

---

## ② 维度 4：闭环性

### 4.1 符号唯一性总表（逐符号核实）

| 符号 | 声明处（唯一） | 被引用于 | 结论 |
|---|---|---|---|
| `createTtsRouter(repoRoot, getActiveStore)` | `01 §2.1`（`routes/tts.ts` NEW） | 00 §4.1、06 §2/§4.1 | ✅ 唯一 |
| `synthesise(opts)` | `01 §2.1` | 00 §4.1、01 §3.D 步7 | ✅ 唯一 |
| `hashOf(model,voice,languageType,text)` | `01 §2.1`/`§7.B`，**MUST export**（`00 §15.7`） | 06 §2/§4.1 | ✅ 唯一；06 未同步 15.7（4.4） |
| `readTtsConfig()` | `01 §3.D 步0`（私有，**不导出**） | 06 §4.2 硬要求 | ⚠️ 私有名承载硬要求（4.4） |
| `writeAtomic`/`mapSynthError`/`TtsUpstreamError` | `01 §7.A`/`§7.C`（私有） | 01 §3.D/§3.E | ✅ 唯一 |
| `playVoice`/`stopVoice`/`isVoicing`/`voiceDebugState` | `02 §2`/`§11` | 00 §5、03 §5、06 §2/§5 | ✅ 唯一；插入锚点实测正确（5.3） |
| `parseEmoPages`/`clampPageIndex`/`parseEmoTag`/`charDelay`/`EMO_PREFIX_GUARD`/`GREETING_LINE` | `03 §2.1`（`dialogue-pages.ts` NEW） | 00 §6/§7/§13、06 §2/§5 | ✅ 唯一 |

**结论**：10 个被点名符号**全部有唯一定义处**，无一漂浮。这是本批最强的一面。

### 4.2 「动作 → 文件 + 函数」抽验（用户四条需求）

| 需求 | 动作落点 | 核实 |
|---|---|---|
| galgame 一句一页 + 点击切换 + 每页配音 | `syncPages`/`enterPage`/`advance`/`prefetchVoice`/`onVoiceResolved`，全在 `03 §3.1/§3.2/§4.4/§5.1/§5.2`；唯一翻页入口 `enterPage`（`03:167`） | ✅ |
| 补分页 | `parseEmoPages`（`03 §2.1`）+ `dialogue-pages.ts` NEW | ✅ |
| 改样式 | 仅 `03 §8.1`（`.line-stage.is-advanceable` cursor+hover）+ `03 §8.2`（`max-height:9.6em;overflow-y:auto`） | ❌ 未兑现（4.3） |
| mock 引导语不进上下文 | `03 §6.1`：只写 `pagesRef`、从不写 `lineRef` + 可 grep 判据（`03:464`） | ✅ |

### 4.3 `MAJOR` 「样式要改」只落成两行功能 CSS，未触及聊天框根因

- 证据：`03 §8.1`（`03:505-510`）只加 `cursor:pointer` + 一个 hover 底色；`03 §8.2`（`03:516-518`）只加 `max-height`+`overflow-y`。两条都是**可点性/溢出**需要，不是「不像聊天框」的视觉改造。
- 用户原话：**「样式要改（现在像聊天框，丑）」**。`00 §6` 与 `03` 通读未见任何「纸面/气泡/输入框视觉重构」条目；`03 §8` 标题虽是「布局/样式」，正文只谈可点区与溢出。
- `03 §8.4`（`03:529`）明确「页指示器：本批不做」——galgame 形态的**标志性视觉元素**（`3 / 5`）被以「用户只提点击+配音」为由砍掉，可辩，但叠加「样式整体未动」后，**四条需求里最弱的一条只落了功能边角**。
- **分级：`MAJOR`**（非不能实现，而是需求强度未兑现）。
- 修订：`03 §8` 增 `8.5 视觉改造清单`，至少覆盖 `.speech-paper`(`index.css:441`)/`.line-stage`(`:503`)/`.speech-input`(`:548`) 三块与聊天框观感的差异；或由主 agent 明确「样式需求降级为功能微调」并记录用户确认。

### 4.4 `MINOR` 三个「闭环半开」点

1. **`hashOf` 导出面**：`00 §15.7` 已裁「MUST export」，但 `06 §2`（`06:36`）与 `06 §12.1`（`06:809`）仍写「**未冻结**……两条路都能编译」→ 读者以为仍待拍板。**`MINOR`**（不阻塞：本地镜像公式与契约 §3.2 一致，实测相同）。修订：`06 §2` 注释与 `§12.1` 改「已由 `00 §15.7` 冻结为 MUST export；测试优先 import」。
2. **`readTtsConfig` 私有名承载硬要求**：`06 §4.2`（`06:377`）以该私有函数为「每请求读 env」的行为前提，但该名未进 `00 §4.1` 冻结表，`01` 可自由改名 → 06 注记与实现脱钩。**`MINOR`**。修订：`00 §4.1` 补一行或在 `01 §3.D 步0` 注明「此名被 06 注入方案依赖，重命名须同步」。
3. `03` 章节编号断档：`03 §④` 直接跳 `### 4.4`/`### 4.5`（`03:257`/`03:289`），缺 4.1–4.3，而 `00 §11` 又引「`03 §4.3`」。**`MINOR`**。

### 4.5 `MAJOR` P 组（分页纯函数）测试：两个文件名、两套编号，无单一 owner

- `03 §10.1`（`03:565`）声明新文件 **`apps/web/test/dialogue-pages.test.mjs`**，编号 **P1–P11**（`03:567-579`）。
- `00 §11`（`00:435-437`）与 `06 §2`（`06:19-20`）只声明**两个**测试文件；`06 §5`（`06:398`）把分页断言放进 **`voice-engine.test.mjs`**，编号 **P1–P8**（`06:466-511`）。
- 两套编号**同名不同义**：`03` P2=空页丢弃 / `06` P2=多行一页一行；`03` P5=emo 继承 / `06` P5=超长不截断。
- 影响：实现者不知测试写进哪个文件，且 `00 §11` 冻结清单里**根本没有** `dialogue-pages.test.mjs`。P 编号在 03/06 间不可交叉引用。**`MAJOR`**（直接致返工/漏测）。
- 修订（二选一，需主 agent 裁决）：(a) 采纳 06：删 `03 §10.1` 的独立文件与 P1–P11，改为「纯函数断言见 `06 §5` P1–P8」；(b) 采纳 03：`00 §11`+`06 §2` 补第三文件，`06 §5` 的 P 组抽出。无论哪种，**P 编号必须单一定义**。

### 4.6 `MAJOR` 契约 §13 锚点回写的 ownership 悬空（ticket ② 核验结果）

**ticket ② 假设核实：成立，且比预期更严重——不是「会不会漂移」，而是「谁改」未定，且 §13 里多处引用在实现后必然指向不存在的符号。**

- `00 §13`（`00:464`）：「`parseEmoTag`/`parseEmoLines`/`charDelay` 可复用 | `CharacterModal.tsx:65-118`」。**实测**：`parseEmoTag` 在 `:65-70`、`parseEmoLines` 在 `:80-111`、`charDelay` 在 `:114-118`。`03 §2.1` 会把前两者**搬走并删除**（`parseEmoLines` 更名 `parseEmoPages`，`03:75`）→ 实现后该行引用**已不存在的符号**与**已清空区间**。
- 另有：`00 §6.1`（`00:251`）引 `:65-111`、`00 §6.3`（`00:275`）引 `:113-118`、`00 §13`（`00:466`）引 `:65-118`——三处都会漂到 `dialogue-pages.ts`。
- 回写归属**明确矛盾**：
  - `00 §15.8`（`00:563`）：「§13 里凡引用……**实现时 MUST 改为指向 `dialogue-pages.ts`**……**`06` 负责回写**。」
  - `06 R7`（`06:690`）：「**契约正文改动由主 agent 裁决，本清单只登记 03 的请求**。」——**06 自己拒绝执行**。
- 即：契约说 06 改，06 说主 agent 改，**没人认领**。**`MAJOR`**（影响文档真相源；`AGENTS.md §6.3` 要求文档随代码走）。
- 修订：主 agent 在 `00 §15.8` 与 `06 R7` 间写死 owner。建议改为「`06` MUST 在本批 commit 内把 `00 §6.1/§6.3/§13` 的 `CharacterModal.tsx:*` 锚点改为 `dialogue-pages.ts`（按符号重定位），主 agent 已在 §15.8 授权」；若坚持契约正文只由主 agent 改，则须主 agent **实现前先改 §13**。

---

## ③ 维度 5：可实施性

### 5.1 `MAJOR` `App.tsx` 的「精确行锚」大量漂移（1–42 行），且被当作编辑目标

逐条实测（当前 HEAD，`awk NR==n` 取值）：

| 文档引用 | 声称内容 | 实测该行 | 漂移 | 实测正确锚点 |
|---|---|---|---|---|
| `00:430` `02:238` `03:291` `05:266` `06:90` | `App.tsx:585` = 打开遮罩的点击（`onChatWithCharacter`） | `:585` 是 `onToggleFollow` 块内 `}}` | **+41** | `onChatWithCharacter={openCharacterModal}` 在 **`:626`**（`RightSidebar` 起 `:620`） |
| `00:478` `03:77/91/433` `05:124/307/340` | `App.tsx:597-612` = `<CharacterModal>` 下推 props | `:597` 不在遮罩区 | **+42** | 遮罩挂载在 **`:639-652`**（`{activeModalCharId && activeChar && (` 于 `:639`） |
| `00:480` `03:434` `05:274` | `App.tsx:605-611` = `character_prompt` 发送 | 错位 | **+42** | `sendMessage({type:'character_prompt',…})` 在 **`:647-652`** |
| `03:440` | `App.tsx:172` = `fetchManifest()` 调用 | `:172` 是帧过滤回调内 | **+14** | 调用点在 **`:151`**；定义在 **`:186`** |
| `05:182` | `App.tsx:441` = `activeChar = characters.find(...)` | 错位 | **+27** | **`:468`** |
| `03:463` / `01:306` | `App.tsx:37` = `manifest` state | `:37` 是 `characters` state | **-2** | `manifest` state 在 **`:35`** |
| `05:113` | `App.tsx:245` = `setCharacters(...)` | `:245` 空行 | **+6** | **`:251`** |
| `00:271` `05:75/191` | `App.tsx:39-41` = 浏览器语言猜测 | `:39` 是 `toastMessage` state | **-1** | `navigator.language…` 在 **`:41`**（state 起 `:40`） |

- 影响：`05 §8` 是「改动 + 落点」施工表，其第 4 行（`05:302`）直接以 `App.tsx:597-612` 为编辑目标；按行号定位会改到 `RadialMenu` 附近。
- 「给了符号锚」判定：多数引用**同时**给了 `<CharacterModal>`/`onChatWithCharacter`/`openCharacterModal` → **可恢复**，故非 BLOCKER。但 `05` 表行与 `00 §13`（现状实测表）混用裸行号会误导。
- **分级：`MAJOR`**。
- 修订：`00 §13`、`03 §6.1`、`05 §8` 所有 `App.tsx:<行>` 统一改「符号名 + 当前行（快照日期）」，实现前用一次 `grep -n` 刷新；尤其 `openCharacterModal`/`onChatWithCharacter` 与遮罩挂载两处（unlock 风险论据落点）。

### 5.2 `MAJOR` `world.ts` 锚点整体漂移 110–500 行（含 edit target）

`world.ts` 当前 **1076 行**（实测），较文档基线显著增长。逐条：

| 文档引用 | 声称 | 实测该行 | 实测正确锚点 |
|---|---|---|---|
| `00:80` `00:473` `01 §6`(`01:408`) | `world.ts:549-563` = 反穿越先例 | `:549-563` 是 `reseatLayer` 循环 | **`:1046-1076`**（`/audio` handler；`realpathSync` 在 `:1057-1058`） |
| `01:192/574` `01:639` `04:146/364` `05:138/155` | `world.ts:545-555`（/545-550 /541-556）= `/characters` 的 map 回包 | `:545-555` 是 `reseatLayer` | `/characters` 在 **`:660-687`**；try 块 `:667-676`；`return {...c, avatar, bio}` 在 `:677-682` |
| `01:593` | `world.ts:901` = `/api/audio` 平台池 | `:901` 是 `player/` 校验 | `/api/audio` 在 **`:1046`**；`AUDIO_ROOT` 定义在 `:318` |
| `00:472` | `world.ts:546-548` = 读 frontmatter avatar | 错位 | `/characters` 的 `parseFrontmatter` 在 **`:668-670`** |
| `00:59` `05:263` | `world.ts:56-74` = `ActionError.toHttp()` | `:56-74` 是 `reply()` 尾部+注释 | `reply()` 在 **`:59-77`**（差 3 行，危害小） |

- 影响：`01 §8` 表（`01:574`）以 `world.ts /characters handler :551-555` 为 **voice 回传的编辑目标**；`05 §8` 表第 3 行写 `(:541-556)`。按错行号施工会改到 `reseatLayer`。
- 符号锚判定：`01 §8` 表行**只写** `world.ts` + `:551-555`（**无符号名**）；`05 §8` 表行也是裸行号 → **部分缺失符号锚**。
- **分级：`MAJOR`**。
- 修订：`01 §3.C′`/`§8`、`05 §3.2`/`§8`、`04 §6`、`00 §13` 的 `world.ts:<行>` 全改为「`/characters` handler 的 `map` 回调（实测当前 `:667-682`，快照 2026-09-13）」，去掉裸行号。理由：`world.ts` 是本批增长最快的文件，行号锚在此不可靠，必须符号化。

### 5.3 ✅（已核实、值得肯定）`lib/audio.ts` 与门禁落点可机械核验

**`lib/audio.ts` 插入锚点全部实测命中**：

| 文档锚点 | 声称 | 实测 |
|---|---|---|
| `02 §11 编辑 A`（`02:281`） | `STINGER_SAMPLE_LEVEL` 在 `:481`，新常量插其后 | `:481` = `const STINGER_SAMPLE_LEVEL = 0.8;` ✅ |
| `02 §11 编辑 B`（`02:288`） | `audioDebugState()` 闭合在 `:882`，`/* Mute / unlock */` 在 `:884` | `:882` = `}`（函数闭合）；`:884-886` 是分节注释 ✅ |
| `02:46` | `interface Clip` 在 `:457-462` | `:457`=`interface Clip {`，`:462`=`}` ✅ |
| `02:115` / `02:119` | `loadSample` 在 `:532` / `playClip` 在 `:564` | 两者均命中 ✅ |
| `02:232` | `rampClips()` 在 `:676-688` | `:676`=`function rampClips(): void {` ✅ |
| `02:66-70` | `SAMPLE_LEVELS` `:479`、`FOLEY_SAMPLE_LEVEL` `:480`、`STINGER_SAMPLE_LEVEL` `:481` | 三行全中 ✅ |
| `00:467` | `playStinger` 降级范式 `:851-860` | `:851`=`export function playStinger`；`:854`=`if (c.state !== 'running') return; // drop, don't queue` ✅ |

> ⚠️ 唯一例外：`00:430` `02:238` `03:291` `05:266` `06:90` 复述的 `Canvas.tsx:180`（`unlock()` 调用点）**实测为 `:191`**（`grep -n "void unlock()"`）。结论不变（同处给了符号 `unlock()`），属行号老化。**分级 `MINOR`**。修订：五处统一改 `Canvas.tsx:191`（快照 2026-09-13）。另实测 `DiceRoller.tsx:219`/`:265` 正确。

**门禁机械约束，本机复跑 `bodyKeysFor`/`compare` 逐条证实**：

- `JSON.stringify({ text, ...(voice ? {voice} : {}), language })` → 返回 **`{keys:['text']}`**（`03 §5.1`/`05 §4.3` 声称「提取到 `[text]`，缺 voice 且 language」——**实测确认** ✅）。
- `const payload={text,voice,language}; fetch(..., JSON.stringify(payload))` → **`null`**（`missing-call`）✅。
- 多行内联对象 → `['text','voice','language']` ✅（`05 §4.2` 声称合法，确认）。
- `{ text, voice }`（少 `language`）→ `compare` 报 `key-drift`，消息 `body keys = [text, voice], contract says [language, text, voice]` ✅（与 `05 §10.1` 非空性反证逐字吻合）。
- 起点基线：`node tools/check-request-bodies.mjs` → `clean (1 route(s) pinned)` ✅（`05 §1`「1→2 route」前提成立）。
- **该部分文档质量全批最高**：门禁的文本提取模型被准确复述，非「看起来对」。

### 5.4 `MINOR` `03 §5.1` 的 `prefetchVoice` 写法与 `§9` 自相矛盾

- `03 §5.1`（`03:309`）写成**模块级** `async function prefetchVoice(page, i)`（无 `useCallback`、未缩进于组件），函数体却引用 `voice`/`language`/`voiceTurnRef`/`onVoiceResolved`（组件闭包）；`03 §9`（`03:557`）声明「新增函数……`prefetchVoice`……**全部 `useCallback`**」。
- **分级 `MINOR`**（语义无歧义：必须在组件内），但照 §5.1 抄会写出引用不到的变量。
- 修订：`03 §5.1` 改 `const prefetchVoice = useCallback(async (page, i) => { … }, [voice, language])`，或加一行「（实为组件内 `useCallback`，见 §9）」。

### 5.5 `MINOR` `03 §2.3` refs 清单漏 `mockSeededRef`

- `03 §2.3`（`03:104-105`）列了 `mockRef`/`stingerGraceRef`/`inputRef`，**缺 `mockSeededRef`**；而 `03 §6.2`（`03:473`）以「`mockSeededRef`（NEW）」为 StrictMode 幂等的**唯一守卫**，`03 §9`（`03:544`）也列入 diff。声明点与使用点都依赖它，refs 清单却缺项。
- **分级 `MINOR`**。修订：`03 §2.3` 表补 `mockSeededRef | useRef<boolean> | NEW | mock 引导语只 seed 一次 | 见 §6.2`。

---

## ④ 维度 6：诚实性

### 6.1 `BLOCKER` `worldId === undefined` 的 mock 持久化：契约裁决与 `03` 设计直接冲突，且 `03` 内部自相矛盾

（与 `② 4.6` 同源但不同面：4.6 是锚点 owner，此处是**行为契约**。同时踩「契约冻结遵守」与「动作没落到唯一处」，实现时必二选一违反。）

- 冻结裁决 `00 §15.13`（`00:584-586`）：
  > 「`worldId` 为 `undefined` 时，**仍显示 mock 引导语，但不写 `localStorage`**（避免 `airp:greeted:undefined:nanami` 的假持久化）。`03` 已按此实现（`03 §2.2`）。」
- `03 §2.2`（`03:92`）**相反**：
  > 「`worldId` 缺省（`undefined`）时：mock 引导语的 key 退化为 `airp:greeted:<charId>`（无世界段）——**仍能去重**，只是跨世界共享。**不阻塞**。」→ 即「仍然写 localStorage」。
- `03 §6.1` 伪代码（`03:443`）又与 `03 §2.2` 的 key 形状不同：
  > `key = \`airp:greeted:${worldId ?? ''}:${characterId}\`` → `undefined` 时得 **`airp:greeted::nanami`**（空世界段、双冒号），不是 §2.2 说的 `airp:greeted:nanami`；且 `03:445-450` **无条件** `localStorage.setItem(key,'1')` → **确凿会写**，正是 §15.13 明令禁止的。
- `05 §3.1`（`05:129`）与 `05 §11 冲突 4`（`05:394`）站在契约一边：「仍显示 mock 引导语，**但不写 `localStorage`**」。
- 三处口径：`00 §15.13`（不写）/ `05 §3.1`（不写）vs `03 §2.2`（写、key 无世界段）vs `03 §6.1`（写、key 空世界段）。**实现者无从选择。**
- **分级：`BLOCKER`**（契约冻结已裁，`03` 未遵守；且 `03` 自身不可能同时满足 §2.2 与 §6.1）。
- 修订（精确到节）：
  1. `03 §2.2` 第三句改：「`worldId === undefined` 时：**仍显示** mock 引导语（`enterPage(0)`），但**不写** `localStorage`——本次会话不持久化，下次打开仍显示。依据 `00 §15.13`。」
  2. `03 §6.1` 伪代码改：`if (worldId !== undefined) { try { localStorage.setItem(\`airp:greeted:${worldId}:${characterId}\`, '1'); } catch {} }`；`getItem` 判定前先判 `worldId` 空则视为 `null` → seed。
  3. `03 §6.1` StrictMode 段（`03:466/473`）对齐：`mockSeededRef` 守留，localStorage 写入受 `worldId` 守卫。

### 6.2 `MAJOR` `00 §13` 把「未证实的一句话一行」写成已证事实（ticket ③ 核验）

**ticket ③ 假设核实：这条实测警告是「半吸收」——`00 §6.2`/`03 §3.1`/`06 §5` 吸收正确，但 `00 §13` 自相矛盾。**

- **吸收正确处（证据）**：
  - `00 §6.2`（`00:260-269`）以整段「⚠️ 实测警告」声明「未证」，并列出 4 条鲁棒规则；`00:269` 明确「把『一句一行』当成已证事实来设计……正是本批要防的失败」。
  - `03 §3.1`（`03:155`）落地：`parseEmoPages` 对无 `\n` 整段返回**恰好 1 页**，1 页与 N 页走同一条代码路径、无特判；`03 §7`（`03:486`）把「无换行整段」列为显式错误边界。
  - `06 §5` P1（`06:468`）标 `NON-EMPTINESS`：`parseEmoPages('done',{final:true})` → `pages.length===1`；`06 §5.2`（`06:522`）直言「这是本批最大风险点」。`06 §3.4`（`06:80`）同理。
  - **分页鲁棒规则被真正兑现**：`06 §5` P1/P5/P6 三条非空性断言分别覆盖「无换行=单页」「超长不截断」「不按标点切页」，与 `00 §6.2` 第 1/2/3 条**一一对应**（我实测 P 组断言文本与契约条款吻合）。
- **矛盾处（证据）**：`00 §13`（`00:462`）在「现状实测（带 `file:line`）」表里写：
  > 「角色输出**一句一行**且带 `[emo: tag]` | `extensions/instructions.ts:178-186`」
  - 该表标题是「**现状实测**」，且同表其它行都是代码事实。但本行的「证据」是 `instructions.ts:178-186` ——**提示词文本**（我实测该处即 `[One sentence per line]` 段落），**不是任何真实模型输出**。同一份 `00` 的 `§6.2` 明写「尚未被任何真实模型输出证实」「0 条多行输出」。
  - 即：`00 §13` 与 `00 §6.2` **同一文件内互相打脸**；`§13` 把「提示词要求」当成了「模型行为实测」。
  - `00 §13` 另一行（`00:463`）「遮罩**无分页**，整段单游标逐字 | `CharacterModal.tsx:141-142…:196-221`」是**真代码事实**（我实测 `lineRef`在`:141`、`shownRef`在`:142`、`pump`在`:196`），故 `§13` 表整体不可信度只在这一行。
- **分级：`MAJOR`**（不直接阻塞实现，但违反本批最看重的方法论；`00:462` 会被实现者当「模型会分行」的确证，与 §6.2 的警告对冲）。
- 修订：`00 §13`（`00:462`）改为：
  `| 角色提示词**要求**一句一行且带 [emo: tag]（**模型是否照做未证实**，见 §6.2 ⚠️） | extensions/instructions.ts:178-186（提示词文本，非模型输出实测） |`
  并删去「实测」表内对模型行为的任何肯定措辞。

### 6.3 `MINOR` 标注恰当但需补一句的 `[推断]` 项

- `03 §5.4`（`03:395`）/`03 §12.5`（`03:617`）把 `STINGER_VOICE_GRACE_MS = 1200` 标 `[推断]`，且 `00 §15.11` 明确「保留为 `[推断]` 初值，真机可调」——**标注正确**。
- `01 §3.D 步4/步7`、`01 §7.A 步3`（`01:470`）等 8 处 `[推断]` 方向正确（把「厂商未明说」标成未明说）。
- `04 §3.1`（`04:52`）明确**撤回**「完整清单」地位、全部标「候选 `[未逐条核验]`」，并给出 curl 实测路径（`04 §9.1`）——**诚实性示范**。
- **唯一可补**：`04 §4.1` 的 9 席中 3 席（`Neil`/`Mia`/`Vincent`）不在 8 个安全集合内，`04 §12.4`（`04:379`）已诚实登记「本模块唯一未闭合的实质项」。**无需改**，仅提示实现时这 3 个 ID 必须 curl 实测。

### 6.4 未被伪装成已定的未知项（正面清单）

核实下列均**正确标为未知/待拍板**，无伪装：
`01 §12` 8 项（magic bytes / 缓存失效 / `.tmp` 清扫 / `characters` 口径 / 超时拆分 / baseUrl 归一化）；`02 §14` 4 项（ducking / decodeCache 淘汰 / `VOICE_FADE` 数值 / `voiceDebugState` 淡出观测）；`03 §12` 5 项（页指示器 / `truncated` 呈现 / 点空白推进 / testing-library 缺失 / 1200ms 取值）；`05 §12` 4 项；`06 §12.2` 4 项；`04 §12` 6 项。**总计约 31 项未知均带 `[推断]`/`[未实测]`/「待拍板」标记，未发现伪装。**

> 我方（评审 B）**无新增未核实断言**：本报告所有结论均以本机 HEAD 的 `read`/`grep`/`awk`/`node` 实测为准；推断之处已在该条内注明。

---

## ⑤ 跨文档不一致清单（本报告从「动作落点」角度收集；形状/字段名层面归评审 A）

| # | 主题 | 各文档写法 | 分级 |
|---|---|---|---|
| I1 | mock 引导语持久化（`worldId` 空） | `00 §15.13` 不写；`05 §3.1` 不写；**`03 §2.2` 写（key 无世界段）**；**`03 §6.1` 写（key 空世界段）** | **BLOCKER** |
| I2 | 分页纯函数测试文件与编号 | `03 §10.1`=`dialogue-pages.test.mjs` P1–P11；`00 §11`+`06 §5`=`voice-engine.test.mjs` P1–P8 | **MAJOR** |
| I3 | 契约 §13 锚点回写 owner | `00 §15.8` 说 `06` 负责；`06 R7` 说主 agent 负责 | **MAJOR** |
| I4 | `hashOf` 是否导出 | `00 §15.7` MUST export；`06 §2/§12.1` 仍写「未冻结」 | MINOR |
| I5 | `Canvas.tsx` unlock 调用点行号 | 5 篇写 `:180`；实测 `:191` | MINOR |
| I6 | `App.tsx` / `world.ts` 行锚 | 各篇复述同一批已漂移行号（见 5.1/5.2） | **MAJOR** |
| I7 | `03 §④` 混排「副作用表 + 点击推进」 | `4.4`/`4.5` 无前序小节，`00 §11` 却引「`03 §4.3`」 | MINOR |

> I1/I4 已被评审 A 或主 agent 部分触及（`00 §15.13` 本身即裁决记录），但 **`03` 未遵守**是本报告独立发现。

---

## ⑥ 契约违反清单（子文档私改 `00` 冻结形状）

**未发现子文档私改 `00` 冻结形状**：逐一核对 `schema`（`world.json.characters[]` 无 `voice`）、`audioDebugState` 四字段、WS 帧零增删、响应体五字段、env 名、hash 公式——**全部一致**，无 BLOCKER 级契约改动。

唯一的「行为违反」是 **I1**（`03` 违反 `00 §15.13` 已裁的 mock 持久化行为），属**裁决已下而子文档未落实**，不是私改形状。清单：

| # | 违反 | 违反者 | 被违反的冻结条款 | 分级 |
|---|---|---|---|---|
| V1 | `worldId` 空时仍写 `localStorage` | `03 §2.2` + `03 §6.1` | `00 §15.13`（裁决：不写） | **BLOCKER** |
| V2 | `parseEmoLines` 在 `00 §13` 仍被列为「可复用」 | `00` 自身（应由 `06`/主 agent 回写） | `03 §2.1`（删除并更名） | MAJOR |

（`02 §7` 选「遮罩直调、`useAudio` 零改动」是 `00 §5.1` 授权的二选一，**非违反**；`04` 的音色清单降级是 `00 §15.2` 裁决的**执行**，非违反。）

---

## ⑦ 建议的修订动作（精确到文档与章节）

**必须在实现前修（BLOCKER）**

1. `03 §2.2`：mock 持久化改为「`worldId === undefined` 时不写 `localStorage`」，与 `00 §15.13`/`05 §3.1` 对齐。（对应 I1/V1）
2. `03 §6.1`：伪代码 `localStorage.setItem` 加 `worldId !== undefined` 守卫；key 形状统一为 `airp:greeted:${worldId}:${characterId}`，删去「空世界段」退化写法；StrictMode 段同步。（I1/V1）

**实现前应修（MAJOR）**

3. `03 §10.1` 与 `06 §5`：P 组测试**选一个文件、一套编号**（建议落 `voice-engine.test.mjs`、编号以 03 语义表为准并与 06 合并）；`00 §11` 的测试清单同步。（I2）
4. `00 §15.8` 与 `06 R7`：写死 §13 锚点回写 owner（建议 `06` 执行、主 agent 授权），否则实现后留下假锚点。（I3/V2）
5. `00 §13`、`01 §3.C′`/`§8`、`04 §6`、`05 §3.2`/`§8`、`03 §6.1` 的所有 `world.ts:<行>` 与 `App.tsx:<行>`：**行号 + 符号名双写**，实现前用 `grep -n` 刷新一次。优先刷新 `world.ts` 的 `/characters`（现 `:660-687`）、`/api/audio`（现 `:1046-1076`）与 `App.tsx` 遮罩挂载（现 `:639-652`）。（I6）
6. `03 §8`：补「视觉改造清单」，兑现用户「样式要改」；或由主 agent 显式记录「样式需求降级为功能微调」+ 用户确认。（`② 4.3`）

**实现时可顺手修（MINOR）**

7. `06 §2` 注释 + `§12.1`：`hashOf` 导出改「已由 `00 §15.7` 冻结为 MUST export」。（I4）
8. 五篇的 `Canvas.tsx:180` → `:191`（快照 2026-09-13）。（I5）
9. `03 §5.1`：`prefetchVoice` 改标为组件内 `useCallback`，或加注解。
10. `03 §2.3`：refs 清单补 `mockSeededRef`。
11. `03 §④`：补 `4.1–4.3` 小节号或重排为连续编号。（I7）
12. `01 §3.D 步0`：给 `readTtsConfig` 加「此名被 `06` 注入方案依赖，重命名须同步」注记（或纳入 `00 §4.1`）。

---

## ⑧ 评审结论

**不通过（需修订后复审）**。1 个 `BLOCKER`（I1/V1，mock 持久化三方冲突）、5 个 `MAJOR`（I2 测试文件双写、I3 owner 悬空、I6 行锚漂移、样式需求未兑现、`00 §13` 把未证写成已证）必须在上实现前修完；其余 MINOR 可随 commit。

**给主 agent 的一句话**：本批的**符号闭环与门禁复述是典范**，真正要拦的是三类「文档自己已经知道、却没落到唯一处」的裂缝——mock 持久化（裁决已下、03 未执行）、P 组测试（两文件两编号）、§13 回写（契约说 06 改、06 说主 agent 改）。修完这三条即可放行实现。

> 复核时请按**符号名**而非行号核对（行号有保质期，本报告已给出实测快照日期 2026-09-13）。