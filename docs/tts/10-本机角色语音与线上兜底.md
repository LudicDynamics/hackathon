# 七海：本机 TTS 与线上兜底

## 最新覆盖规则（2026-09-14）

用户最新确认：所有世界的女性音色默认共用七海本机 `setsuna`，Vera 与 Nanami 共用；下文原有「仅初雪七海」限制被本节取代。服务端先验证活跃世界角色表，再读取角色 README 的 `gender`（如有）和 `voice`；显式 male 不自动改，female 或音色调色板的女性类别采用本机默认，未知保持线上。原 README 音色保留作为故障兜底，不批量改故事文件。

设置入口为「声音与设置 → 角色语音 → 本机角色音色」。角色 ID 可从当前世界列表选择，也可输入其他世界的稳定 ID；添加覆盖后点击「保存角色音色」生效。`AIRP_TTS_CHARACTER_VOICES` 格式为 `vera=setsuna,nanami=setsuna`，按角色 ID 跨世界生效，优先于默认分类。值 `online` 表示保留原线上音色；删除该条恢复默认。可填的本机 voice ID 必须已安装在语音服务上，不会凭输入自动生成新音色。

### 中文世界：显示中文、本机日语音色念日文（2026-09-14）

用户确认：`setsuna` 是日语音色，**不得念中文**。中文版（`world.json.locale` 为 `zh-CN`）的角色台词在界面上照常显示中文；暂时只在 **setsuna 本机合成这一步**生效：选中的本机音色是 `setsuna`（可用 `AIRP_TTS_JA_LOCAL_VOICES` 逗号列表扩充）时，服务端先把这句中文译成口语日文，只把日文交给本机合成（`language: ja`）。线上 qwen 路径不变，照常念中文；其他本机音色不翻译。判断依据是活跃世界的 manifest locale（请求的 `language` 为 `zh-CN`/`zh` 也算），且该句含汉字；不含汉字的句子（如「……！」）原样合成。实测本机服务忽略 `language` 参数、按文本本身的语言发音，所以只能靠翻译，不能靠参数。

翻译由 `apps/server/src/routes/tts-translate.ts` 调用 DeepSeek 兼容 OpenAI 的 `/chat/completions`，默认 `https://api.deepseek.com`、模型 `deepseek-v4-flash`、`thinking: { type: "disabled" }`（否则推理耗尽预算、正文为空），密钥复用 `DEEPSEEK_API_KEY`；可用 `AIRP_TTS_TRANSLATE_BASE_URL` / `_MODEL` / `_API_KEY` / `_TIMEOUT_MS`（默认 15000）覆盖。单句实测约 1.1 秒。本机缓存键使用原中文句子与 `zh-CN>ja` 标记，同一句只翻译、合成一次。

翻译失败（无密钥、HTTP 错误、超时、返回不含假名）时绝不调用本机音色，走既有 `local-to-online` 兜底：线上 qwen TTS 以 `language_type: Chinese` 念中文。关闭本机基址时，中文世界同样直接使用线上中文语音。验证：`apps/server/test/tts-voice-bridge.test.mjs`；另以真实 DeepSeek 与本机服务端到端合成，本地 whisper 转写判定为日语。

模型默认同时调整：未单独设置的世界，writer / character 都使用 `deepseek/deepseek-v4-flash` 与 `low`；显式存档偏好仍可在 Agents 面板覆盖。此次已有本地存档偏好统一迁移，旧偏好备份在本地 `.artifacts/`，不改引擎、不改世界剧情。

2026-09-14 用户确认：first-snow 女主七海（nanami）优先使用本机角色 TTS，经 Tailscale 地址访问；失败警告并回落既有线上服务。本节作为对 00 / 01 / 09 旧单供应商契约的增补。

浏览器仍只调用 AIRP 的 `/api/tts`，请求扩为 `{ text, voice?, language?, characterId?, emotion? }`。`characterId` 来自当前角色，`emotion` 来自该页标签，不发送角色背景或私密设定。无新增字段的旧调用继续走线上。返回 JSON 形状不变；本机失败的响应附带 `X-AIRP-TTS-Fallback: local-to-online`，前端据此 `console.warn`，包括线上也失败的情况。

服务端依据活跃世界 manifest 的 ID 与角色表核实归属，`first-snow`、`first-snow-jp`、旧 `firstsnow`、校园恋爱 `sakura-academy` 及上述世界带后缀的派生存档中的 nanami 使用本机；澄及其他角色继续使用既有线上音色。世界 README 的 `voice` 保留为线上兜底声明，不需要迁移旧存档。

2026-09-14 全剧本检查：正式模板中七海仅在初雪英日版出现；归档另有 `archive/templates/pre-bilingual-2026-09-14/school-romance`（显示名 Sakura Academy，实际世界 ID 为 `sakura-academy`），以及已覆盖的初雪旧版和测试版。校园版的七海是玩家同桌，按用户「有的话都补上」的要求共用本机声音，线上仍保留其 `shy-sweet` 声明。归档仍留在归档目录，不自动重新发布或创建存档。

在“声音与设置 → 角色语音 → 七海本机语音”中配置以下字段，独立保存后下一次合成立即生效。本机配置沿用 `/api/connection-settings` 的本机访问限制、原子保存与密钥不回显约束；已有 `.local.env` 时优先保存该文件，否则保存 `.env.local`。直接手改环境文件仍需重启后端。

| 变量 | 默认 | 作用 |
|---|---|---|
| `AIRP_TTS_LOCAL_BASE_URL` | 空，关闭 | 本机 Fish TTS 基址；本机部署为 `http://100.120.116.13:8090` |
| `AIRP_TTS_LOCAL_VOICE` | `setsuna` | 本机服务加载的声音 |
| `AIRP_TTS_LOCAL_TIMEOUT_MS` | `30000` | 包含读取音频的单次总预算 |
| `AIRP_TTS_LOCAL_API_KEY` | 空 | 服务启用鉴权时使用，仅服务端持有 |

表单超时单位为秒，范围 1–120；接口存整数字符串毫秒。基址不包含 `/v1/tts`，明确清空基址并保存表示关闭本机路径，七海随后直接使用线上配置。声音 ID 限字母、数字、下划线和短横线（1–64 字符，首位为字母或数字）；其他字段留空保持原值，密钥永不回显。未提交字段不覆盖。配置保存仅验证格式，不自动调用供应商。

调用 `POST <base>/v1/tts`，发送 `{ text, voice, language, emotion }`，接收 RIFF/WAVE 字节。`normal/shock/thinking` 映射 `neutral`，`smile` 映射 `happy`，`sad/angry` 原样；缺省或未知标签使用 `neutral`。线上仍遵守 09 的固定语气指令，不接收 emotion。

本机 HTTP 错误、断连、超时和非 WAV 均触发服务端警告，然后走原 DashScope 缓存与合成流程。两条链路各有独立超时预算。线上缺少密钥或请求失败仍返回原错误码并警告，不伪造音频。本机不需要 DashScope 密钥；`GET /api/tts/config` 的 configured 表示至少一个供应商已配置，不保证网络可用，model/defaultVoice 仍指线上兜底配置。

本机缓存使用 `sha256(JSON.stringify(['fish-tts-v1', baseUrl, voice, language, emotion, text])).slice(0,20)`，与原线上缓存键分开。本机缓存命中优先；否则先尝试本机，失败后才读线上缓存或合成。线上兜底音频不写入本机键，下次请求会重新尝试本机，服务恢复后不会永久留在兜底声音。缓存目录及音频 URL 仍沿用 00 的世界隔离和反穿越规则。更换同一服务地址背后的模型时，应同步修改本机 voice 标识或清理对应本机缓存。

验证：本机真实日语合成与 AIRP 音频下载；自动测试覆盖本机成功、角色/世界隔离、HTTP/超时/非 WAV 降级、情绪缓存及服务恢复。用户明确不测试线上真实调用，线上密钥未配置时不宣称线上已验证。
