# docs/tts/06 — 验证与文档回写

> 子文档 `06`。遵守 `docs/tts/00-共同上下文.md`（下称「契约」）的权威层级；本批 6 篇并行，**本篇不改任何代码**，只产出可执行的测试断言骨架、回写清单、手测清单与收工命令。
> 上游：契约 §2/§3/§5/§6/§8/§11/§12/§13/§15；`docs/tts/qwen3-tts-flash.md`（厂商调用形状）；`apps/server/test/audio-routes.test.mjs`（harness 体例）。
> 冻结日期 2026-09-13。**owner 边界**：两个新测试文件、契约 §12 的四类回写、§15 裁决的登记、验收清单。

---

## 1. 一句话定位

**把 TTS 批次所有「已冻结但尚未被任何测试守住」的行为，翻译成两个可编译、可落地的测试文件，各含至少一条「不实现就必失败」的断言；再把契约 §12 的四类文档修订与 §15 的裁决逐字写死到目标文件。** 本篇是给实现者交底：测试先写（红），实现落地后转绿。

---

## 2. 交付物与依赖符号

| 交付物 | 形状 | 归属 |
|---|---|---|
| `apps/server/test/tts-routes.test.mjs` | **NEW**，`node:test`，A–F 六组 | 本篇 |
| `apps/web/test/voice-engine.test.mjs` | **NEW**，`node:test` + jiti 直导 TS | 本篇 |
| 回写清单（§6） | §12 四类修订 + §15 六条裁决 + `AGENTS.md` / `package.json` 结论 | 本篇 |
| 手测清单（§7） | 10 步「预期观察」 | 本篇 |

**引用的真实符号**（由兄弟文档定义，本篇只引用）：

| 符号 | 来源 | 冻结依据 |
|---|---|---|
| `createTtsRouter(repoRoot, getActiveStore)` | `apps/server/src/routes/tts.ts`（NEW，01） | 契约 §4.1 |
| `hashOf(model, voice, languageType, text)` | 同上 | 契约 §3.2/§4.1 |
| `parseEmoPages(raw, {final})` / `clampPageIndex` | `apps/web/src/components/overlay/dialogue-pages.ts`（NEW，03） | 03 已确认签名 |
| `playVoice` / `stopVoice` / `isVoicing` / `voiceDebugState` | `apps/web/src/lib/audio.ts`（NEW，02） | 契约 §5；02 §12.2 |
| `audioDebugState()` | 既有 `apps/web/src/lib/audio.ts:871-882` | 契约 §5（形状冻结） |
| `/api/characters` 的 `voice?` | 既有路由 `world.ts:536-562`（01 扩展） | 契约 §15.5 |
| `LocalWorldStore` | `@airp/shared` | 既有 |

> **`hashOf` MUST `export`**（契约 **§15.7 已裁定**，非待拍板）：测试直接 `import { hashOf } from ...` 断言缓存键公式，**不**本地镜像（镜像必漂移）。见 §12.1。

---

## 3. 行为契约逐步（测试要守的每一步）

> 「漏了会怎样」正是非空性断言（契约 §8）的来源。

### 3.1 服务端 `POST /api/tts`（契约 §4.2）

| 步 | 行为 | 漏了会怎样 → 测试 |
|---|---|---|
| 1 | `getActiveStore()` 为 `null` → **400 `no_active_world`** | 缓存根未知，写盘落随机 CWD → **D2** |
| 2 | `text` 非字符串 / `trim()===''` → **400 `invalid_argument`** | 空文本合成怪音频污染缓存 → **F1** |
| 3 | `text.length > 500` → 截断至 500 且 `truncated:true` | 超长被上游拒 → 整页无声 → **B1/B2** |
| 4 | `voice` 未知/畸形 → 回落默认 + warn | typo 让上游 400 → 整页无声 → **C3**（已扩为 alias/裸 id/含空格/typo 四例）|
| 5 | `language` 短码 → `language_type`（§4.4） | 透传 `ja` → 上游非法枚举 400 → **C1/C2** |
| 6 | 命中缓存 → 直接返回 `cached:true`，**不联网** | 每页重复合成，延迟/成本飙升 → **A2（非空性核心）** |
| 7 | 未命中 → `synthesise` → **原子写** `<cacheDir>/<hash>.wav` | 半截 wav 被当命中，decode 永久失败 → **A3** |
| 8 | 返回 `{ok,url,cached,characters,truncated}` | 形状漂移 → 前端读不到字段 |

### 3.2 服务端 `GET /api/tts/audio/:file`（契约 §2.2）

| 行为 | 漏了会怎样 | 测试 |
|---|---|---|
| `:file` 不匹配 `^[a-f0-9]{20}\.wav$` → **403** | 任意路径被解析 → 目录穿越 | **E1/E3** |
| `path.resolve` + `startsWith` + `realpathSync` 双重校验 | 符号链接逃逸读任意文件 | **E4** |
| 命中 → `sendFile` + `immutable` | 缓存头缺失 → 每次重下 | **E2** |
| 形状合法但文件不存在 → **404** | 前端收 HTML 报错页 | **E5** |

### 3.3 前端引擎（契约 §5 / 02 §12.2）

| 行为 | 漏了会怎样 | 测试 |
|---|---|---|
| 无 ctx 时 `playVoice` **不抛**且**记录 url** | 未 unlock 时崩 | **V1/V6** |
| `stopVoice` 幂等、清 `voiceUrl` | 残留、二次 stop 抛错 | **V2** |
| `playVoice` 新调用打断旧（最后胜出） | 两句叠放 | **V3** |
| `audioDebugState()` 仍严格四键 | 诊断面被污染（§5 冻结） | **V4** |
| `isVoicing()` 无 ctx 恒 `false` | 遮罩误判「在播」而不打字 | **V6** |

### 3.4 遮罩分页（契约 §6 / §6.2 实测警告 / 广播 #1）

| 行为 | 漏了会怎样 | 测试 |
|---|---|---|
| 无 `\n` → **整段 = 1 页**（非空、不报错） | 真实模型不听话时遮罩空白/退化异常 | **P1（非空性核心）** |
| 空输入 → 0 页 | 空页污染队列 | **P2** |
| 多 `\n` → 一页一行且全封口；空行丢弃 | 页数错 | **P3 / P4** |
| `[emo:]` 继承上一页 + 标签剥离 | 表情错乱 | **P5** |
| 未闭合 `[emo` 前缀冻结保留（`EMO_PREFIX_GUARD`） | 半个标签泄漏成文字 | **P6** |
| 未封口末页（`final:false`）首行封口、末行未封口 | 末页提前可翻 | **P7** |
| `clampPageIndex` 边界 | 越界读 `undefined` | **P8** |
| 韵律表未被删（`charDelay`） | 失去角色节奏 | **P9** |
| `GREETING_LINE` 冻结文案 | 引导语漂移 | **P10** |
| 超长单行不截断 | 内容丢失 | **P11** |
| **不按标点自动切页** | 与模型意图打架 | **P12/P8b**（负向） |

### 3.5 首句解锁时序（契约 §11 实测裁定 / 广播 #3）

| 行为 | 漏了会怎样 | 验证 |
|---|---|---|
| 点开遮罩的**首个手势**补 `void unlock()` | 全仓 `unlock()` 只被 `Canvas.tsx:191` / `DiceRoller.tsx:219,265` 调用；`RightSidebar`（`App.tsx:585`）打开的遮罩**不经 Canvas** → ctx 未 running → `playVoice` drop → 引导语+首句**静默无声** | **手测 §7 第 3 步**（node 单测无法覆盖手势/ctx.state，见 §5.3） |

---

## 4. 服务端测试骨架：`apps/server/test/tts-routes.test.mjs`（NEW）

### 4.0 运行契约

- 跑 **`dist/`**（`AGENTS.md §6.5`）：**先 `pnpm build`**，import `../dist/routes/tts.js`（与 `audio-routes.test.mjs:2` 同款）。
- `node:test` 同文件默认**串行** → `process.env` 读写安全；harness 仍显式保存/还原。

### 4.1 完整骨架

```js
// TTS batch (docs/tts) — /api/tts + /api/tts/audio server assertions.
// Runs against built dist (AGENTS.md §6.5): `pnpm build` first.
// Groups A (cache/atomic write), B (truncation), C (voice+language mapping),
// D (unconfigured / no world), E (anti-traversal), F (empty text).
//
// DashScope is REPLACED by a local stub: 01's readTtsConfig() reads the 5 env
// vars PER REQUEST (not at module top level), so setting AIRP_TTS_BASE_URL after
// import redirects all synthesis to the stub.
import assert from 'node:assert/strict';
import { createHash } from 'node:crypto';
import fs from 'node:fs/promises';
import http from 'node:http';
import os from 'node:os';
import path from 'node:path';
import express from 'express';
import { test } from 'node:test';
import { LocalWorldStore } from '@airp/shared';
import { createTtsRouter } from '../dist/routes/tts.js';

const MODEL = 'qwen3-tts-flash-2025-11-27';
const DEFAULT_VOICE = 'Cherry';
const WAV = Buffer.from('RIFF\x24\x00\x00\x00WAVEfmt '); // a distinct fake blob

/** Mirror of contract §3.2. Import 01's hashOf instead once it is exported. */
const HASH_OF = (model, voice, languageType, text) =>
  createHash('sha256').update(`${model}|${voice}|${languageType}|${text}`).digest('hex').slice(0, 20);

/** Stub DashScope: captures POST bodies, serves a fake OSS blob. */
async function dashStub({ status = 200 } = {}) {
  const calls = [];
  const server = http.createServer((req, res) => {
    if (req.url.includes('/services/aigc/multimodal-generation/generation')) {
      let raw = '';
      req.on('data', (c) => (raw += c));
      req.on('end', () => {
        calls.push({ auth: req.headers.authorization, body: JSON.parse(raw) });
        res.setHeader('content-type', 'application/json');
        if (status !== 200) {
          res.statusCode = status;
          res.end(JSON.stringify({ status_code: status, code: 'InvalidApiKey', message: 'stub' }));
          return;
        }
        res.end(JSON.stringify({
          status_code: 200, request_id: 'stub',
          output: { audio: { id: 'a', url: `http://127.0.0.1:${server.address().port}/blob/out.wav`, data: '', expires_at: 0 } },
          usage: { characters: 1 },
        }));
      });
      return;
    }
    res.setHeader('content-type', 'audio/wav');
    res.end(WAV); // the fake OSS blob
  });
  await new Promise((r) => server.listen(0, '127.0.0.1', r));
  return { base: `http://127.0.0.1:${server.address().port}`, calls, close: () => new Promise((r) => server.close(r)) };
}

/** Temp repo + temp world + express(json) + createTtsRouter + stub + env swap. */
async function harness({ stub = true } = {}) {
  const savedEnv = { ...process.env };
  const repo = await fs.mkdtemp(path.join(os.tmpdir(), 'airp-tts-repo-'));
  const world = path.join(repo, 'world-root');
  const store = new LocalWorldStore(world);
  await store.writeFile('world.json', JSON.stringify({
    id: 'p', name: 'P', description: '', author: '', genre: 'test', createdAt: '', updatedAt: '',
  }));

  const ds = stub ? await dashStub() : null;
  process.env.AIRP_TTS_BASE_URL = ds ? ds.base : 'http://127.0.0.1:1'; // unreachable when stub:false
  process.env.DASHSCOPE_API_KEY = 'test-key';
  process.env.AIRP_TTS_MODEL = MODEL;
  process.env.AIRP_TTS_DEFAULT_VOICE = DEFAULT_VOICE;

  const app = express();
  app.use(express.json());
  app.use('/api', createTtsRouter(repo, () => store));
  const server = http.createServer(app);
  await new Promise((r) => server.listen(0, '127.0.0.1', r));
  const base = `http://127.0.0.1:${server.address().port}/api`;
  const cacheDir = path.join(world, '.airpworld', 'tts-cache');

  return {
    base, repo, world, store, ds, cacheDir,
    close: async () => {
      server.close();
      if (ds) await ds.close();
      try { store.close(); } catch {}
      await fs.rm(repo, { recursive: true, force: true });
      for (const k of ['AIRP_TTS_BASE_URL', 'DASHSCOPE_API_KEY', 'AIRP_TTS_MODEL', 'AIRP_TTS_DEFAULT_VOICE']) {
        if (savedEnv[k] === undefined) delete process.env[k]; else process.env[k] = savedEnv[k];
      }
    },
  };
}

const post = (base, body) => fetch(`${base}/tts`, {
  method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify(body),
});

// ── A 组：缓存命中（非空性核心） + 原子写 ──

test('A1 首次合成 → 200 / cached:false / url 形状合法 + 上游恰好 1 次', async () => {
  const h = await harness();
  const j = await (await post(h.base, { text: 'おはよう' })).json();
  assert.equal(j.ok, true);
  assert.equal(j.cached, false);
  assert.equal(j.truncated, false);
  assert.match(j.url, /^\/api\/tts\/audio\/[a-f0-9]{20}\.wav$/);
  assert.equal(h.ds.calls.length, 1);
  await h.close();
});

test('A2 同文本二次 → cached:true 且上游调用数不变（不联网）', async () => {
  const h = await harness();
  await post(h.base, { text: 'same line' });
  const j2 = await (await post(h.base, { text: 'same line' })).json();
  assert.equal(j2.cached, true);
  // NON-EMPTINESS: no cache → 2 calls here → red.
  assert.equal(h.ds.calls.length, 1, 'cached hit must not re-synthesise');
  await h.close();
});

test('A3 缓存文件字节完整（原子写未留半截） + 无 .tmp 残留', async () => {
  const h = await harness();
  const j = await (await post(h.base, { text: 'blob check' })).json();
  const hash = HASH_OF(MODEL, DEFAULT_VOICE, 'Auto', 'blob check');
  assert.equal(j.url, `/api/tts/audio/${hash}.wav`);
  assert.deepEqual(await fs.readFile(path.join(h.cacheDir, `${hash}.wav`)), WAV);
  assert.deepEqual((await fs.readdir(h.cacheDir)).filter((f) => f.endsWith('.tmp')), []);
  await h.close();
});

// ── B 组：截断 ──

test('B1 >500 字符 → truncated:true / characters===500 / 上游收到截断文本', async () => {
  const h = await harness();
  const long = 'あ'.repeat(600);
  const j = await (await post(h.base, { text: long })).json();
  assert.equal(j.truncated, true);
  assert.equal(j.characters, 500);
  assert.equal(h.ds.calls[0].body.input.text.length, 500);
  await h.close();
});

test('B2 截断后 hash 按截断文本算（缓存键不因原长漂移）', async () => {
  const h = await harness();
  const long = 'あ'.repeat(600);
  const j = await (await post(h.base, { text: long })).json();
  assert.equal(j.url, `/api/tts/audio/${HASH_OF(MODEL, DEFAULT_VOICE, 'Auto', long.slice(0, 500))}.wav`);
  await h.close();
});

// ── C 组：voice + language_type 映射 ──

test('C1 language=ja → input.language_type==="Japanese"', async () => {
  const h = await harness();
  await post(h.base, { text: 'a', language: 'ja' });
  assert.equal(h.ds.calls[0].body.input.language_type, 'Japanese');
  await h.close();
});

test('C2 en → "English"；未列出短码 / 缺省 → "Auto"', async () => {
  const h = await harness();
  await post(h.base, { text: 'x', language: 'en' });
  assert.equal(h.ds.calls[0].body.input.language_type, 'English');
  await post(h.base, { text: 'y', language: 'fr' });
  assert.equal(h.ds.calls[1].body.input.language_type, 'Auto');
  await post(h.base, { text: 'z' });
  assert.equal(h.ds.calls[2].body.input.language_type, 'Auto');
  await h.close();
});

test('C3 请求 voice 透传；畸形 voice 回落默认且不 400（§15.1 正则不变）', async () => {
  const h = await harness();
  await post(h.base, { text: 'v1', voice: 'Eric' });
  assert.equal(h.ds.calls[0].body.input.voice, 'Eric');
  const r = await post(h.base, { text: 'v2', voice: '../evil' });
  assert.equal(r.status, 200);
  assert.equal(h.ds.calls[1].body.input.voice, DEFAULT_VOICE);
  // §15.1: 含空格的拼接 ID 非法 → 回落默认（Eldric Sage 不是音色）
  await post(h.base, { text: 'v3', voice: 'Eldric Sage' });
  assert.equal(h.ds.calls[2].body.input.voice, DEFAULT_VOICE);
  await h.close();
});

// ── D 组：未配置 key / 无世界 ──

test('D1 无 DASHSCOPE_API_KEY → 503 tts_unconfigured 且不发网络请求', async () => {
  const h = await harness();
  delete process.env.DASHSCOPE_API_KEY;
  const r = await post(h.base, { text: 'hi' });
  const j = await r.json();
  assert.equal(r.status, 503);
  assert.equal(j.ok, false);
  assert.equal(j.code, 'tts_unconfigured');
  assert.equal(h.ds.calls.length, 0);
  await h.close();
});

test('D2 无活跃世界 → 400 no_active_world', async () => {
  const repo = await fs.mkdtemp(path.join(os.tmpdir(), 'airp-tts-noworld-'));
  const app = express();
  app.use(express.json());
  app.use('/api', createTtsRouter(repo, () => null));
  const server = http.createServer(app);
  await new Promise((r) => server.listen(0, '127.0.0.1', r));
  const r = await post(`http://127.0.0.1:${server.address().port}/api`, { text: 'hi' });
  const j = await r.json();
  assert.equal(r.status, 400);
  assert.equal(j.code, 'no_active_world');
  server.close();
  await fs.rm(repo, { recursive: true, force: true });
});

// ── E 组：/api/tts/audio 反穿越 ──

test('E1 :file 形状非法 → 403', async () => {
  const h = await harness();
  for (const bad of ['notahash.wav', `${'g'.repeat(20)}.wav`, `${'a'.repeat(19)}.wav`, 'x.mp3']) {
    assert.equal((await fetch(`${h.base}/tts/audio/${encodeURIComponent(bad)}`)).status, 403, bad);
  }
  await h.close();
});

test('E2 形状合法且存在 → 200 + immutable', async () => {
  const h = await harness();
  const hash = HASH_OF(MODEL, DEFAULT_VOICE, 'Auto', 'hit');
  await fs.mkdir(h.cacheDir, { recursive: true });
  await fs.writeFile(path.join(h.cacheDir, `${hash}.wav`), WAV);
  const r = await fetch(`${h.base}/tts/audio/${hash}.wav`);
  assert.equal(r.status, 200);
  assert.match(r.headers.get('cache-control') ?? '', /immutable/);
  await h.close();
});

test('E3 .. 逃逸（编码进 :file）→ 403', async () => {
  const h = await harness();
  assert.equal((await fetch(`${h.base}/tts/audio/..%2f..%2fetc%2fpasswd`)).status, 403);
  await h.close();
});

test('E4 符号链接逃逸 → 403（POSIX）', { skip: process.platform === 'win32' ? 'posix only' : false }, async () => {
  const h = await harness();
  const hash = HASH_OF(MODEL, DEFAULT_VOICE, 'Auto', 'link');
  await fs.mkdir(h.cacheDir, { recursive: true });
  await fs.symlink('/etc/passwd', path.join(h.cacheDir, `${hash}.wav`));
  assert.equal((await fetch(`${h.base}/tts/audio/${hash}.wav`)).status, 403);
  await h.close();
});

test('E5 形状合法但文件缺失 → 404', async () => {
  const h = await harness();
  const hash = HASH_OF(MODEL, DEFAULT_VOICE, 'Auto', 'missing');
  assert.equal((await fetch(`${h.base}/tts/audio/${hash}.wav`)).status, 404);
  await h.close();
});

// ── F 组：空文本 ──

test('F1 text 缺失 / 非字符串 / 空串 / 纯空白 → 400 invalid_argument', async () => {
  const h = await harness();
  for (const body of [{}, { text: 42 }, { text: '' }, { text: '   ' }]) {
    const r = await post(h.base, body);
    assert.equal(r.status, 400, JSON.stringify(body));
    assert.equal((await r.json()).code, 'invalid_argument');
  }
  assert.equal(h.ds.calls.length, 0);
  await h.close();
});
```

### 4.2 mock 方案（为什么可行）

- **注入点 = env（请求时读）**：`createTtsRouter` 签名冻结为 `(repoRoot, getActiveStore)`，无 http client 可注入，唯一合法注入点是 `AIRP_TTS_BASE_URL`。**01 已确认**：5 个 env 由 `readTtsConfig()` 在**每次 POST handler 内**现读 `process.env`，**不在模块顶层读一次** → harness 在 import 后 set env 生效。这是对 01 的**硬要求**（写进其文档；若改成顶层读，A/C/D 组静默失效）。
- **对 01 的第二条硬要求**：`synthesise` 下载 `output.audio.url` 时**必须原样接受 stub 给的 `http://127.0.0.1`**，不得强制 https / 校验域名，否则 stub 无法供 blob。
- **缓存不联网的证明**：`ds.calls.length` 是 stub 侧计数，与 01 内部实现无关，是最强的外部可观察证据（A2）。

### 4.3 非空性断言（MUST，对照契约 §8）

| 用例 | §8 降级矩阵行 | 「不实现就失败」机理 |
|---|---|---|
| **A2** | 缓存命中 | 不查缓存 → 上游调 2 次 → `calls.length===1` 红 |
| **A3** | 原子写 | 非原子写 → `.tmp` 残留 / 半截字节 → 红 |
| **B1/B2** | 超长截断 | 不截断 → `characters!==500` / hash 不符 → 红 |
| **C1/C2** | 语言映射 | 透传短码 → `language_type==='ja'` ≠ `'Japanese'` → 红 |
| **C3** | voice 形状校验 | 不校验 → 上游收到畸形 voice（stub 断言 ≠ 默认）→ 红 |
| **D1** | 未配置 key → 503 | 静默/500 → 状态码或 `code` 红 |
| **D2** | 无活跃世界 → 400 | 无守卫 → 落随机 CWD / 500 → 红 |
| **E1/E3/E4** | `:file` 非法/逃逸 → 403 | 只拼字符串 → 200/404 → 红 |
| **F1** | `text` 空 → 400 | 无校验 → 上游 400 / 污染缓存 → 红 |


---

## 5. 前端测试骨架：`apps/web/test/voice-engine.test.mjs`（NEW）

仿 `apps/web/test/audio-engine.test.mjs:8-19`：jiti 直导 TS（`apps/web/test/` → 仓库根 = 上溯 3 级）。只测**请求态**（node 无 Web Audio，`initAudio()` 返回 `null`），与 E2 体例一致。

02 已确认：导出 `voiceDebugState(): { url: string | null }`；无 ctx 时 `playVoice(url)` **先记录 `voiceUrl = url` 再早退**。

```js
// Voice channel + pagination request-state assertions (TTS batch, docs/tts).
// Web Audio is unavailable in node → initAudio() returns null, so V-group ONLY
// asserts the request state the engine recorded, never ctx/decode/mixing.
// P-group drives the pure pagination fn (docs/tts/03) — the "no newline = 1
// page" degenerate form is the batch's key non-emptiness assertion (§6.2 warning).
// Run: node --test apps/web/test/voice-engine.test.mjs
import assert from 'node:assert/strict';
import { test } from 'node:test';

let audio = null;
let pages = null;
try {
  const { createJiti } = await import('../../../vendor/pi-rp/node_modules/jiti/lib/jiti.mjs');
  const jiti = createJiti(import.meta.url, { moduleCache: false, tryNative: true });
  audio = await jiti.import('../src/lib/audio.ts');
  pages = await jiti.import('../src/components/overlay/dialogue-pages.ts');
} catch (err) {
  console.error('jiti/bootstrap unavailable, skipping engine group:', err?.message ?? err);
}
const skip = audio && pages ? false : 'jiti or pi-rp submodule unavailable';

// ── V 组：语音通道（契约 §5 / 02 §12.2） ──

test('V1 playVoice 无 ctx 不抛，且记录 url；isVoicing() 恒 false', { skip }, () => {
  assert.equal(audio.initAudio(), null);
  const url = '/api/tts/audio/0123456789abcdef0123.wav';
  assert.doesNotThrow(() => audio.playVoice(url));
  assert.equal(audio.voiceDebugState().url, url);
  assert.equal(audio.isVoicing(), false);
});

test('V2 stopVoice 清空 url 且幂等（连调两次不抛）', { skip }, () => {
  audio.playVoice('/api/tts/audio/aaaaaaaaaaaaaaaaaaaa.wav');
  assert.doesNotThrow(() => audio.stopVoice());
  assert.equal(audio.voiceDebugState().url, null);
  assert.doesNotThrow(() => audio.stopVoice());
  assert.equal(audio.voiceDebugState().url, null);
});

test('V3 playVoice 新调用打断旧调用（最后胜出）', { skip }, () => {
  audio.playVoice('/api/tts/audio/aaaaaaaaaaaaaaaaaaaa.wav');
  audio.playVoice('/api/tts/audio/bbbbbbbbbbbbbbbbbbbb.wav');
  assert.equal(audio.voiceDebugState().url, '/api/tts/audio/bbbbbbbbbbbbbbbbbbbb.wav');
});

test('V4 audioDebugState 形状仍严格四键（契约 §5 冻结）', { skip }, () => {
  assert.deepEqual(Object.keys(audio.audioDebugState()).sort(), ['ambient', 'bgm', 'loaded', 'theme']);
});

test('V5 四个新导出均为 function', { skip }, () => {
  for (const name of ['playVoice', 'stopVoice', 'isVoicing', 'voiceDebugState']) {
    assert.equal(typeof audio[name], 'function', `${name} must be exported`);
  }
});

test('V6 playVoice("") 不抛；无 ctx 下 isVoicing 恒 false', { skip }, () => {
  assert.doesNotThrow(() => audio.playVoice(''));
  assert.equal(audio.isVoicing(), false);
  audio.stopVoice();
});

// ── P 组：分页纯函数（契约 §6 / §6.2 实测警告） ──

test('P1 NON-EMPTINESS: 无换行 → 整段 = 1 页（不空白、不报错）', { skip }, () => {
  const r = pages.parseEmoPages('done', { final: true });
  assert.equal(r.pages.length, 1);
  assert.equal(r.pages[0].text, 'done');
  assert.equal(r.pages[0].sealed, true);
});

test('P2 空输入 → 0 页（空页丢弃）', { skip }, () => {
  assert.equal(pages.parseEmoPages('', { final: true }).pages.length, 0);
});

test('P3 多行 → 一页一行且全部封口', { skip }, () => {
  const r = pages.parseEmoPages('a\nb\nc', { final: true });
  assert.deepEqual(r.pages.map((p) => p.text), ['a', 'b', 'c']);
  assert.ok(r.pages.every((p) => p.sealed));
});

test('P4 空行丢弃（连续 \\n 不产生空页）', { skip }, () => {
  assert.deepEqual(pages.parseEmoPages('a\n\n\nb', { final: true }).pages.map((p) => p.text), ['a', 'b']);
});

test('P5 [emo:] 继承上一页 + 标签剥离', { skip }, () => {
  const r = pages.parseEmoPages('[emo: smile]a\nb', { final: true });
  assert.equal(r.pages[0].emo, 'smile');
  assert.equal(r.pages[1].emo, 'smile');
  assert.equal(r.pages[0].text, 'a');
});

test('P6 未闭合 [emo 前缀冻结保留（EMO_PREFIX_GUARD）', { skip }, () => {
  const r = pages.parseEmoPages('[emo: smi', { final: false });
  assert.equal(r.tailOpen, true);
  assert.equal(r.pages[0].text, '');
  assert.equal(r.pages[0].sealed, false);
});

test('P7 未封口末页（final:false）：首行封口、末行未封口', { skip }, () => {
  const r = pages.parseEmoPages('a\nb', { final: false });
  assert.equal(r.pages[0].sealed, true);
  assert.equal(r.pages[1].sealed, false);
});

test('P8 clampPageIndex 边界', { skip }, () => {
  assert.equal(pages.clampPageIndex(-1, 3), 0);
  assert.equal(pages.clampPageIndex(5, 3), 2);
  assert.equal(pages.clampPageIndex(0, 0), 0);
});

test('P8b 负向：不按标点自动切页（分页单位只有 \\n）', { skip }, () => {
  assert.equal(pages.parseEmoPages('one. two. three.', { final: true }).pages.length, 1);
});

test('P9 韵律表未被删（charDelay）', { skip }, () => {
  assert.equal(pages.charDelay(','), 300);
  assert.equal(pages.charDelay('。'), 150);
  assert.equal(pages.charDelay('a'), 45);
});

test('P10 GREETING_LINE 冻结文案（ja/en）', { skip }, () => {
  assert.equal(pages.GREETING_LINE.ja.text, 'どうしたの？');
  assert.equal(pages.GREETING_LINE.en.text, 'Something on your mind?');
});

test('P11 超长单行不被截断（页文本原样保留）', { skip }, () => {
  const r = pages.parseEmoPages('x'.repeat(2000), { final: true });
  assert.equal(r.pages.length, 1);
  assert.equal(r.pages[0].text.length, 2000);
});
```

### 5.1 P 组归属说明

契约 §15.16 裁定：P 组**只写在 `06` 的 `apps/web/test/voice-engine.test.mjs`**（`03 §10.1` 声明的第三文件 `dialogue-pages.test.mjs` 作废）。编号以 **`03 §10.1` 的 P1–P11 为准**（本节与之一一对齐）；03 测组件行为，06 测纯函数退化形态。

### 5.2 前端非空性断言（MUST）

| 用例 | 「不实现就失败」机理 |
|---|---|
| **P1** | 分页实现若假设「必有换行」而返回空/异常 → `pages.length===1` 红。这是本批最大风险点（§6.2：无真实模型多行输出被证实） |
| **P11** | 实现若为「保演出」而截断长行 → `text.length===2000` 红 |
| **P6** | 实现若加「按标点切页」兜底 → `pages.length===1` 红 |
| **V4** | 实现若把 `url` 混进 `audioDebugState()` 四键 → `deepEqual(keys)` 红 |
| **V1** | 无 ctx 时若 `playVoice` 直接 throw → `doesNotThrow` 红 |

### 5.3 单测覆盖不到、只能靠手测的（诚实边界）

- **解锁时序**：node 无 `AudioContext`，`ctx.state !== 'running'` 的 drop 语义不可测（§3.5）。**只能手测 §7 第 3 步**。
- **真实音频下载/解码**：需要真 key 与 Web Audio。
- **`voiceToken` 竞态**：无 ctx 下不可观测（02 已确认）。

---

## 6. 文档回写清单（逐条：位置 / 原断言 / 逐字改成什么）

> 纪律（`AGENTS.md §6.3`）：**文档里已被推翻的说法直接改掉，不要另起一段解释**；每处修订 **MUST 与代码同 commit**（契约 §12）。行号为 2026-09-13 快照，漂移时按引用文本重定位。

### R1 — `docs/ui/doc-06-演出与交互设计.md §3.2`

**位置**：`:154`（标题）、`:161`（逐字流式）、`:162`（玩家输入）、`:166`（全脸眨眼/口型）。

**`:154`** 原 → 改：

```markdown
### 3.2 遮罩的解剖（2026-09-10 更新：立绘 + 角色 agent）
```

```markdown
### 3.2 遮罩的解剖（2026-09-13 更新：一句一页 + 语音朗读）
```

**`:161`** 原 → 改：

```markdown
| **逐字流式** | **节奏=角色性格的主秀场**（老周慢半拍在遮罩里被放大感受——同一句话节奏天差地别） |
```

```markdown
| **一句一页 + 页内逐字** | 角色每句独占一行，**一行 = 一页**；页内仍逐字流式（节奏=角色性格的主秀场）。点击「纸面」：未出完则快进本页，出完则翻下一页（`docs/tts/00 §6`）。**无换行输出退化为单页**（§6.2） |
```

**`:162`** 原 → 改：

```markdown
| **玩家输入** | 纸条通道内嵌（对话框底部一行输入）——对话形态=角色流式+玩家自由输入 |
```

```markdown
| **玩家输入** | 纸条通道内嵌（对话框底部一行输入）——**仅当本轮所有页翻尽且演出终结时启用**；演出期间 disabled（`docs/tts/00 §6.6`） |
```

**`:166`（保持舍弃，仅显式登记）** 原 → 改：

```markdown
| **全脸眨眼/口型** | 先舍（全套 6 情绪差分立绘 + 呼吸动效已足够支撑丰富情绪张力） |
```

```markdown
| **全脸眨眼/口型** | 先舍（全套 6 情绪差分立绘 + 呼吸动效已足够支撑丰富情绪张力）；**语音朗读（`docs/tts/00`）不含口型同步**，口型登记为后续 |
```

**依据**：契约 §12 第 1/2/5 行。

### R2 — `docs/gameplay/doc-19-多模态与游戏性交互升级.md §2.3`（`:124`）

原（Layer 2 末句）→ 改：

```markdown
角色台词句首的 `[emo: tag]` 只触发与主轨正交、播完即止的瞬时 stinger，绝不改 BGM 主轨。
```

```markdown
角色台词句首的 `[emo: tag]` 只触发与主轨正交、播完即止的瞬时 stinger，绝不改 BGM 主轨。**自 2026-09-13 起，角色直聊遮罩的页面带语音朗读（`docs/tts/00`）：有语音的页不再播 stinger**（人声与音效叠加会打架）；无语音时（TTS 未配置 / 失败）保留 stinger 作为情绪提示。
```

**依据**：契约 §12 第 3 行、§6.5。

### R3 — `docs/development/doc-07-AIRP黑客松作战计划.md`（`:22` / `:65` / `:67`）

**`:22`** 原 → 改：

```markdown
当前冲刺先评审**五个世界的整体体验骨架**，再逐个搭一条 6–10 分钟短 Demo；不以完整后日谈、全结局或下一章作为完成标准。TTS、GPT Live L1、Voice Agent 与 Voice Agent 操作光标 / 角色原点都保留在工作池，但不作为世界内容成立的前置条件。
```

```markdown
当前冲刺先评审**五个世界的整体体验骨架**，再逐个搭一条 6–10 分钟短 Demo；不以完整后日谈、全结局或下一章作为完成标准。TTS 已于 2026-09-13 落地（角色直聊语音朗读，`docs/tts/00`）；GPT Live L1、Voice Agent 与 Voice Agent 操作光标 / 角色原点保留在工作池，不作为世界内容成立的前置条件。（TTS 落地后**仍不是**世界成立的前置条件——它服务演出，不阻塞关卡。）
```

**`:65`** 原 → 改：

```markdown
| **P2：世界骨架之后再评估** | 接 TTS、GPT Live L1、Voice Agent、Voice Agent 操作光标与角色原点 | Writer / Character 通路稳定，文字与物件关卡已能完整跑通 |
```

```markdown
| **P2：世界骨架之后再评估** | ~~接 TTS~~（**2026-09-13 已落地**，见 `docs/tts/00`）、GPT Live L1、Voice Agent、Voice Agent 操作光标与角色原点 | Writer / Character 通路稳定，文字与物件关卡已能完整跑通 |
```

**`:67`** 原 → 改：

```markdown
暂缓语音不是否定多模态，而是避免用语音掩盖世界关卡尚未连通。当前验收先看：……
```

```markdown
TTS 已落地；GPT Live / Voice Agent 暂缓不是否定多模态，而是避免用语音掩盖世界关卡尚未连通。当前验收先看：……
```

**依据**：契约 §12 第 4 行。

### R4 — `docs/gameplay/worlds/doc-24-五世界可玩Demo体验设计.md`（`:32`）

原（§0.1 列表项）→ 改：

```markdown
- TTS、GPT Live、Voice Agent 或 launcher 成为世界成立的前置条件。
```

```markdown
- TTS、GPT Live、Voice Agent 或 launcher 成为世界成立的前置条件。（**TTS 已于 2026-09-13 落地**——角色直聊语音朗读，见 `docs/tts/00`；本条约束不变：TTS 是演出增强，任何世界 Demo 不因缺语音而不成立。）
```

**依据**：契约 §12 第 4 行。注：doc-24 本条是「**不作为前置条件**」约束而非排期，故只做澄清式回写。

### R5 — `AGENTS.md §2` 与 §4 加 `docs/tts/` 条目

**结论：加。** `§2` 只概述 `docs/`（`:98`），真登记子目录的是 **§4 导航表**（`:164-167` 已有 `docs/tools/` / `docs/hooks/` / `docs/wiring/`）。TTS 与三者同构（冻结契约 `00` + 分篇 + 回写），必须登记。

**§2 目录块**（`:98` 后追一行）：

```text
docs/                   # 设计文档（真相源）；docs/tools/ 是 B1 工具面设计 + 评审报告
```

```text
docs/                   # 设计文档（真相源）；docs/tools/ 是 B1 工具面设计 + 评审报告
docs/tts/               # 角色语音（TTS）批次真相源：00 是冻结契约（端点/请求响应体/页语义/env），01–06 分篇
```

**§4 导航表**（`:167` `docs/wiring/` 行后追一行）：

```markdown
| `docs/tts/` | **角色语音 TTS 批次真相源**：`00-共同上下文.md` 是冻结契约（`POST /api/tts` 请求/响应体、缓存 key、角色 `voice` frontmatter、前端 `playVoice`/`stopVoice`/`isVoicing`、页语义、env），`01`–`06` 逐模块设计。**动 `routes/tts.ts` / `lib/audio.ts` 语音面 / `CharacterModal` 分页前必读** |
```

**依据**：`AGENTS.md §6.3`（`:260` 要求架构变动同步 §2 §3）。

### R6 — `package.json` 是否加 `check:tts`？

**结论：不加 `check:tts`。可选加 `test:tts`。**

1. 本仓 `check:*` 全是**跨端契约门禁**（`check:ws` diff 两端帧、`check:bodies` diff 前端 body 与冻结表、`check:skills` 校验语料）。TTS 两测试是普通 `node:test` 单测，**不 diff 两端、不属门禁语义**；命名为 `check:tts` 会误导。
2. 本仓测试历来裸跑 `node --test <file>`（`audio-engine.test.mjs:4`、`character-preset-parity.test.mjs`），**无 `pnpm test`**。
3. 若团队要一键：加 `test:tts`（非 `check:tts`）：

   ```json
   "test:tts": "node --test apps/server/test/tts-routes.test.mjs apps/web/test/voice-engine.test.mjs"
   ```

   **仍须先 `pnpm build`**（服务端测 dist）。本批默认不加（避免诱导跑全量）。

**依据**：契约 §11；`package.json:6-22`。

### R7 — 契约 §13 锚点漂移（03 点名要求）

03 把 `parseEmoTag` / `parseEmoLines` / `charDelay` / `EMO_PREFIX_GUARD` 搬到新文件 `apps/web/src/components/overlay/dialogue-pages.ts`。契约 §13（`:464`、`:462` 等）引用 `CharacterModal.tsx:65-118` / `:59,97-107` 的行 MUST 按符号重定位到新文件。**契约正文改动由主 agent 裁决，本清单只登记 03 的请求。**

### R8 — 契约 §15 裁决的登记（广播 #4）

§15 是**主 agent 对子文档冲突的裁决**，本身即契约，无需回写外部文档。但下列裁决触及**内容文件/前端行为**，实现者 MUST 落实（登记在此，避免遗漏）：

| §15 | 裁决 | 落点 | 验证 |
|---|---|---|---|
| 15.1 | 【已被 `07` 推翻】原判「`Eldric Sage` 非法」为假；现放宽正则 + `resolveVoice` 调色板 | `routes/tts.ts`（01）+ `packages/shared/src/rules/voices.ts`（07）；测试 C3 | C3 + `check:voices` + `voices.test.mjs` |
| 15.2 | 04 音色清单**降级为候选**；安全集合 8 个（`Cherry/Serena/Ethan/Chelsie/Momo/Eldric/Sage/Radio Gol`）；落地前每个 ID 跑 curl 实测 | 04 文档 + README | 手测 §7 第 2 步（有声） |
| 15.4 | TTS 的 `language` **MUST NOT** 用 `App.tsx:39` 浏览器语言猜测；缺 locale 时前端传 `'en'` | 05 / 03 | 手测 §7（日文世界 `ja`、英文世界 `en`） |
| 15.5 | `GET /api/characters` MUST 新增 `voice?: string`（读 README frontmatter，与 avatar 同处） | `routes/world.ts:536-562`（01）+ 05 消费 | 手测 §7 第 2 步（音色按 README 生效） |
| 15.6 | `useAudio.ts` 零改动（02 直调）；遮罩侧补 `unlock()`；无 limiter / decodeCache 淘汰为既有债务不修 | 03 / 02 | §7 第 3 步 |

---

## 7. 端到端手测清单（可操作）

**前置**：有 key → `cp .env.example .env` 填 `DASHSCOPE_API_KEY`（新加坡）；`pnpm dev`（web 5173 / server 3001）。无 key（本机当前默认）→ 验降级（第 5 步）。

| # | 操作 | 预期观察 |
|---|---|---|
| 1 | 打开世界 → 单击角色头像/名字（`App.tsx` `openCharacterModal`） | 遮罩升入（压暗虚化 + 纸面）；**首次**打开该角色显示本地引导语（en `Something on your mind?` / ja `どうしたの？`，随 **UI `locale`**，契约 §15.17）；立绘 `normal`；**不触发 stinger** |
| 2 | 观察引导语 | 逐字出字（45ms/字）**同时**朗读（音色 = 该角色 README `voice`，缺省 `Cherry`，`§15.5` 经 `/api/characters.voice` 下发）。首句语音可能晚于文字（合成预取窗口），但**必须有声** |
| 3 | **首句解锁时序**（§11 裁定） | 因打开遮罩的点击在 `RightSidebar`（不经 Canvas），遮罩 MUST 在首个手势补 `unlock()`；**否则此步静默无声**。观察到引导语有朗读 = 解锁生效 |
| 4 | 点击「纸面」 | 若引导语未出完 → **本页立即补全**（不翻页、不重播语音）；再点 → 进入下一页（真实角色回复到达后） |
| 5 | 无 key 分支 | 全程**无语音**；`console.warn('[AIRP TTS] ...')` **仅一次**；`POST /api/tts` → 503 `tts_unconfigured`；文字/点击推进/stinger 照旧（有 stinger，因无语音） |
| 6 | 翻到**最后一页且出完** | 页不再推进；输入框由 disabled 变**可用**（`CharacterModal.tsx:462` 的 `busy` 退去） |
| 7 | 输入并发送（Enter，`handleSend` `:379`） | 输入框清空；玩家上一句回显（`playerEcho`）；进入 `thinking`（`... is thinking…`）；输入框 disabled；页队列清空、`pageIndex=0` |
| 8 | 等下一轮真实帧 | 新回复从第 1 页重新开始；每翻到新页听到该页语音；**上一页语音在翻页瞬间被 `stopVoice()` 打断**（不叠） |
| 9 | 重复打开同一角色（第 2 次） | **不**再显示引导语（`localStorage` key `airp:greeted:<worldId>:<charId>` 命中） |
| 10 | 回归：BGM/环境音 | 遮罩前后 ambient/bgm 主轨**不变**；语音不打断主轨（正交通道） |
| 11 | **视觉：不像聊天框**（契约 §16，用户需求第 3 条） | 演出期间输入框**不常驻**（默认隐藏、仅页翻尽后淡入）；纸面右下有 `▼` 推进暗示，翻尽后 `▼` 消失、输入框接力出现。这是视觉需求，**只能肉眼确认、无法单测**（如实标注） |

**手测覆盖不到的**（只能靠单测）：缓存命中、原子写、反穿越、`parseEmoPages` 退化形态、无 Web Audio 降级——见 §4/§5。

---

## 8. 收工自检命令（本批该跑 / 不该跑）

### 该跑（按序）

```bash
# 1) 先 build —— 服务端测试跑 dist（AGENTS.md §6.5）
pnpm build

# 2) HTTP 请求体门禁（05 给 BODIES 加了 POST /api/tts 一条）
pnpm check:bodies

# 3) 两个新测试
node --test apps/server/test/tts-routes.test.mjs
node --test apps/web/test/voice-engine.test.mjs
```

合并一行（若已加 `test:tts`，见 R6）：

```bash
pnpm build && pnpm check:bodies && node --test apps/server/test/tts-routes.test.mjs apps/web/test/voice-engine.test.mjs
```

### 不该跑（本批不碰，跑了只掩盖信号）

| 命令 | 为什么不跑 |
|---|---|
| `pnpm check:ws` | 契约 §10.1：本批**不增删任何 WS 帧**。与 TTS 无关，且**不应因此变色**（变色 = 有人偷改帧，另案） |
| `pnpm probe` / `probe:tools` / `probe:inject` / `probe:prompt` | 本批不碰引擎/preset/注入面/提示词正文（§10.2）；探针真 spawn 引擎，慢且无关 |
| `pnpm check:skills` | ~~本批不碰 `skills/**`~~ **2026-09-13 起该跑**：`docs/tts/08` 新增平台 skill `voice-casting` 并改了 `check-skills.mjs` 的 A0/A7，**必须跑**（`check:voices` 同期加入收工自检） |
| `pnpm check:docs` | 那是 **hooks 文档门禁**（`check-hooks-docs.mjs`），**不覆盖 `docs/tts/`** |
| `pnpm typecheck:extensions` | 本批不改 `extensions/*.ts` |

> **例外**：若 01 发现必须改 `packages/shared` schema（§3.1 声明本批不改），则 `pnpm build` 红线照常，并须重跑受影响的既有测试（如 `character-preset-parity.test.mjs`）。

---

## 9. 文件与副作用（测试自身）

| 文件 | 副作用 | 清理 |
|---|---|---|
| `apps/server/test/tts-routes.test.mjs` | 每 harness 建 `os.tmpdir()/airp-tts-*`、起 2 个 loopback server（app + stub）、改 `process.env` | `h.close()`：关 server + stub、关 store、`fs.rm(repo)`、**还原 env** |
| `apps/web/test/voice-engine.test.mjs` | **无** | 无（模块态随进程退出） |

**隔离性**：全部 `listen(0)` 动态端口 → 可与其它测试并行不撞端口；`mkdtemp` 不共享状态；`node:test` 同文件默认串行 → env swap 安全。

---

## 10. 代码落点（精确到文件与函数）

| 交付 | 文件 | 函数/块 |
|---|---|---|
| 服务端测试 | `apps/server/test/tts-routes.test.mjs`（NEW） | `dashStub()` / `harness()` / `post()`；A1–A3 / B1–B2 / C1–C3 / D1–D2 / E1–E5 / F1 |
| 前端测试 | `apps/web/test/voice-engine.test.mjs`（NEW） | jiti bootstrap；V1–V6（`lib/audio.ts`）、P1–P12（`dialogue-pages.ts`，编号以 `03 §10.1` 为准） |
| 回写 R1 | `docs/ui/doc-06-演出与交互设计.md` | `:154` / `:161` / `:162` / `:166` |
| 回写 R2 | `docs/gameplay/doc-19-多模态与游戏性交互升级.md` | `:124` |
| 回写 R3 | `docs/development/doc-07-AIRP黑客松作战计划.md` | `:22` / `:65` / `:67` |
| 回写 R4 | `docs/gameplay/worlds/doc-24-五世界可玩Demo体验设计.md` | `:32` |
| 回写 R5 | `AGENTS.md` | `§2`（`:98` 后）、`§4`（`:167` 后） |
| 回写 R6 | `package.json` | `scripts`（结论：不加 `check:tts`；可选 `test:tts`） |
| 回写 R7/R8 | `docs/tts/00-共同上下文.md` §13 / §15 落点登记 | 由主 agent 裁决契约正文 |

---

## 11. 与现状差异

| 项 | 现状（2026-09-13） | 本批后 |
|---|---|---|
| 服务端 TTS 测试 | **无** | 新增 `tts-routes.test.mjs`（A–F） |
| 前端语音测试 | **无** | 新增 `voice-engine.test.mjs`（V/P） |
| mock 上游能力 | 无 | `dashStub()` 经 `AIRP_TTS_BASE_URL`（**要求 01 请求时读 env**） |
| `doc-06 §3.2` | 「逐字流式」整段 + 常驻输入框 | 一句一页 + 点击推进 + 页触发语音 + 输入框仅翻尽后 |
| `doc-19 §2.3` | `[emo:]` 触发 stinger | 有语音时不播 stinger |
| `doc-07` / `doc-24` | TTS = P2 待评估 | 标记 2026-09-13 已落地 |
| `AGENTS.md` | 无 `docs/tts/` 条目 | §2 + §4 登记 |
| `package.json` | 无 TTS 脚本 | 不加（可选项见 R6） |

---

## 12. 发现的冲突 / 仍未知待拍板

### 12.1 发现的冲突（不回改契约）

1. **`hashOf` 的导出面**：契约 **§15.7 已裁定 `hashOf` MUST `export`**（供测试断言缓存键）。测试 MUST `import`，**MUST NOT** 本地镜像公式（两份真相必漂移）。
2. **广播 #2 与契约 §2.3 的矛盾已由主 agent 自修**：契约 §2.3 现版本（`:92-98`）已改「需要本批补 `.gitignore`」，且 `.gitignore:25-28` 实测已落地 `**/.airpworld/tts-cache/`。本清单**不再引用**「`.airpworld/` 已忽略」这一假断言。
3. **契约 §13 锚点漂移**：`CharacterModal.tsx:65-118` 将被 03 搬到 `dialogue-pages.ts`（R7）。§13 未更新前，按符号名重定位。
4. **契约 §11 与 `audio-routes.test.mjs` 的 404 措辞不一致**（观察，非本批阻塞）：契约 §2.2 规定 `:file` 形状非法 → **403**，但契约 §8 表把「缓存文件缺失（被清）」列为 **404**。两者不冲突（形状非法 vs 形状合法但文件缺），测试 E1/E3/E5 已按此分别断言。
5. **`unlock()` 的手势时序无法单测**（§5.3）。03 补了 `unlock()` 后，node 侧零可观测信号——只能手测（§7 第 3 步）。登记为「验证盲区」，不伪装为已测。

### 12.2 仍未知待拍板

1. **`hashOf` 是否 export**（12.1-1）。
2. **`test:tts` 脚本加不加**（R6）：本批默认不加；若主 agent 要一键收工则加。
3. **`voiceDebugState().url` 的时序语义**：02 定为「最近一次 `playVoice` 收到的 URL」（verbtim），非「当前实际在播的 URL」。V 组按此断言；若将来要「实际在播」需另开字段（不影响本批）。
4. **04 的 8 个安全音色 ID 未经逐条 curl 实测**（§15.2）：落地前必须实测出声。本测试**不断言具体音色 ID 有效**（那是 04 的 curl 实测职责），只断言 voice 字段透传与形状回落。