# docs/tts 终审报告（评审门第二次复核）

> 评审人：TtsFinalReview（独立复核，未参与撰写与修订）。
> 复核对象：`docs/tts/00`–`06` + 两份前轮 REVIEW。
> 复核日期：2026-09-13。方式：**只读**；`read`/`grep`/`git show` + 单文件行号核对。**未改任何文件，未跑项目级命令。**
> 分级：`BLOCKER`（不修不能实现）/ `MAJOR`（会造成缺陷或返工）/ `MINOR`（措辞/美观）/ `已解决`。
> 行号证据以符号名为准（工作树正在被并行批次改动，行号有保质期）。

---

## ① 判定：**通过（可进入实现）**

前两轮合计 **4 个 BLOCKER（A:B1/B2/B3 + B:I1）全部已解决**，且修订**未引入新 BLOCKER**。剩余问题均为 `MAJOR`/`MINOR` 级文档缺陷（追溯表失同步、行锚漂移、若干措辞），**不阻断编码**，其中 3 项建议在实现前顺手修（见 §③）。

| 前轮 BLOCKER | 复核结论 |
|---|---|
| A-B1 mock 页无发声路径 | **已解决** |
| A-B2 样式需求零设计 | **已解决** |
| A-B3 / B-I1 `worldId===undefined` 私改 §15.13 | **已解决** |

---

## ② 六大核验重点（逐条带证据）

### 重点 1 — mock 页语音路径（B1）：**已解决** ✅

- **mock seed 后确实调用了 `prefetchVoice(mockPage, 0)`**：`03:454`（`prefetchVoice(mockPage, 0); // §15.18 B1`）。§5.5 同步声明「mock 页与真实页共用同一条语音路径」（`03:426`）。
- **`mockRef` 已不再排除 mock 页的语音分支**：`enterPage` 语音段（`03:375-385`）中 `ready` 分支（`:375-378`）与 `pending` 分支（`:379-380`）**无 `mockRef`**；`mockRef` 仅出现在 stinger 分支（`:381`）与宽限定时器（`:411`）、以及真实首帧整块清页处（`:458`）。`03:387` 专门注解「`mockRef` 只出现在『不响 stinger』与『退出时整块清页』两处」。
- **时序自洽**：`mockPage` 初始 `voiceState:'idle'`（`03:450`）→ `prefetchVoice` 首行同步置 `'pending'`（`03:312`）→ 随即 `enterPage(0,{announce:true})`（`03:455`）命中 `pending` 分支挂宽限 → fetch resolve 后 `onVoiceResolved`（`03:356-360`）在「仍是当前页」时 `playVoice`。链路闭合。

### 重点 2 — 退化 key：**已解决** ✅

- 全篇 `airp:greeted` 的**活跃写法**只剩 `03:445` 的 `key = worldId === undefined ? null : \`airp:greeted:${worldId}:${characterId}\``，以及 `03:92`/`03:469` 的「不读写」声明。
- `airp:greeted::` / `airp:greeted:undefined:` 的**出现处全是「禁止示例」文本**（`03:92`、`03:469`、`03:731` 修订记录），非实现写法，不算违规。
- 与契约 `00:586-588`（§15.13：`MUST NOT` 写）与 `05:129` 三处口径现已一致。

### 重点 3 — §8 galgame 视觉重设计：**已解决** ✅

`03:503-627` 已是完整视觉章（旧稿只有 cursor/max-height）：

- **输入框默认隐藏 + 条件淡入**：新增包裹类 `.speech-input-row` / `.speech-input-row.is-ready`，默认 `opacity:0; max-height:0`，`busy=false` 时淡入（`03:547-576`、JS 落点 `inputReady = !busy` 见 `03:573`）。
- **`▼` 推进指示器**：`.advance-indicator` 绝对定位于纸面右下 + `advanceBlink` 闪烁（`03:578-598`），并明标「**MUST 做**，契约 §16.2/§15.11 升格」（`03:578-580`）。
- 目标形态结构（台词为主体、一页一屏、立绘层级）、`.speech-paper`/`.line-stage`/`.speech-input` 具体属性齐备（`03:515-545`）。契约 `00:626-659`（§16）与之一致，§16.3 的四项产出（①–④）均落地。

### 重点 4 — 03 与 06 的 P 组编号：**基本一致，残留标签漂移** ⚠️

- **测试文件双重定义已消除**：`00:602`、`03:664`、`06:539` 三处一致裁定「P 组只写在 `apps/web/test/voice-engine.test.mjs`；`dialogue-pages.test.mjs` 作废」。
- **内容一一对齐**：`03:668-678` 的 P1–P11 与 `06:468-534` 的 P1–P11 语义逐条吻合（`P1` 无换行单页、`P6` 未闭合 emo、`P8` clamp、`P9` charDelay、`P10` GREETING、`P11` 长行不截断）。
- **残留 3 处标签不一致（MINOR，见 §③-4）**：03 第 12 条标 `P12`（`03:679`）而 06 同一用例标 `P8b`（`06:515`）；`03:664` 表头写「P1–P11」却列到 P12；`06:539` 写「P1–P11 为准」而 `06:801` 写「P1–P12」。

### 重点 5 — 契约措辞（§13 一句一行 / §6.1·§6.3 parseEmoLines）：**已解决** ✅

- **§13 不再是「已证事实」**：`00:466` 现写「角色输出**格式要求**是一句一行且带 `[emo: tag]`（**这是提示词正文的约束，非实测的模型行为**——见 §6.2 的实测警告）」，与前轮 B-6.2 要求逐字吻合。§6.2 的整段 ⚠️ 实测警告（`00:260-269`）保留完整。
- **parseEmoLines 措辞**：`00:251`（§6.1）写「原 `parseEmoLines` 本批**删除更名**为 `parseEmoPages`」；`00:468`（§13）写「`parseEmoLines` 本批删除更名为 `parseEmoPages`」。§6.3（`00:275`）只引 `charDelay`，无旧名。三处均正确。
- `03:580` 的 `parseEmoPages` 已按 §15.16/§15.8 定稿。

### 重点 6 — 子文档私改契约冻结形状：**未发现** ✅

| 冻结形状 | 复核 |
|---|---|
| 请求体三键 `{text,voice,language}` | `03:318` 内联字面量恒三键、`03:347` 显式「MUST NOT 条件构造」；`05` 门禁 `BODIES` 同键集。一致 |
| 响应体五字段 `{ok,url,cached,characters,truncated}` | `01:293`「不扩，只日志」、`01:351` 逐字；`04:327` 示例同形。一致 |
| `audioDebugState` 四字段 | `02:206`「绝对不动」、`02:333` V4「严格等于 `['ambient','bgm','loaded','theme']`」；`06:450-451` V4 断言。一致 |
| WS 帧零增删 | `00:385`、`03:433`、`05:272` 三处一致「沿用 `docs/tools/12 §6.2`」。一致 |
| env 名（5 个 `AIRP_TTS_*` + `DASHSCOPE_API_KEY`） | `01:130-134,233-239` 逐字；无自造 env（全批 grep 无第 6 个 `AIRP_TTS_*`）。一致 |
| hash 公式 `sha256(model\|voice\|languageType\|text).slice(0,20)` | `01:48-49,518-519`、`06:127-129` 镜像、`00:126`。逐字一致 |

---

## ③ 修订引入的新问题 / 仍未闭合项

### 3-1 `[MAJOR]` 06 §3.4 的「行为→测试」追溯表与自身 §5 严重失同步（**修订的副作用**）

`06:76-84` 的映射表把行为对到旧编号：

| 06 §3.4 声称 | 06 §5 实际用例 |
|---|---|
| `多 \n`/空行 → **P2/P3**（`06:81`） | 实际 P3/P4（`06:479,485`） |
| `[emo:]` 继承 → **P4**（`06:82`） | 实际 **P5**（`06:489`） |
| 超长不截断 → **P5**（`06:83`） | 实际 **P11**（`06:530`） |
| 不按标点 → **P6**（`06:84`） | 实际 **P8b**（`06:515`） |

5 行里 4 行错位。属 P 组编号对齐时**只改了 §5 的用例体、漏改 §3.4 的映射表**。不阻断实现（§5 的用例本身正确），但追溯性失效——维护者会追错用例。**建议实现前修**。

### 3-2 `[MAJOR]` 契约 §16.3 的 MUST 未落地到 06 手测清单

`00:654`（§16.3）明写：「**`06` 的手测清单 MUST 含一条「肉眼确认：不像聊天框、有推进暗示」**（视觉需求无法单测，只能手测）」。但 `06:732-743` 的手测清单 10 行**无任何视觉/推进暗示条目**（全文 grep `聊天框`/`galgame`/`推进暗示`/`肉眼` 零命中）。这是 §16 新增 MUST 后**未回落到 06** 的缺口。**建议实现前补一行**。

### 3-3 `[MAJOR]` 06 手测 step 1 的引导语语言来源仍写 `world.json.locale`，与 §15.17 冲突

`00:604-608`（§15.17）已裁定：引导语文案取 **`locale` prop（UI 语言）**，并明令「`06 §7` step 1 的『随 `world.json.locale`』**改为『随 UI locale』**」。但 `06:734` 仍写「（en … / ja …，**随 `world.json.locale`**）」，且 `03:449` 已按 §15.17 实现为 `GREETING_LINE[locale]`。手测预期与实现/契约三方不一致（日文浏览器 + 无 locale 世界下会误判）。**建议实现前改 06 step 1**。

### 3-4 `[MINOR]` P 组编号标签漂移（见 §② 重点 4）

`P12`（03）↔ `P8b`（06）、表头 `P1–P11` vs 实际 P12、`06:539` vs `06:801` 的 P1–P11/P1–P12 不一致。

### 3-5 `[MINOR]` 前轮未闭合的文档瑕疵（本轮仍在）

1. **`hashOf` 导出措辞未随 §15.7 更新**：`00:560`（§15.7）已裁 `MUST export`，`01:32,49` 已 `export`，但 `06:36`、`06:831`、`06:839` 仍写「**未冻结**……两条路都能编译」。（前轮 B-I4）
2. **`Canvas.tsx:180` 行号老化**：`00:430`、`02:238`、`03:291`、`05:266`、`06:90` 五处写 `:180`；实测工作树该调用为 **`:191`**（`grep -n "void unlock()" apps/web/src/components/canvas/Canvas.tsx`）。
3. **`03 §2.3` refs 清单仍缺 `mockSeededRef`**：该表（`03:97-120`）未列，而它在 `03:472,479,641,648` 是 StrictMode 幂等的唯一守卫。（前轮 B-5.5）
4. **`03 §5.1` `prefetchVoice` 仍写成模块级函数**（`03:309`），函数体却引用组件闭包（`voice`/`language`/`voiceTurnRef`/`onVoiceResolved`），与 `03:654`「全部 `useCallback`」矛盾。（前轮 B-5.4）
5. **`02 §5` 伪代码 `ctx` 未声明**（`02:109-123` 先 `c = initAudio()` 后用 `ctx`/`master`）。

---

## ④ 行锚漂移（前轮 B-I6）：**仍未解决，但已充分缓解**

主 agent 的修订清单未包含「刷新行锚」（只补了 §13 的回写**归属**）。实测工作树：

| 文档引用 | 文档写 | 工作树实测 | 漂移 |
|---|---|---|---|
| `00:480-482` / `03:435` / `05:124,307` | `App.tsx:597-612` 遮罩挂载 | `:647`（`activeModalCharId && activeChar &&`） | +50 |
| `00:482` / `05:274` | `App.tsx:605-611` `character_prompt` | `:655` 起 | +50 |
| `00:430` / `05:266` | `App.tsx:585` `onChatWithCharacter` | `:634` | +49 |
| `01:592` | `/characters` 回包 `world.ts:551-555` | `/characters` 起 `:660`，回包 `:678-685` | +127 |
| `05:135,155` | `world.ts:536-562` / `:545-555` | 同上 | ~+124 |
| `01:593` | `/api/audio` `world.ts:901` | `/api/audio` 起 `:1046`（`path.resolve(AUDIO_ROOT…)` `:1053`） | +145 |
| `00:472` | `world.ts:546-548` 读 frontmatter | `:671-673` | +125 |

**缓解到位**：契约 `00:461` 已加「回写归属」——本节锚点由**实现阶段主 agent 落地时**逐个符号重定位（评审 I3 owner 悬空已消除，`06:710-712` 与之一致，`06:808` 佐证）。§13 头（`00:459`）与 `03`/`05` 头部均有「行号有保质期、按符号重定位」声明，且多数引用**同时给了符号名**（`<CharacterModal>`/`onChatWithCharacter`/`openCharacterModal`）。故**非 BLOCKER**，但实现前必须按符号刷新一遍（见 §⑥）。

---

## ⑤ 诚实性复核

**未发现「伪装的已定/已证」未知项。** 关键项逐条：

- `[正例]` 「一句一行」的未证状态**已修复到自洽**：`00:466`（§13）已改为「提示词约束，非实测模型行为」；§6.2 的整段实测警告（`00:260-269`：130 session / 156 文本块 / 0 条多行）保留；`02:151-153`、`03:155`、`06:80-84` 均据此设计鲁棒。**§13 与 §6.2 的自相矛盾（前轮 B-6.2）已消除。**
- `[正例]` 音色清单仍标「候选 `[未逐条核验]`」，`Eldric Sage` 拼接已证伪并撤回（`04:355`），8 个安全集合保留 curl 实测路径（`04:315`）。
- `[正例]` `STINGER_VOICE_GRACE_MS=1200` 标 `[推断]`（`03:719`、`00:580`）；`06 §5.3` 如实登记「解锁时序 / 真实音频 / voiceToken 竞态**单测覆盖不到，只能手测**」；`03 §4.5` 明写「否则此步静默无声」。
- `[正例]` mock 页「仍显示但不写 localStorage」的**代价**（每次重显）在 `03:92`、`03:469`、`03:472` 如实登记，未伪装为「完美去重」。
- `[观察]` 无新伪装项；§15.17 的 locale 来源虽未同步到 `06:734`（见 §③-3），但那是**滞后**而非伪装（§15.17 自身记录完整）。

---

## ⑥ 实现阶段必须注意的 TOP 风险

1. **行锚必须按符号重定位后再动手**（最高优先）。`world.ts` 工作树已 1076 行：`/characters` 在 `:660`、`/api/audio` 在 `:1046`；`App.tsx` 遮罩挂载在 `:647`、`onChatWithCharacter` 在 `:634`。**照抄文档里的 `597-612`/`551-555`/`901` 会改错块**（如 `:551` 附近是 `reseatLayer`）。实现完必须回填 `00 §13`（owner 已定为落地主 agent，`00:461`）。
2. **「一句一行」未证**：分页实现**不得**假设页数 ≥ 2 或页短。P1（无换行=单页非空）、P11（2000 字不截断）、P6/P8b（不按标点切页）是**非空性防线，必须真跑**（`06:545-547`）。真实模型若整段输出，退化路径是「1 页 + 内部滚动」，不是空白/异常。
3. **首句解锁时序**：遮罩侧必须补 `void unlock()`（`03:293-299` 两处冗余），否则 mock 引导语与首句因 `ctx.state !== 'running'` 被 `playVoice` **静默丢弃**。此路径 node 单测覆盖不到，**只能手测**（`06:90`、`06:736`）。
4. **门禁写法硬约束**：`fetch('/api/tts', …)` 的 route 字面量与 `fetch(` **同行**，body 用**内联三键对象字面量**（`{ text, voice, language }`），`voice` 键恒在源码中（`03:342-347`、`00:414-428`）。跨行或抽变量会让 `check:bodies` 报 `missing-call`。
5. **stinger 判定点**：以 `page.voiceState === 'ready'` 为等价形（`00:296`、`03:389-393`）；`ready` 抑制 stinger、`failed`/宽限到点保留 stinger。`mockRef` 只挡 stinger、**不挡语音**。

---

## ⑦ 结论

**通过，可进入实现。** 4 个 BLOCKER 全部确证已解决，冻结形状无被私改，诚实性达标。建议在实现开工前顺手修 §③-1/3-2/3-3 三项 `MAJOR` 文档缺陷（均为单点改动、不涉设计），并在动手时严格执行 §⑥ 的行锚重定位与 P 组非空性断言。
