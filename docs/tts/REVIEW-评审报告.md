# docs/tts/REVIEW — T1「角色语音（TTS）」设计批次独立评审报告

> 评审对象：`docs/tts/` 六篇子文档 + 冻结契约 `00-共同上下文.md` + 厂商参考 `qwen3-tts-flash.md`（**仅文档，无代码**）。
> 评审日期：2026-09-13。评审人：独立评审员（未参与撰写）。
> 评审纪律：只读；未修改任何文档或代码；未跑项目级命令。所有结论带 `file:line` 或原文引用；「已核实」= 本次以 `read`/`grep`/`git show` 亲验，「[推断]」= 未亲验。
> **重要前提（影响所有行号证据）**：工作树内 `apps/server/src/routes/world.ts` 已被并行批次（N1/nook）改动（`git diff --stat`：+149/−4），`/characters` handler 由 HEAD 的 `:536` 漂到 `:660`，`/api/audio` 反穿越块由 `:901-915` 漂到 `:1046-1060`。**凡引用 world.ts 的行锚在本报告时点均已过期**，故本报告一律以「符号名 + 当前行号」双给。

---

## ① 总评

**结论：设计面质量整体偏高，但存在 3 个 BLOCKER，当前状态不允许直接进入实现。**

亮点（值得保留）：

- 冻结契约 `00` 体例完整：端点/请求响应体/缓存 key/env/前端 surface/页语义/降级矩阵/反模式逐条冻结，且**把「文档断言未被实测证实」的最大风险点显式写死**（`00:260-269`）——`[One sentence per line]` 被明确标注为**未经真实模型证实**（现存 130 个 session 全为探针产物、0 条多行输出），并要求分页实现对「模型不听话」鲁棒。**这一条诚实性做得正确，是本批最值得保留的设计决定。**
- 子文档保留「发现的冲突」节且不回改 `00`（`00:5`），主 agent 裁决集中在 `00 §15`（`:513-591`），形成可追溯闭环。
- `06` 的测试骨架给出「不实现就必红」的非空性断言（A2 缓存命中、P1 无换行单页），符合契约 §8 降级矩阵。
- `02` 对既有债务（无 limiter 削顶、`decodeCache` 无淘汰）**不伪装成「绝对不削顶」**（`02:79`），诚实性达标。

但 3 个 BLOCKER 必须先修：

| # | 级别 | 问题 | 一句话 |
|---|---|---|---|
| B1 | **BLOCKER** | mock 引导语**没有任何取语音的代码路径** | `03 §6.1` seed 直接写 `pagesRef` + `enterPage`，`enterPage §5.3` 对 `voiceState:'idle'` 只走 stinger 分支（且被 `mockRef` 跳过）→ mock 页**永不发声**，违反 `00 §7.3`（mock 也朗读）与 `06 §7` 手测 step 2（「必须有声」）。 |
| B2 | **BLOCKER** | 用户第 3 条需求「样式要改（现在像聊天框，丑）」**全批零设计** | `docs/tts/` 全仓 grep `聊天/丑/美化/样式改/redesign` 无命中；`03 §8`（`:497-529`）只加 `cursor`/`max-height`（功能性），**没有一处重设计「像聊天框」的视觉**。语义未兑现。 |
| B3 | **BLOCKER** | `03` 私改 `00 §15.13` 的 mock 持久化裁决 | `00:585-587` 冻结「`worldId===undefined` 时**仍显示、但不写 localStorage**」，而 `03:92` 写「key 退化为 `airp:greeted:<charId>`（仍能去重）」、`03:443` 实现为 `` `airp:greeted:${worldId ?? ''}:${charId}` `` → 产生 `airp:greeted::nanami` 假持久化 key，正是 §15.13 明令禁止的那一类。且 `00 §15.13` 自称「`03` 已按此实现」——与 `03 §2.2` 原文直接矛盾。 |

另有 5 项 MAJOR（会造成缺陷或返工）与若干 MINOR，见 §②/§③/§④。

---

## ② 逐维度发现

### 维度 1 — 跨文档一致性

逐形状对照见 §③ 的对照表。核心结论：**冻结形状（请求体三键、prop 名、函数名、缓存路径、hash 公式、env 名）在 6 篇之间高度一致，未发现重命名漂移**；不一致集中在**行锚、测试文件归属、mock 语言来源、计数**四处。

- **[MAJOR] 测试文件归属冲突（跨文档不一致 + 闭环缺口）**：`03 §10.1`（`03:565`）声明纯函数测试写在 **`apps/web/test/dialogue-pages.test.mjs`（NEW）**，列 P1–P11；`06 §5`（`06:398`）声明同一批 P1–P8 写在 **`apps/web/test/voice-engine.test.mjs`**，且 `06 §5.1`（`06:516`）自称「P 组写在本文件」。同一组断言被两篇分派到两个不同文件。契约 `00 §11`（`:434-437`）与 `00 §14`（`:505`）只授权**两个**新测试文件（`tts-routes.test.mjs`、`voice-engine.test.mjs`），`03` 引入的第三个文件**不在契约规划内**。→ 违反「每个符号有唯一定义处」。
- **[MAJOR] mock 引导语的语言来源三处不一致**：`03 §2.2`/`§6.1`（`03:91`、`03:446-447`）用 **`locale` prop**（浏览器语言猜测）取 `GREETING_LINE[locale]`；`00 §7.1`（`:317`）说「与 `locale` 对齐」；但 `06 §7` 手测 step 1（`06:712`）写「随 `world.json.locale`」（= 世界内容语言 = `language` prop）。在「日文浏览器 + 英文世界」下三者给出不同结果（`locale='ja'` 但 `world.json.locale` 缺失 → `language='en'`）。`00 §15.10` 只冻结了「两个 prop 不可合并」，**未裁定引导语文案跟哪个**。
- **[MINOR] 同一处代码三套行锚**：`/api/characters` 回传 `voice` 的落点，`04 §6`（`04:146`）写 `world.ts:545-550`、`01 §3.C'`（`01:194`）写 `:551-555`、`05 §3.2`（`05:155`）写 `:541-556`。HEAD 实测该 handler 为 `:536-562`、map 回包为 `:551-555`（`git show HEAD:apps/server/src/routes/world.ts`）。三处至少两处错。
- **[MINOR] 计数自相矛盾**：`04 §9.1`（`04:315`）写「必跑清单（§4.1 的 **6 个不同 ID**）」，随后列出 `Serena/Cherry/Eldric/Sage/Momo/Neil/Mia/Vincent` = **8 个不同 ID**（`§4.1` 表 9 行、`Eldric` 重复一次）。
- **[MINOR] 手测示例音色漂移**：`05 §10.2` step 1（`05:374`）写「`nanami` 若 README 声明了 `Cherry`」；`04 §4.1`/`§7.1` 给 `firstsnow/nanami` 分配的是 **`Serena`**（`04:97`、`04:165`）。
- **[MINOR] `V` 组用例数漂移**：`02 §12.2`（`02:330-336`）列 V1–V7，`06 §5`（`06:428-463`）只落 V1–V6（V7 为括注、未成用例）。
- **[MINOR] 契约内部对 `parseEmoLines` 的措辞未随 §15.8 更新**：`00 §13`（`:464`）仍称「已有 `parseEmoTag`/`parseEmoLines`/`charDelay` 可复用」，`00 §6.1`（`:251`）仍引用 `parseEmoLines` 的剥离逻辑；而 `00 §15.8`（`:560-562`）已批准 `03` **删除并更名** `parseEmoLines` → `parseEmoPages`。同一份冻结文档内自相矛盾（`06 R7` 已登记回写）。

### 维度 2 — 契约冻结遵守

- **[BLOCKER] `03` 私改 §15.13 的 mock 持久化语义**：见 §①B3 / §④V1。
- **[MAJOR] `03` 单方面重定义「stinger 判定点」这一冻结语义**：`00 §6.5`（`:294`）冻结「判定点唯一：**该页是否真的发出了语音**」；`03 §5.4`（`03:387-398`）改为「判定点 = `page.voiceState === 'ready'`（拿到了非空 `url`）」，并自认是「**本文件的拍板**」。该改动**未进 `00 §15` 任何裁决**（§15.11 只裁了点击目标/键位/`busy`）。虽然 `03` 给了合理理由（引擎无法观测真出声），但这是**对冻结语义的实质改写而未修订契约**，违反 `00:5`「子文档发现问题写进冲突节、不回改本文，由主 agent 裁决」的纪律。
- **[MINOR] `00 §5.1` 内部矛盾未彻底消解**：`00:236` 首句「`useAudio` 暴露 `playVoice`/`stopVoice`（薄壳转发）」与次句「遮罩直接调 `lib/audio.ts` 亦可」并存；`02 §7`/`§13` 已选「直调」并由 §15.6 追认——**裁决正确**，但冻结正文未删首句，读者仍可能误判「`useAudio` 漏改」。
- **[MINOR] `03 §11.1` 是过时的冲突登记**：`03:603` 请主 agent 把契约措辞从「固定高 3.2em」改为「下限高」；但 `00 §6.2`（`:265`）**已自行修正**（明写「初稿写固定高是错的」）。该冲突项已成历史，留在 `03` 会误导实现者以为契约仍待修。
- **未发现以下违规（核实通过）**：未在 `world.json.characters[]` 加 `voice`（`00:119`、`04:343` 均声明保持不含）；未新增/删除任何 WS 帧（`00 §10.1`）；未把 TTS 混进 `audioDebugState()` 四字段（`02:206` 明令新开 `voiceDebugState`）；请求体恒为 `{text,voice,language}` 三键（`00 §11`/`03 §5.1`/`05 §2.2` 逐字一致）；响应体未扩字段。

### 维度 3 — 语义兑现 / 需求强度

用户四条需求逐条核对：

| 需求 | 是否真正设计到 | 证据 |
|---|---|---|
| ① galgame：一句一页 + 点击切换 + 每页配音 | **是**（设计完整） | `03 §3.1-§3.4`（分页状态机）、`§4.4`（快进→翻页→钳制）、`§5`（页触发语音）；`00 §6` 冻结 |
| ② 补分页 | **是** | `03 §2.1` `parseEmoPages` + `§3` 封口/prefetch；`00 §6.2` 退化鲁棒 |
| ③ 改样式（现在像聊天框，丑） | **否 —— 零设计** | `docs/tts/` 全仓 grep `聊天/丑/美化/样式改/redesign` 无命中；`03 §8`（`:497-529`）仅 `cursor` + `max-height`，无视觉重设计。**BLOCKER** |
| ④ mock 引导语（纯给玩家看、不进上下文） | **部分** —— 不进上下文设计到位，**但「有声音」路径缺失** | 不进上下文：`03 §6.1`（`03:464` 可 grep 验证）+ `00 §7.2`（`:328`）设计严密；**发声：BLOCKER**（§①B1）。另 mock 文案语言来源未裁定（维度 1 MAJOR） |

**结论**：需求 ③ 完全未兑现；需求 ④ 的「朗读」半边是空设计。其余两条达标。

### 维度 4 — 闭环性

- **[BLOCKER] mock 页 → 语音：无取 URL 路径**（§①B1）。`prefetchVoice` 的调用点仅存在于 `syncPages`（`03:145,148,149`）与自身定义（`03:305-309`）；mock 走的是 `§6.1` 的直接 seed，绕过 `syncPages`，且 `enterPage §5.3` 对 `idle` 不调 `prefetchVoice`。→ 闭环断裂。
- **[MAJOR] 测试文件双重定义**（维度 1）：`parseEmoPages`/`clampPageIndex`/`charDelay` 的断言在 `dialogue-pages.test.mjs`（`03`）与 `voice-engine.test.mjs`（`06`）两处定义（P4/P5/P8 内容还部分重复）。
- **引用符号闭环良好**（核实通过）：`createTtsRouter`/`synthesise`/`hashOf`（`00 §4.1` ↔ `01 §2.1` ↔ `06 §2`）、`playVoice/stopVoice/isVoicing/voiceDebugState`（`00 §5` ↔ `02 §2` ↔ `06 §2`）、`parseEmoPages/clampPageIndex`（`03 §2.1` ↔ `06 §2`）签名逐字一致；`hashOf` 导出面已由 `00 §15.7`（`:556-558`）补齐。全部有唯一定义处。
- **动作落点闭环良好**：`/api/tts`、`/api/tts/audio`、`.env` 加载、`.gitignore`、`/api/characters` 回传、门禁 BODIES、App 传参，均落到 file+函数（`01 §8`、`05 §8`、`06 §10`）。

### 维度 5 — 可实施性

- **[MAJOR] 反穿越先例行锚：错到另一个函数**。`00 §2.2`（`:80`）与 `00 §13`（`:473`）两处都写「与 `/api/audio` 同款双重校验（`routes/world.ts:549-563` 先例）」。**实测 HEAD 的 `world.ts:549-563` 是 `/characters` 的 map 回包块，不是反穿越**；反穿越（`path.resolve`+`startsWith`+`realpathSync`）实际在 HEAD `:901-915`（`git show HEAD:...world.ts` 核验；工作树已被并行批次漂到 `:1046-1060`）。这不是「行号保质期」式漂移，而是**引用到了错误函数**，会把实现者引向无关代码。
- **[MAJOR] 大量 world.ts 行锚已被并行批次作废**：`01 §8` 落点表（`01:574`）写 `/characters` handler `:551-555`、（`05 §8` 表 3，`05:301`）写 `:541-556`；实测工作树 `/characters` 已在 `:660-686`。**文档已声明「行号有保质期、按符号重定位」（`00 §13` 头、`05`/`03` 头），且同时给了函数名锚**，故可接受，但所有 world.ts 行锚在本批落地时**必须按符号重算**（`01`/`05`/`00 §13` 三处）。
- **正例**：`audio.ts` 锚全部核实无误——`loadSample:532`、`playClip:564`、`stopClip:595`、`playStinger:851`、`audioDebugState:871-882`、`unlock:905`、`unlockOnFirstInteraction:926`、`SAMPLE_LEVELS:479-481`（`read` 核验，与 `02 §11` 锚一致）。`CharacterModal.tsx` 锚核实无误（`EMO_PREFIX_GUARD:59`、`parseEmoTag:65`、`parseEmoLines:80`、`charDelay:114`、`lineRef:141`、`shownRef:142`、`busy:398`、`disabled={busy}:462`）。`index.css` 锚无误（`.speech-paper:441`、`.line-stage:503`、`.speech-input:disabled:574`）。门禁锚无误（`BODIES:32-39`、`bodyKeysFor:80-105`、`compare:115-144`；`check-request-bodies.mjs` 实测 169 行）。
- **[MINOR] 02 伪代码变量不一致**：`02 §5`（`:112-123`）先 `c = initAudio()`，随后 `.then` 内却用 `ctx`/`master`（`ctx` 未声明）。伪代码层笔误，实现者会意即可。

### 维度 6 — 诚实性

- **[正例，最重要] 「角色真的输出一句一行」被正确当成未证事实**：`00 §6.2`（`:260-269`）逐字记录「全仓 130 个 session、156 条 assistant 文本块，**0 条多行输出**，现存 session 全为探针产物」，并据此要求分页对「无换行=单页」「超长单行不截断」「不按标点切页」鲁棒；`06 §3.4`/`§5.2` 用 P1/P5/P6 做非空性断言，`02 §5.3` 明确「引擎不假设页短/页数≥2」。**该假设未被伪装成已证事实。**
- **[正例] 音色清单降级为候选**：`00 §15.2`（`:523-527`）把 `04` 的「完整清单」降级为「候选 `[未逐条核验]`」，并指出 `Eldric Sage` 是**两个 ID 被拼接**（已证伪），要求落地前逐条 curl 实测；`04 §3.1`/`§9.1`/`§12.4` 已同步标注。诚实性达标。
- **[正例] 验证盲区如实登记**：`06 §5.3`（`:528-532`）明写「解锁时序、真实音频、`voiceToken` 竞态**单测覆盖不到，只能手测**」；`02 §3.2` 第 4 点如实说明无 limiter 下理论削顶。
- **[MINOR] `03 §5.4` 的结论强度略超证据**：`STINGER_VOICE_GRACE_MS=1200` 已标 `[推断]`（`03:617`）✓；但「503 通常 <200ms 返回、合成 1–3s」**本机无 key 未实测**，却用作宽限取值依据。已标 `[推断]`，可接受；建议落地后真机校准。
- **[MINOR] 对「广播 #1/#2/#3」的引用不可自证**：`03`/`06` 多处引用「广播 #1/#2/#3」（如 `06:86`、`06:500`），但广播内容未落到任何文档，读者无法核验。建议把广播结论并入 `00 §15` 或 `00 §13`。

---

## ③ 跨文档不一致清单（对照表）

图例：✅ 全篇一致｜⚠️ 存在不一致｜❌ 冲突/缺失。

| 被多次引用的形状 | 00（冻结契约） | 01 | 02 | 03 | 04 | 05 | 06 | 判定 |
|---|---|---|---|---|---|---|---|---|
| 请求体三键 | `{text,voice?,language?}`（`:57`） | 逐字（`:259`） | — | 内联 `{text,voice,language}`（`:318`） | 引用（`:23`） | 门禁 `['text','voice','language']`（`:44`） | 断言（`:50`） | ✅ |
| 响应体五字段 | `{ok,url,cached,characters,truncated}`（`:58`） | 逐字（`:348`） | — | — | `characters:27`（`:327`） | `{ok,url,…}`（`:263`） | A1（`:211`） | ✅ |
| `voice` prop 名 | — | — | — | `voice?: string`（`:85`） | 引用（`:24`） | `voice?: string`（`:69`） | 引用 | ✅ |
| `language` prop 名 | 独立于 `locale`（`:271`） | — | — | `language?: string` 缺省 `'en'`（`:87`） | — | `language?: string` 缺省 `'en'`（`:71,98`） | 引用（`:700`） | ✅ |
| `worldId` prop 名 | — | — | — | `worldId?: string`（`:83`） | — | `worldId?: string`（`:67`） | key 形态（`:720`） | ✅ |
| **`worldId===undefined` 行为** | **不写 localStorage**（`:585-587`） | — | — | **写退化 key `airp:greeted:<charId>`**（`:92`、`:443`） | — | **不写**（`:129`） | — | ❌ **B3** |
| `playVoice`/`stopVoice`/`isVoicing` | 冻结签名（`:217-223`） | — | 逐字（`:34-36`） | 调用（`:359,373`） | — | — | 断言（`:454-457`） | ✅ |
| `voiceDebugState()` | 留给 02 决定（`:232`） | — | `{url:string\|null}`（`:37`） | — | — | — | 断言（`:432`） | ✅ |
| `audioDebugState` 四字段 | 形状冻结（`:232`） | — | 明令不动（`:206`） | — | — | — | V4（`:451`） | ✅ |
| `parseEmoPages` | §6 语义冻结 | — | — | 签名（`:60-63`） | — | — | 调用（`:469`） | ✅ |
| `clampPageIndex` | 边界语义（`:286`） | — | — | 签名（`:69`） | — | — | P8（`:507`） | ✅ |
| 缓存路径 | `<worldRoot>/.airpworld/tts-cache/<hash>.wav`（`:88`） | 逐字（`:78,312`） | — | — | 引用（`:285`） | — | harness（`:183`） | ✅ |
| hash 公式 | `sha256(model\|voice\|language_type\|text).slice(0,20)`（`:126`） | 逐字（`:519`） | — | — | — | — | 镜像（`:128`） | ✅（`language_type`↔`languageType` 仅命名层，值一致） |
| env 名（5 个） | `:137-141` | 逐字（`:130-134`） | — | — | 引用 DASHSCOPE（`:303`） | — | harness（`:172-175`） | ✅ |
| `:file` 正则 | `^[a-f0-9]{20}\.wav$`（`:81`） | `FILE_RE`（`:64`） | — | — | — | — | E1/E3（`:322`） | ✅ |
| 页语义（一行=一页） | 冻结（`:246`） | — | 不假设页数（`:153`） | 状态机（`:12`） | — | — | P1（`:468`） | ✅ |
| **反穿越先例行锚** | `world.ts:549-563`（`:80,473`） | — | — | — | — | — | — | ❌ 指向错函数（实测应 `:901-915` @HEAD） |
| **新测试文件** | `tts-routes` + `voice-engine`（`:434-437`） | 引用 | 引用 | **`dialogue-pages.test.mjs`**（`:565`） | — | — | **P 组写进 `voice-engine`**（`:516`） | ❌ 归属冲突 |
| **mock 文案语言来源** | 「与 locale 对齐」（`:317`） | — | — | `GREETING_LINE[locale]`（`:446`） | — | — | 「随 `world.json.locale`」（`:712`） | ⚠️ |
| **`/api/characters` voice 落点行锚** | — | `:551-555`（`:194`） | — | — | `:545-550`（`:146`） | `:541-556`（`:155`） | 符号名（`:33`） | ⚠️ 三套行锚、两套错 |

---

## ④ 契约违反清单

| # | 级别 | 违反方 | 内容 | 证据 |
|---|---|---|---|---|
| V1 | **BLOCKER** | `03` | 私改 `00 §15.13`：`worldId===undefined` 写退化 `localStorage` key（§15.13 明令不写，避免假持久化）。且 `00 §15.13` 自称「`03` 已按此实现」，与 `03 §2.2` 原文矛盾。 | `00:585-587` vs `03:92`、`03:443`；`05:129` 站在 `00` 一侧 |
| V2 | **BLOCKER** | 全批 | 用户需求「改样式（像聊天框，丑）」**零设计**——无一篇文档承接。 | `docs/tts/` grep 无命中；`03 §8`（`:497-529`）只有 cursor/max-height |
| V3 | **BLOCKER** | `03` | mock 页**无发声路径**，违反 `00 §7.3`「mock 页也朗读」。 | `00:337` vs `03 §6.1`（`:437-451`）、`03 §5.3`（`:370-385`）；`prefetchVoice` 未被 mock 路径调用 |
| V4 | **MAJOR** | `03` | 单方面把冻结语义「stinger 判定点 = 是否真的发出了语音」改写为「`voiceState==='ready'`」，未走 `00 §15` 裁决。 | `00:294` vs `03:387-398` |
| V5 | **MAJOR** | `03` | 引入契约未规划的第 3 个测试文件 `dialogue-pages.test.mjs`，与 `06` 的 P 组归属冲突，违反「唯一 owner / 唯一定义处」。 | `00 §11`:434-437、`00 §14`:505 vs `03:565` vs `06:516` |
| V6 | **MAJOR** | `00` | 冻结文档内部对 `parseEmoLines` 措辞未随 `§15.8` 更新（`:464`/`:251` 仍称可复用，`:560` 已批准删除更名）。 | `00:464`、`00:251` vs `00:560-562` |
| V7 | **MAJOR** | `03` | 反穿越先例行锚 `world.ts:549-563` 指向 `/characters` 回包块，非反穿越（实测反穿越 @HEAD `:901-915`）。 | `00:80`、`00:473` |
| V8 | **MINOR** | `00` | `§5.1` 首句「`useAudio` 暴露 play/stopVoice」与次句「直调亦可」并存，未按 §15.6 删首句。 | `00:236` |
| V9 | **MINOR** | `04` | 「6 个不同 ID」实为 8 个（`§4.1` 表 9 行、`Eldric` 重复）。 | `04:315` vs `04:95-105` |
| V10 | **MINOR** | `05` | 手测示例音色与 `04` 分配漂移（写 `Cherry`，实为 `Serena`）。 | `05:374` vs `04:97` |
| V11 | **MINOR** | `00` | `§13` 未登记 `03` 的纯函数搬家（`§15.8` 已批准），同行锚共 4 处仍指向 `CharacterModal.tsx:65-118`。 | `00:464` |
| V12 | **MINOR** | `02`/`06` | `02` 列 V1–V7，`06` 只落 V1–V6，用例数漂移。 | `02:330-336` vs `06:428-463` |

> **未违反项（核实通过，明确记录以免误伤）**：请求体三键/响应体五字段/hash 公式/env 名/缓存路径/`:file` 正则/前端 surface 名，全批逐字一致，**未被任何子文档私改**；`world.json.characters[]` 未加 `voice`；WS 帧零增删；`audioDebugState` 四字段未被污染；`hashOf` 导出面已补齐。

---

## ⑤ 建议的修订动作（精确到文档与章节）

> 排序：先解 BLOCKER，再 MAJOR，最后 MINOR。所有动作**由主 agent 裁决后**落 `00`（子文档不回改契约的纪律保持），子文档据裁决定稿。

### 解 BLOCKER

1. **[B1/V3] mock 页得语音** —— 改 `03 §6.1`（`:437-456`）与 `§5.3`（`:370-385`）：mock seed 后**必须为 mock 页发起 `prefetchVoice(mockPage, 0)`**（`voiceState` 由 `'idle'` → `'pending'` → `'ready'`），且 `enterPage` 的 `pending` 分支对 mock 页要**挂起宽限而非跳过**（当前 `!mockRef.current` 把 mock 页的 stinger 分支整段禁掉，`ready` 分支却因 `voiceState==='idle'` 从未进入）。或在 `00 §7.3` 明确「mock 页语音走 `syncPages` 之外的一条显式预取路径」并给出锚点。
2. **[B2] 样式需求补设计** —— 新增一节（建议归 `03` 的 `§8` 扩写，或单开 `07-遮罩视觉重设计.md`，由主 agent 定归属）：先取样现状（`.speech-paper:441`、`.line-stage:503`、`.speech-input:548` 的「聊天框」观感），给出**galgame 对话框形态**的具体目标（纸面/文本框/立绘的层级、点击推进的视觉反馈、页指示器是否上），并写清改哪些 CSS token。`00 §12` 需补一行修订（用户 2026-09-13 需求第 3 条）。
3. **[B3/V1] 对齐 §15.13** —— 改 `03 §2.2`（`:92`）与 `§6.1`（`:443`）：`worldId===undefined` → **仍显示 mock 页，但不 `localStorage.setItem`**（`getItem` 用 `airp:greeted:undefined:<charId>` 亦可，只要**不写**）。同时修正 `00 §15.13`（`:587`）末句「`03` 已按此实现」为「`03` MUST 按此实现」。

### 解 MAJOR

4. **[V4] stinger 判定点** —— 在 `00 §6.5`（`:294`）补一句裁定：「引擎无法观测真出声，判定点等价形为 `page.voiceState==='ready'`（`03 §5.4`）」，把 `03` 的拍板升格为契约，消除「子文档私改」。
5. **[V5] 测试文件唯一化** —— `00 §11`（`:434-437`）裁定纯函数测试归属：二选一后**删掉另一篇的声明**。建议**并入 `voice-engine.test.mjs`**（`06` 已写好完整骨架），删除 `03 §10.1`（`:565`）的 `dialogue-pages.test.mjs` 引用并改为「断言由 `06` 的 P 组覆盖」。
6. **[V6] `parseEmoLines` 措辞** —— 改 `00 §13`（`:464`）与 `§6.1`（`:251`）为 `parseEmoPages`，并在 `§13` 行锚改为指向 `dialogue-pages.ts`（承 `§15.8`）。
7. **[V7] 反穿越行锚** —— 改 `00 §2.2`（`:80`）与 `§13`（`:473`）的 `world.ts:549-563` → **符号锚**「`/api/audio` handler 的 `path.resolve`+`startsWith`+`realpathSync` 三连」（行号由实现时定位；本报告时点工作树为 `:1046-1060`）。
8. **[维度 5/MAJOR] world.ts 行锚全面按符号重算** —— `01 §8`（`:574`）、`05 §8`（`:301-303`）、`00 §13`（`:472,474`）中所有 `/characters` 与 `/audio` 行锚，落地时以函数名重定位（工作树 `/characters` 现为 `:660`）。

### 解 MINOR

9. **[V8]** 删 `00 §5.1`（`:236`）首句「`useAudio` 暴露 …」，只留「直调」并注明 §15.6。
10. **[V9]** 改 `04 §9.1`（`:315`）「6 个不同 ID」→「8 个不同 ID」。
11. **[V10]** 改 `05 §10.2` step 1（`:374`）`Cherry` → `Serena`。
12. **[V12]** `02 §12.2` 与 `06 §5` 用例集对齐（V7 要么落成用例，要么从 `02` 删）。
13. **[维度 1]** 裁定 mock 文案语言来源（建议**用 `locale`**，与 `03 §6.1` 实现一致，`06 §7` step 1 改为「随 UI locale」），写进 `00 §7.1`。
14. **[维度 1]** `03 §11.1`（`:603`）标注「已由 `00 §6.2` 修正」（过时冲突登记清理）。
15. **[维度 6]** 把「广播 #1/#2/#3」的结论并入 `00 §15` 或 `§13`，使 `03`/`06` 的引用可核验。

---

## ⑥ 评审结论

| 维度 | 判定 |
|---|---|
| 1 跨文档一致性 | ⚠️ 冻结形状一致；行锚/测试归属/mock 语言来源/计数 4 类不一致 |
| 2 契约冻结遵守 | ⚠️ 1 私改（§15.13）+ 1 未裁改写（stinger 判定点）+ 1 内部矛盾（§5.1） |
| 3 语义兑现 | ❌ 样式需求零设计；mock 朗读路径缺失 |
| 4 闭环性 | ❌ mock→语音断链；1 测试文件双定义（其余闭环良好） |
| 5 可实施性 | ⚠️ 主代码锚（audio/modal/css）全部可核实；world.ts 锚失效且 1 处指错函数 |
| 6 诚实性 | ✅ 达标（未证事实/未核验音色/验证盲区均如实标注） |

**门禁结论：不通过。** 须先解 B1–B3 三个 BLOCKER（mock 语音路径、样式需求、§15.13 对齐），并升格/清理 MAJOR 4 项，方可进入实现。
