# doc-14 上帝模式设计（待完善）

> 状态：**待设计（2026-09-11 立项）**。doc-05 §2（玩家双模）+ §11（切换交互细节）展开为独立专题。
> 关联：doc-05 §2（双模）、§4.1.1（背包与上帝模式关系）、§5.1（god_patched_file 事件）、§8.3（move()）；doc-01 §2.1（圈选闭环继承）；doc-02（圈选/锚点/直接编辑可借鉴清单）；doc-09（frontmatter schema——上帝改文件会触碰前叙事）。

## 背景与现状（已定案，不重谈）

- 玩家身份：上帝之手 + 角色双模（doc-05 §2）；平时是角色，可切换上帝自由捏世界；
- 上帝模式**直接继承 Nodesign 的圈选 / 评论 / 直接编辑**手感（doc-01 §2.1：上帝模式直接继承，玩家是上帝不圈改世界只生活其中）；
- 上帝操作进事件表（ghost：`god_patched_file` 已有事件类型，doc-05 §5.1）；
- 背包 = 角色模式也能用的"文件搬移"动作；上帝模式拖文件 = 捏世界（doc-06 §5.3：同一套操作，两种语义无关）；
- 上帝模式与角色模式的切换细节（快捷键/按钮/过渡）doc-05 §11 待展开。

## 待设计清单

| # | 项 | 说明 |
|---|---|---|
| 1 | **双模切换交互** | 快捷键/按钮/过渡动画；切换后 UI 变化的**可见边界**（工具栏变身？光标变身？）；切换时画布/相机怎么过渡 |
| 2 | **上帝操作集** | 圈选 → 评论 → Agent 改；双击改文字；拖拽物件；新建/删除文件/物件；**能改角色 preset 吗**（改世界参数 vs 改角色人格的边界）——每项操作的"进事件表"语义（哪些变 god_patched_file） |
| 3 | **上帝改动 vs 已落定叙事的冲突边界** | 玩家把已落定的场景物件/叙事卡改了，与角色视角的**连贯性怎么保**（回滚？作家注册新事件后重新征询？）；与 move() 引用重写（doc-05 §8.3）的衔接 |
| 4 | **上帝光标呈现** | 上帝模式的玩家在场形态（对比角色模式）；圈选框/红笔划痕等节点直接继承 Nodesign（doc-04 选中 = 红笔划痕） |
| 5 | **上帝模式与骰子/choice 的边界** | 上帝模式下能"伪造骰子结果"吗（能，但进事件表并被标记为 god 改动） |

## 边界

- 上帝改动**不绕过作家感知**——所有写世界动作进事件表（doc-05 §5.1 已定）；
- 上帝档位下玩家仍是"玩家"，不是新增的第二个 agent（没有独立的上帝 agent，改动由引擎执行 + 作家感知）。

> **标记：待办**。赛时 MVP 可只做最小上帝集：双击改物件文字 + 拖拽物件 + 背包拖拽，全进事件表。圈选/评论赛后再补。