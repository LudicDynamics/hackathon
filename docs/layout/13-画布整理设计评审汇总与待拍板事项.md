# 画布重叠治理与整理 Agent：设计评审汇总

> 日期：2026-09-15。本文汇总 06–12 设计稿和三位只读评审的结果。C-01～C-13 已按 06 §4.5 canonical contract 逐条收敛，契约已冻结，允许进入实现；本轮不改源码。

## 1. 已确认的产品解法

### 1.1 生成源与画布写入分两层治理

- 生成源先修 Writer 同一 `agent_start` 内反复注入同一 `llmRole:'user'` 状态块导致的 Chalk 自激；`extensions/context.ts:125-193` 是证据，跨进程实测旧 guard 在 max=2 时 50 次均放行。guard 只能限流，不能当根因修复。
- 坐标源修复 `seatNear` 读—算—写竞态、旧 arrange 子集忽略未选障碍、玩家多卡逐条 last-writer、footprint 时序和幻影接管。所有持久写入仍只经过 ActionService/LocalWorldStore，`canvas.db` 是画布坐标真相。
- 生成中 Chalk 不能绘到邻卡。07 必须最终选定动态占位、safe clipping 或 delayed readable 之一；正式 row 接管是另一条验收线。

### 1.2 整理 Agent 的最小闭环

```text
玩家点击 → 独立 functional canvas-arranger session
  → view_canvas(auto) 读取真实 rows/overlaps/unplaced/links/presence/viewpoint
  → 必要时 screenshot_canvas 读取同一 Canvas DOM 的真实 PNG
  → arrange_canvas 通过服务端 planner + 版本/事务安全闸
  → view_canvas(auto) 复读最新 rows 并全层 AABB 校验
  → canvas_patched + agent_activity 到达前端
  → 本地状态完成/partial/failed/conflict/cancelled
```

关键边界：不占 Writer queue、不写 Markdown/frontmatter、不改玩法、不直接 SQL、不新增第二 WebSocket。模型坐标、截图视觉印象、action 回包和 staging 都不能单独证明完成。

## 2. C-01～C-13 收敛结果（契约已冻结）

| 编号 | 已收敛的冲突 | 冻结结果 |
|---|---|---|
| C-01 | 08/10 arrange HTTP request、ack、cancel 字段曾不同 | 唯一 request/ack/cancel 见 06 §4.5：opaque `worldId`；request 必含 `layer/mode/requestId/expectedRevision/expectedCanvasVersion/snapshotId/screenshotPolicy`；ack 固定 HTTP 202 + `ok:true/operationId/requestId/worldId/layer/agentId/turnId/stage`；cancel 固定带 `worldId/layer/requestId` 与 `stage` |
| C-02 | 工具名曾混用 | 仅 `view_canvas(auto)`、`screenshot_canvas`、`arrange_canvas`；不使用图像模式或其他 arrange 别名 |
| C-03 | Action/内核曾有多套名称 | ActionService 唯一 `arrangeCanvas(ctx,input)`；store 内核唯一 `arrangeCanvasLayer(store,input)`；不复制 planner |
| C-04 | version/revision/snapshot 曾混用 | `canvasVersion:number` 为 canvas.db 单调位置版本；`canvasRevision:string` 为当前 canvas 值 digest；`snapshotId:string` 绑定 rows 与 source/content digest；三者不可互换 |
| C-05 | functional 身份未受控 | exact role `functional:canvas-arranger`、scope `functional-canvas-arranger`、Actor `{type:'functional',id:'canvas-arranger'}`；缺失/未知 fail-closed，禁止 writer/engine fallback |
| C-06 | 截图 route/transport 能力边界不唯一 | screenshot 请求含短时 server-issued opaque `captureCapability`；仅 active worldId + same-origin/loopback + ready + serial gate + bounded limits；不收 root/URL/executable/proxy，child 不猜 active world |
| C-07 | DB rows 与内容 digest 读边界不一致 | snapshot reader 对 DB `canvasVersion` 与 content/source digest 做 before/after fence；变化返回 machine `conflict`，不以 hash 掩盖 torn snapshot |
| C-08 | second vision model 增加未落地复杂度 | 删除 required second model；截图/DOM 是 evidence，确定性最新 rows 全层 AABB 是完成闸 |
| C-09 | reading order/group/adjacency 无机器基线 | 删除结构承诺；只承诺可验证 links 与 card identity/count，并在 receipt 明示范围 |
| C-10 | screenshot failure 前后语义曾冲突 | 提交前按 policy 要求的 screenshot 失败 = `failed`/no-write；提交后 screenshot/DOM proof 失败保留 rows = `partial`，currentness 不明 = `conflict` |
| C-11 | local conflict 曾缺失 | D local phase 含 `conflict`（并含 `partial`）；Activity phase 仍只 `started|completed|failed` |
| C-12 | delayed readable 与 overflow 矛盾 | 07 已冻结 delayed-readable：provisional 仅 shell/ink-tip/phase，正文待正式 row 接管；不裁切、不把 overflow 当安全 |
| C-13 | Writer 自激登记不完整 | 保留 E-GEN-01；07 Phase 0 one-shot continuation boundary，guard 仅限流，不当根因修复 |

## 3. 评审后的推荐默认裁决

契约已冻结，以下不再是建议默认：

1. **工具**：仅 `view_canvas(auto)`、`screenshot_canvas`、`arrange_canvas`；保留既有 `view_canvas`，不新增别名。
2. **完成闸**：不接 required second vision model；截图/DOM 是 evidence，服务端最新 rows 全层真实 AABB 是硬完成闸。
3. **失败**：提交前按 policy 要求的 screenshot failure = failed/no-write；提交后 visual/DOM evidence failure = partial + 保留事实；currentness 不明 = conflict。
4. **外部身份**：HTTP 只暴露 opaque `worldId`；内部解析 active world。`canvasVersion`、`canvasRevision`、`snapshotId` 各自单一语义。
5. **结构承诺**：只承诺机器可复核的 links 与 card identity/count；删除 reading order/group/adjacency/relative structure 的无基线承诺。
6. **生成中 Chalk**：冻结 delayed-readable；先展示 shell/ink-tip/phase，正式文件落地后由正式卡 renderer 接管正文；不让 CSS overflow 假装安全，也不改 cards 真相。

## 4. 实施入口（契约已冻结，允许进入实现）

实现顺序仍按依赖关系执行：

1. 先实现并证明 E-GEN-01：修复同一 Writer run 的 one-shot continuation，防止座位算法掩盖额外 Chalk。
2. 按 06 §4.5 落地 Actor/Scope、worldId、error/outcome、tool/HTTP/schema 的精确集合与双向机械检查。
3. 建立 `canvasVersion` 与唯一 atomic canvas writer，迁移 near、seat/reseat、footprint、drag batch、旧 position/arrange 的写入口；所有旧入口不能绕过安全闸。
4. 落实 delayed-readable phantom 与正式 footprint 接管，执行真实 DOM red→green。
5. 实现 read-only `CanvasSnapshotV1`、server-side Playwright gate/capability 和 `screenshot_canvas`；禁止写 viewpoint/footprint/WS。
6. 实现独立 functional runtime、preset、白名单和取消/世界切换 fence；只加载三项 canonical 模型工具，不能复用 Writer session/queue。
7. 实现左侧按钮/本地 conflict/partial 状态/i18n；运行 HTTP/WS/浏览器/无障碍验收。
8. 实现后按 §5 回写代码事实与相关设计引用；不得把实现状态或文件存在冒充契约证明。

## 5. 当前交付边界

本轮完成的是 06–13 的跨篇契约收敛：C-01～C-13 均已有唯一字段、工具、动作、身份、版本、截图、失败和验收口径；契约已冻结，允许进入实现。本轮没有改源码，也没有把实现尚未存在伪装成已实现。
