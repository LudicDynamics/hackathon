# docs/init/01 — 纯函数与校验

> 归属：I1 批次 `01` 篇（`docs/init/00-共同上下文.md` §12：`emptiness.ts` / `init-fallback.ts` / `characters.ts` + `dirOf` 导出 + `index.ts` 导出纪律）。
> 上游冻结：`docs/init/00-共同上下文.md`（下称「契约」）、`docs/init/doc-11-场景与小天地初始化协议.md` §3.1/§4.1/§3.3/§5（下称「doc-11」）、`docs/nook/00-共同上下文.md` §5.2（下称「nook 00」）。
> 冲突时按契约 §0 的权威层级裁决；本文只登记冲突，**不回改契约**。
> 行号日期 2026-09-13。行号有保质期，实现前按符号名复核。
>
> **落地状态**：本批的代码已落盘（`99ae55d` 纯函数内核 → `71bd888` 补 `nookCardPaths` → `91f0724` brief 下沉 `shared` → `86ca3d4` 评审裁决修订），本文 `file:line` 以**当前工作树**为准。与契约冻结签名不一致处，全部进 §⑪。

---

## ① 一句话定位

**初始化执行的所有"该不该动"与"动了算不算成"的判断，都收在三个零依赖纯函数文件里**：`rules/emptiness.ts`（判空与判成品）、`rules/init-fallback.ts`（W2 兜底文案）、`rules/characters.ts`（角色 id 形状与路径归属）。它们吃**调用方喂进来的文件清单**，不碰 store、不碰 fs、不碰 zod——因此可以在 `node --test` 里直接表驱动穷举边界，而且 `airp-init` 命令、`GET /api/nook`、`arrangeCards` 的 nook 分支、服务端路由**共用同一份口径**，不可能出现"两套正则/两套判空"。

一句话反例（本批要消灭的状态）：契约 §1 逐字记录，本批前 `isLayerEmpty`/`isNookEmpty`/W2 兜底常量**不存在**，`isValidCharacterId` 也**不存在**；`dirOf` 是 `layers.ts` 的私有函数。本批把这三件补上，并把 `dirOf` 导出。

---

## ② 签名 / 参数

### 2.1 产物总览

```
rules/emptiness.ts        NEW   isLayerEmpty / isNookEmpty / hasInitProduct / directChildrenOf （契约 §3.1 / §3.2 / §3.4）
rules/init-fallback.ts    NEW   w2SceneTemplate                                 （契约 §3.3）
rules/characters.ts       NEW   isValidCharacterId / nookIdOf / characterIdOfPath / nookCardPaths （nook 00 §5.2 / nook 01 §2.4）
store/layers.ts           MODIFY dirOf 私有 → export（逐字移植，无逻辑改动）    （契约 §3.4）
index.ts                  MODIFY 补 `export *`（`rules/*` ×3 + `render/brief.js`；无 glob，漏行即静默不可达） （契约 §3.5 / §4）
```

### 2.2 精确签名（以已落地代码为准，`file:line` 见 §⑧）

```ts
// packages/shared/src/rules/emptiness.ts
/** Paths in `files` that are direct children of `dir` — the remainder after `dir + '/'` has no `/`. Exported: `nookCardPaths` reuses it. */
export function directChildrenOf(files: readonly string[], dir: string): string[];
export function isLayerEmpty(files: readonly string[], dir: string): boolean;
export function isNookEmpty(files: readonly string[], dir: string): boolean;
export function hasInitProduct(
  files: readonly string[],
  dir: string,
  kind: 'scene' | 'nook'
): boolean;

// packages/shared/src/rules/init-fallback.ts
export interface W2SceneFiles {
  'README.md': string;
  'opening.md': string;
}
export function w2SceneTemplate(dirName: string): W2SceneFiles;

// packages/shared/src/rules/characters.ts
export function isValidCharacterId(id: string): boolean;
export function nookIdOf(id: string): string | null;
export function characterIdOfPath(path: string): string | null;
export function nookCardPaths(allFiles: readonly string[], nookId: string): string[];

// packages/shared/src/store/layers.ts
export function dirOf(p: string): string;   // 本批从私有改为导出
```

**`files` 参数的语义（唯一口径，契约 §3.1 末段）**：`files` = `store.listFiles(dir)` 的返回值。三条既定语义（`packages/shared/src/store/local-store.ts:210-233`，实测）：

1. **跳过 dotfile 与 `node_modules`**：`entry.name.startsWith('.') || entry.name === 'node_modules'`（`:218`）。
2. **返回相 worldRoot 相对（POSIX 分隔符）路径**，即使传了 `prefix`（`:232` 的 `path.relative(this.worldRoot, abs).split(path.sep).join('/')`；`chalk.test.mjs:361` 实测 `listFiles('world/room') === ['world/room/README.md']`）。
3. **不返回目录本身**，只 push 文件（`:222-224`），且**递归**（`:220-221` 对子目录继续 walk）。

因此「`dir` 的直接子级」的精确口径 = `files` 里去掉 `dir + '/'` 前缀后**不含 `/`** 的项（`emptiness.ts:19-29` 的实现）。这一点是整个文件的地基：`isLayerEmpty`/`isNookEmpty`/`hasInitProduct` 全部建立在它之上，`nookCardPaths`（`characters.ts`）也复用它。

---

## ③ 行为契约逐步（每步写"漏了会怎样"）

### 3.1 `directChildrenOf(files, dir)`（导出，供 `nookCardPaths` 复用）

1. `prefix = dir === '' ? '' : dir + '/'`（`emptiness.ts:20`）。`dir === ''` 代表世界根本身。漏了 → 对 `dir=''` 会拼出 `'/'`，所有路径都不匹配，世界根永远"空"。
2. 逐条 `f`：不 `startsWith(prefix)` → 跳过（`:23`）。漏了 → 兄弟层/子目录的文件会污染本层判定。
3. `rest = f.slice(prefix.length)`；`rest === ''`（不可能是文件，兜底）或 `rest.includes('/')` → 跳过（`:25`）。漏了 → 深层子目录被算作直接子级（`world/a/sub/README.md` 会被当成 `world/a` 的 README）。
4. 其余 push 进 `out`，返回**名字**（不含 dir 前缀，如 `README.md`）。漏了 → 调用方拿到的是全路径，`includes('README.md')` 这类比较会错。

### 3.2 `isLayerEmpty(files, dir)`（doc-11 §3.1）

1. `!directChildrenOf(files, dir).includes('README.md')`（`emptiness.ts:37-39`）。
2. **判据只有"直接子级有没有 `README.md`"**，大小写**敏感**、逐字匹配。漏了 → `Readme.md`/`readme.md` 会被误判为"已初始化"，一个从未写过的层永远不再初始化。
3. **只看直接子级**。漏了 → 子目录里的 README 把父层"填满"，父层变永远 stub。
4. `dir` 是**相 worldRoot 相对目录**，如 `world/baker-street/crime-scene`（`emptiness.ts:31-36`）。

### 3.3 `isNookEmpty(files, dir)`（doc-11 §4.1）

1. `directChildrenOf(files, dir).every((name) => name.endsWith('.json'))`（`emptiness.ts:48-50`）。
2. **空 ⟺ 直接子级全是 `*.json`**：`preset.json` 是配置不是内容，任意其它 `.json` 同样不算内容。漏了 → 一个有 `preset.json` 的角色目录永远不是"空"，小天地初始化永不触发。
3. **只看直接子级**（契约 §3.1 冻结）：`characters/<id>/sub/letter.md` 不算填满父目录。理由：doc-11 §4.3 的产出（README.md + identity.md + 内容文件）本就落在直接子级，直接子级判定与产出规范自洽。
4. 空目录（`[]` 或只有子目录）→ `every` 对空数组返回 `true` → 空。漏了 → 无 `preset.json` 的裸目录永远不初始化。

### 3.4 `hasInitProduct(files, dir, kind)`（契约 §3.2）

1. `children = directChildrenOf(files, dir)`（`emptiness.ts:68`）。
2. `kind === 'scene'` → `children.includes('README.md')`（`:69-70`）。理由：doc-11 §3.3 要求 README 覆盖 stub 占位；README 没写成，层仍是 stub。
3. `kind === 'nook'` → `children.some((name) => !name.endsWith('.json'))`（`:71`）。任一非 `.json` 文件（README.md / identity.md / 内容文件）即合格。
4. **不合格视同 `failed`**（契约 §3.2 末段），命令走 W2（§2.3 第 6 步）。漏了 → 模型"completed 但什么都没写"会落一条 `layer_initialized` 谎报，层仍是 stub，下次进入再触发，形成死循环。
5. 与 `isLayerEmpty` 的关系：对 `kind:'scene'` 两者是**互反**（`hasInitProduct` 即 `!isLayerEmpty`）；对 `kind:'nook'` **不等价**——`isNookEmpty` 排除所有 `.json`，`hasInitProduct` 只要求存在非 `.json`，二者是同一判据的两面。

### 3.5 `w2SceneTemplate(dirName)`（doc-11 §5）

1. 返回**两个文件的完整内容**，键即文件名（`init-fallback.ts:29-35`）。
2. `'README.md'` = 占位 readme：frontmatter `type: readme` / `name: <dirName>` / **`material: stub`**，正文 `# <dirName>` + `(This place has not taken shape yet.)`（`:31-32`）。
3. `'opening.md'` = 空 chalk：frontmatter `type: chalk`，正文 `(You stand here. Nothing is clear yet.)`（`:33`）。
4. **文件名冻结**为 `README.md` 与 `opening.md`（契约 §3.3）。`opening.md` 与 `03` 篇正文一致；**不得**用 `evening.md`（那是 holmes 世界的手写内容）。漏了 → 写到 `evening.md` 会覆盖模板世界既有内容。
5. **`material: stub` 是合法的**（`MATERIAL_SKINS` 含 `stub`，`schemas/forms.ts:193-198`），是"这里还没长出来"的皮肤。与"成功产出的 README 用 brief 给的默认材质"（doc-11 §3.3）**不冲突**：两条分属"成功产出"与"W2 兜底"两个不同路径（契约 §3.3 末条）。
6. `isLayerEmpty` 与 W2 自洽：W2 落下 `README.md` 之后该层不再是 stub，不会二次触发。

### 3.6 `isValidCharacterId(id)` / `nookIdOf(id)` / `characterIdOfPath(path)`（nook 00 §5.2）

1. `CHARACTER_ID_RE = /^[a-z0-9][a-z0-9-]*$/`（`characters.ts:21`）；`isValidCharacterId` 即 `.test(id)`（`:24-26`）。
2. `nookIdOf(id)` = 合法 → `` `characters/${id}` ``，否则 `null`（`:35-37`）。**这是唯一拼 nook 路径形状的地方**（`:28-34` 注释；nook 00 反模式 2）。
3. `characterIdOfPath(path)`（`:46-53`）：
   1. `path` 不以 `characters/` 开头 → `null`（`:48`）。
   2. 去掉前缀取 `rest`；取 `rest` 里**第一个 `/` 之前**的段作 `id`（`:49-51`）。因此 `characters/ryo` 与 `characters/ryo/letters/unsent.md` 都得 `'ryo'`（它只回答"归属哪个角色"，不回答"是不是直接子目录"）。
   3. `<id>` 必须通过 `isValidCharacterId`，否则 `null`（`:52`）。
4. **穿越天然被挡住**：id 段不含 `/`，且正则不接受 `.`。`characters/a/../..` → `'a'`（剩余段无关）；`characters/` → `rest=''` → 非法 → `null`；`characters`（无尾斜杠）→ `startsWith('characters/')` 假 → `null`。漏了正则这道闸、改用"路径 normalize"→ 会把 `characters/<id>/../..` 放过（nook 00 §3.2 明禁）。

### 3.7 `nookCardPaths(allFiles, nookId)`（nook 01 §2.4，契约 §3.5 裁决新增）

1. 复用 `directChildrenOf(allFiles, nookId)`（`characters.ts:66`），**不自写第三份**「直接子级」（契约 §3.4 纪律）。
2. 过滤 `.md` 且**不等于 `README.md`**（`:67`），再拼回 `nookId + '/' + 文件名`（`:68`）。
3. 与 `characterIdOfPath` 是**两个问题**，不得合并：本函数回答「页面显示什么」（只看直接子级），后者回答「路径归属谁」（含子目录）（nook 01 §2.2 教训）。
4. 输出是**全路径**（`characters/<id>/x.md`），而 `directChildrenOf` 输出**名字**——调用方注意口径差。

---

## ④ 文件与副作用

| 文件 | 性质 | 副作用 |
|---|---|---|
| `packages/shared/src/rules/emptiness.ts` | 零依赖纯函数（不 import zod/yaml/node builtins） | **无** |
| `packages/shared/src/rules/init-fallback.ts` | 零依赖纯常量构造 | **无** |
| `packages/shared/src/rules/characters.ts` | 零依赖纯函数 | **无** |
| `packages/shared/src/store/layers.ts` | 单一改动：`dirOf` 加 `export` | **无** |
| `packages/shared/src/index.ts` | `export *` × 4（`rules/*` ×3 + `render/brief.js`） | **无** |

**为什么放 `rules/`**：nook 00 §5.2 逐字：`rules/` 是「零依赖纯规则」的既有归属地（先例 `rules/dice.ts`、`rules/interactive.ts`）。不要再造一个 `utils/` 或 `lib/`。
**零依赖是硬要求**：`interactive.ts:1-6` 逐字写明"ZERO dependencies on purpose"，`dice.ts` 同。这样 `node --test packages/shared/test/init-rules.test.mjs` 可以直接 import `dist/index.js` 跑（`init-rules.test.mjs:3-14` 实测如此），不依赖整条 barrel 绿。
**无副作用 = 可直接单测**：这些函数不读盘、不读时间、不读随机数，输入输出完全确定（§⑩ 用例表即全部行为）。

---

## ⑤ 落账

**本文件不落任何事件。** 三个文件是纯函数/纯常量，没有 `ActionContext`、不 `appendEvent`。

落账发生在调用者：`hasInitProduct` 的返回值驱动 `airp-init` 命令分流到 `recordLayerInitialized` / `recordLayerInitFailed`（`packages/shared/src/actions/layer.ts:105-171`，契约 §2.3 第 6 步）。事件的 `type` 与 detail 形状**本批零新增**（契约 §6：`layer_initialized: { layer, name, by, files }` / `layer_init_failed: { layer, name, reason, fallback }`，`schemas/events.ts:96-106`）。`w2SceneTemplate` 的产物由命令写盘后，`files` 数组按实际落盘路径构造，交给 `recordLayerInitialized` / `recordLayerInitFailed`。

---

## ⑥ WS 与前端

**本文件不产生、不消费任何 WS 帧。** 无 `world_event`、无前端消费面。函数结果只经 `airp-init` 命令 → 动作层 → `world_event` 广播（A 档接线，属 `03` 篇）。契约 §9 的边界声明适用：本批不碰 `useWorld.ts` 的 switch。

---

## ⑦ 错误边界

| 情形 | 行为 | 依据 |
|---|---|---|
| `dir` 带尾斜杠（`'world/x/'`） | **未定义**：`prefix` 会拼成 `'world/x//'`，全部不匹配 → 一律判"空"。调用方 MUST 传归一化目录（无前导 `./`、无尾 `/`） | `emptiness.ts:20`；契约 §3.1 只说"worldRoot 相对" |
| `dir` 相对世界根但 `files` 来自别的前缀 | 判定结果无意义（前缀不匹配） | `files` 语义见 §2.2 |
| `dir = ''` | 世界根本身：`directChildrenOf` 收根下的直接文件（如 `world.json`） | `emptiness.ts:20` |
| 目录里只有 dotfile（`.gitkeep`） | `listFiles` 跳过 dotfile → 判"空" | `local-store.ts:218` |
| 目录里只有 `node_modules/` | 同上被跳过 → 判"空" | `local-store.ts:218` |
| 文件大小写不匹配（`Readme.md`） | 视为**没有** README → 判 stub；`.JSON` 亦不算 `.json` → 算内容 | 字节级 `includes`/`endsWith`（`emptiness.ts:38,49,70`） |
| `w2SceneTemplate('')` | 生成 `name:` 与 `# ` 为空的 README；不报错 | 无入参校验（纯字符串插值） |
| `dirName` 含 `/` | 原样插进 frontmatter/标题；不报错、不 sanitize | `init-fallback.ts:31-32` |
| `isValidCharacterId(null/undefined)` | 运行时 `.test` 收到非字符串会抛 `TypeError`（TS 签名排除）；调用方 MUST 先保证是 `string` | `characters.ts:25` |

---

## ⑧ 代码落点（精确到文件与函数）

| # | 文件 | 符号 | 行 | 说明 |
|---|---|---|---|---|
| 1 | `packages/shared/src/rules/emptiness.ts` | `directChildrenOf`（**export**） | `19-29` | 直接子级提取，三函数 + `nookCardPaths` 共用 |
| 2 | 同 | `isLayerEmpty` | `37-39` | doc-11 §3.1 |
| 3 | 同 | `isNookEmpty` | `48-50` | doc-11 §4.1 |
| 4 | 同 | `hasInitProduct` | `63-72` | 契约 §3.2 |
| 5 | `packages/shared/src/rules/init-fallback.ts` | `W2SceneFiles`（interface） | `18-23` | 键 = 文件名 |
| 6 | 同 | `w2SceneTemplate` | `29-35` | 契约 §3.3 |
| 7 | `packages/shared/src/rules/characters.ts` | `CHARACTER_ID_RE`（私有） | `21` | `^[a-z0-9][a-z0-9-]*$` |
| 8 | 同 | `isValidCharacterId` | `24-26` | nook 00 §5.2 |
| 9 | 同 | `nookIdOf` | `35-37` | 唯一 nook 路径构造 |
| 10 | 同 | `characterIdOfPath` | `46-53` | path → id，穿越被正则挡 |
| 11 | 同 | `nookCardPaths` | `65-69` | 页面成员 = 直接子级 .md − README（nook 01 §2.4） |
| 12 | `packages/shared/src/store/layers.ts` | `dirOf`（私有 → **export**） | `46-49` | 契约 §3.4，逐字移植 |
| 13 | `packages/shared/src/index.ts` | `export *` × 4（`rules/*` ×3 + `render/brief.js`） | `15-17,60` | 契约 §3.5 / §4 |
| 14 | `packages/shared/test/init-rules.test.mjs` | 10 条单测 | `18-127` | §⑩ 的已落地子集 |

**已落地的单测**（`packages/shared/test/init-rules.test.mjs`，10 条，实测 `node --test` 报 `tests 10`）：`isValidCharacterId`（`:18-23`）、`nookIdOf`（`:25-29`）、`characterIdOfPath`（`:31-40`）、`dirOf`（`:44-48`）、`isLayerEmpty`（`:52-68`）、`isNookEmpty`（`:70-82`）、`hasInitProduct`（`:84-93`）、`w2SceneTemplate`（`:97-103`）、`directChildrenOf`（`:108-114`）、`nookCardPaths`（`:116-127`）。
**另**：`packages/shared/test/brief.test.mjs`（12 条，实测）覆盖 `buildSceneInitBrief` / `buildNookInitBrief`（`packages/shared/src/render/brief.ts`，契约 §4；`02` 篇引用）。

**调用点（本批其他篇）**：
- `isLayerEmpty` / `isNookEmpty` → `airp-init` 命令的判空短路（契约 §2.3 第 2 步；`02` 篇）。
- `hasInitProduct` → 命令的产物校验（契约 §2.3 第 6 步；`02` 篇）。
- `w2SceneTemplate` → 命令的 W2 兜底写盘（契约 §2.3 第 6 步；`02` 篇）。
- `isValidCharacterId` / `nookIdOf` / `characterIdOfPath` → `GET /api/nook`、`arrangeCards` 的 nook 分支、`POST /api/card/footprint` 的 nook 分支（nook 00 §5.2，`docs/nook/01`/`04`）+ 本批 `airp-init` 的 target 归一（契约 §2.2）。
- `nookCardPaths` → `GET /api/nook` 服务端 + `arrangeCards` 的 nook 分支（nook 01 §2.4，契约 §3.5 裁决）。
- `dirOf` → **本批无消费者**（在 `emptiness.ts` 内）：`emptiness.ts` 的"直接子级"走 `directChildrenOf` 的前缀切法（`emptiness.ts:19-29`），并未调用 `dirOf`；`layers.ts` 内部 `cardsOfLayer:111` 仍用它。导出是为契约 §3.4 的"杜绝第三份"（见 §⑪-8）。

---

## ⑨ 与现状差异

以本批改动的**前状态**（commit `09131cb`）为基准：

| # | 前状态 | 本批 | 理由 |
|---|---|---|---|
| D1 | `isLayerEmpty` / `isNookEmpty` / `hasInitProduct` 全不存在（契约 §1 逐字记录） | 新建 `rules/emptiness.ts` | doc-11 §9.1 第 3 项 |
| D2 | W2 兜底常量不存在（契约 §1） | 新建 `rules/init-fallback.ts` | doc-11 §9.1 第 5 项 |
| D3 | `isValidCharacterId` 等三函数不存在（nook 00 §2.3 表）；`nookCardPaths` 亦不存在 | 新建 `rules/characters.ts`（四函数） | nook 00 §5.2 + nook 01 §2.4（契约 §3.5 裁决） |
| D4 | `dirOf` 是 `layers.ts:42` 的**私有**函数 | 加 `export`（`layers.ts:46`），签名与实现逐字不变 | 契约 §3.4；避免 `emptiness.ts` 写第三份（但实际未使用，见 §⑪-8） |
| D5 | `index.ts` 无 `rules/*` 三个模块与 `render/brief.js` 的导出（`index.ts` 无 glob） | 补 `export *` × 4（`:15-17,60`） | 契约 §3.5 / §4；漏行即"静默不可达" |
| D6 | 无纯函数单测 | 新增 `test/init-rules.test.mjs`（10 条，实测） | 契约 §10 门禁 |

**行为层面零回归**：所有新增都是 additive（新文件 + 一个 `export` 关键字 + 四条 barrel 导出），既有 `deriveLayers`/`cardsOfLayer`/`childLayers`/`layerOfPath` 的行为一字未改。`dirOf` 的导出不改其行为，`cardsOfLayer:111` 的调用照旧。

**下游可机械核验的差异**：`pnpm build`（契约 §10：`extensions/` 按相对路径 import `dist`，改了 `src` 不 build 运行时跑旧代码，AGENTS §6.5）；`node --test packages/shared/test/init-rules.test.mjs`。

---

## ⑩ 验收测试（纯函数用例表）

**运行方式**：`pnpm --filter @airp/shared build && node --test packages/shared/test/init-rules.test.mjs`（测试 import `../dist/index.js`，必须先 build；`init-rules.test.mjs:3-14`）。

### 10.1 `isValidCharacterId(id)`

| # | 输入 | 期望 | 备注 |
|---|---|---|---|
| 1 | `'watson'` | `true` | 基本 kebab |
| 2 | `'a'` | `true` | 单字符 |
| 3 | `'a1'` / `'1a'` | `true` | 数字可开头 |
| 4 | `'sherlock-holmes'` | `true` | 多段 |
| 5 | `''` | `false` | 空 |
| 6 | `'Watson'` / `'A'` | `false` | 大写 |
| 7 | `'a_b'` | `false` | 下划线 |
| 8 | `'-lead'` | `false` | 前导连字符 |
| 9 | `'a/b'` | `false` | 路径分隔 |
| 10 | `'a b'` | `false` | 空格 |
| 11 | `'a..b'` | `false` | 点 |
| 12 | `'café'` | `false` | 非 ASCII |
| 13 | `'a-'` / `'a--b'` | `true` | **尾随/连续连字符是合法的**（正则允许；见 §⑫-1） |

### 10.2 `nookIdOf(id)`

| # | 输入 | 期望 |
|---|---|---|
| 1 | `'watson'` | `'characters/watson'` |
| 2 | `'Bad'` | `null` |
| 3 | `'../../etc'` | `null` |
| 4 | `''` | `null` |

### 10.3 `characterIdOfPath(path)`

| # | 输入 | 期望 | 备注 |
|---|---|---|---|
| 1 | `'characters/watson'` | `'watson'` | 无尾段 |
| 2 | `'characters/watson/README.md'` | `'watson'` | 直接子级 |
| 3 | `'characters/a/b'` | `'a'` | **`characters/a/b` 归属 `a`**（id 段不含 `/`） |
| 4 | `'characters/watson/nested/deep.md'` | `'watson'` | 深子目录仍归属 |
| 5 | `'world/baker-street/x.md'` | `null` | 前缀不对 |
| 6 | `'characters/'` | `null` | 空 id |
| 7 | `'characters'` | `null` | 无尾斜杠 |
| 8 | `'characters/a/../..'` | `'a'` | 穿越段被忽略 |
| 9 | `'characters/Bad/x.md'` | `null` | 非法 id |

### 10.4 `dirOf(p)`

| # | 输入 | 期望 |
|---|---|---|
| 1 | `'a/b/c.md'` | `'a/b'` |
| 2 | `'a'` | `''` |
| 3 | `''` | `''` |

### 10.5 `isLayerEmpty(files, dir)`（`dir = 'world/baker-street/crime-scene'`）

| # | `files` | 期望 | 命中边界 |
|---|---|---|---|
| 1 | `[]` | `true` | **空目录** |
| 2 | `[dir + '/evening.md']` | `true` | 有内容、无 README |
| 3 | `[dir + '/README.md']` | `false` | **有 README** |
| 4 | `[dir + '/readme.md']` | `true` | **大小写**（小写不算） |
| 5 | `[dir + '/Readme.md']` | `true` | **大小写**（混合大小写不算） |
| 6 | `[dir + '/sub/README.md']` | `true` | **深层子目录不算直接子级** |
| 7 | `[dir + '/README.md', 'world/baker-street/README.md']` | `false` | 兄弟层不污染 |
| 8 | `['world/other/README.md']` | `true` | 完全无关的层 |
| 9 | `[dir + '/.gitkeep']` | n/a | dotfile 根本不会出现在 `listFiles` 结果里（§2.2） |

### 10.6 `isNookEmpty(files, dir)`（`dir = 'characters/watson'`）

| # | `files` | 期望 | 命中边界 |
|---|---|---|---|
| 1 | `[]` | `true` | **空目录** |
| 2 | `[dir + '/preset.json']` | `true` | **仅 preset.json** |
| 3 | `[dir + '/preset.json', dir + '/other.json']` | `true` | **仅 json** |
| 4 | `[dir + '/preset.json', dir + '/README.md']` | `false` | 有 README |
| 5 | `[dir + '/letter.md']` | `false` | 有 md |
| 6 | `[dir + '/sub/letter.md']` | `true` | **深层子目录不算直接子级** |
| 7 | `['characters/other/preset.json']` | `true` | 别的角色不污染 |
| 8 | `[dir + '/asset.webm']` | `false` | 非 json 非 md 也算内容（见 §⑪-5） |
| 9 | `[dir + '/preset.JSON']` | `false` | 大小写：`.JSON` 不算 `.json` → 算内容 |

### 10.7 `hasInitProduct(files, dir, kind)`

| # | `dir` / `kind` | `files` | 期望 |
|---|---|---|---|
| 1 | scene | `[]` | `false` |
| 2 | scene | `[dir + '/evening.md']` | `false`（无 README） |
| 3 | scene | `[dir + '/README.md']` | `true` |
| 4 | scene | `[dir + '/sub/README.md']` | `false`（子目录不算） |
| 5 | nook | `[dir + '/preset.json']` | `false`（只有 json） |
| 6 | nook | `[dir + '/preset.json', dir + '/letter.md']` | `true` |
| 7 | nook | `[dir + '/README.md']` | `true` |

### 10.8 `w2SceneTemplate(dirName)`

| # | 输入 | 期望 |
|---|---|---|
| 1 | `'crime-scene'` | 键集合 = `['README.md', 'opening.md']` |
| 2 | 同上 | `README.md` 以 `---\ntype: readme\nname: crime-scene\nmaterial: stub\n---` 开头 |
| 3 | 同上 | `README.md` 含 `# crime-scene` 与 `(This place has not taken shape yet.)` |
| 4 | 同上 | `opening.md` 以 `---\ntype: chalk\n---` 开头，含 `(You stand here. Nothing is clear yet.)` |
| 5 | 同上 | **不含** `evening.md` 键（文件名冻结） |
| 6 | 同上 | `README.md` 的 `material` 解析结果为 `stub`，且 `materialSkinOf('stub') === 'mat-stub'`（非静默回落 `mat-warm`） |

> §10.1–10.8 中的已落地子集见 `packages/shared/test/init-rules.test.mjs`；上表新增的边界（§10.1#13、§10.5#5/#8、§10.6#8/#9、§10.7#4/#7、§10.8#5/#6）建议补进该文件。

---

## ⑪ 发现的冲突

> 逐条带 `file:line`。**不回改契约**，由主 agent 裁决。

### 冲突 1 — `w2SceneTemplate` 的返回形状：`{readme,chalk}` vs 文件名键 —— **已解决（契约/落地，2026-09-13）**

- 初稿分歧：契约 §3.3 曾冻结 `{ readme: string; chalk: string }`，代码（`init-fallback.ts:18-23,29-35`）给**文件名键** `W2SceneFiles { 'README.md'; 'opening.md' }`。
- **裁决已落**：契约 §3.3（`docs/init/00` §3.3）已改为文件名键，与代码逐字一致：
  ```ts
  export interface W2SceneFiles { 'README.md': string; 'opening.md': string }
  export function w2SceneTemplate(dirName: string): W2SceneFiles;
  ```
- 保留此条仅作历史：`{readme,chalk}` 作废；文件名即键，写入侧零映射（`Object.entries` 直接落盘）。

### 冲突 2 — W2 模板文案：doc-11 §5 是中文，契约 §3.3 冻结英文

- doc-11 §5:309-310 逐字给中文模板：`"（这里还没长出来。）"` / `"（你站在这里。还看不清什么。）"`。
- 契约 §3.3（`docs/init/00` 第 157-158 行）冻结英文：`(This place has not taken shape yet.)` / `(You stand here. Nothing is clear yet.)`。
- 代码（`init-fallback.ts:32-33`）取**英文**。
- 二者不可同时成立。按权威层级（契约优先级 1 > doc-11 优先级 2）代码取英文是对的；但这是**契约单方面改写了 doc-11 的正文**，应回写 doc-11 §5（归 `04` 篇回写清单）或由主 agent 拍板 W2 使用哪种语言。

### 冲突 3 — `layerIdOf`（`actions/layer.ts:23`）与 `layerOfDir`（`store/layers.ts:37`）是同一条映射的两份实现

- `packages/shared/src/actions/layer.ts:22-25`：
  ```ts
  /** The layer id a directory maps to, mirroring store/layers.ts (`world/` → `map`). */
  function layerIdOf(dir: string): string { return dir === 'world' ? 'map' : dir; }
  ```
- `packages/shared/src/store/layers.ts:36-39`：
  ```ts
  export function layerOfDir(dir: string): string { return dir === WORLD_DIR ? MAP_LAYER : dir; }
  ```
- 语义**逐字等价**（`WORLD_DIR='world'`、`MAP_LAYER='map'`，`layers.ts:27-29`）；`layerIdOf` 的注释自认"mirroring store/layers.ts"。而且 `actions/layer.ts:66,113,154` 又各自内联了 `layer === 'map' ? 'world' : layer`（即 `dirOfLayer` 的手写版）。
- 契约 §8 反模式 4 只点名了 `dirOf`/`layerOfDir`/map↔world 映射"不许重写"，但 `layerIdOf` **是既存的第三份**（契约 §3.4 只提 `dirOf` + `actions/layer.ts:23`，未细说它就是 map↔world 的一份）。本批**未动它**（不在授权范围）。
- 建议：由主 agent 裁决是否把 `layerIdOf` 改为直接引用 `layerOfDir`、把 `actions/layer.ts:66,113,154` 的内联改为引用 `dirOfLayer`。这是**既有重复**，非本批引入。

### 冲突 4 — `dirOf`（`store/layers.ts:46`）与 `dirname`（`actions/refs.ts:65`）是同一实现的两份拷贝

- `layers.ts:46-49` 与 `refs.ts:65-68` 是**逐字相同**的实现（`lastIndexOf('/')` → `-1 ? '' : slice(0, i)`），仅函数名不同（`dirOf` vs `dirname`）。
- 契约 §3.4 只要求导出 `dirOf` 以"避免第三份"，但**第二份 `dirname` 已存在**。`actions/chalk.ts:24`（import）、`:371,375` 与 `local-store.ts:15` 用的是 `refs.ts` 的 `dirname`。
- 本批**不动**（改 `refs.ts` 超出授权，且 `dirname` 是 refs 模块的公开面）。建议主 agent 登记：未来统一为一处，避免三份继续分叉。

### 冲突 5 — `isNookEmpty` 对"非 md 的非 json 文件"（如 `assets/*.webm`）的口径已被代码固化，但 nook 侧仍标"待拍板"

- `docs/nook/04-占位与回写.md:313`（原文）建议"只要存在任一非 `.json` 文件即非空"，标为**待拍板**；`docs/nook/RESEARCH-初始化链路.md:271` 同样列为待拍板。
- 代码（`emptiness.ts:48-50`）已把**所有**非 `.json` 文件（含 `.webm`）算作内容 → 该问题在代码层**已经裁决为"算内容"**，但 nook 侧文档仍写"待拍板"。
- 建议：nook `04`/RESEARCH 与契约 §3.1 同步该结论（`isNookEmpty` 的判据是"直接子级是否全 `.json`"，与扩展名白名单无关）。

### 冲突 6 — `characters.ts` 是否含第四个函数 `nookCardPaths` —— **已解决（契约/落地，2026-09-13）**

- 分歧：nook 00 §5.2 冻结三函数；`docs/nook/01-取数端点.md:27,56` 要求新增 `nookCardPaths(allFiles, nookId)`。
- **裁决已落**：契约 §3.5（`docs/init/00` §3.5）采纳 nook 01 §2.4，`characters.ts` 现有**四**函数，第四为 `nookCardPaths`（`characters.ts:65-69`）；`emptiness.ts` 相应导出 `directChildrenOf`（`emptiness.ts:19-29`）供其复用。本文 §2.2 / §3.7 / §⑧ 已补齐。

### 冲突 7 — doc-11 §9.1 第 1 项 `buildInitBrief(kind, ctx)` 与契约的两函数拆分不一致（登记，非本文件）

`docs/init/doc-11:448` 写"纯函数 `buildInitBrief(kind, ctx)`，两个入口共用"，而契约 §4 冻结的是 `buildSceneInitBrief` / `buildNookInitBrief` **两个**函数（落点 `packages/shared/src/render/brief.ts`；原 `apps/server/src/engine/brief-builder.ts` 已删除，契约 §4 裁决下沉）。归 `02` 篇，本文只登记以免遗漏。

### 冲突 8 — 契约 §3.4 要求导出 `dirOf` 以"供 emptiness 复用"，但已落地的 `emptiness.ts` 并未用它
- 契约 §3.4（`docs/init/00` 第 164-173 行）："本批 MUST 导出一个共用的路径工具……**MUST NOT** 在别处重写第四份"，且 §3.1（`docs/init/00` 第 130 行）逐字写"「取直接子级」需要一个 `dirOf`"。
- 实际落地：`emptiness.ts:19-29` 的 `directChildrenOf` 走的是**前缀切片**（`f.startsWith(dir + '/')` + 剩余部分不含 `/`），**没有** import 或调用 `dirOf`。`dirOf` 目前**唯一**的调用者是 `layers.ts:111`（既有）。
- 建议：契约 §3.1（`docs/init/00` 第 130 行）那句"需要一个 `dirOf`"应放宽为"需要一个统一的前缀/目录提取口径，可导出 `dirOf` 或等价实现"；或让 `emptiness.ts` 改用 `dirOf(f) === dir`（`cardsOfLayer` 的写法）以真正复用——**由主 agent 拍板**（纯风格，无行为差异）。

### 对齐记录（2026-09-13）
- 对齐对象：契约 `docs/init/00-共同上下文.md`（已冻结）与已落地代码（`packages/shared/src/rules/{emptiness,characters,init-fallback}.ts`、`store/layers.ts`、`index.ts`、`test/{init-rules,brief}.test.mjs`）。
- 本次改：原私有 helper 名改为实际导出的 `directChildrenOf`；补 `nookCardPaths`（§2.2/§3.7/§⑧）；`w2SceneTemplate` 返回形状确认为文件名键 `W2SceneFiles`（冲突 1 关闭）；冲突 6 关闭；全篇 `file:line` 按当前工作树重算；单测计数改实际（init-rules 10 条 + brief 12 条）。
---

## ⑫ 仍未知待拍板

1. **尾随/连续连字符是否合法**：正则 `^[a-z0-9][a-z0-9-]*$` 接受 `'a-'`、`'a--b'`（`characters.ts:21`）。"kebab-case"直觉通常不允许尾随连字符。若要收紧，应改正则并同步 nook 00 §5.2——**改契约级常量**，需主 agent 拍板。当前行为：合法。
2. **`dir` 的归一化责任在谁**：`directChildrenOf`（`emptiness.ts:20`）直接用 `dir + '/'`，不剥尾斜杠、不 trim 前导 `./`。既有调用方（契约 §2.2 的 target 归一）会先过 `dirOfLayer`/`layerOfDir`，但那两个也不 normalize。是否在纯函数内做一次 `replace(/\/+$/,'')` 归一？**未定**。当前契约：调用方保证。
3. **`w2SceneTemplate` 是否需要一个 nook 版**：doc-11 §5 只给场景模板；小天地的 W2 兜底（超时 45s 后落什么）**无模板**。契约 §3.3 也只定义 `w2SceneTemplate`。小天地失败是否落 W2、落什么，**未定**。
4. **`hasInitProduct` 之 `kind:'nook'` 是否要求 README**：当前只要求"任一非 json"，因此只落一个 `letter.md` 就判成功，README 仍缺。nook 门面（`docs/nook/01 §3.1` 的 `scene === null` 空态）可能因此一直空。是否收紧为"必须含 README.md"？**未定**（收紧会与 doc-11 §4.3「README 若缺则补」的宽松口径分离）。
5. **`material: stub` 与前端"幻影占位"的接合点**：W2 落 `material: stub` 的 README 后，前端是否据此显示"还没长出来"的幻影态（doc-11 §3.4），还是要另设标志？纯函数侧已尽力（`material: stub` 是唯一信号），前端消费归 `03`/前端批。**未定**。
6. **`W2SceneFiles` 的键顺序**：`Object.keys` 顺序目前是 `README.md`（先）→ `opening.md`（后）（`init-fallback.ts:30-34`）。落盘顺序是否有语义（例如先 README 保证判空短路立即生效）？**未定**；当前测试只用 `.sort()` 断言集合（`init-rules.test.mjs:99`）。
