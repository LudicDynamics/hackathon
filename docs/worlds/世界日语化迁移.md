# 世界日语化迁移

> 2026-09-12 用户确认五个样板世界迁日语，先迁初雪为 `first-snow-jp`。2026-09-14 已完成六组体验版的英日正式模板收拢；英文稳定模板 ID `firstsnow` 沿用旧存档兼容口径，日文使用 `first-snow-jp`。所有体验版文件、目录和人物 ID 统一英文 kebab-case，标题、正文和世界提示词随世界语言。只复用图形，内部设计文档保留中文。迁移工具与验收见 `体验版稳定路径与门禁演出.md`。

## 1. 当前入口（2026-09-14）

| 世界 | 当前状态 |
|---|---|
| 初雪 | `templates/firstsnow`／`templates/first-snow-jp`：最新体验的英文／日文；英文稳定 ID 沿用 `firstsnow`，不产生 `first-snow` 正式路径 |
| 雾坞镇 | `templates/wuwu`／`templates/wuwu-jp`：保留零级导入、地点主视图与雾外生长 |
| 福尔摩斯 | `templates/whitechapel`／`templates/whitechapel-jp`：保留 Holmes 身份、Watson 来信与地点导航 |
| 时间线 | `templates/divergence`／`templates/divergence-jp`：保留零级导入 → 时间 → 对应地点 |
| 魔法学院 | `templates/magic-academy`／`templates/magic-academy-jp`：最新体验内容 |
| 未写之门 | `templates/unwritten-door`／`templates/unwritten-door-jp`：门外按需生成 |
| 月下之誓 | `templates/moonlit-contract`／`templates/moonlit-contract-jp`：契约与逐层探索 |

路径均在 `templates/` 下。以上不只是 UI 切换：世界正文、人物资料、选项与提示词分别本地化。旧 18 个模板已归档；`*-playtest` 只作为被忽略的历史／实验 fixture，玩家存档未迁移。当前清单及验证以 [七世界双语模板与归档](七世界双语模板与归档.md) 为准。以下保留初雪首次迁移时的范围说明，旧目录结构对应归档原型，不是现行游戏入口。

## 2. 初雪重写范围

- 世界名、描述、玩家身份、人物显示名；
- 根 README、玩家文档与世界语言 slot；
- 所有场景 README、Chalk、信件、物件、选项；
- 七海与雪村澄各自的身份、性格、空白记忆、preset 名称与描述；
- 英文与日文世界 skill、角色资料和资产说明。

协议键、工具名及引擎保留名不翻译；**普通内容路径保持英文稳定 ID**。角色目录、可配置文件名若变更，连同角色引用与 preset 文件槽一起迁，不只改显示名。复用九张 WebP 的图形字节，既有资源文件名与引用保持不变；不复制旧会话、数据库或生成结果。图中文字仍是图片复用边界，不能声称已重绘。

## 3. 主观流程与目录

玩家先在零级放送室听到七海叫自己，读到纸上消不干净的约定；进入一级“今夜の約束”，再选择放送室、咖啡店或屋顶。场景里的物件让玩家有具体话题可问，而不是只点角色名字选恋爱结局。

正式模板结构（稳定英文 ID；内部场景路径以当前内容包为准）：

```text
world/                         零级：放送終了まで九十秒
  README.md                    场景第一眼
  01-opening.md                首次可行动 Chalk
  request-sheet.md             可携带的节目用纸
  tonight/                     一级：今夜の約束
    radio-studio/              二级：七海与台本
    amber-cafe/                 二级：澄与四十分钟
    campus-rooftop/             二级：初雪旧约
    first-snow/                 二级：短结算的场景骨架
```

当前前端以 `map` 对应 `world/`，因此零级直接放根目录；不是绕过导入进入一级。每个目录仍用自己的 README 描述场景，没有在 manifest 里重复声明 layers。

体验版命名示例：`world/tonight-promises/radio-studio/README.md`、`world/tonight-promises/first-snow/snow-shelter/README.md`、`player/tonight-letter.md`。日本語写在 title、正文和世界指令里，跳转与关联始终使用稳定路径。

`README.md`、`world.json`、`SKILL.md` 保留；人物资料也用英文 ID（如 `personality.md`、`memory.md`）。`first-snow-jp` 继续标识日语内容包，不要求目录名本身写成日语。

## 4. 生成语言与显示接线

当前 Writer 通过世界 skill 加载 `skills/first-snow-play/SKILL.md`（英文）或 `skills/first-snow-jp-play/SKILL.md`（日本語）；语言说明由各角色 preset 的 `world-language` slot 提供，不再依赖独立 `language.md`。角色 preset 使用平台 `system-char`，其后读取世界 skill 与人物文档。共享指令和动态 brief 的人类可读部分仍按统一提示词契约处理；工具字段与协议不翻译。

两个角色 preset 里的语言槽相同：

```json
{
  "kind": "slot",
  "id": "world-language",
  "slot": "file",
  "options": {
    "path": "language.md",
    "baseDir": "cwd",
    "onMissing": "skip"
  }
}
```

`world.json.locale = "ja"` 让 UI 在世界载入时切日语；`characters[].name` 是可选显示名，不改变 `characters[].id` 或文件路径。未声明的旧模板保持原回退行为。

## 5. 本轮不算完成的内容

初雪已有现场赴约选项、两份约定、信件 Gate 与可选避雪规则，但真实 Writer 首测漏写关键文件。最新目标是“玩家触发 ending → 既有 CG → 根据此前各层 RP 实时生成短后日谈”，还未按此完成实现。见 [初雪验收](../gameplay/初雪短闭环与AI生长验收.md)；不扩展长后日谈或完整重玩系统。

角色回复接线以 [Agent 前端接线](../agent/Agent前端接线.md) 的当前实现为准；它不代表全语言提示词、角色记忆与结局选图已经实玩通过。

## 6. 验收

- `node --test tools/first-snow-jp.test.mjs`：manifest、日文文本、图片一致性、场景层级、角色 preset 文件槽与无旧存档；
- `pnpm build && pnpm probe`：全仓构建与既有离线探针；
- 在用户入口 `http://localhost:5173/` 的世界列表选择“初雪ラジオ · 日本語”，检查导入、角色名、物品、一级导航与选项；
- 图片复用和静态验收不代表真实模型已完成一整局 RP。
- 先建立旧路径 → 新路径映射，再在新模板副本同步 README target、requires.items、entry、home、preset path/baseDir、skill 中的指令路径、图片引用与测试。本轮只对体验版存档做离线备份迁移，不删除进度；非体验版不改。
- 验证英文稳定路径与日语标题从文件扫描、Gate 进入、工具读写到持久化回访全部连通；不将字符串显示成功等同路径链已兼容。
- 采样最终运行提示词，确认世界 skill、动态 brief 与共用指令的人类可读部分均为对应语言；仅检验输出语言不足以验收“所有提示词”。
