# doc-15 世界模板规范与开局引导（待完善）

> 状态：**待设计（2026-09-11 立项）**。doc-05 §11（模板题材清单、模板=预写骨架的内容规范）+ doc-07 §3（模板世界目录/scaffold）展开为独立专题。
> 关联：doc-05 §8.4（manifest schema）、§8.6（zip 分享）、§1（懒加载/现编）；doc-07 §4.4（演示脚本 D3b）；doc-20（四个世界的导言、可玩关卡与动态生长设计）；holmes-world（现有模板参考）。

## 背景与现状（已定案，不重谈）

- **模板 = 预写骨架**（doc-05 §0）：世界设定/关键事件/结局条件预写，剧情细节/支线/NPC 反应由作家现编；
- 世界 = 一个目录 + world.json manifest（doc-05 §8.4，已定案合并为单一 manifest）；
- 拷贝目录即开世界（doc-07 §3 scaffold：从模板世界拷贝出玩家世界）；
- `.airpworld.zip` 打包/分发（holmes-world READMEWORLD 已示范：`zip -r xxx.airpworld.zip .`）；
- 开放性设计原则（doc-05 §1 + holmes）：不预设全部剧情，线索串联。

## 模板体验契约（2026-09-12 定案）

模板不是题材素材包，而是一份**能完整游玩、又能继续生长的世界种子**。四个世界都必须包含：

1. **世界内导言层**：完成身份、地点、事件动机三种信息职责；载体可是一至多个 Markdown / letter / component，不固定为三张纸；
2. **入口 Chalk**：玩家通过点开、展开或回应 Chalk 跨过门槛；Chalk 可以引用信物或解锁 Gate，并立即给出视听、空间和持久状态反馈；
3. **第一幕关卡链**：从明确目标走到高潮与阶段结局，包含角色、物件、风险判定和可见世界变化；
4. **结局拓扑**：声明结算组合、最终承诺动作、一个或多个 Outcome Folder，以及结算后的继续 / 下一章 / 检查点重置出口；开放世界也必须有篇章结局；
5. **生成语法**：约束新地点、角色、物件、危险升级、文风和多模态风格，保证越过预写边界后仍属于同一个世界；
6. **持久事实起点**：玩家身份、初始关系、持有物、承诺与世界状态能够被 Writer / Character Agent 继续继承。

导言应复用现有 Folder / Layer、`type: chalk` 与实体类型，不新建 `type: prologue` 或 `type: chunk`。导言对应的具体目录路径、作为目录派生 Layer 时如何显示、以及如何从 launcher 进入，仍属实现待办；未定案前不得让四个世界各自发明一套目录协议。

## 待设计清单

| # | 项 | 说明 |
|---|---|---|
| 1 | **模板 = 预写骨架的文件规范** | 把上述体验契约落实成目录与字段：导言 folder / 第一幕 Chalk 链 / 结算组合 / 最终 Gate / Outcome Folder / 结算后出口 / 关键节点 / 角色预设 / 初始物件 / 生成语法分别放在哪里（**2026-09-12：不含"初始 journal"，journal 已撤销**，见 doc-12 文末）；“结局条件预写 + 细节现编”哪些写死、哪些留给 Writer |
| 2 | **题材清单** | 当前四个世界体验：雾坞镇中世纪冒险 / 福尔摩斯×开膛手杰克变体 / 时间线科幻 / 白色相簿感恋爱世界（详 doc-20）——每个题材的第一个 demo 页该长什么样；素材/材质词汇配套（doc-04 §6） |
| 3 | **模板 → 玩家世界的拷贝语义** | 从模板拷出时**剥离玩家旧进度**（player/ 与 history.db 清零）、生成**新 world.json id**、保留 extensions 声明；拷贝后模板原样保留（不 mutate） |
| 4 | **新玩家引导流程** | 进入模板世界 → **进入世界内导言层**：用身份 / 地点 / 动机三类内容回答“我是谁、在哪里、为什么行动”，再由一次打开 / 拿取 / 接受动作把玩家送入主场景；身份是预写、选择还是创建，由四世界逐个定案；不另做脱离世界的功能教学关卡（详 doc-20 §2.1–§2.3） |
| 5 | **分发规范** | `.airpworld.zip` 的包内结构约定（world.json 必在根部、assets 相对引用）、版本/校验、hub 发布前置（UGC 详 doc-17） |

## 边界

模板可声明 `world.json.player: { id, name, avatar? }`，用于玩家身份与世界相对头像；NPC 仍由 `characters` 声明。资产校验和、同步范围及文档版本差异见 `模板资源对齐清单.md`，日常补图不重建剧情与玩家进度。

- 模板不写死剧情——写死的骨架（关键节点）可以被玩家和作家改写，模板只是"第一张纸"；
- 模板不做离线剧本测试工具（那是赛后 hub 的事）。

> **标记：待办**。当前 4 个世界都要完成体验设计与 Mock，并分别证明“可完整打完的预写关卡 + 越过预写边界后的动态生长”；3 个 world 故事主线是当前人力分工，不是四选三的产品范围。`holmes-world` 与 `school-romance` 可直接迭代，其他模板的保留 / 改造 / 新建关系见 doc-20 §7。拷贝脚本已由 doc-07 scaffold 路径承接。
