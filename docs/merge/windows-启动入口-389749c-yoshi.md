# 合入 Windows 一键启动入口（角色素材批次 · 侧线小合并）

> 合并 `389749c` · 父提交 `35ef3c8`（ours=角色素材批次）与 `0cb2093`（theirs=origin/main，Windows 启动入口）· 作者 yoshi · 2026-09-13
> **事后补登记**（立法当天回溯）。

## 1. 这次合了什么

一次**小合并**（改动 4 个文件），把队友的「Windows 一键编译与启动」入口（`Start-AIRP.cmd` / `tools/start-local.ps1` / README 说明）并入角色素材批次分支。

- **对方带来的**：`Start-AIRP.cmd`、`tools/start-local.ps1`、`README.md` 的 Windows 日常更新与启动说明。
- **我方带来的**：角色 6 情绪差分 + 微动立绘批次（`35ef3c8`）。
- **两侧同时改的**：只有 `AGENTS.md`（1 处）。

## 2. 冲突处理

| 文件 | 两侧各是什么 | 裁决 | 理由 |
|---|---|---|---|
| `AGENTS.md` | 我方在 §7.8 加了「改角色素材时」段落（素材批次的操作口径）；对方在 §5/README 相关处加了 Windows 启动入口 | **两边全留** | 改动落在文件不同区域、互不重叠，是纯追加——不构成真冲突 |

**静默丢失自查**：改动面小（4 文件），无自动合并吃掉的候选；`git diff --diff-filter=D` 无删除。

**遗留**（立法时回溯发现，**未在本条处置**）：本条的「角色素材批次」后续暴露出 21 个角色缺 `voice:`（后被 `813c0f3` 一并补齐）。记录在此以免重复踩。

## 3. 验证

```bash
pnpm build   # → PASS
pnpm test    # → 605/605（当时）
```

**浏览器冒烟**：无需（纯 Windows 启动脚本 + 文档，不涉 UI）。

**未决/遗留**：无（本条自身的改动）。
