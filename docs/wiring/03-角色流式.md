# docs/wiring/03 — 角色流式（A3：`CharacterModal` 接真实流）

> 2026-09-12。本文只写设计，**不改任何代码**。权威地基是 `docs/wiring/00-共同上下文.md`（下称 00），本文件 MUST NOT 改动 00 冻结的形状。
> 帧名清单唯一真相源：`docs/tools/12-工具注册与路由统一.md §6.2`。两条通道模型：`docs/tools/00 §5.3`。
> 每条断言带 `file:line`；不确定标 `[推断]`。
> **UX 批次权威回写（2026-09-13）：** 本文的原始 `incoming`/`activeModalFrame` 方案已废止，不能作为实现指令。当前唯一数据链路是：`useWorld` 派发 `airp:character-frame` → App identity router 严格校验 `characterId` → `character-frame-queue.ts` 入队 → `CharacterModal` drain。本文后文若仍出现旧名，均只作为迁移前证据；实现 MUST 服从 `docs/ux/00 §4.6` 与 `docs/ux/06`，不得恢复 prop latest-frame 或第二个队列。

---

## 1.1 当前接缝摘要

| 所有权 | 当前约束 |
|---|---|
| `useWorld` | 只在 WS `case` 中转发原始角色帧，不维护角色台词缓冲，不保存 latest frame |
| App | 只做 `characterId` 身份路由、active character 过滤和队列生命周期，不把帧改造成 `useState` prop |
| `character-frame-queue.ts` | 唯一队列、唯一 delivery sequence、唯一 drain 接缝 |
| `CharacterModal` | 只消费队列快照；本地负责逐字泵、表情解析、错误显示和终结 |


---

## 1. 一句话定位

把 `CharacterModal` 从"本地随机假台词"改造成**真实角色 agent 的流式显示端**：`useWorld` 把 `character_delta`/`character_message`/`character_idle` 帧转成一个 window 事件，`App` 按 `activeModalCharId` 路由，`CharacterModal` 用一个"可增长缓冲 + 节拍泵"把帧渲染成逐字台词、把 `[emo: tag]` 变成立绘表情与瞬时 stinger；假台词库降级为**沉默兜底**，开场硬编码台词删除。

---

## 2. 签名参数

### 2.1 新增前端契约：`airp:character-frame`（useWorld → App → queue）

| 项 | 值 |
|---|---|
| 事件名 | `airp:character-frame` |
| `detail` | 原始帧 `msg` 逐字（`{ type, source, characterId?, delta?/text?, timestamp }`） |
| 派发者 | `apps/web/src/state/useWorld.ts` 的 `onmessage`，新增三条角色帧和带 guard 的角色 `error` 分支 |
| 身份路由 | App identity router；只接受存在且匹配 active character 的 `characterId` |
| 缓冲 owner | `apps/web/src/lib/character-frame-queue.ts`（NEW，UX06 owner） |
| 消费者 | `CharacterModal` drain queue，不接收 latest-frame prop，不监听原始 window frame |

缺 ID 或不匹配的帧必须丢弃并通过 `airp:notice` 显示诊断；不得用 active modal id 补值。App 是身份路由 owner，但不是台词缓冲或 latest-frame state owner。

### 2.2 `useWorld` 侧

`UseWorldApi` 不新增角色帧成员。`onmessage` 只负责字面量 `case` → `window.dispatchEvent(new CustomEvent('airp:character-frame', ...))`；不碰 React state、不 refetch、不进 `seenEventIds`。队列入队、delivery sequence 和 drain 均在 UX06 接缝完成。

必须保留字面量 `case` 标签：`tools/check-ws-contract.mjs` 只认 `case 'x'` / `msg.type === 'x'`。

### 2.3 `CharacterModal` 输入接缝

`CharacterModal` 不新增 `incoming`、`activeModalFrame` 或同义 latest-frame prop。它接收 `frameQueue`（或通过唯一 queue owner 的订阅函数取得快照），按 `drainUntil` 消费帧，并在本地维护逐字泵、表情解析、错误显示和终结状态。

这样可以保证：

- identity filter 先于 enqueue，错误角色帧不会污染缓冲；
- delivery sequence 不从 WS timestamp 猜测；
- burst delta 不会被 React latest-frame state 覆盖；
- 关闭遮罩、切角色和断线时只清理 queue owner，不残留上一角色帧。

### 2.4 帧类型

`CharacterFrame` 是内部消费类型，放在 `apps/web/src/lib/character-frame-queue.ts` 或等价的 UX06 source owner；不得以 `CharacterModal.tsx` 作为跨层协议 owner。WS 层继续保持原始消息不依赖 UI 类型。


---

## 3. 行为契约逐步（每步写"漏了会怎样"）

### 步骤 0：`useWorld` 收到角色帧 → 只派发 `airp:character-frame`

帧源：`event-bridge.ts:59`（`character_delta`）、`:70`（`character_message`）、`:183`（`character_idle`）；`timestamp` 由 `push` 统一附加（`event-bridge.ts:52`），服务端 migration adapter 在生产路径补齐 `characterId`。`useWorld` 的角色 lane 只派发 `airp:character-frame`；`airp:agent-frame` 仅承载 writer lane。

**漏了会怎样**：角色帧进入通用 agent channel 或缺少 ID → App 无法 strict route，必须丢弃并通过 `airp:notice` 显示诊断。

### 步骤 1：`App` identity router → `CharacterFrameQueue` → `CharacterModal`

`apps/web/src/lib/character-frame-queue.ts`（NEW，UX06 owner）是唯一 queue。App identity router 订阅 `airp:character-frame`，严格检查 `characterId`，通过 `nextDeliverySeq()` 生成内部序号后 enqueue；`CharacterModal` 不再接收 `activeModalFrame/incoming`，只订阅 queue 并 drain。

```ts
const frameQueue = getCharacterFrameQueue(); // NEW module owner

useEffect(() => {
  const onCharacterFrame = (e: Event) => {
    const msg = (e as CustomEvent).detail as CharacterFrame | undefined;
    if (!msg || typeof msg.type !== 'string') return;
    if (msg.characterId === undefined || msg.characterId !== activeModalCharId) {
      dispatchCharacterNotice('character_frame_unassigned', msg); // existing airp:notice seam
      return;
    }
    frameQueue.enqueue({ ...msg, characterId: msg.characterId }, frameQueue.nextDeliverySeq());
  };
  window.addEventListener('airp:character-frame', onCharacterFrame);
  return () => window.removeEventListener('airp:character-frame', onCharacterFrame);
}, [activeModalCharId, frameQueue]);
```

- queue 入队发生在 identity filter 之后，避免错误角色污染；
- `nextDeliverySeq()` 是本地单调序号，不从 WS timestamp 猜测；
- `CharacterModal` 接收 `frameQueue`，订阅后按 `drainUntil` 消费，不能再监听 `airp:agent-frame` 或保存 latest frame。

**漏了会怎样**：

- 不做 ID 过滤 → nanami 与 sumi 的 delta 混进同一个缓冲；
- 把 queue 放在过滤前 → 错误角色帧先污染缓存；
- 继续保存 `activeModalFrame` → burst delta 又会被 React state 覆盖；
- 缺 ID 静默丢弃 → 玩家无法知道角色流断开，必须走 `airp:notice`。

### 步骤 2：开遮罩时清空上一轮的残留

`openCharacterModal`（`App.tsx:120-124`）在 `setActiveModalCharId(charId)` 之前调用 `frameQueue.clear('close')`，再启动新角色。`CharacterModal` 的 `frameQueue` prop 始终指向同一模块 owner。

**漏了会怎样**：上一个角色（或同一角色上一轮）的最后一条帧仍挂在 queue 上，新遮罩挂载时会先把旧台词演一遍。

### 步骤 3：`CharacterModal` 拆掉"整句驱动"，改成"可增长缓冲 + 节拍泵"

现状 `streamLine(fullText, mood)`（`:106-144`）**不能复用**，必须重写驱动器：

- 它在入口就把 `line` 设成整句、`shown` 清空（`:107-110`），并递归排一个跑到底的定时器（`:125-140`）。
- 它的取消哨兵是 `streamTimer.current === null`（`:126`）。
- 若对每条 delta 调用它：`clearTimers()`（`:108`）+ `setShown('')`（`:110`）会让已显示的文字**每个 token 都归零闪烁**，韵律表完全失效。

所以：**保留它的韵律与沉思窗，替换它的输入契约**。`streamLine` 整体删除（不是保留旁路）——同一片状态有两个写者是本仓库明令禁止的第二套约定。

保留/抽取的部分：

| 保留 | 现状位置 |
|---|---|
| 沉思窗 `100 + Math.random() * 300` ms | `:114` |
| 逐字节拍 45ms / 逗号 300ms / 句末 150ms | `:134-138` |
| `setPhase('thinking')` → `setEmo('thinking')` → `streaming` 的相位语义 | `:111,116,117` |
| `playStinger(mood)` 一次一响、绝不碰 BGM 主轨 | `:118-123` |

新增状态与 ref：

```ts
const lineRef = useRef('');        // 已收到的全文（缓冲）
const shownRef = useRef(0);        // 已显示的字符数（唯一游标）
const streamingRef = useRef(false);
const messageEndedRef = useRef(false);
const channelIdleRef = useRef(false);
const phaseRef = useRef<Phase>('idle');
const phaseBeforeTurnRef = useRef<Phase>('idle');
const lastEmoRef = useRef<Emotion>('normal');
const stingerFiredRef = useRef(false);   // 见步骤 4 的"一响"语义
const turnWatchdog = useRef<number | null>(null);
```

**逐步：`character_delta`（流式正文增长）**

1. 若 `!streamingRef.current`：`beginStream()` —— `streamingRef.current = true`；`messageEndedRef = false`；`channelIdleRef = false`；`stingerFiredRef = false`；`lineRef = ''`；`shownRef = 0`；`setLine('')`、`setShown('')`、`setPhase('thinking')`、`setEmo('thinking')`；起沉思窗（`:114` 的公式），揭幕时 `setPhase('streaming')` 并调 `pump()`。
2. 从 queue drain 得到 `frame` 后，若 `frame.type === 'character_delta'`，执行 `lineRef.current += frame.delta`。
3. `pump()`：若 `streamTimer.current !== null` 直接返回（已有一步在排）；否则取 `parseEmoLines(lineRef.current)`，把可显示文本设为 frontier（见步骤 4 的 `tailOpen` 规则），按 `charDelay(prev)` 排下一步，每步 `shownRef.current++` 并 `setShown(text.slice(0, shownRef.current))`。
4. `streamTimer.current === null` 且 `shownRef.current >= text.length` 时**停止排程但不改 phase**——流还开着，可能在等下一个 token。
5. `settleIfDrained()`：当 `shownRef.current >= text.length` 且（`messageEndedRef` 或 `channelIdleRef`）→ `setPhase('done')`、`streamingRef.current = false`、清 watchdog。

**漏了会怎样**：
- 不做 `beginStream()` 的幂等判断 → 每个 delta 重启沉思窗，立绘永远在 `thinking` 抖动，`shown` 不断归零。
- 把缓冲写进 `useState` 而非 ref → 每 token 一次全量 render，且 effect 依赖链抖动（`line`/`shown` 本就每步 render，够用）。
- 每步不重算 `text.length` 而是闭包捕获一个常量 → token 到了但长度阈值不变，泵停在旧长度上等一个永不到来的终点。
- 在 `shown >= length` 时立刻 `setPhase('done')` → 第一条 delta 之后（缓冲只有 5 个字）就"演完"，`busy` 提前解除，玩家能发第二条 prompt（见步骤 6 的竞态 R6）。

**逐步：`character_message`（落定，权威文本）**

- `messageEndedRef.current = true`。
- 若 `lineRef.current === ''`（delta 全丢：断线重连 / 非流式客户端 / 服务端只在 `message_end` 给文本）：`beginStream()` 后取 queue 中的权威 `character_message.text`，立即把 `shownRef` 推到末尾（一次性 `setShown(text)` + `settleIfDrained()`）。
- 若 `lineRef.current` 非空但与权威 `character_message.text` 不等：以权威文本为准重置 `lineRef`，并让泵自然追上。
- 若泵已停（`streamTimer.current === null`）且已追平 → 直接 `settleIfDrained()`。

**漏了会怎样**：不处理"delta 全丢"→ 断线重连后遮罩空白且 `thinking` 卡死（`character_message` 是唯一的救援帧，`docs/tools/12 §6.2` 注明它是"非流式客户端的保留路径"，所以它 MUST 被消费）；不等 `character_message`/`idle` 就用 delta 收尾 → `phase` 停在 `streaming`，`busy` 永不解禁（`handleSend` 的 `:176` 守卫会把输入锁死）。

**逐步：`character_idle`（收工）**

- `channelIdleRef.current = true`；`settleIfDrained()`。
- 若本 turn **一个字符都没产生**（`lineRef.current === ''`，例：纯工具轮，`message_end` 被 `messageText` 过滤成空，`event-bridge.ts:68-71`）：不动 `line`/`shown`，只把 `phase` 归还给 `phaseBeforeTurnRef`（有上一句就是 `done`，否则 `idle`），并清 watchdog。

**漏了会怎样**：把 `idle` 一律当成"演完"→ 纯工具轮结束时空纸一张（`shown === ''`）；更糟的是若把 `phase` 置回 `idle` 而渲染条件仍是 `phase === 'streaming' || 'done'`（`:230`），上一句台词会**从纸上消失**。这正是步骤 7 要改渲染条件的原因。

### 步骤 4：`[emo: tag]` 如何作用于流式正文

角色 preset 明确要求**逐行打标**：`extensions/instructions.ts:179`（`[One sentence per line]`）要求一句一行，`:181-184`（`[How your face is shown]`）给出六个合法标签 `[emo: normal]`/`[emo: smile]`/`[emo: shock]`/`[emo: sad]`/`[emo: angry]`/`[emo: thinking]`，`:186` 要求"首行必打、之后凡表情与前一行不同也打"。而现状 `parseEmoTag`（`CharacterModal.tsx:60-65`）只剥**行首**一个标签（`/^\[emo:\s*([a-z]+)\]\s*/`）。一次 assistant turn 可以有多行台词 → 第 2 行起标签会**原样显示在纸上**，且表情不再切换。这是"真实流一接就穿帮"的点，必须处理。

设计：保留 `parseEmoTag`（单行单元），新增一个多行折叠：

```ts
/** Fold per-line [emo:] tags: returns tag-free display text, the mood of the LAST
 *  tagged line, and whether the frontier line is an unterminated tag prefix. */
function parseEmoLines(raw: string, opts?: { final?: boolean }):
  { text: string; emo: Emotion; tailOpen: boolean }
```

规则（逐条）：

1. 按 `\n` 切行，对每行跑 `parseEmoTag`；若该行**确实带标签**（`/^\[emo:\s*[a-z]+\]/.test(line)`）则把 `emo` 更新为该行解析值（**最后一条带标签的行胜出**）。`parseEmoTag` 对"无标签"与"非法标签"都返回 `normal`（`:62,64`），所以必须额外判定"是否带了标签"，否则 `[emo: bogus]` 与无标签不可分辨。
2. 拼回的 `text` 是**去标签**的显示文本；`shown` 游标始终在这份去标签文本上推进——标签占位不进入逐字节拍（标签是元数据，不是台词）。
3. **前沿未闭合标签冻结**：仅当最后一行匹配 `/^\[emo[\s:]*[a-z]*$/`（`[emo`、`[emo:`、`[emo: sm`、`[emo: smi` … 命中）时才 `tailOpen = true`——冻结前缀**必须以 `[emo` 开头**，绝不能把以 `[` 开头的普通正文（如 `[System] …`）误冻。把**该行**暂时输出为空串，泵不推进前沿，`[emo: smi` 永不上屏。
   - **保险阀**：该行已见 `[emo` 却迟迟没有 `]`（前缀长度超过 **24** 字符）视为普通文本，照常显示（一个野生 `[emo` 绝不能把遮罩挂死）。
   - `opts.final`（`character_message` 路径）为真时**不冻结**——消息已收尾，不会再有字符到来。
4. **stinger 一响**：`emo` 一旦从上一值变化（且 `emo !== 'thinking'`），立即 `setEmo(emo)` 并 `playStinger(emo)`（`apps/web/src/lib/audio.ts:851`）；`thinking` 不发音（它已是揭幕期的暂态）。只在**揭幕之后**才判定（`streamingRef.current === true`），避免沉思窗里误响。

**漏了会怎样**：

- 只做行首标签 → 多行台词把 `[emo: smile]` 当正文显示；评委一眼看出"提示词泄漏"。
- 只更新 `emo` 不更新已验证的 `prev` → 每条 delta 都重放一次 stinger（`playStinger` 是一次性短音，`docs/audio/00-共同上下文.md:327`），变成连发噪音。
- 无保险阀 → LLM 输出一个迟迟不闭合的 `[emo` 前缀就永久冻结前沿，遮罩像死机。
- 在 `thinking` 期间就按 `normal` 发 stinger → 每次开聊都先响一声"正常"音。

> 现状 `streamLine` 的注释（`:118-123`）已把这条纪律钉死：`[emo:]` 只触发瞬时 stinger、**绝不碰 BGM 主轨**（`docs/audio/02 §3.8`、`docs/audio/04 §3.2`）。本改造保留该调用点与语义。

### 步骤 5：`FALLBACK_REPLIES` 降级为沉默兜底

删除 `FALLBACK_REPLIES`（`:40-47`）与 `FALLBACK_REPLIES_JA`（`:49-54`）两个 6/4 条随机库，改为每条 locale **一条**：

```ts
/** 真实流缺席时的沉默兜底——一句诚实的"舞台指示"，绝不是伪造的角色台词。 */
const SILENCE_LINE: Record<'en' | 'ja', { text: string; emo: Emotion }> = {
  en: { text: '[emo: sad] (She does not answer right now.)', emo: 'sad' },
  ja: { text: '[emo: sad] （今は、答えが返ってこない。）', emo: 'sad' },
};
```

**何时用**：仅在步骤 6 的 watchdog 超时（发了 prompt 却整轮没有 delta/message）时调一次。**何时不用**：任何收到过真实帧的 turn（包括只在末尾收到 `character_message` 的）。

命名从 `FALLBACK_REPLIES` 改成 `SILENCE_LINE` 是刻意的：让"这是兜底不是回复"在类型名上就不可混淆（旧名字的注释自称 `T3.1 STOPGAP`，与"随机角色台词"的行为不符，`docs/wiring/前端接线体检.md:139`）。

**漏了会怎样**：留着随机库 → 本改造的核心诉求（"不再说假话"）直接失败：任何未接上的路径都会静默回落到假台词，且**看起来完全正常**——这比崩溃更难发现。

### 步骤 6：开场硬编码台词删除、`handleSend` 不再本地取假回复

**开场（`:147-157`）** → 删除定时器与那句硬编码台词，替换成一个幂等的重置 effect（挂载时 `line=''`/`shown=''`/`emo='normal'`/`phase='idle'`/`playerEcho=''`）。

删除理由（两条，任一条独立成立）：

1. 它是**冒充角色说话的另一个人格**：`locale === 'ja' ? '（あなたを見つめて）話したいこと、ある？' : '(Watching you) Is there something you would like to know?'`（`:151`）是前端作者写的台词，与真实 preset 人格（`templates/firstsnow/characters/nanami/preset.json` 的 persona 块）无关。
2. 它**与真实帧抢同一个纸面**：开遮罩后 420ms（`:154`）开演，而角色 agent 的启动（`lifecycle.startCharacter` → `RpcClient.start()`，`lifecycle.ts:94-111`）+ 首个 token 远晚于 420ms，于是玩家会先看到一句假台词、再被真实流打断。

**漏了会怎样**：不删则"接真流"变成"假台词 + 真台词叠播"，且每次开遮罩都多一次 `setTimeout` 与真实流竞争 `setShown`。

**`handleSend`（`:174-184`）** 改为：

```ts
const handleSend = () => {
  const msg = inputText.trim();
  if (!msg || busyRef.current) return;              // one turn at a time（保留 :176 语义）
  setInputText('');
  setPlayerEcho(msg);
  // 乐观占位：真实首帧要等 agent 启动 + 首个 token，期间不能露出"可再发一条"的窗口（R6）
  phaseBeforeTurnRef.current = phaseRef.current;
  streamingRef.current = false;
  messageEndedRef.current = false;
  channelIdleRef.current = false;
  setPhase('thinking');
  setEmo('thinking');
  onSendMessage?.(msg);                              // character_prompt 协议，App 装消息类型（App.tsx:578-584）
  armTurnWatchdog();                                 // 95s：服务端 90s 超时（lifecycle.ts:28）+ 5s 余量
};
```

- 删掉 `:180-183` 的 `bank` / `Math.random()` / `parseEmoTag` / `streamLine`。
- `busyRef` 是 `busy` 的 ref 镜像：`busy` 由 `phase` 派生（`:186`），事件回调读 state 会拿到闭包旧值；用 ref 才与 `setPhase('thinking')` 同步。

**漏了会怎样**：

- 不做乐观 `thinking` → 发完到首 token 之间 `busy === false`（`:186`），`disabled={busy}`（`:248`）为假，玩家可以连按 Enter 发第二、第三条 `character_prompt`；`index.ts:148-155` 会走 `character.prompt(...)`，两条 turn 的 delta 交错进同一个缓冲 → 台词变乱码。
- 不做 watchdog → 覆盖"既无 `error` 帧也无文本"的静默情形（agent 不存在时 `error` seam 已能立即报错，§11 冲突 1；但进程活着却不产出时仍会永久 `thinking`），`busy` 永不解禁，输入框永久禁用。
- watchdog 设得比服务端 90s（`lifecycle.ts:28`）短 → 服务端还在跑的 turn 被前端误判失败并插入沉默兜底句。

### 步骤 7：渲染条件改为"以 `line` 为准"

`:230` 的 `(phase === 'streaming' || phase === 'done') && line !== ''` 改为 `line !== ''`；思考提示仍绑 `phase === 'thinking'`（`:227-229`），`.line-stage` 的固定高（`index.css:503`）保证切换时不跳动。

**漏了会怎样**：`phase` 一旦回落到 `idle`（纯工具轮、watchdog 复位、`character_idle` 早到），上一句还没落定的台词会**从纸上凭空消失**，读起来像 bug。

---

## 4. 文件与副作用

| 文件 | 改动 | 副作用 |
|---|---|---|
| `apps/web/src/state/useWorld.ts` | `onmessage` switch 加三条角色帧和带 guard 的角色 `error` case | 只派发 `airp:character-frame`；不碰 state、不 fetch、不进 `seenEventIds` |
| `apps/web/src/App.tsx` | 挂载唯一 identity router，按 `characterId` 过滤后写入 queue；切角色/关闭/断线时清理 queue owner | 不新增 `activeModalFrame`，不把帧写入 latest-frame state，不向 Modal 传 `incoming` |
| `apps/web/src/lib/character-frame-queue.ts` | NEW：queue、`nextDeliverySeq`、enqueue、drain、clear | 唯一角色帧缓冲与交付顺序 owner |
| `apps/web/src/components/overlay/CharacterModal.tsx` | 消费 queue；保留本地逐字泵、表情解析、watchdog、错误显示 | 不监听 window 原始帧，不维护第二个 queue |
| `apps/web/src/index.css` | **不动** | 复用既有 `.line-stage` / `.thinking-hint` |

本批不新增路由、WS 帧名或持久化字段；`character-frame-queue.ts` 是明确的 NEW 文件，不得被“无新文件”的旧表覆盖。

---

## 5. 落账

- **本批 A3 不产生任何落账**：`character_delta`/`character_message`/`character_idle` 是**演出帧**（瞬时传输，`docs/tools/00 §5.3`、`docs/tools/12 §6.1`），不进 `history.db`。
- 与角色对谈相关的既有落账都在**服务端、且不在本改造路径上**：关遮罩时 `character_talked`（`apps/server/src/index.ts:176-183`）与角色游标结算 `settleTurnCursor(store, 'character:<id>', …)`（`index.ts:190-203`，`docs/hooks/03 §4.3`）。本改造只把 `character_stop` 的**发送方**留在 `App.tsx:127`，一字不动（`turns` 依旧省略，M-8 的估计语义由服务端承担）。
- **本改造不新增、不修改任何持久化字段。**

---

## 6. WS 前端（入方向数据流全图与竞态）

```text
event-bridge.mapEngineEvent       useWorld.onmessage       App identity router       frame queue        CharacterModal
  character_delta ──WS──► case ──airp:character-frame──► characterId guard ───────► enqueue ───────► drain/pump
  character_message ─WS──► case ──airp:character-frame──► characterId guard ─────► enqueue ───────► settle/recover
  character_idle ──WS──► case ──airp:character-frame──► characterId guard ───────► enqueue ───────► finish
  error(character) ─WS──► guard ──airp:character-frame──► characterId guard ─────► enqueue ───────► visible error
```

**三条帧的语义分工**（`docs/tools/12 §6.2`）：`_delta` = 实时增长；`_message` = 落定全文和丢帧救援；`_idle` = 收工。所有帧必须经过同一 identity router 和 queue，不允许另开 latest-frame state。

### 6.1 竞态：先收 delta，再打开遮罩

遮罩未打开或 `characterId` 不匹配时，identity router 丢弃帧并发出可见诊断；不得把 active modal id 当作帧身份补值。若需要保证打开前零丢字，必须扩展 queue owner 的生命周期，不能在 App 再造一份临时缓冲。

### 6.2 竞态：帧到达时遮罩已关

关闭时由 queue owner 执行 clear；迟到的 `character_message`/`character_idle` 不得重新打开遮罩、恢复上一角色或写入下一个角色的缓冲。Modal 的 close animation 期间也不消费已取消 projection 的新帧。

### 6.3 竞态：两条 turn 交错 / 早到的 idle

`character_idle` 可能早于 `character_message` 到达。queue 的 delivery sequence 与 Modal 的 `settleIfDrained()` 必须共同保证：只有文本前沿追平且收到终结信号后才进入 `done`；不得用 timestamp 排序，也不得只因 idle 到达就截断未显示文本。

---

## 7. 错误边界

| 边界 | 处置 | 漏了会怎样 |
|---|---|---|
| 角色 agent 不存在 | 服务端 `error` 帧带 `source:'character'` 与 `characterId`，经 identity router 入 queue；Modal 显示服务端文案，复位 phase、清 watchdog | 遮罩停在 `thinking`，玩家只能等 watchdog |
| `character_start` 抛出 | 同上；writer error 没有 `characterId`，不得进入角色 queue | writer 错误污染角色遮罩 |
| `characterId` 缺失或不匹配 | 丢弃并发出 `airp:notice`；不得用 active modal id 补值 | 多角色串台，或错误帧进入当前角色 |
| delta 全丢 | `character_message` 作为权威全文进入同一 queue，Modal 以全文补齐并 settle | 遮罩空白或台词卡在 thinking |
| `character_idle` 早到 | queue sequence + `settleIfDrained()` 等待文本前沿追平后再 done | 最后几个字被截断 |
| 组件卸载/切角色 | queue owner clear；Modal cleanup 清本地 timer/watchdog | 旧角色帧写入新角色或卸载后继续 setState |

错误文案由服务端提供；Modal 只显示已经通过 identity guard 的错误，不自行猜测 agent 状态。`console.warn` 不能代替玩家可见反馈。

---

## 8. 代码落点（精确到文件与函数）

### 8.1 `apps/web/src/state/useWorld.ts`

在 `onmessage`（`:265-322`）的 `switch (msg.type)`（`:276`）内，`case 'tool_end'`（`:309`）之后、`default`（`:316`）之前插入：

```ts
case 'character_delta':
case 'character_message':
case 'character_idle':
  // 演出帧（docs/tools/12 §6.2）：只转给遮罩，不进 world_event 的去重/重取路径。
  window.dispatchEvent(new CustomEvent('airp:character-frame', { detail: msg }));
  break;
case 'error':
  // 只接角色车道的错误（裁决 A-err）：writer 错误无 characterId，留 default。
  if (msg.source === 'character' && typeof msg.characterId === 'string') {
    window.dispatchEvent(new CustomEvent('airp:character-frame', { detail: msg }));
  }
  break;

**无其他改动**：`UseWorldApi`（`:50-72`）、`fetchLayer`（`:92-120`）、`moveCard`、`sendMessage`（`:174-179`）均不动。特别注意 `:268-275` 的 `airp:world-event` 转发集合**不含** `character_*`（00 §5 冻结）。

### 8.2 `apps/web/src/App.tsx`

1. 保留 `activeModalCharId` 作为当前遮罩身份；
2. 新增唯一 identity router：订阅 `airp:character-frame`，严格校验 `characterId`，调用 `frameQueue.nextDeliverySeq()` 后 enqueue；
3. `openCharacterModal`、`closeCharacterModal`、世界切换和 WS 断线时调用 queue owner 的 clear；
4. `<CharacterModal>` 不传 `incoming` 或 `activeModalFrame`，只传 queue owner 的消费接缝。

### 8.3 `apps/web/src/lib/character-frame-queue.ts`

NEW 文件，归 UX06。至少提供：

```ts
export type CharacterFrame =
  | { type: 'character_delta'; characterId: string; delta: string; timestamp?: string }
  | { type: 'character_message'; characterId: string; text: string; timestamp?: string }
  | { type: 'character_idle'; characterId: string; timestamp?: string }
  | { type: 'error'; source: 'character'; characterId: string; message: string; timestamp?: string };

export interface CharacterFrameQueue {
  enqueue(frame: CharacterFrame, deliverySeq: number): void;
  nextDeliverySeq(): number;
  drainUntil(characterId: string): CharacterFrame[];
  clear(reason: 'close' | 'switch' | 'world-change' | 'disconnect'): void;
}
```

`characterId` 在进入 queue 前已经是严格字符串且与当前 active character 匹配；queue 不负责猜身份。

### 8.4 `apps/web/src/components/overlay/CharacterModal.tsx`

Modal 消费 queue 的 drain 结果；保留 `parseEmoLines`、`charDelay`、`beginStream`、`pump`、`settleIfDrained`、watchdog 和错误显示。不得导出跨层帧协议，不得声明 `incoming`/`activeModalFrame`，不得直接监听 `airp:character-frame`。


---

## 9. 与现状差异

| # | 现状（`file:line`） | 改造后 |
|---|---|---|
| 1 | 三类角色帧**无人消费**（`check:ws` 报 `character_delta`/`character_idle` 两条 DARK） | useWorld 四个 `case`（三条角色帧 + 带 guard 的 `error`）+ App 订阅 + Modal 消费；两条 DARK 消失（判据见 §10.1） |
| 2 | 台词来自 `FALLBACK_REPLIES` 随机库（`:40-54`、`:180-183`） | 台词来自真实帧；随机库缩成一句沉默兜底 |
| 3 | 每次开遮罩先播一句硬编码开场（`:147-157`） | 开遮罩不主动说话；第一句台词来自角色的首个 turn |
| 4 | `streamLine` 一次吃整句（`:106-144`） | `beginStream`/`pump` 吃增量缓冲；韵律与沉思窗保留 |
| 5 | 表情只认**行首**一个 `[emo:]`（`:60-65`），多行会泄漏标签 | `parseEmoLines` 折叠每行标签；未闭合标签前沿冻结 |
| 6 | 渲染条件绑 `phase`（`:230`） | 绑 `line`，`phase` 只决定思考提示与输入禁用 |
| 7 | `handleSend` 同步 `streamLine(假回复)`（`:183`） | `handleSend` 只发 prompt + 进 `thinking` + 起 watchdog |
| 8 | 无失败可见面：agent 挂了就永久 `thinking` | `error` 帧经 seam 显示服务端文案 + 复位（**本批必做**，§11 冲突 1）；95s watchdog 降为最后兜底 → 沉默兜底 + 复位 |
| 9 | `App.tsx` 没有角色帧队列接缝 | 新增唯一 identity router + `character-frame-queue.ts`；Modal 通过 queue drain 消费 |
**不做的**（与本篇边界）：`writer_delta` 湿墨演出、`character_stop` 的 `turns` 计数（M-8）、角色 `turn_aborted`（00 §3.6 登记为后续）、表情精灵表资产（T3.2，`docs/前端改造计划.md:281`）。

---


## 10. 验收测试

### 10.1 门禁（机械核验，本条对应 finding 必须消失）

**跑 `pnpm check:ws` 后，`character_delta` 与 `character_idle` 两条 `DARK` finding 消失**（`character_message` 已在 `INTENTIONALLY_UNCONSUMED`，`tools/check-ws-contract.mjs:62`，不产生 finding，但本设计消费它以支持非流式/重连路径）。

**实测（本机运行 `node tools/check-ws-contract.mjs`）**：基线 **14 findings** = 2 GHOST（`item_moved` @ `useWorld.ts:270`、`god_action` @ `:271`，归 02 删）+ **12 DARK**（`writer_delta`、**`character_delta`**、`chalk_writing`、`image_generation_progress`、`chalk_landed`、`canvas_patched`、`dice_result`、`image_landed`、`writer_idle`、**`character_idle`**、`world_event`、`show_frame`）。

**机械断言落点（R4-9）**：把"`character_*` 类 finding 清零"落成 `tools/check-ws-contract.test.mjs` 的**一条用例**（该文件已 `import { compare } from './check-ws-contract.mjs'`，`:18`）。用例用 `compare()` 驱动合成的"本批落地态"：`consumed` = 基线 8 条字面量删 `item_moved`/`god_action`、加 `world_event` + `character_delta`/`character_message`/`character_idle` + `error`；`contract` 取真实帧名。断言 `findings.every((f) => !/character_/.test(f.message))`（等价于"三类角色帧全部有消费者"）。**本文只定落点与判据，不写测试代码。**

**判据（00 §9）**：A3 使 `findings` 从 14 → 12（仅算 A3 增量），且不再含任何 `character_*`；`consumed` 计数见 §11 冲突 5。A 档全落的总判据以 `00 §9` 为准（`findings = 9`，全 DARK）。`show_frame` 与其余暗帧按 00 §9 属"明确登记为后续批次"，不要求本批清零。

### 10.2 行为验收（手工走查，本改造无既有前端测试框架）

`apps/web/package.json` 无 test 脚本（`:1-24`），`tools/` 下只有 `check-ws-contract.test.mjs`（Node 单测，不跑 browser）。故行为验收 = 跑 dev server 手走 demo 动线：

```bash
pnpm dev            # server + web
# 浏览器打开 → 切到 firstsnow → 点 nanami 头像开遮罩
```

| # | 步骤 | 期望观察 |
|---|---|---|
| V1 | 开 nanami 遮罩，等 2s | 纸面**空白**（无硬编码开场）；立绘 `thinking` |
| V2 | 输入一句话回车 | 立刻进 `thinking`、输入框禁用；首 token 到达后逐字出现，表情切到台词行首的 `[emo:]` |
| V3 | 台词多行 | 纸上**不出现**任何 `[emo: …]` 字样 |
| V4 | turn 结束 | caret 消失、输入框解禁（`phase === 'done'`）；台词仍留在纸上 |
| V5 | 开 sumi 遮罩，再开 nanami 遮罩 | 各自只显示自己的台词；无串台 |
| V6 | 关遮罩后立刻再开 | 不重播上一轮的台词（步骤 2 清空生效） |
| V7 | 角色 preset 用一个不存在的 id 打开 | **立即**出现服务端错误文案（`error` 帧经 seam 到 Modal），`phase` 复位、输入框解禁；95s watchdog 仅作最后兜底 |
| V8 | console 注入单帧：`window.dispatchEvent(new CustomEvent('airp:character-frame', { detail: { type:'character_message', characterId:'nanami', text:'…' } }))` | 台词**一次落定**（不逐字），不白屏、不卡 `thinking`（`character_message` 单帧救援路径） |
（V8 原依赖的"断网重连"在实现里不存在——`useWorld.ts:258` 是唯一 `new WebSocket`，无 `onclose` 重连，`02 §12` 也列断线重连为不做——故改用可控的 console 注入路径。）

**验收的机械前提**：`pnpm check:ws` 与 `pnpm check:docs` 都必须跑（00 §9）。`check:docs`（`tools/check-hooks-docs.mjs:21`）只扫 `docs/hooks/`，本次改动落在 `docs/wiring/`，**不产生新 finding**；但改 `docs/ui/前端改造计划.md` 的 T3.1 段落由 05 篇负责（`docs/wiring/05-文档回写.md` R2）。

---

## 11. 发现的冲突

### 冲突 1（P1，**已裁决：本批必做**）：`error` 帧无人消费 → "已消灭假话"会以另一种方式复发

- **事实**：`index.ts:99、112、140、145、158` 五处 `ws.send({type:'error', …})`；其中 `:140`/`:145`/`:158` 带 `source:'character'` + `characterId`，`:99`/`:112` 是 writer 车道（无 `characterId`）。`error` 被登记为 `INTENTIONALLY_UNCONSUMED`（`tools/check-ws-contract.mjs:57`：*"diagnostics go to the server console; a UI surface is unbuilt"*）。
- **冲突点**：本改造的判据是"角色说真话"，但 agent 不存在（`index.ts:145`）时前端**没有任何信号**——只能等 95s watchdog 兜底，期间玩家看着一个 `thinking` 的遮罩。这是"静默失败"，正是 00 §8.3 要禁止的形状。
- **Modal 侧**：对 queue drain 得到的 `error` 帧直接显示其 `message`（服务端已给英文文案），把 `phase` 复位并清 watchdog；不从 App latest-frame state 读取错误。
- **门禁影响**：加 `case 'error'` **不产生新 finding**（`error` 已在 `INTENTIONALLY_UNCONSUMED`，`tools/check-ws-contract.mjs:57`；"未消费不算缺陷"，消费它同样不算），但让 `consumed` 计数 +1（见冲突 5）。

### 冲突 2（P2）：`character_message` 被登记为 `INTENTIONALLY_UNCONSUMED`，与本设计"必须消费"矛盾

- **事实**：`tools/check-ws-contract.mjs:62` 的理由是*"retained for a non-streaming client; _delta is the live path"*。
- **冲突点**：本设计把 `character_message` 当作**丢帧救援路径**（delta 全丢 / 断线重连 / `message_end` 过滤后仅存文本）必需的一条；一旦它在某次真实故障中是唯一到达的帧，不消费就是白屏。
- **建议**：实现时**保留**该 `case`（不必从 `INTENTIONALLY_UNCONSUMED` 删除——门禁口径不变），但把上面那句理由更新为*"retained as the authoritative settle frame; the modal consumes it for lost-delta recovery"*。这是门禁注释级修正，不改比较逻辑，收益是下一位维护者不会按旧理由把 `case` 删掉。**归 00 §9 维护者（主 agent）**。

### 冲突 3（P2）：A3 的消费面在 `check:ws` 里只有 `useWorld.ts` 一个文件被扫描 —— App/Modal 的消费不可见

- **事实**：`CONSUMERS = ['apps/web/src/state/useWorld.ts']`（`tools/check-ws-contract.mjs:48`）。本设计的 `App.tsx` 订阅 effect 与 `CharacterModal.tsx` 的帧消费 effect **都不在扫描面内**。
- **冲突点**：门禁只认"有人 `case`"这一个证据。若实现时把 `case` 写全、却在 App 侧漏了订阅（或订阅了但事件名拼错），`check:ws` 依旧全绿——**门禁证明不了端到端可通**。本批的机械核验只覆盖"帧名有了消费者"，行为正确性必须走 §10.2 的手工走查。
- **处置**：**接受**（不扩扫描面：`CONSUMERS` 的显式清单纪律是刻意的，`check-ws-contract.mjs:47`）。记录在此，避免下一位维护者把"`check:ws` clean"误读成"角色遮罩已端到端验证"。**归主 agent 决定是否把 `App.tsx` 加入 `CONSUMERS`**（若加，本改造会更早暴露拼错的事件名；代价是 `App.tsx` 里 `airp:character-frame` 的字面量会被当成帧名误判，**故不建议**）。

### 冲突 4（P2，已由 UX 批次裁决）：任务书的 prop/handler 示例与唯一队列 owner 冲突

- **旧方案**：用 `incoming` / `activeModalFrame` 把 latest frame 从 App 下推给 Modal。
- **当前方案**：App 只做 identity routing；`character-frame-queue.ts` 负责唯一缓冲与 delivery sequence；Modal 通过 queue drain 消费。
- **处置**：旧 prop 方案保留在本节仅作迁移记录，实施不得恢复；类型与队列归 `docs/ux/06`，本篇只描述 wiring。

### 冲突 5（P2）：`00 §9` 的 `consumed === 9` 与门禁实测不符（实测 10；含 A-err 为 11）

- **事实**：`docs/wiring/00-共同上下文.md:151` 把总判据写死为 `summary.findings === 9` 且 `summary.consumed === 9`。用门禁自身的 `consumedFrom()`（`tools/check-ws-contract.mjs:124-134`，取自真实 `useWorld.ts` 文本）实测"本批落地态"：
  - 基线 `consumed = 8`：`file_changed, item_moved, god_action, world_frozen, world_thawed, card_position, tool_start, tool_end`。
  - A2（删 `item_moved`/`god_action`、加 `world_event`）→ **7**。
  - A3（再加 `character_delta`/`character_message`/`character_idle`）→ **10**；`findings = 9`（全 DARK，名单见 §10.1）。
  - 本批 A-err（加 `case 'error'`）→ **11**。
- **冲突点**：基线 8 − 2 + 1 + 3 = 10，**不可能**回到 9；`REVIEW-R2` §1:48 的独立合成复算也给出 `A2+A3 → consumed = 10`（与 `00 §9:151` 的 `consumed === 9` 差 1，含 A-err 差 2）。`findings = 9` 三方一致，无需改；**要改的是 `consumed`**。
- **处置**：`00 §9` 的 `consumed` 计数须由主 agent 更正为 **10**（不含 A-err）或 **11**（含本批 A-err），或删掉该字面量改由门禁输出比对。**归 `00` 维护者（主 agent）**；本篇只登记，不改 00。

---

## 12. 仍未知待拍板

| # | 未知 | 我的默认处置 | 需要谁拍 |
|---|---|---|---|
| 1 | `error` 帧是否接进 `airp:character-frame` | **已决**：接，且必须带 `source === 'character'` 与字符串 `characterId` guard；随后进入唯一 queue | — |
| 2 | `CharacterFrame` 类型与队列的家 | **已决**：`apps/web/src/lib/character-frame-queue.ts`（UX06 owner）；Modal 不导出跨层帧协议 | — |
| 3 | watchdog 阈值定 95s | **已决**：按服务端 `DEFAULT_TURN_TIMEOUT_MS = 90000`（`lifecycle.ts:28`）+ 5s 余量；A-err 落地后它降为最后兜底 | — |
| 4 | 事件名 `airp:character-frame` 的最终拼写 | 沿用 `airp:world-event` 风格 | 与 01/02 已 IRC 对齐，**已定** |
| 5 | 超长台词会不会撑破 `.speech-paper` | **不额外截断**（00 §7） | `[推断]` 需要看 `.speech-paper` 的 CSS（本次未查） |
| 6 | 角色 `turn_aborted` 的 `characterId`（00 §3.6 登记为后续）要不要显示 | 本批不做；到 `turn_aborted` 的帧带 id 时，Watchdog 与它合并 | 后续批次 |
| 7 | 是否需要"重连后补帧"的离线队列 | 不做（§6.1 论证：窗口内丢一条 delta，下一条补齐） | 主 agent 若要求"零丢字"才需要 |
| 8 | StrictMode 下 effect 双跑的行为验收是否需要额外断言 | 手工走查 V2 即可；如需自动化得先建前端测试框架（`apps/web/package.json` 无 test 脚本） | 主 agent |
