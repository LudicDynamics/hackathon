# docs/wiring/05 — 文档回写清单（C 档核心）

> 2026-09-12。A 档接线同批要做的**文档回写**。遵守 `docs/wiring/00-共同上下文.md` 权威层级。
> 依据：`docs/wiring/前端接线体检.md §6`（现状核实，带 `file:line`）。
> 纪律（`AGENTS.md §6.3`）：**文档里已被推翻的说法直接改掉，不要另起一段解释**。

---

## 1. 一句话定位

把**已知为假**的文档断言改对。这些不是"待补"，是"已错"——读它们会得到与实际相反的结论。

---

## 2. 回写清单（逐条：位置 / 原断言 / 改成什么 / 依据）

### R1 — `character_start`/`character_stop` 前端发送方"一个都没有"

**位置**：`docs/hooks/00-共同上下文.md:192`、`docs/hooks/03-事件段与人话渲染.md:514`、`docs/hooks/06-接线与生命周期.md:287`。

**原断言**：`character_start`/`character_stop` 两个前端发送方「今天 `apps/web` 一个都没有（`grep -rn character_start apps/web/src` 零命中）」。

**现实**：`apps/web/src/App.tsx:122`（start）与 `:127`（stop）**已发送**；服务端 `apps/server/src/index.ts:119-141`（高水位 pin + spawn）与 `:165-203`（`settleTurnCursor` + stop）已实现。

**第一人称注**：这三处是 B2/B3 我们自己写的。写契约时发送方确实不存在，落地后**未回写**——是"文档对现状的断言有保质期"的活例。

**动作**：**已在本批之前完成**（见 commit `ebacd6a`）。本条保留为记录，验证门禁不再报。

**验收**：三处均含 `2026-09-12 核实更正` 字样，行号指向 `App.tsx:120-131`。

---

### R2 — `docs/ui/前端改造计划.md:271-275`（T3.1）的现状描述

**原断言**（逐句判）：
1. 「`CharacterModal.tsx` 的回复是本地 `sampleReplies` 随机数组」——**变量名错**，实为 `FALLBACK_REPLIES`（`CharacterModal.tsx:40`）。
2. 「服务端 `character_prompt` 分支是个空注释」——**已过期**：`apps/server/src/index.ts:142-159` 是完整实现（转发 `steer`/`followUp`/`prompt`）。
3. 「流式 token 经 WS `character_delta` 逐字推回，收笔发 `character_message`」——**准确**（`event-bridge.ts:58,70`）。
4. 「前端删掉 `sampleReplies`，改订阅 WS 流」——**未做**，本批 A3 做。

**动作**：把 (1)(2) 改成现状，(4) 保留为任务但指向 A3；标注 (3) 已兑现。

**改成**（示例，落地时按此写）：

```markdown
### T3.1 角色 agent 流式打通

**服务端已就绪**：`character_prompt` 分支（`apps/server/src/index.ts:142-159`）转发 `steer`/`followUp`/`prompt`；流式 token 经 WS `character_delta`（`event-bridge.ts:58`）、收笔 `character_message`（`:70`）。

**剩余（前端）**：`CharacterModal.tsx:40-54` 的 `FALLBACK_REPLIES` 仍是本地假回复；改为订阅 WS 流（`docs/wiring/03-角色流式.md`）。
```

---

### R3 — `docs/ui/前端改造计划.md:361-368`（T3.7）的落点

**原断言**：「统一动作服务」落点 `apps/server/src/engine/`。

**现实**：**已实现且落点写错**——动作服务在 `packages/shared/src/actions/service.ts`（`createActionService`，`:129`），路由已全部改调它（`routes/world.ts:578-784`）。

**动作**：改为已完成的现状 + 正确落点。

---

### R4 — `docs/ui/前端改造计划.md:27` 的摘要

**原断言**：「**剩余**：T3.1 服务端角色 agent 流式（`character_prompt` 仍为空注释，前端处为本地回退，已注释标注）」。

**动作**：删「服务端…`character_prompt` 仍为空注释」，只留「前端处为本地回退」。

---

### R5 — `docs/ui/前端改造计划.md:549` 收工检查表 P3 行

**原断言**：「剩余 T3.1 服务端角色流式（前端仍为标注的本地回退）」——「服务端角色流式」**已完成**，「前端仍为本地回退」仍真。

**动作**：删「服务端角色流式」，保留「前端本地回退」，并指向 `docs/wiring/03`。

---

### R6 — `docs/prompts/00-共同上下文.md:214` 与 `docs/prompts/05-装配与验证.md:432` 的 `show_frame` 断言

**原断言**：「`show` 工具返回的 `show_frame` 在全仓**连发布点都没有**」。

**现实**：`extensions/tools.ts:62` 已注册 `show`；`packages/shared/src/actions/show.ts:157-161` 产出 `details.frame`；`event-bridge.ts` 的 `show` 分支已逐字广播。

**动作**：⚠️ **`docs/prompts/**` 是队友未跟踪的产物**（`git status` 显示 `?? docs/prompts/`）。**只登记、不改**——避免卷入队友未提交的工作。写进本文档「发现的冲突」并知会，由队友决定。

---

### R7 — `docs/tools/**` 中所有 `useWorld.ts:<n>` 行号引用（原只点名 2 文件）

**目标集合的判据**：`grep -rn "useWorld\.ts:[0-9]" docs/tools/`。命中 8 份文件即全部目标：

| 文件 | 命中行 |
|---|---|
| `docs/tools/02-chalk.md` | `:455`、`:479` |
| `docs/tools/03-look-at与画布感知.md` | `:986` |
| `docs/tools/04-move与delete.md` | `:559`、`:561`、`:670` |
| `docs/tools/05-move-to与set-following.md` | `:8`、`:631`、`:711`、`:716`、`:802`、`:816`、`:845`、`:1010` |
| `docs/tools/07-roll-dice.md` | `:771`、`:915`、`:1029` |
| `docs/tools/08-use-item-on.md` | `:320`、`:518`、`:534`、`:722` |
| `docs/tools/09-link与arrange.md` | `:19`、`:497`、`:502`、`:507`、`:513`、`:579`、`:580`、`:609`、`:610`、`:612`、`:651` |
| `docs/tools/12-工具注册与路由统一.md` | `:190`、`:585`、`:588`、`:643`、`:679`、`:1405`、`:1545` |

**原断言**：多处引 `useWorld.ts:171-181` / `:178-210` / `:172/180` / `:196-205` / `:207-208` / `:25-30` 等。

**现实**：**全部漂移**——WS `switch` 实际在 `useWorld.ts:276-325`（`default` `:316`）；`fetchLayer` `:92-124`；`PresenceEntry` `:32`；`INITIAL_LAYER` `:74`。原引用集中于 170–210 一带，整体偏移约 +100 行。

**动作**：**扩成全部目标**（判据即上表 grep），逐处改正。优先**改成语义引用**（如「`useWorld.ts` 的 WS `onmessage` switch」「`useWorld.ts` 的 `fetchLayer`」）以免再漂；必须留行号处按现状写（switch `:276-325`、`fetchLayer` `:92-124`）。**唯一例外**：`docs/tools/12 §6.2`（帧名清单）不动（05 §5）。

---

### R8 — `docs/protocols/doc-21-事件表协议.md:25,204` 的 `world_frozen`/`world_thawed` 断言

**原断言**（两处）：「`world_frozen` / `world_thawed` 既无写入点也无消费者，按准入第 3 问**删除**」。

**现实**：**确实广播**（`apps/server/src/routes/world.ts:806` 的 `/freeze` 路由）**也确实消费**（`apps/web/src/state/useWorld.ts:282-287`）；`docs/tools/12:588` 明确列为"保留"。

**动作**：这是**文档间矛盾**（doc-21 vs tools/12），且 doc-21 的判断**已被实现证伪**。按 §6.3「已被推翻的说法直接改掉」：
- 把 doc-21 这两处改为："`world_frozen`/`world_thawed` 是**演出帧**（非世界事件），由 `/freeze` 路由直接广播，前端消费；它们**不在事件表**里"。
- 保留"不入事件表"的正确部分（它确实不落账）。

---

### R9 — `docs/development/后端实现计划.md:80` 与 `:204`

**原断言**：「`event-bridge.ts`(56 行) 只广播 `file_changed`」「现在只广播 `file_changed`，补一张映射表」。

**现实**：`event-bridge.ts` 405 行，已实现全部映射。该节是 B0 时代快照（`:284` 已自标"本节以下是落地前的旧口径"，但 `:80`/`:204` 未标）。

**动作**：给 `:80`/`:204` 补"（B0 快照，已落地）"标注，或直接改为现状。

---

### R10 — `docs/hooks/05-视点链路.md:72,512,524` 的 `useWorld.ts` 行号引用

**依据**：设计 02 改动 `useWorld` 的 switch/行号后本处引用需同批更新（判据同 R7）。三处现引 `useWorld.ts:65`（`:72`）、`useWorld.ts:67`（`:512`、`:524`），行号已漂移（实际 `INITIAL_LAYER` 在 `:74`、`layer` state 在 `:78`），随本批改正或改语义引用。**由实现批同 commit 回写**（§6.3）。

---

## 3. 文件与副作用

| 文件 | 改什么 | 是否本批 |
|---|---|---|
| `docs/hooks/{00,03,06}` | R1（**已完成**） | ✅ 已回写 |
| `docs/hooks/05-视点链路.md` | R10 | ✅ 本批 |
| `docs/ui/前端改造计划.md` | R2/R3/R4/R5 | ✅ 本批 |
| `docs/tools/**`（8 份，见 R7） | R7 | ✅ 本批 |
| `docs/protocols/doc-21-事件表协议.md` | R8 | ✅ 本批 |
| `docs/development/后端实现计划.md` | R9 | ✅ 本批 |
| `docs/prompts/**` | R6 | ❌ 队友产物，只登记 |

---

## 4. 落账

无——纯文档。**不写事件、不碰 WS。**

---

## 5. 错误边界

- **绝不改** `docs/tools/12 §6.2` 的帧名清单（那是契约本体，本批只加载荷字段）。
- **绝不改** `docs/prompts/**`（队友未跟踪）。
- 回写**同 commit** 完成，不"回头补"（§6.3 硬要求）。

---

## 6. 验收测试

1. `pnpm check:docs` —— **只扫 `docs/hooks/`**（`tools/check-hooks-docs.mjs:28` 硬编码 `docs/hooks`），对本批 **R2–R9 零覆盖**（R2/R3/R4/R5 在 `docs/ui/前端改造计划.md`、R8 在 `docs/protocols/doc-21-事件表协议.md`、R9 在 `docs/development/后端实现计划.md`，均不在扫描面）。R1 已完成，`docs/hooks/{00,03,06}` 带「2026-09-12 核实更正」。**R2–R9 靠下面收窄 grep + 人工核对**，不靠 `check:docs`。
2. **收窄 grep 原断言文本**（只扫**被回写目标文件**，显式排除引用方 `docs/wiring/05-*.md` 与只读 `docs/wiring/前端接线体检.md`）：

```bash
grep -n "sampleReplies"           docs/ui/前端改造计划.md docs/development/后端实现计划.md   # 应为空
grep -n "character_prompt.*空注释" docs/ui/前端改造计划.md                      # 应为空
grep -n "只广播 file_changed"      docs/development/后端实现计划.md                      # 应为空
grep -n "按准入第 3 问删除"        docs/protocols/doc-21-事件表协议.md                 # 应为空
```

（全仓口径 `grep -rn … docs/` **恒红**：05 自身 `:36/:39/:176`（`sampleReplies`）与 `:37/:67/:69/:177`（`character_prompt` + 空注释）逐字引用原断言，只读体检 `:165` 亦然——**禁用**。）
3. R7：`grep -rn "useWorld\.ts:[0-9]" docs/tools/` 逐处人工核对指向正确符号（或已改为语义引用）。
4. `pnpm check:ws` 不受影响（文档改动不改帧名）。

**可固化为断言（登记为后续）**：`tools/check-hooks-docs.mjs` 已守 `docs/hooks`；「被推翻短语黑名单」门禁（`{file, phrase}` 常量表 + 扫描面扩到 `docs/**`、排除 `docs/wiring/05-*.md` 与 `docs/wiring/前端接线体检.md`）**本轮不实现**。

---

## 7. 发现的冲突

1. **doc-21 vs tools/12**：`world_frozen`/`world_thawed` 一删一留（R8）。**本批裁决：以 tools/12 为准（保留），改 doc-21**——理由是**实现既成事实 + tools/12 是更新更晚的裁决**。
2. **`docs/prompts/**` 的 `show_frame` 断言已过期**（R6），但该目录未跟踪、内容属队友在建的提示词批次。**只登记，不改**，知会队友。
3. **`docs/development/后端实现计划.md:525` 的 `WS_COMMANDS`**（`input/choice/character_open/...`）与现状 `writer_prompt`/`character_prompt`/`character_start`/`character_stop` **命名完全不同**。属 T6.1 未实现的网关设计，**不是过期而是未来**——补一句"（未实现，T6.1 目标态）"即可。
4. **`docs/hooks/05-视点链路.md` 的 `useWorld.ts` 行号已漂移**（R10 重定向时核实）：`:72` 引 `:65`（实际 `:74`）、`:512`/`:524` 引 `:67`（实际 `:78`）。与 R7 同源，随本批（R10）一并改正。

---

## 8. 仍未知 / 待拍板

1. **R7 的行号 vs 语义引用** —— **已决**：R7 扩成全部 `docs/tools/**`（8 份，判据见 R7），默认改语义引用；「行号漂移门禁」仍登记为后续。
2. **R6 的队友产物** —— 仍待：`docs/prompts/**` 未跟踪，需队友确认后再定由谁回写。
