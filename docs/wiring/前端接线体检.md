# AIRP 前端接线体检 — 跨端 WS 契约静默漂移

> 2026-09-12，主控亲笔。核实范围：**服务端广播面 ↔ 前端消费面 ↔ 冻结契约**（`docs/tools/12-工具注册与路由统一.md §6.2`）。
> 方法：主 agent 逐条读代码带 `file:line`，两个只读 scout 独立复核（`FrontUI` 前端演出面 / `DocContract` 文档契约面），并新建机械门禁 `tools/check-ws-contract.mjs` 对两端求 diff。

---

## 1. 一句话结论

**前端不是"没接线"，而是"接的是已经被删掉的线"。**

服务端在 B1/B4 把交互事件统一成 `world_event` 一条流、删掉了 `item_moved`/`god_action`/`use_item_on` 三条裸帧、改名 `roll_resolved`→`dice_result`；前端 `apps/web/src/state/useWorld.ts` 的 WS switch **原封未动**——它仍在监听那三条已被删除的帧，而对 `world_event` 与全部 11 条演出帧**零消费**。

两端**各自编译通过、各自有测试绿**，所以没人发现。

| 面 | 数量 |
|---|---|
| 服务端实际广播的帧名 | 24 |
| 前端真正消费的帧名 | 8 |
| 契约（`docs/tools/12 §6.2`）列出的帧 | 24 |
| **机械核验 findings** | **14**（2 死监听 + 12 暗帧） |

---

## 2. 事实基线

### 2.1 契约（唯一真相源）

`docs/tools/12-工具注册与路由统一.md:565-604` §6.2「前端契约：完整帧清单」逐帧给了载荷、谁发、前端做什么。配套：`:606-628` §6.3 `world_event` 帧形状、`:630-634` §6.4 前端 MUST 按 `event.id` 去重、`:677-694` §6.6 `airp:world-event` 转发集合。

### 2.2 服务端发射面（实读）

| 帧 | 发射点 |
|---|---|
| `connected` | `apps/server/src/index.ts:90` |
| `error`（单播 `ws.send`，非广播） | `index.ts:99,112,140,145,158` |
| `writer_delta` / `character_delta` | `engine/event-bridge.ts:58-59` |
| `writer_message` / `character_message` | `event-bridge.ts:70` |
| `tool_start` | `event-bridge.ts:77` |
| `chalk_writing` | `event-bridge.ts:79` |
| `image_generation_progress` | `event-bridge.ts:88` |
| `tool_end` | `event-bridge.ts:107` |
| `chalk_landed` | `event-bridge.ts:114-123`（局部对象 `landed`，按名 `push`） |
| `canvas_patched` | `event-bridge.ts:128` |
| `dice_result` | `event-bridge.ts:143` |
| `show_frame` | `event-bridge.ts:162`（逐字广播 `details.frame`） |
| `image_landed` | `event-bridge.ts:169` |
| `writer_idle` / `character_idle` | `event-bridge.ts:183` |
| `world_event` | `event-bridge.ts:280`（尾部读表唯一通道） |
| `file_changed` | `event-bridge.ts:380-386` |
| `turn_aborted` | `engine/lifecycle.ts:168` |
| `replay_entry` | `engine/lifecycle.ts:257` |
| `card_position` | `routes/world.ts:611` |
| `world_frozen` / `world_thawed` | `routes/world.ts:806` |

**从不广播**：`item_moved`（`routes/world.ts:565-567` 注释明确删除）、`god_action`（`routes/world.ts:819+` 只走动作服务，不发帧）、`use_item_on` 裸帧（`routes/world.ts:701-703` 删除）、`roll_resolved` 帧（改名 `dice_result`）。

### 2.3 前端消费面（实读）

唯一 WS 消费点：`apps/web/src/state/useWorld.ts:256-325` 的 `onmessage`。真正有分支的只有：

- `file_changed` / `item_moved` / `god_action` → 整层重取（`:277-281`）
- `world_frozen` / `world_thawed`（`:282-287`）
- `card_position`（`:288-305`）
- `tool_start` / `tool_end`（`:306-315`，仅喂 footprint 忙闲计数）

其余全部落 `default: break`（`:316-317`）。

全仓 `apps/web` grep `world_event` —— **零命中**（`airp:world-event` 是前端自定义 DOM 事件，与 WS 帧同名不同物，且它只在 `file_changed`/`item_moved`/`god_action` 三条上被 dispatch，见 `useWorld.ts:268-275`）。

---

## 3. 机械核验：`tools/check-ws-contract.mjs`

体检若只停在"读代码下结论"，下一批改动会再漂一次。所以本次落了**可重复执行的门禁**（skill 教训 1：冻结契约若不能被机械核验就等于没冻结）。

### 3.1 它做什么

对三个集合求差：

1. **CONTRACT** — `docs/tools/12 §6.2` 第一张表的帧名（"要删的帧"子表不算承诺）。
2. **EMITTED** — 服务端真正的 `broadcast(...)` / `push(...)` / `frameSink?.(...)` 调用里的帧名。
3. **CONSUMED** — `apps/web/src/state/useWorld.ts` 里 `case '...'` / `msg.type === '...'` 的帧名。

四类漂移：`GHOST`（消费了但无人发射＝死监听）、`DARK`（发射了但无人消费＝暗帧）、`UNDECLARED`（发射了但契约没写＝契约失真）、`MISSING`（契约写了但无人发射＝承诺落空）。

```
node tools/check-ws-contract.mjs          # 退出码 0=clean / 1=findings
node tools/check-ws-contract.mjs --json   # 机器可读
```

### 3.2 当前输出（14 findings，全部为真）

```
GHOST  apps/web/src/state/useWorld.ts:270  listens for `item_moved` but no server file emits it
GHOST  apps/web/src/state/useWorld.ts:271  listens for `god_action` but no server file emits it
DARK   event-bridge.ts:58   writer_delta              / character_delta
DARK   event-bridge.ts:79   chalk_writing
DARK   event-bridge.ts:88   image_generation_progress
DARK   event-bridge.ts:115  chalk_landed
DARK   event-bridge.ts:128  canvas_patched
DARK   event-bridge.ts:143  dice_result
DARK   event-bridge.ts:169  image_landed
DARK   event-bridge.ts:183  writer_idle               / character_idle
DARK   event-bridge.ts:280  world_event
DARK   event-bridge.ts:0    show_frame                （逐字广播 details.frame，无字面量）
```

> `show_frame` 的重构发现：门禁第一版用"missing 特例"豁免它，等于**它从未被查过 dark**。抽出纯函数 `compare()` 后该特例消失，它作为一条真暗帧正确浮出——门禁本身也经历了一次"静默漏检"的修复。

### 3.3 非空性证明（防"假绿"）

门禁自身必须能证明"没有这个缺陷就会变绿"。实测：把 `useWorld.ts` 按目标形态补上 `world_event` + 12 条演出帧、删掉两条死监听后，

```
check-ws-contract: clean (24 emitted, 18 consumed, 24 in contract)   exit=0
```

恢复原状后回到 `14 finding(s) exit=1`。**门禁随修复真的会翻绿，不是恒红**。该证明已固化为永久单测 `tools/check-ws-contract.test.mjs`（直接驱动纯函数 `compare()`，含 4 类检测的非空性用例）。

---

## 4. 缺陷分级

### P0 — 真实功能损坏（不是演出问题）

**掷骰 HTTP 路径必失败。**

- 前端发 `{ filePath, rollType, expect }`（`apps/web/src/components/narrative/DiceRoller.tsx:151-155`）；
- 服务端 `/api/dice` 只读 `body.path`，缺 `path` 直接 400（`apps/server/src/routes/world.ts:679-690`，契约见 `docs/tools/12:710`）；
- 于是 `parseDiceVerdict(raw)`（`DiceRoller.tsx:46-51`）拿到 400 体 → 返回 `null` → `throw` → `closeOverlay()`（`:160-165`）。

后果：玩家**主动掷骰**永远失败；只有"服务端已经预结算过"的骰子卡（`DiceRoller.tsx:59-63` 的 `rollDice.result/passed` 分支）还能显示。这是真 bug，与演出无关。

### P1 — 演出层整体死亡

12 条暗帧意味着：作家/角色的流式正文、笔尖落下、幻影落地、骰子仪式、画布增量合并、生图进度、idle⇄writing 状态机、`show_frame` 演出——**一条都没接**。

最扎眼的是角色对话：`CharacterModal.tsx:35-47` 有一段本地假回复库 `FALLBACK_REPLIES`，注释自称 `T3.1 STOPGAP until the backend character agent streams real responses`。而**服务端其实早在发 `character_delta`/`character_message`**（`event-bridge.ts:58,70`），`character_prompt` 分支也已是完整实现（`index.ts:142-159`）。**是前端没接，不是后端没做。**

### P1 — `airp:world-event` 转发集合错

契约要求转发 `file_changed` + `card_position` + `world_event{entity_*}`（`docs/tools/12:677-694`）。现状 `useWorld.ts:268-275` 转发的是 `file_changed` + `item_moved` + `god_action` —— 后两条永不触发。`App.tsx:148-152` 的背包/角色视图刷新靠这条链，目前能跑是因为 `file_changed` 恰好覆盖了绝大多数变更。

### P2 — 契约漂移（见 §6）

---

## 5. 为什么一直没被发现

1. **两端各自绿**。`node --test` 的 `apps/server/test/map-engine-event.test.mjs` 只断言**服务端映射出什么帧**，没有任何测试断言**前端消费了什么帧**。跨端契约只测了一端＝最常见的假绿形状。
2. **`file_changed` 意外兜底（已修正）**。过去 `canvas.db` 的 WAL 写入未被过滤，footprint/画布状态写入会额外触发整层重取，掩盖事件通道并造成重复 reseat。现在 watcher 仍唤醒 history tail reader，但 `.airpworld/canvas.db*` 不再广播 `file_changed`；画布状态由 `card_position` / `canvas_patched` / `world_event` 与重连 fetch 覆盖。
3. **没有跨端门禁**。文档侧有 `tools/check-hooks-docs.mjs` 守 hooks 文档，但**没有任何机制比对两端的帧名**。
4. **契约文档自己也过期**，读文档会得到错误结论（§6）。

---

## 6. 文档漂移清单（`file:line`）

契约文档里存在**已知为假**的断言。这些不是"待补"，是"已错"：

| # | 位置 | 断言 | 现状 |
|---|---|---|---|
| 1 | `docs/hooks/00-共同上下文.md:192`、`docs/hooks/03-事件段与人话渲染.md:514`、`docs/hooks/06-接线与生命周期.md:287` | `character_start`/`character_stop` 两个前端发送方「今天 `apps/web` 一个都没有」 | **已过期**：`apps/web/src/App.tsx:122`（start）与 `:127`（stop）已发送；服务端 `index.ts:119-141`/`:165-203` 也已落地。**这三处是 B2/B3 自己写的** |
| 2 | `docs/ui/前端改造计划.md:271-275`（T3.1） | 服务端 `character_prompt` 分支「是个空注释」；前端变量叫 `sampleReplies` | **已过期**：`index.ts:142-159` 是完整实现；变量实为 `FALLBACK_REPLIES`（`CharacterModal.tsx:40`） |
| 3 | `docs/ui/前端改造计划.md:361-368`（T3.7） | 「统一动作服务」落点 `apps/server/src/engine/` | **已实现且落点写错**：动作服务在 `packages/shared/src/actions/service.ts`（`createActionService`），路由已全部改调它 |
| 4 | `docs/ui/前端改造计划.md:27` | 「剩余：T3.1 服务端角色 agent 流式（`character_prompt` 仍为空注释）」 | 同上，已过期 |
| 5 | `docs/prompts/00-共同上下文.md:214` 与 `docs/prompts/05-装配与验证.md:427-431` | `show_frame`「全仓连发布点都没有」 | **已过期**：`packages/shared/src/actions/show.ts:157-161` 产出 `details.frame`，`event-bridge.ts:162` 已广播 |
| 6 | `docs/tools/02-chalk.md:455`、`docs/tools/09-link与arrange.md:497` | 引用 `useWorld.ts` 的行号（`:171-181` / `:178-210`） | **行号已漂移约 100 行**（实际 switch 在 `:256-325`） |
| 7 | `docs/protocols/doc-21-事件表协议.md:25,204` vs `docs/tools/12:588` | `world_frozen`/`world_thawed`「无写入点无消费者，删除」 vs 「保留播出」 | **文档互相矛盾**；现状是**确实广播也确实消费**（`routes/world.ts:806` / `useWorld.ts:282-287`），doc-21 的判断被实现证伪 |
| 8 | `docs/development/后端实现计划.md:525` T6.1 的 `WS_COMMANDS`（`input/choice/character_open/...`） | 与现状 `writer_prompt`/`character_prompt`/`character_start`/`character_stop` | **命名完全不同**；T6.1 是未实现的未来网关设计，但文档未标注"未实现" |

> 注：`docs/prompts/**` 是队友未跟踪的产物，只登记不改。

---

## 7. 修复范围（待拍板）

核实到此为止。下面的**范围**需要你定，再进设计 → 评审 → 实现：

| 档 | 内容 | 量级 | 依赖 |
|---|---|---|---|
| **A. 止血** | 修 P0 掷骰请求体；删两条死监听；补 `world_event` case + `event.id` 去重；修 `airp:world-event` 转发集合；接 `writer_delta`/`character_delta`/`character_message`（角色不再说假话） | 中 | 无 |
| **B. 演出补齐** | A + `chalk_writing`/`chalk_landed` 幻影落地、`dice_result` 仪式、`canvas_patched` 增量合并、`image_*`、`*_idle`、`show_frame` 演出库 | 大（对应前端计划 T3.1/T3.4/T3.5） | A |
| **C. A + 文档回写 + 门禁入 CI** | 只做 A，外加把 §6 的假断言改掉、门禁接进 `package.json` | 中 | 无 |

独立小活：**T0.6 并发保护**（`expectedRevision`，全仓零命中，`docs/ui/前端改造计划.md:124-134` 描述仍准确）。

**注意**：`docs/ui/前端改造计划.md` 的 **T3.4/T3.5/T3.6/T0.6 描述与现状一致（确实未做）**，可继续当任务书；过期的是 T3.1 的现状描述、T3.7 的落点、以及 `:27` 的摘要。

---

## 8. R 系列计划的有效范围（2026-09-14 现场核实）

`docs/ui/前端改造计划.md` 的「R0–R9 整壳替换」与 P5 秀感写于 2026-09-12；**该文件的属主已于 2026-09-14 自行回写**（`docs/ui/前端改造计划.md:3` 的更新行、`:158-174` 的「R 系列落地状态」与 §2.2b「已被取代的组件名」）。本节不重复那份状态表，只补它**没给**的东西：每条「已被取代」项的**取代者 `file:line`**，以及「哪些条目仍可当任务书」。

> 行号基准：`docs/ui/前端改造计划.md` blob `24818821`（2026-09-14 当次）。该文件由他人并发编辑，引用时请以 `grep -n` 现场复核。

### 8.1 权威级别（与 `AGENTS.md §4` 一致）

**R 计划不在仓库级「修改前先读」入口表内。** `AGENTS.md:67-89` 的入口表没有 `docs/ui/前端改造计划.md` 这一行；该表只列 `docs/tools|hooks|perform|presence|skeleton|agent-awareness|init|nook|command|footprint|layout|audio|assets|tts|live-voice|prompts|settings` 与 `docs/wiring/*`。因此**任何改动都不得以 R 计划作为「先读的冻结契约」**——它是历史任务书，不是契约。凡与上表任一契约冲突，以上表为准。（计划属主在同一回写里也自认这一点：`:158` 逐字「实际外壳不是 `WorldShell` 组件」，`:172-174` 列出四个零命中符号。）

> 相邻发现（不属本节改动范围，只登记）：`AGENTS.md:75` 把 WS 帧域的入口写成 `docs/前端接线体检.md`，但该路径**不存在**（实读仓库只有 `docs/wiring/前端接线体检.md`）。这是表里的失效指针，归 AGENTS 属主。

### 8.2 仍可当任务书的条目

| 项 | R 计划位置（2026-09-14 blob） | 现场证据 | 有效范围 |
|---|---|---|---|
| **T0.6** 命令边界与 revision | `docs/ui/前端改造计划.md:215-221`（§14 复核 `:648`） | `expectedRevision` 在 `apps/**`、`packages/**` **零命中**（实读 grep） | **全文有效**：请求带 `expectedRevision`、409 + 快照、同进程串行化、临时文件 + rename |
| **T3.6** 时间印章数据接线 | `docs/ui/前端改造计划.md:453-455`（§14 复核 `:651`） | 无 `TimeSeal` 组件（`apps/web/src/components/chrome/` 实读无此文件）；`apps/web/src/App.tsx:857` 的 `prototype-time-label` 只渲染固定文案 `THE FIRST MOMENT / THE STORY CONTINUES` | **目的有效**（把「第 N 天 · 时刻」从 chalk `status.data` 派生成常驻 UI）。**前提句已过期**：计划 `:455` 说「`App.tsx` 顶栏的硬编码字符串」——该字符串现在在 `apps/web/src/lib/legacy-ui-copy.ts:32`（`timestamp: 'Day 1 · Rain Subsides 17:40'`）；`apps/web/src/App.tsx:625` 已把 `chalk.frontmatter.status.data` 读进 `prototype-stat`（只取前 3 键，与时间印章无关） |
| **T3.4 的三条配套**（切层撤回 / perm vs temp / 主画布自由输入） | `docs/ui/前端改造计划.md:428`（横幅「已完成」，三条配套标「仍**未做**」）、正文 `:441-444` | **切层撤回**：`apps/web/src/lib/phantom.ts` 无 `clearAll`，`apps/web/src/state/useWorld.ts:239` 的 `enterLayer` 未接撤回（`docs/perform/01-作家演出通道.md:643-646` 记为待拍板）。**perm vs temp**：`apps/web/src/lib/writer-state.ts`、`apps/web/src/components/chrome/WriterBar.tsx` 均无 8s 淡出/不落盘分支（全栈 grep `ephemeral` 零命中）。**主画布自由输入**：`apps/web/src/App.tsx` 全局只有 `:517` 的 `input, textarea` 键盘守卫；输入面仍是 `WriterBar.tsx` 的 dock（`apps/web/src/App.tsx:1096`），非计划要求的「折叠 pill → 展开横格纸」 | 仅这三条有效；T3.4 主体（状态机/湿墨/幻影）已于 `docs/perform/01` 落地 |

> 对 §7 本文件旧注的更正：`§7:187` 称「T0.6 … `docs/ui/前端改造计划.md:124-134` 描述仍准确」——**行号已漂移**（`:124-134` 现在是「8792 可迁移清单」表，T0.6 在 `:215-221`）。`§7:189` 称「T3.4/T3.5/T3.6/T0.6 …描述与现状一致（确实未做）」**不成立**：T3.5 带「已完成」横幅且代码已落地（`:448-449`，见 8.3），T3.4 主体已完成。真正仍是未做项的只有 **T0.6** 与 **T3.6**，外加 T3.4 的三条配套。

### 8.3 已被取代的条目与取代者

| 项 | R 计划位置（2026-09-14 blob） | 取代者（代码 `file:line`） |
|---|---|---|
| **R0** 固定迁移基线（快照 `../worldlines-canvas/app/src`） | `docs/ui/前端改造计划.md:160` | **前提消失**：`../worldlines-canvas/` 在磁盘上**不存在**（实读 `ls -d`；计划 `:160` 亦自认「不在盘」）。基线被实际提交取代：`3efb586`（迁移四世界素材与沉浸式前端骨架）→ `d7541bc`（重构世界入口与沉浸式界面） |
| **R1** 整壳移植（新建 `WorldShell`） | `docs/ui/前端改造计划.md:161`、`:147` | **niko 批次外壳**：`apps/web/src/App.tsx:847`（root `className="airp-prototype …"`）+ `apps/web/src/prototype.css` + `apps/web/src/lib/ui-shell.mjs:1-6`（只导出 `initialShell`/`transitionShell`/`separateBounds`）。`WorldShell` 在 `apps/**` **零命中** |
| **R2** 唯一 AIRP gateway | `docs/ui/前端改造计划.md:162` | **已建**：`apps/web/src/lib/airp-gateway.ts:73`（`airpGateway`，含 `:85` `manifest`、`:86-87` `layer`）。**未兑现的残项**（「页面组件不得各自裸 fetch」）：`apps/web/src/components/narrative/DiceRoller.tsx:70`、`apps/web/src/components/overlay/CharacterModal.tsx:286`、`apps/web/src/components/narrative/EntityInteractions.tsx:203` 等仍是裸 `fetch` |
| **R3** 后台数据归一化（定义 `WorldViewModel`） | `docs/ui/前端改造计划.md:163` | **服务端归一化 + gateway**：`apps/server/src/routes/world.ts:723`（`/api/layer`）、`:564`（`/api/manifest`）→ `apps/web/src/lib/airp-gateway.ts:85-87`。`WorldViewModel` **零命中**；残留硬编码见 `apps/web/src/App.tsx:623`/`:1042`（`manifest?.id === 'wuwu'`） |
| **R4** 命令与演出桥 | `docs/ui/前端改造计划.md:164` | **已落地**：`apps/web/src/state/useWorld.ts:324` 发 `writer_prompt`；`apps/web/src/App.tsx:803`（`character_start`）、`:828`（`character_stop`）；骰/物品/排座/冻结经 `apps/web/src/lib/airp-gateway.ts:91,107,116,121,126,127` |
| **R5** Chalk 与世界序章接线（先进「导言层」） | `docs/ui/前端改造计划.md:165` | **落点被 init 批次取代**：「导言层」在 `apps/web/src` **零命中**；首屏由 `apps/web/src/App.tsx:857`/`:993` 的 `THE FIRST MOMENT`（map 层）表达；场景初始化链归 `docs/init/03-触发链路与前端.md:15`（`airp_init` 入站消息）。语义由 `docs/init/**` 拥有，本节不裁 |
| **R6b** 音频与署名迁移 | `docs/ui/前端改造计划.md:167` | **素材已就位、per-world 懒加载未做**：音频在仓库根 `assets/audio/{ambient,themes,foley}/**` + `assets/audio/CREDITS.md`（实读存在），经 `apps/server/src/routes/world.ts:478` 的 `AUDIO_ROOT` 与 `:1463-1468` 的 `/api/audio` 下发，前端 `apps/web/src/App.tsx:492-498` 的 `setAmbient/setBGM` 播放；`templates/*/assets/` 下**无音频文件**（实读，`.opus` 计数 0） |
| **R7** 唯一产品修正（hover 只展示 `EntityPeek`） | `docs/ui/前端改造计划.md:168` | **内联 chrome 取代**：`EntityPeek` **零命中**；hover 预览现由 `apps/web/src/components/canvas/CardRenderer.tsx:196-198`（card face 只留两行摘要）+ `:224` 的 `.gate__detail` 浮层实现（样式 `apps/web/src/index.css:1216-1245`）；直聊入口是 `apps/web/src/App.tsx:803` 发 `character_start` 走对话栈，其归属见 `docs/ux/04-动作语义与事实反馈.md:80` |
| **R8** 响应式与双态金标 | `docs/ui/前端改造计划.md:169` | **断点已落地，金标截图不可核**：`apps/web/src/prototype.css:724`（≤1180px）、`:730`（≤700px）、`:165`/`:809`（`prefers-reduced-motion`）。计划要求的 asset/no-asset 金标截图对比无机械核验物 |
| **R9** 切断旧前端并总验收 | `docs/ui/前端改造计划.md:170` | **已执行**：`apps/web/src/components/sidebar/RightSidebar.tsx` 已删，`apps/web/test/character-rail.test.mjs:304` 以 ENOENT 锁死；`chrome/` 下 `Minimap/HintBar/LayerBadge/CompassRose` 已删（提交 `f73b93b`） |
| **§14「8792 体验基线：R0–R9 完成」整行** | `docs/ui/前端改造计划.md:647` | **框架失效**：R 系列的「整壳替换」目标已由 niko 外壳达成，不再以「R0–R9 完成」计数为验收单位；该行应改引 `docs/ux/**` 批次 |
| **T3.1** 角色 agent 流式 | `docs/ui/前端改造计划.md:361` | **已完成**（横幅 + 代码）：`apps/server/src/index.ts:252`（`character_prompt` 分支）→ `apps/server/src/engine/event-bridge.ts`；前端经 `docs/wiring/03-角色流式.md`。计划旧句「`character_prompt` 仍为空注释 / `sampleReplies`」已由本文件 §6 登记为假 |
| **T3.2** 6 表情差分（精灵表） | `docs/ui/前端改造计划.md:369-375`、资产表 `:626` | **独立透明 webp 取代**：计划自己在 `:375` 标「**精灵表方案作废**」；裁决源 `docs/assets/01-六情绪差分.md:548-551`（冲突 C1：assets 00 §3.1/§5.2 冻结独立 webp + `<img src>`）。**落地已按 webp 实现**：`apps/web/src/index.css:470-473` 注释明写「the old CSS-filter simulation is gone」，`:474-496` 是 `.portrait-emo` 容器 + `.portrait-still` 的 `<img>` 切换——该文件内**已无 `background-position` 精灵表注释**，故 assets/01 §11 C1 点名的「`index.css:403-405` 需改写」已随重编号失效（现 `:403-405` 是 `.character-modal-layer` 的 backdrop-filter） |
| **T3.3** 遮罩重做 | `docs/ui/前端改造计划.md:377` | **已完成**（横幅）：`apps/web/src/components/overlay/CharacterModal.tsx` |
| **T3.5** 幻影落地与座位过户 | `docs/ui/前端改造计划.md:448-449` | **已完成**（横幅 + 代码）：`apps/web/src/lib/ghost.ts`、`apps/web/src/components/narrative/GhostCard.tsx:26`、`apps/web/src/components/canvas/PhantomLayer.tsx:43`（挂载 `apps/web/src/components/canvas/Canvas.tsx:637`）、座位过户 `apps/web/src/lib/phantom.ts:208 reconcileLanded`（接 `apps/web/src/state/useWorld.ts:227`） |
| **T3.7** 统一动作服务 | `docs/ui/前端改造计划.md:457-461` | **已完成**（横幅）：`packages/shared/src/actions/service.ts:135`（`createActionService`），路由已改调它 |
| **T3.8** 资产探测降级（`lib/assets.ts`） | `docs/ui/前端改造计划.md:468-470` | **被 `assetUrl` + 服务端 `/api/asset` 取代**：计划的落点 `apps/web/src/lib/assets.ts` **不存在**；现机制是 `apps/web/src/App.tsx:126` 的 `assetUrl()` 与 `:507` 的 `probe.src` 预探 + `apps/web/src/lib/airp-gateway.ts:131`。缺图回退的「首字徽记」形态见 `apps/web/src/components/sidebar/CharacterRail.tsx:110`（`view.id.charAt(0)`） |
| **P5 T5.1 戏剧追光（`overlay/Spotlight.tsx`）** | `docs/ui/前端改造计划.md:531` | **`performance/PerformanceLayer.tsx` 取代**：`apps/web/src/components/performance/PerformanceLayer.tsx:161 showSpotlight`、`:402`（注册表 `spotlight`）；`overlay/Spotlight.tsx` 不存在。裁决见 `docs/perform/05-演出库.md:780-784`（冲突 6） |
| **P5 T5.2 线索风暴** | `docs/ui/前端改造计划.md:532` | **同上**：`performance/PerformanceLayer.tsx:405` 的 `evidence_burst` + 自有 `<svg>`（不进 `LinkLayer.registry`，`docs/perform/05-演出库.md:399`） |
| **§14「P5：追光与线索风暴各能触发一次」** | `docs/ui/前端改造计划.md:653` | **落点失效**（事项本身仍开放）：追光/线索风暴的归属是 `PerformanceLayer`，不再是 `overlay/Spotlight.tsx` |

### 8.4 一句话用法

要动前端**演出/幻影/右侧栏/外壳**，读 `docs/perform/00-共同上下文.md`、`docs/presence/00-共同上下文.md`、`docs/ux/**`、`docs/skeleton/00-共同上下文.md`（均已在 `AGENTS.md:76-79` 入口表内）；R 计划与 §14 只在做 **T0.6**、**T3.6 数据接线**、**T3.4 三条配套**时当任务书，其余条目一律视为历史记录。
