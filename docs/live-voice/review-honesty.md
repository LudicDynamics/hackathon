# review-honesty — L2 诚实性与非空性评审（评审 UnderlyingRat，2026-09-15）

> 范围：`10`（契约）/`11`（store）/`12`（界面）/`13`（门禁与回写）的**只读**评审。
> 判据：未知项有没有被伪装成已定；已定项有没有被写成未知；验收断言能不能**真的变红**。
> 证据：所有 `file:line` 已实测核对；推断项标 `[推断]`。基线实跑：`check-ws-contract` → `clean (27 emitted, 24 consumed, 27 in contract)`、`check-request-bodies` → `clean (11 route(s) pinned)`，均 exit 0。

---

## 一、待拍板 / 冲突逐条复核

### 1.1 真未知（只有 2 条，且都没人认领）

| 项 | 位置 | 状态 |
|---|---|---|
| `11 §13-U4` / `11 §12-C2`：`00 §15.4` 的 4 字段 `LiveCallState` 是否标「被 `10 §2.2` 取代」 | `11:574`、`11:558` | **真未知且无落点**。契约 §9 无此行；`13` 全文 **0 次** `15.4`；`13 §8` 的 W1–W23 也没有。建议直接进 W 清单（一行），别挂「待拍板」。 |
| `12 §11-C5`（`aria-pressed` vs tablist） | `12:460`、`12:478` | 真未知（未进契约 §9，`12` 已显式声明为残余项）。**非阻断**。 |

### 1.2 已裁定却仍列在「仍未知待拍板」节（反向不诚实，共 7 处）

- `11 §13`：`U1`（`tick(now)`）、`U2`（deps 缝）、`U3`（对话框按钮态）、`U5`（`loadWorld` 自清）——四条**已被契约 §9 裁决**（见 `10:495`、`10:496`、§9 的「`12 §12 前置（`11` §12-U3）已定」行、`10:497`），但 `11:571/572/573/575` 仍是普通待定行（只有 `U6` 被划掉，`11:576`）。
- `13 §12`：第 3 项（L4/L5 时机，`13:330` 已由 `10:451` 裁定）、第 4 项（`00 §9` 指路行，`13:331` 已由 `10:449` 裁定并**已落**）、第 5 项（变量键，`13:332` 已由 `10:450` 裁定「否」）。

影响：按 `11 §13` / `13 §12` 收口会**重新讨论已裁定项**。修法：划掉这 7 行并指回 `10 §9`。

### 1.3 伪装成已知的实现风险

1. **`DEFAULT_LIVE_VOICE` 的「按角色性别选择回落值」没有数据源**（`10:411`）。修法写 MUST「按角色性别选择回落值」，但**全仓没有任何角色性别字段**：`grep -rl '^gender:' templates worlds` → 0 命中；`gender` 只存在于调色板的 `VoiceEntry.gender`（`packages/shared/src/rules/voices.ts:29`，描述**音色**）。这与 `10:365` 自认的「性别来自网页检索 + 社区共识」互相矛盾。**这不是待拍板项，是一条没人指出的前置缺口**：要么加手工表，要么把修法降为「选性别中立音色」。
2. **`10:385` 的语料表用 `world.json` 正文的 `his` 反推角色性别**——`[推断]` 级别的方法，却被当作 L5 的机械断言依据。

### 1.4 冲突项复核

`11 §12` C1–C6、`12 §11` C1–C8、`13 §11` C1–C10、`10 §9.1` (a)–(h) 的定性我全部复核，**无异议**，其中：

- `13 §11-C9`（`INTENTIONALLY_UNCONSUMED` 4 条失效）：逐条实跑核对通过（`writer_message` 被 `apps/web/src/lib/writer-state.ts:167` 消费；`character_message`/`error`/`turn_aborted` 被 `useWorld.ts:761/790/811` 消费）。它们**不进 `dark` 分支**（对应帧已在 `consumed` 里），故「只登记不清理」不改门禁行为，正确。
- `13 §11-C10`（`CONSUMERS` 靠人维护）：**建议升级为 P1**——本轮已证明它能漏 4 个真分派文件，漏的后果是「帧名写错门禁不变红」。

---

## 二、假绿风险清单（每条给「什么实现会让它白过」）

| # | 断言 | 什么实现会让它白过 |
|---|---|---|
| **F1** | `12 §10 A1`（`12:430`） | **极性盲**：只查 token 出现、不查 `!`。五处门全写成 `if (callActiveRef.current) void prefetchVoice(...)`（漏掉非）——TTS 门含义反转（通话中**只**发 TTS），A1 仍全绿。 |
| **F2** | `12 §10 A1′`（`12:431`，本轮新增） | **位置盲**：只断言源码含 `callActiveRef.current = callActive`。把该行放进 `useEffect`/`useCallback` —— 字符串命中、A1′ 绿，但赋值不在渲染期，`:388/:400/:406` 读到陈旧 false → 契约 §9 末尾「补充冻结」价值归零。需断言赋值在组件体顶层（与 `CharacterModal.tsx:811` 的 `busyRef.current = busy` 同处、首个 `return` 之前）。 |
| **F3** | `12 §10 A2–A8 / A3′ / A13`（渲染类） | **harness 把失败转成 skip + SSR 缺参**：① `12:425` 指定体例为 `card-skeleton-render.test.mjs`，该体例把 bootstrap 失败吞成整组 skip；② `renderToStaticMarkup` 对 `useSyncExternalStore` **要求第三参 `getServerSnapshot`**，缺它直接抛（本机实测：`Error: Missing getServerSnapshot, which is required for server-rendered content.`），而 `11:479` 把绑定体例**指定为 `agent-cursor.ts:412` 的两参形式** → 按此写整组渲染断言只会抛/只会 skip，永不真判定。 |
| **F4** | `12 §10 A8`（`12:440`） | **无法区分修与不修**：`CharacterModal.tsx:880` **既有** `<ActivityRail surface="character-modal">`，已产出同一字符串；`12:440` 自己写了「出现两次」的退让判据。删掉 `.call-stage` 那条新 rail，A8 仍绿。反向也坏：空闲时 `ActivityRail` 返回 `null`（`ActivityRail.tsx:30`）→ 静态渲染下可能不出现 → 假红。 |
| **F5** | `12 §10 A10 / A12`（`12:442`、`12:444`） | 同 F1 极性盲：`if (!callActiveRef.current) return;` 写反会让文字模式瘫痪，而两条断言只看「首行含 `callActiveRef.current`」，仍绿。 |
| **F6** | `12 §10 A11`（`12:443`） | **键已存在 → 免测即绿**：`messages.json` **已有 `Close`**（实测 `{"zh-CN":"关闭","ja":"閉じる"}`，共 426 键），而 `12:209` 断言「上表 10 个键全部不存在…`Close` 逐个查过均 free」——**该句是事实错误**（与 `12:207` 自称「`Close` 是新键（实测 free）」自相矛盾）。真实影响：`Close` 不写也能过 A11，`check:i18n` 也不会红。 |
| **F7** | `11 §11.1 A15/A16`（`11:528-529`） | **负向文本断言的绕法**：A15 断 store 源码不含 `from 'react'` —— 改双引号或经 shim 间接 import 即漏判；A16 断 `live-call.ts` 不含 `fetch(`/`addEventListener` —— 用 `.bind(window)` 或经 deps 间接调用即绕过。应断行为而非源码字符串。 |
| **F8** | `11 §11.1 A2/A3/A5/A6` 的 fetch 计数 | `[推断]` 依赖 `deps.fetch` 注入；若实现里 close 走 `deps.fetch`、session 走 `globalThis.fetch`（混用），计数永远对不上。契约应把「三个 fetch 全部经 `deps.fetch`」写成 MUST（`11 §2.1` 目前只写「缺省用真实实现」）。 |

---

## 三、证据强度不匹配清单

| # | 断言/结论 | 声明了什么 | 实际证据 |
|---|---|---|---|
| **E1** | `13:176` / `13:298`：「**L5 必红**」「实测今天这条断言**立刻变红**」 | 断言今天必红，故满足 `00 §9.3` 非空性教训 | **今天实测是绿的**。① `elias` 已在 `templates/fpal/characters/elias/README.md:4` 补 `voice: cool-composed`（`10 §6.1.3`），不再走回落值；② 门禁语料枚举**有意排除 fpal**（`tools/world-editions.mjs:21-44`），我实跑语料函数：未声明 `voice:` 的只剩 `divergence{,-jp,-zh}/young-ryo`，全部 → `marin`，而 young-ryo 是**女性**（README *a seven-year-old girl*）→ 与回落值同性别 → L5 第二析取项成立；③ `DEFAULT_LIVE_VOICE` 落回不适用。结论：**L5 今天无法变红**，而 `13` 把它当「本批最重要、红即证据」排期。 |
| **E2** | `10:417` + `13:169` 的 L5 析取项「要么性别中立」 | 提供一个今天可判的支 | 代码里无「中立」取值：`VoiceGender = 'female' \| 'male'`（`voices.ts:22`），`LIVE_VOICE_GENDER` 尚不存在 → 该支**今天不可求值**。 |
| **E3** | `10:334-338` 第一层「结构性、**这才是最可能的直接原因**」「任何没写 `voice:` 的男角色都会得到女声」 | 以「结构性」把结论提到最高可信度 | 该因果链依赖的正是同一份外部网页性别来源，而 `10:365` 的免责边界**只覆盖第二层**；且实测语料里「未声明 `voice:` 的男角色」今天**一个都没有** → 这是**条件句而非现状**。措辞强度高于证据等级。 |
| **E4** | `12:441` A9：「行号比较」→ 证明 `stop` 早于 `onClose` | 视作「先挂断、先于 `character_stop`」的证明 | 行号只证明**同一函数体内的书写次序**；两者都是异步（`character_stop` 在 220ms 后经 `onClose()` → `App.tsx:1117`）。同 tick 书写次序**不构成服务端到达次序**。应断实际调用序列（spy）。 |
| **E5** | `10:493`：「共提出 **11 项**待拍板」 | 计数 | `10 §9` 的表**有 13 行**（`10:497-509`）。计数与其自身表不符（其中「12 §12 前置」在 `12` 里本就标「已定」）。 |
| **E6** | `10:490`（§9-U3）：「**登记为已知缺口**交给 `13`…**MUST NOT 在本批悄悄留着不记**」 | 裁定已兑现 | `13` 全文 `errorText` 出现 **0 次**；W1–W23（`13:240-263`）无此行；`13 §12` 也无。`10 §9.1(f)` 只说「评审 F2」，**没有替代登记落点**。裁定要求的登记**未发生**——正是裁定自己禁止的形态。 |
| **E7** | `10:422-424` §6.1.3「`fpal/elias` 已补 `voice:`」 | 修复已生效 | 模板侧准确（实跑 `resolveLiveVoice('cool-composed') === 'alloy'`，第 4 行确为 `voice: cool-composed`，与模板 37 处位置一致）。**未覆盖**排除范围：`worlds/exp-{27e0d860,35bbc20f,927cfb26,cc58c435}/characters/elias/README.md` **仍无 `voice:`**（`[推断]`：从旧存档续玩仍得 `marin`）。建议补范围限定。 |

> **⚠️ 更名 + 存档注（2026-09-15）**：上表路径已改为 `templates/fpal`（世界当日由 `templates/exp` 更名）。E7 行引用的 `worlds/exp-{…}` 是 **gitignored 的开发期运行时存档，已于 2026-09-15 清理**，故「旧存档续玩仍得 `marin`」的 `[推断]` 已不可复现——保留为当日范围限定的建议。

---

## 四、一句话总判

**契约 §9 的裁决本身站得住，但有两条落空：`U3` 的缺口没进 `13`（E6）、`L4/L5` 的「先红」依据不成立（E1/E2）；而 `12 §10` 的验收在「极性、赋值位置、harness skip、A8 锚点、`Close` 键已存在」五处仍能白过（F1–F6）——修完这 7 条，本批的「非空性」才是真的。**

---

## 五、契约引用的「现状」不成立（伪装成已知，追加）

| # | 结论 | 实际证据 |
|---|---|---|
| **E8** | `10:195`「**已有的**单向守卫保留：`NookView.tsx:217-226` 在 nook 通话中阻止打开对话框」、`10:203`「**实测缺口**（主 agent 核对 `NookView.tsx:213-226`）：现有守卫是…」——两处都把该守卫当作**运行中的现状**，§4.3 整节的「放宽为同一角色」以它为前提 | **该守卫是死代码**。`NookView.tsx:197` 定义 `handleOpenCharacterModal`，其唯一引用就是自身的定义行（`:197-205`）；`:626` 传给 `<Canvas>` 的是**原始 prop** `onOpenCharacterModal`（`:156` 解构），完全绕开守卫。`App.tsx:1196`/`:1245` 也只把 `openCharacter` 直连给 `Canvas`。**因此 `liveCallModalBlocked` 这条提示在本批前后都不会触发**：§4.2 宣称的「双向互斥」第一支（nook 侧硬挡）**没有任何强制点**，§4.3 的「放宽」在行为上是零。既有缺陷（git 史 `2c1684b` 接线正确、`95f2b55` 断开），但本设计是它唯一的引用者，且把「未接线」写成了「已保留」。 |
| **E9** | `12:36`：`callAlert`「**NEW** `useState('')` … **`call.error` 是它的直接来源**，因此它在 `phase:'error'` 时非空」 | `12` 全文 **`setCallAlert(` 零命中**（除 `:36` 的符号声明）。没有生产者 → `callAlert` **恒为空串** → `.dialogue-call-alert` 永不渲染。`12 §10 A5` / `A3′` 之所以会红，正是因为**设计里缺这一环**，而不是实现漏写。该行「因此它在 `phase:'error'` 时非空」是一句**无代码支撑的因果断言**。 |
| **E10** | `12:160` + `12:395`：`hangUpRef` 的焦点 effect「漏了会怎样：…键盘玩家在对话框里失去落点」 | `12` 全文 **`ref={hangUpRef}` 零命中** → `hangUpRef.current` 恒为 `null` → `?.focus()` 短路，effect 是空转。`12` 已实测确认现状无 `role="alert"`，但未发现自己新增的 ref 同样没有挂载点。 |

> 这三条与 §二/§三 的 F/E 同属一类：**设计文本描述了一个它自己没有落地的行为**。`11 §11.1` 的 A 系列（store 侧）不受影响；受影响的只有 `12` 的 A5/A3′（会红，但红的原因是设计缺口）与 §3.6 的焦点承诺（无断言覆盖，静默空转）。

---

## 六、复核期新增验证（2026-09-15，工作树已前进）

> 复核时工作树已被实现方推进：`apps/web/src/lib/messages.json` 现为 **435 条**、`check:i18n` 报 **274 used keys**（本评审基线快照是 426/265）。以下三条经我独立实跑核过，与 MagicLoon 的闭环线结论一致。

| # | 结论 | 实测 |
|---|---|---|
| **E11** | `13:21`（基线表）与 `13:220`（§6.2）写 `check:i18n` 现状「+ **4 条 warn**」——**数字错** | 实跑 `node tools/check-i18n.mjs`：`ok i18n keys: 274 used keys resolve with full zh-CN/ja translations (435 entries)`，**warn 161 条**（`grep -c warn`）。`used`/`entries` 两数与文档一致，只有 warn 数错。后果：验收时把 warn 增长当异常，或反过来把 161 条真 warn 当噪音忽略。修法：基线表改「161 warn」，或把「warn 是死键不是失败」的判据写进表内。 |
| **E12** | `13:106` 引 `read()` 的行号 → **`check-ws-contract.mjs:182`** | 实为 **`183`**（`grep -n '^const read = '`）。`ENOENT` 的论证本身成立（无 try），只是锚点差 1 行。 |
| **E13** | `12:209`「10 个键全部不存在」在复核时**已随实现变为「10 键全部存在」** | 实读 `messages.json`：`Start a call`/`Text`/`Dialogue mode`/`On a call`/`Listening…`/`Hang up`/`They say`/`You`/`Could not connect`/`Close` **全部 EXISTS**，总数 **435 = 426 + 9**。与 `Close` 复用（新增 9 键：`Could not connect` + 其余 8）**一致**。故 **E/F6 的「免测即绿」在新树上已消解**（键已被真写入），但 `12:209` 那句「全部不存在」若保留，会与本树直接矛盾——实现期须改为「本批新增 9 键，`Close`/`Connecting…` 复用既有」。 |

**计数口径（三方对齐后可引）**：本批**新增 9 键**、**用到 11 个键**（9 新 + `Close` + `Connecting…`）、`messages.json` 总数 **426 → 435**（不是 436）。主 agent 给 12 号的「新增键 2→1、总数 8→9」把「`Close` 复用」与「其余 9 键仍须写」混成一条，该口径不成立。
