【裁决】整批**不通过**（`05` 不通过，其余五篇有条件通过）。语义与诚实性基本达标，失分集中在「非空性断言有效性」——`05` 是判决书，它的假绿/假红清单不能有错。

【必答 1：修复前是否真会失败】A1 ✅真（复跑 /tmp/nook05-route.mjs：`404 HTML Cannot GET /api/nook`，且同脚本 `/layer?layer=characters/ryo → 200` 的对照成立）。A8 ✅真（同上脚本 `404 {"code":"not_found","error":"layer must be an existing layer id"}`）。A13 ✅真（/tmp/nook05-seat2.mjs：4 卡全 `860 456`，distinct 1/4；/tmp/real2.mjs：3 卡全 `820,450`）。A14 ✅真且设计好（把红钉在 seatUnplaced 上，防「去改路由」）。A23 ✅真（/tmp/nook05-arrange.mjs：`not_found`，msg 与 canvas.ts:457-464 一致）。A25 ✅正向真（/tmp/nook05-layout.mjs：两次 not_found，分别 `Unknown layer` 与 `not on a canvas layer`）。A27 ✅真（/tmp/nook05-z.mjs：`z per seat 1,1,1`）。**但**：A12 是**假红**（CardRecord 无 `metadata` 字段，字段在顶层；实测 row.metadata===undefined → 正确实现下必失败）；A24 **假红**（fixture 没写 characters/ryo/diary.md → 走 assertCardExists 抛 Card not found，先于 nook 分支，修复后仍失败）；A25 负向护栏**假红**（与已落地 characterIdOfPath 互斥，子目录卡返回 'ryo' 故 layout 会成功）；A26 **悬空编号**（只在错误边界表与文件清单被引用，全篇无定义）；A17 ② **零覆盖**（只剩一行注释，无 fixture 无断言）。

【必答 2：假绿】真·假绿未出现——五条非空性判据全部锚在「修复前 404 / distinct 1/N」上，A7/A10 作者还主动声明「不构成非空性判据」（诚实高分）。**但存在「假绿风险清单式」的失效**：① 探针 §8.1 的「互不重叠」是**死代码**（05:743-748 算了 ox/oy 从不使用），`console.log(… 'none overlapping.')` 在任何实现下都打印；② A13 标题写 `DISTINCT positions`，会被读成「A13 绿=坐席修完」；③ A17 ① 把 sumi（README+preset.json）称作 doc-11 的空小天地，而实测 `isNookEmpty(sumi) === false`（README 不是 *.json）——它锁的其实是不触发初始化的那一档，真该锁的 old-sailor（`isNookEmpty===true`）没有对应断言。

【必答（A13 专项）】**不能**。比题面更糟：A13 **无论按中心还是按 (x,y) 都抓不到 nextZ**。实证 /tmp/revc-a13.mjs（复制 seatUnplaced，**只补 occupied.push、不补 nextZ++**）：中心 4/4 绿、(x,y) 4/4 绿、distinct z 1/4 红；补上 nextZ++（/tmp/revc-both.mjs）→ A13 4/4、A27 4/4、无重叠全绿。机理：z 不参与坐席几何（occupied/overlapsOccupied/spiralCells 只吃 cx,cy,w,h）。**A27 不可省、不可并入 A13**，且建议在 A13 注释与 §10.1 期望输出里写明「只锁 x/y；两条须同时绿」。

【必答 3：诚实性】`[推断]` 使用**收敛且诚实**，**未发现**把未知伪装成已定的实例；被推翻时的留痕（00 §3.7.1 证据撤回、01 §6.5、04 附B、02 §⑫-3）比一般批次好。冲突节也诚实地列了项目自身问题（check:docs 只扫 docs/hooks 且实测输出一致、overlapsOccupied 未 export、pnpm test 不存在——均复跑核实；contract 24/emitted 24/findings 14、`check-hooks-docs: clean (7 docs…)`）。**三处已过期陈述需回写**：05:185 的 isValidCharacterId 落点写成 apps/server（主 agent 已裁决）；04:322-324/334/348 把已落地的 isNookEmpty（rules/emptiness.ts:36，签名 (files,dir)）列为「P0 纯函数未写」；02:174 的 `characters/watson/old-photo.md` 全仓不存在（结论对——我用现场 fixture 复现 moveEntity 确实可用，文件名错）。A22 的「mime-types 实测已能给 video/webm」我无法复核（apps/server/node_modules 下无该包）→ 我这边标 [推断]。

【必答 4：需求强度】01/04/03 兑现（03 局部过度设计：`!loaded && posterOk` 的 <img> 与 <video poster> 双取同一 URL，作者自己也承认多余）。02 兑现但欠味。**两处把产品意图做懦**：(a) doc-06 §4.1「存在过的证据」/§4.3 的「角色存在核心（头像+状态行）」「思绪连线」被连带砍掉——它们不依赖任何后端新数据（存在核心只需头像+一行 frontmatter），本批「逛小天地」只剩平铺的卡，建议在 NookView 加一条只读存在核心条（零新增通路）；(b) **doc-06 §4.2 三种写入情境里唯一能证明「小天地是活的」的情境一（「角色留下了东西」提示）被完全丢弃**，且未在 00 §4 边界表里显式登记（隐身于「实时协作」条目后）。最小兑现不需新端点/新帧：文件变化已经 event-bridge 广播、airp:world-event 消费面已在。

【最重的一条：判据无人负责】00 §4 判据逐字含「看见那里的陈设（**含一件会动的立绘**）」，但 `grep -rl "component: portrait" templates/` 零命中、`find templates -name '*.webm' -o -name 'base*.png'` 零命中（真资产只在 gitignored 的 assets/ 车间树）、03:625 明写素材生产「本批不执行」、03 §10.5/§10.6 点名要求 05 **MUST 保留**的 portrait DOM 断言（.portrait__video 计数 1/0、回退矩阵）在 05 里**完全不存在**（05:674 把 portrait 的回退推给 §10.4 手测，而手测第 8/9 条没有任何 fixture）。「会动的立绘」这条判据今天三处互推、无人负责。另附：我把 `occupied.push + nextZ++` 打进一份 dist 副本跑 shared 全集 → 314/315 通过，唯一失败是与修复无关的路径问题 → 两行修复无回归，可放心断言 A13/A27。

【报告落盘】评审人无写文件工具，全文在此；请主 agent 落盘为 docs/nook/REVIEW-C-语义与诚实性.md（findings 分节已含 12 条带 file:line 的问题，另有回写清单 C1–C15：C1 A12 字段、C2 A24 fixture、C3 A25 负例、C4 A26 定义、C5 A17 拆三条、C6 A13 注释分工、C7 探针死代码、C8 05:185 落点、C9 01:446 单参、C10 02:167 帧序、C11 02:236 Canvas、C12 02:174 文件名、C13 04 附A isNookEmpty、C14 00 §3.5 hidden 措辞、C15 00 §4 情境一登记）。