# 05 — 验收与回归（机械可核验的验收 · 探针扩展 · 回写清单）

> 子文档 05 / N1「角色小天地」。owner：**只定义「怎么证明它对」，不实现功能**。
> 上位：`docs/nook/00-共同上下文.md`（冻结契约）§2.7（BLOCKER-N1）、§3/§3.7/§3.7.1（冻结决定）、§4.1/§4.2（立绘判据 + 存在核心）、§5（跨模块契约）、§7（机械核验）。
> 行号纪律（契约 §5.6）：本文所有 `file:line` 于 **2026-09-13** 在本机仓库根 `/home/yoshix7ti/projects/hackathon` 复核；若漂移，以符号名重定位并回写本文。
> **评审门后修订（2026-09-13，ReviewA/B/C + 主 agent）**：A 编号统一加 **`N1-`** 前缀（与 01 §12.2 的 A1–A20 区分）；修 5 处**假红**（N1-A12 字段、N1-A24 fixture、N1-A25 负例、N1-A18 硬编码计数、探针死代码）；补 N1-A17a/b、N1-A26 定义；**新增 §3.13 立绘判据 N1-A28–N1-A33**（契约 §4.1）。
>
> **2026-09-14 语义修订：** 角色自身媒体不再由 `portrait.md` 提供。原 N1-A28–N1-A33
> 中把角色媒体绑定到 `component: portrait` 文件的断言已废止；验收应改测
> `data-nook-zone="character-media"` 的 `/api/characters` metadata 通路、静态回退、
> reduced-motion、hidden 和 Nook 总 video ≤ 1。`portrait` kind 仅作为普通陈设保留。

---

## 1. 一句话定位

**为 N1 给出「实现者交卷时评审照抄命令行就能判对错」的判决书**：一张验收表（每条带修复前/修复后预期 + 落点文件）、三条**非空性断言的完整伪码**（含修复前的真实观测输出）、两个测试文件与一个探针段的落点、以及本批必须回写的上游文档清单。

**05 不实现任何功能**；它只回答一个问题：**「没有这个修复就会失败」如何被机械证明。**

---

## 2. 签名 / 参数（05 产出的测试工件，全部 NEW）

### 2.1 测试文件

| 文件 | 类型 | 覆盖 | 跑法 |
|---|---|---|---|
| `apps/server/test/nook-routes.test.mjs` | NEW | `GET /api/nook` 全套（形状 / 校验 / 空态 / 不落事件）、`POST /api/card/footprint` 的 nook 分支、`POST /api/card/position` 的 nook 路径（§3.1–§3.6、§3.11） | `node --test apps/server/test/nook-routes.test.mjs` |
| `packages/shared/test/nook.test.mjs` | NEW | `portrait` kind 计数（§3.9）、registry 加载不 throw、**BLOCKER-N1 坐席唯一性**（§3.7）、`arrangeCards` 的 place/layout 两条 nook 分支（§3.11/§3.12） | `node --test packages/shared/test/nook.test.mjs` |

> **体例**：`packages/shared/test/*.mjs` 一律 `import` 已构建的 `dist/`（逐字照 `footprint.test.mjs:22` 的 `../dist/store/local-store.js` 写法），`node:test` + `node:assert/strict`，临时目录 + 真 SQLite。`apps/server/test/*.mjs` 的样板是 `apps/server/test/smoke-routes.mjs:1-62`：真 `createWorldRouter` + 真 `LocalWorldStore` + 桩 lifecycle，然后走 HTTP（`smoke-routes.mjs:57-77` 的 `app.use('/api', …)` + `post()` + `check()` 三件套）。
> **`apps/server/test/` 下裸跑 `node --test <file>` 是可行的**：该目录的测试 import `@airp/shared` / `express`（`smoke-routes.mjs:17-19`），解析走 `apps/server/node_modules/`（已核实存在 `apps/server/node_modules/express`）。**测试文件 MUST 沿用与同目录既有测试一致的模块解析路径**（`apps/server/test/` 用裸包名 `@airp/shared`；`packages/shared/test/` 用 `../dist/…` 相对路径），不要混用。

### 2.2 测试内共享 fixture（NEW，只存在于测试文件内，**不进 `src/`**）

```js
// apps/server/test/nook-routes.test.mjs —— 非空 fixture（与 01 §12.3 逐字同形）
/** NEW: 一个「小天地非空」的最小真实世界（每个 md 都是真实 kind，非合成目录）。 */
const NOOK_FILES = {
  'world.json': JSON.stringify({ id: 'nook-probe', name: 'Nook', description: '', author: '', genre: 'test',
    createdAt: '2026-01-01T00:00:00.000Z', updatedAt: '2026-01-01T00:00:00.000Z' }),
  'world/README.md':               '---\nname: Map\ntype: readme\naudio:\n  ambient: rain\n---\n\n# Map\n',
  // 角色门面 = 该角色 README（契约 §3.1：scene 字段取它）
  'characters/ryo/README.md':      '---\nname: Ryo\ntype: readme\nmaterial: warm\n---\n\n# Ryo\n',
  // 陈设：两件**不同**真实 kind（note / letter）—— 形状与同尺寸断言互不干扰（01 §12.3）
  'characters/ryo/diary.md':       '---\ntype: note\ntitle: A Diary\n---\n\nI keep meaning to write.\n',
  'characters/ryo/photo.md':       '---\ntype: letter\ntitle: A Photo\n---\n\nUnfinished.\n',
  // 子目录卡（N1-A8 否定断言 + N1-A25 最后一段）：MUST NOT 进 items
  'characters/ryo/letters/unsent.md':'---\ntype: note\ntitle: Unsent\n---\n\nDraft.\n',
  // 第二个角色目录（跨 nook 负向断言用，01 §12.3）
  'characters/other/diary.md':     '---\ntype: note\ntitle: Other\n---\n\nx\n',
  // 空态 fixture（§3.6）：只有 README + preset.json，没有别的内容（+ 无 README 的角色）
  'characters/sumi/README.md':     '---\nname: Sumi\ntype: readme\n---\n\n# Sumi\n',
  'characters/sumi/preset.json':   '{"id":"sumi"}',
  'characters/old-sailor/preset.json':'{"id":"old-sailor"}',
};
```

> **fixture 形状以 `01-取数端点.md §12.3` 为准**（`NookRoute01` 2026-09-13 声明它是非空 fixture 的权威来源）。若本文与 01 命名不同，**01 赢**，05 跟随改名；本文只固定**「≥2 个不同真实 kind 的 md + 一份 README + 一个子目录卡 + 第二个角色目录」**这一形状。
> **MUST NOT 用 `templates/*/characters/*` 当非空 fixture**：实测 7 个模板的角色目录全部只有 `README.md` + `preset.json`（`01 §12.3` 逐一确认；`templates/cthulhu/characters/old-sailor/` 甚至只有 `preset.json`），`items` 必为空——用它做「非空」断言会得到**修复前也"通过"的假绿**。
> **MUST NOT 用 `watson`**（契约 §3.7.1 证据撤回：`templates/holmes-world` 那行 `characters/watson` 是子代理探针写的，不是既有先例，该 db 已删除）。名字一律用 **constable / nanami / sumi / ryo** 一类。
> **MUST NOT 用 `portrait` kind 或含 `video:` 的文件当 fixture**：`portrait` 归 03，未实现时会被 `cardKindOf` 降级为 `note`，让 fixture 意图失真（01 §12.3 同款纪律）。

```js
// packages/shared/test/nook.test.mjs —— 与 §3.7 同源的坐席 fixture
/** NEW: 一批**同声明尺寸、同 kind** 的无行卡（note = 200×168）——
 *  必须同 kind：尺寸天然不同会让断言「碰巧」通过而掩盖 BLOCKER-N1（01 §12.3）。 */
const SAME_SIZE_NOOK_FILES = ['diary', 'photo', 'sketch', 'ticket'].map((n) => ({
  path: `characters/ryo/${n}.md`, kind: 'note', w: 200, h: 168,
}));
```

### 2.3 探针段（NEW，写进 `tools/probe-writer.mjs`）

```js
// 文件头 :8 改为（`runNookSeat` 只用 SEAT_ANCHOR 与 SEAT_PAD，两者都在 barrel 导出）
import { LocalWorldStore, parseFrontmatter, SEAT_ANCHOR, SEAT_PAD } from '../packages/shared/dist/index.js';

// NEW: 插在 runProbe() 的 Probe 1（:113-134）之后、checkSpawn(Probe 2) 之前
async function runNookSeat() { /* §8.1 给出完整体 */ }
```

**为什么不落在另外两个探针**（看清 `package.json` 的 scripts，`package.json:13-16`）：

- `pnpm probe:tools`（`probe-tools.mjs` + `probe-tools-engine.mjs`）断言的是**工具注册面**与**引擎真执行 AIRP 工具**（`probe-tools.mjs:82-145` / `probe-tools-engine.mjs:52-87`）——本批**不加工具、不加帧**，与该面无关。
- `pnpm probe:inject` / `pnpm probe:prompt` 断言的是**注入块与提示词 wire**——本批不改提示词（契约 §6 反模式 8）。
- `pnpm probe`（`probe-writer.mjs`）的 **Probe 1 已经在用 `LocalWorldStore` 直跑 store 断言**（`probe-writer.mjs:117-134`）。坐席几何是**同一层级的 store 断言**，落在它旁边最省事、最同构。

---

## 3. 行为契约逐步（每条断言写「漏了会怎样」，并给出**修复前的实测观测**）

### 3.0 验收总表（核心产出）

「修复前预期」一列凡标 **【实测】** 的，都是本文作者在 **2026-09-13** 于本机真跑出来的观测，不是推断；其余为「无该路由/无该分支」的结构性预期。

| ID | 断言 | 修复前预期 | 修复后预期 | 落点文件 |
|---|---|---|---|---|
| **N1-A1** | `GET /api/nook?character=ryo` → **200**，且 `items.length >= 1` | **404**（`Cannot GET /api/nook`，HTML 错误页）**【实测】** | 200 + `items.length === 2` | `nook-routes.test.mjs` |
| N1-A2 | 响应体是 `LayerState` 八键形状；`layer === 'characters/ryo'`；`scene.path === 'characters/ryo/README.md'`，`scene` 非 null | 404 | 形状齐 | 同上 |
| N1-A3 | `links === []` 且 `presence === []`（契约 §3.1：本批不做关系线/在场） | 404 | `[]` | 同上 |
| N1-A4 | `GET /api/nook?character=<非 kebab-case>` → **400 `invalid_argument`** | 404（无 JSON 体） | 400 JSON | 同上 |
| N1-A5 | `GET /api/nook?character=ryo/../..` → **400**（校验闸在 id 正则，非路径 normalize） | 404 | 400 | 同上 |
| N1-A6 | `GET /api/nook?character=nosuch` → **404 `{ok:false,code:'not_found'}`** | 404 HTML（无 `code`） | 404 JSON `not_found` | 同上 |
| N1-A7 | `GET /api/nook` **不落事件**（`store.getEventsSince(0)` 前后不变） | 无路由（vacuous 相等 → 本条**不构成**非空性判据） | 事件数不变 | 同上 |
| **N1-A8** | `POST /api/card/footprint {layer:'characters/ryo', boxes:[{path, w, h}]}` → **200 `{ok:true, updated:1}`** | **404 `{"ok":false,"code":"not_found","error":"layer must be an existing layer id"}`【实测】** | 200 | 同上 |
| N1-A9 | 同路由、`layer:'characters/Ryo'`（非法 id）→ **400 `invalid_argument`** | 404 `not_found` | 400 | 同上 |
| N1-A10 | 同路由、`layer:'characters/nosuch'` → 404 `not_found` | 404 `not_found`（**码相同、语义不同**）→ 故 **N1-A10 不是非空性判据**，只锁语义 | 404 `not_found` | 同上 |
| N1-A11 | N1-A8 成功后该行 `cards.layer === 'characters/ryo'`（契约 §3.3 冻结：nook 行存完整 nookId） | 行不存在 | 行在且 layer 正确 | 同上 |
| N1-A12 | `POST` 后该行 **顶层** `measuredAt` 非空、`seatW/seatH` **未被 footprint 动过**（`CardRecord` 无嵌套 `metadata`，实测字段展平：`world-store.ts:13-44`） | —（行不存在） | 见 §5 | 同上 |
| **N1-A13** | `seatUnplaced('characters/ryo', 4 张同尺寸无行卡)` → **distinct 中心数 === 4** | **1 / 4【实测】** | 4 | `nook.test.mjs` + 探针 |
| N1-A14 | 归因锁定：同批 seat 到 **普通层** `world/baker-street`（3 张真实卡）→ distinct 中心数 === 3 | **1 / 3【实测】**（两卡坐标逐字相同 `730,445`） | 3 | `nook.test.mjs` |
| N1-A15 | `portrait` 计数同步后 `components.test.mjs` T2/T10 绿（18→19，共 5 处） | 只加 kind 不改测试 → **红**（`19 !== 18`） | 绿 | `components.test.mjs:35,37,96,180,192` |
| N1-A16 | `CARD_FORMS.portrait` 存在；`import COMPONENT_REGISTRY` 模块加载**不 throw** | 漏加 `CARD_FORMS` 行 → **加载期 throw**【契约 §5.3 实测】 | 加载成功 | `nook.test.mjs` |
| N1-A17a | **不触发初始化那档**：`characters/sumi`（README + preset.json）→ 200 且 `items === []`、`scene` 非 null；且 `isNookEmpty(directChildren) === false`（README 不是 `.json`） | 404 | 200 `items: []` | `nook-routes.test.mjs` |
| N1-A17b | **真空小天地那档**：`characters/old-sailor`（**只有** preset.json）→ 200 且 `items === []`、`scene === null`；且 `isNookEmpty(...) === true`（`04` 的初始化触发输入） | 404 | 200 `items: []`+`scene: null` | 同上 |
| N1-A18 | `node tools/check-ws-contract.mjs --json`：findings 集合**相对实现时基线无新增项**（**不硬编码数**——`consumed` 随前端接线漂移，实测同分钟 8→11、findings 14→9） | 见 §8.3 基线捕获 | 集合 ∖ 基线 = ∅ | CLI（§8.3） |
| N1-A19 | `pnpm check:docs` 仍 clean | clean **【实测】** | clean | CLI（§8.3） |
| N1-A20 | `pnpm build` 退出码 0（`packages/shared` 改了 schema/forms） | — | 0 | CLI（§8.3） |
| N1-A21 | `pnpm probe` 里 `[Probe 1.6] Nook seating` 段无 throw | 段内 **throw**（distinct 1/4） | 通过 | `probe-writer.mjs` |
| N1-A22 | `GET /api/asset?path=<.webm>` → 200 `content-type: video/webm`（缺失则 404，前端降级到 poster） | 现状已支持（契约 §2.5） | 同 | `[手测]`/可选（§3.10） |
| **N1-A23** | `POST /api/card/position {path:'characters/ryo/diary.md', x, y}` → **200**，行 `layer === 'characters/ryo'` | **404 `not_found`**（`actions/canvas.ts:457` 的 `resolveLayer` 门禁）**【契约 §3.7 实测】** | 200 | `nook-routes.test.mjs` |
| N1-A24 | 工具路径同分支：直呼 `service.arrangeCards({place})` → `details.layer === 'characters/ryo'` | `not_found` | 成功 | `nook.test.mjs` |
| N1-A25 | **layout 分支**：`arrangeCards({layout:{mode:'grid', layer:'characters/ryo'}})` → 200；`{layout:{mode:'grid', paths:[两个 nook 卡]}}` → 200 | **404 `not_found`**（① `Unknown layer "characters/ryo"`；② `"characters/ryo/diary.md" is not on a canvas layer`）**【实测】** | 200 | `nook.test.mjs` |
| N1-A26 | 未加载世界时 `GET /api/nook` → `400 {error:'No active world'}` | 路由不存在 → 404 | 400 | `nook-routes.test.mjs` |
| N1-A27 | 同一批 seat 的 **z** 也必须互异：`seatUnplaced(4 张同尺寸卡)` → distinct z === 4 | **1 / 4【实测】**（全部 z=1，`/tmp/nook05-z.mjs`） | 4 | `nook.test.mjs` + 探针 |
| N1-A28 | **portrait DOM**：一张真实 `portrait` 卡 → `document.querySelectorAll('.portrait__video').length === 1` | DOM 无 `.portrait__video`（kind/组件未实现） | 1 | `nook-portrait.test.mjs`（NEW，见 §3.13） |
| N1-A29 | **回退矩阵 ①无 poster**：`video:` 指向不存在文件、无 `poster` → 出现 `.portrait__missing`，`textContent` 非空且 `offsetHeight > 0` | 无该元素 | 可见占位 | 同上 |
| N1-A30 | **回退矩阵 ②有 poster**：`video:` 404、`poster:` 指向**真实 png** → `.portrait__still > img` 的 `naturalWidth > 0`（真解码，非裂图） | 无该元素 | `naturalWidth > 0` | 同上 |
| N1-A31 | **回退矩阵 ③reduced-motion**：`matchMedia('(prefers-reduced-motion: reduce)')` 命中 → `.portrait__video` 计数 **0**，且有 `.portrait__still` | 无该元素 | 0 + still | 同上 |
| N1-A32 | **性能红线 L1/L2**：同层两张 portrait → `.portrait__video` 计数 **1**；`document.hidden`（CDP `Emulation.setPageVisibilityOverride`）→ 计数 **0** | 无该元素 | 1 / 0 | 同上（`03 §10.6` 明示 MUST 保留） |
| N1-A33 | **素材真实性**：`templates/<world>/characters/<id>/` 有 `component: portrait` 的 md 且其 `video:` 指向的文件存在；alpha 真解码后**存在透明像素**（alpha<128 的采样比例 > 0） | 全树 `*.webm` 零命中（真资产只在 gitignored 的 `assets/`） | 文件存在 + 透明像素 > 0 | §3.13 / 资产门禁 |

**五条非空性判据 = N1-A1（取数端存在且非空）+ N1-A8（占位回写通道）+ N1-A13（坐席不叠）+ N1-A23（拖卡 place）+ N1-A25（布局 layout）**（对应契约 §7 末条、§2.7 第 6 条、§3.7/§3.7.1 点名的五条）；**N1-A27 是 N1-A13 的孪生**（同一次 pass 的 z 序，`01 §⑬ 冲突 2`——**z 不参与坐席几何，实测只补 `occupied.push` 时 N1-A13 绿而 N1-A27 红，两条必须同时绿**），N1-A14 是 N1-A13 的**归因锁定对照**，N1-A24 是 N1-A23 的**工具入口对照**，**N1-A28–N1-A33 是契约 §4.1 的立绘判据**（浏览器 DOM + 素材真实性，见 §3.13）。


### 3.1 N1-A1 / N1-A2 / N1-A3：`GET /api/nook` 存在且非空（非空性判据 ①）

**fixture** = §2.2 的 `NOOK_FILES`（`characters/ryo/` 下 **1 份 README + 2 件真实 kind 的陈设**），写入 `fs.mkdtemp()` + `store.writeFile`，然后挂真 router。

**伪码（完整）**：

```js
// apps/server/test/nook-routes.test.mjs
test('nook §N1-A1: GET /api/nook returns the character nook, items non-empty', async () => {
  const { base, close } = await mountWorld(NOOK_FILES);           // 见 §8.2 helper
  try {
    const res  = await fetch(`${base}/nook?character=ryo`);       // ① 必须打到真路由
    const body = await res.json();
    // ② 非空性本体：修复前这里 res.status === 404（Cannot GET /api/nook，
    //    且响应体是 HTML 而不是 JSON，res.json() 会先抛），该断言立刻红；
    //    修复后 200 且 items 里真的有两件陈设。
    assert.equal(res.status, 200, `expected 200, got ${res.status}`);
    assert.ok(Array.isArray(body.items), 'items must be an array');
    assert.ok(body.items.length >= 1, `items must be NON-EMPTY, got ${body.items.length}`);

    // ③ 空性之外：形状与身份（N1-A2/N1-A3）
    assert.equal(body.layer, 'characters/ryo', 'layer carries the nook id');
    assert.deepEqual(Object.keys(body).sort(),
      ['audio','bg','items','layer','links','presence','scene','worldFrozen'].sort(),
      'shaped exactly like LayerState');
    assert.equal(body.scene?.path, 'characters/ryo/README.md', 'scene is the character README');
    assert.deepEqual(body.links, [], 'links empty this batch');
    assert.deepEqual(body.presence, [], 'presence empty this batch');

    // ④ 陈设**内容**也对了（防「非空但错」：items 混进 README 自身/子目录卡/world 的卡）
    const paths = body.items.map((i) => i.path).sort();
    assert.deepEqual(paths, ['characters/ryo/diary.md', 'characters/ryo/photo.md'],
      'items = the direct-child md only (no README, no letters/unsent.md)');
    for (const it of body.items) {
      assert.equal(it.kind, it.path.endsWith('diary.md') ? 'note' : 'letter');
      assert.ok(Number.isFinite(it.x) && Number.isFinite(it.y), 'seated (x/y present)');
      assert.ok(it.w > 0 && it.h > 0, 'real footprint present');
    }
  } finally { await close(); }
});
```

**修复前的实测观测（2026-09-13，本机；脚本 `/tmp/nook05-route.mjs`，真 `createWorldRouter(dist)` + 真 `LocalWorldStore` + express）**：

```
$ node /tmp/nook05-route.mjs
/nook?character=ryo         -> 404 <!DOCTYPE html><html lang="en">…Cannot GET /api/nook…
/nook?character=ryo/../..   -> 404 <!DOCTYPE html>…Cannot GET /api/nook…
/layer?layer=characters/ryo -> 200 {"layer":"characters/ryo","scene":{"path":"characters/ryo/README.md",…}
/card/footprint nook        -> 404 {"ok":false,"code":"not_found","error":"layer must be an existing layer id"}
```

> 第 3 行是**关键的对照**：`/api/layer` 对 `characters/ryo` **返回 200 且能列出 items**（因为 `pageOfLayer` 走的是 `listFiles`，不吃 `resolveLayer` 门禁），而 `/api/nook` 根本不存在——这正是契约 §1「小天地只存在于后端文件层，没有任何取数路径」的物证。
**契约 §5.2 冻结**：`isValidCharacterId(id: string): boolean` 是**唯一**正则实现处，落点 **`packages/shared/src/rules/characters.ts`（NEW，`index.ts` 已 export）**——可从 `@airp/shared` 或 `../dist/index.js` import。**不是** `apps/server` 的文件（`arrangeCards` 在 shared 里也要调它，shared 不能 import server）。三个函数与其签名见 §4.2；`rules/` 的既有先例是 `rules/dice.ts` / `rules/interactive.ts`。
**为什么修复前必红**：路由表里没有 `/nook`，Express 的默认 404 处理返回 **HTML**（不是 JSON），`res.json()` 直接抛解析错——测试在 `assert.equal(res.status, 200)` 处就已经失败，红得干净。

**漏了会怎样**：若只断言「`/api/nook` 返回 200」而不断言 `items` 非空，实现者可以返回一个空数组 + 200 交卷，「走进小天地看见陈设」的第一眼**依然不成立**——这正是契约 §7 强调**非空性**而非「端点存在」的原因。

### 3.2 N1-A4 / N1-A5 / N1-A6：id 校验与存在性（校验闸的唯一实现处）

**契约 §5.2 冻结**：`isValidCharacterId` 是**唯一**实现处，落点 **`packages/shared/src/rules/characters.ts`**（已落地，`index.ts:15` 已 export；测试从 `@airp/shared` 或 `../dist/index.js` import）。实测：`isValidCharacterId('ryo')===true` / `('Ryo')===false`；`characterIdOfPath('characters/ryo/desk.md')==='ryo'`、`('characters/ryo/../../secret')==='ryo'`（穿越被 id 正则挡在取第一段）、`('world/x.md')===null`。

```js
test('nook §N1-A4–N1-A6: id gate rejects non-kebab, traversal, and missing dirs', async () => {
  const { base, close } = await mountWorld(NOOK_FILES);
  try {
    const bad  = await fetch(`${base}/nook?character=${encodeURIComponent('Ryō')}`);
    assert.equal(bad.status, 400);
    assert.equal((await bad.json()).code, 'invalid_argument');

    // 路径穿越 MUST 死在校验闸（正则），不是靠 normalize 兜底（契约 §3.2 末条）
    const trav = await fetch(`${base}/nook?character=${encodeURIComponent('ryo/../..')}`);
    assert.equal(trav.status, 400);
    assert.equal((await trav.json()).code, 'invalid_argument');

    // 合法 id 但目录不存在 → 404（语义上「没有」，不是「非法」）
    const miss = await fetch(`${base}/nook?character=nosuch`);
    assert.equal(miss.status, 404);
    assert.equal((await miss.json()).code, 'not_found');
  } finally { await close(); }
});
```

**修复前**：三条全是 404 + HTML（路由不存在），故 N1-A4/N1-A5 必红（400 vs 404），N1-A6 仅凭 status 会**假绿**——所以 N1-A6 MUST 断言 `code === 'not_found'`（修复前的 HTML 体没有 `code`，`await res.json()` 抛错 → 实际也红）。
**漏了会怎样**：N1-A5 漏了，实现者可能用 `path.normalize` 兜底而非正则闸；`characters/ryo/../../etc` 会先被 normalize 成 `characters/../..`，仍可能拼出一个越界路径——契约 §3.2 明确禁止这条实现路径。

### 3.3 N1-A7：纯读不落账

```js
test('nook §N1-A7: GET /api/nook is a pure read (no events)', async () => {
  const { base, store, close } = await mountWorld(NOOK_FILES);
  try {
    const before = (await store.getEventsSince(0)).length;
    await fetch(`${base}/nook?character=ryo`);     // 触发完整的坐席路径
    const after = (await store.getEventsSince(0)).length;
    assert.equal(after, before, 'a derived read must not append to history.db');
  } finally { await close(); }
});
```

> **本条不构成非空性判据**（修复前无路由 → 前后都是 0，vacuous 相等）。它是**回归护栏**：契约 §3.6 冻结「`GET /api/nook` 不落事件」。
> **注意**：`/api/nook` 内部会触发 `seatUnplaced`/`reseatLayer`（契约 §3.3），而这两个方法**只写 `canvas.db`**——本条同时守住 footprint §3.6「画布状态层不落事件」。

### 3.4 N1-A8 / N1-A11 / N1-A12：`POST /api/card/footprint` 的 nook 分支（非空性判据 ②）

**修复前的实测观测（2026-09-13，本机，真 router）**：

```
$ node /tmp/nook05-route.mjs
/card/footprint nook -> 404 {"ok":false,"code":"not_found","error":"layer must be an existing layer id"}
```

根因可指到行：`apps/server/src/routes/world.ts:648` 的 `if ((await store.resolveLayer(dirOfLayer(layer))) !== layer)`——而 `resolveLayer` 对 `characters/**` **恒返回 null**（`local-store.ts:550-556`，逐字注释「player/**, characters/<id>/**, world.json are not layers」）。契约 §3.1 已据此否决 C 方案。

**伪码（完整）**：

```js
test('nook §N1-A8: footprint writes land for a nook layer (the write channel exists)', async () => {
  const { base, store, close } = await mountWorld(NOOK_FILES);
  try {
    // 先让 /api/nook 给这两件陈设建行（真实时序：先读一次，再回写）
    await fetch(`${base}/nook?character=ryo`);

    const res = await fetch(`${base}/card/footprint`, {
      method: 'POST',
      headers: { 'content-type': 'application/json' },
      body: JSON.stringify({ layer: 'characters/ryo', boxes: [{ path: 'characters/ryo/diary.md', w: 320, h: 480 }] }),
    });
    const body = await res.json();

    // 非空性本体：修复前 res.status === 404（见上方实测输出）
    assert.equal(res.status, 200, `expected 200, got ${res.status}: ${JSON.stringify(body)}`);
    assert.equal(body.ok, true);
    assert.equal(body.updated, 1, 'the nook card row must actually be written');
    // N1-A11: 行落在这个 nook 的 layer 列上（契约 §3.3 冻结 —— seatUnplaced 用 WHERE layer = ? 算 z_index，local-store.ts:595）
    const row = store.getLayerCards(['characters/ryo/diary.md'])[0];
    assert.ok(row, 'row must exist after the write');
    assert.equal(row.layer, 'characters/ryo', 'nook rows carry the full nookId in cards.layer');
    assert.equal(row.w, 320); assert.equal(row.h, 480);

    // N1-A12: footprint 只动 w/h + measuredAt，不动 seatW/seatH（footprint 契约 §5.2 反模式 9）
    // ⚠️ CardRecord 无嵌套 `metadata` —— 字段是展平的顶层属性（world-store.ts:13-44 实测）
    assert.ok(row.measuredAt, 'measuredAt is stamped on the record');
    assert.equal(row.seatH, CARD_FORMS.note.h, 'seatH is the last SEAT height, untouched by footprint');
    assert.equal(row.seatW, CARD_FORMS.note.w, 'seatW likewise untouched');
  } finally { await close(); }
});
```

**漏了会怎样**：N1-A8 漏了，N1 会「读得出来、写不回去」——前端测到的真实占位永远 404（`console.warn` + 本地兜底），`cards.width/height` 永远是 declared；重内容卡片（chalk / 长 component）在小天地里**跨会话永久重叠**，正是 `AGENTS §7.5` 第 7 条想根治的那条腿。

### 3.5 N1-A9 / N1-A10：nook 分支的 400/404 语义

```js
test('nook §N1-A9–N1-A10: the nook branch distinguishes bad id (400) from missing dir (404)', async () => {
  const { base, close } = await mountWorld(NOOK_FILES);
  const post = (layer) => fetch(`${base}/card/footprint`, { method: 'POST',
    headers: { 'content-type': 'application/json' },
    body: JSON.stringify({ layer, boxes: [{ path: 'characters/ryo/diary.md', w: 200, h: 168 }] }) });
  try {
    const bad = await post('characters/Ryo');           // 非法 id → 400
    assert.equal(bad.status, 400);
    assert.equal((await bad.json()).code, 'invalid_argument');

    const miss = await post('characters/nosuch');       // 目录不存在 → 404
    assert.equal(miss.status, 404);
    assert.equal((await miss.json()).code, 'not_found');
  } finally { await close(); }
});
```

**MUST NOT 用 N1-A10 当「修复前会失败」的判据**：修复前该请求**也是 404 `not_found`**（实测），只是**语义完全不同**（先在 `:648` 的层门禁被拒）。这正是**非空性必须锚在 200 上**的原因——只看 404/400 的字面值会得到一条「修复前后都绿」的假判据。**N1-A9** 才是 400/404 分流的那条：修复前 404 → 修复后 400。

### 3.6 N1-A17a / N1-A17b：两档空态（**不是**同一条）

契约 §3.1「`scene` 缺 README 时返回 `null`，不合成 stub」＋ `doc-11 §4.1`「角色根目录为空 = 只含 `preset.json`」。**实测两档语义不同**（ReviewC C5：`isNookEmpty(readme+preset) === false`，因 README 不是 `.json`）：

| 档 | fixture | 语义 | 断言 |
|---|---|---|---|
| **N1-A17a** | `characters/sumi/{README.md, preset.json}` | **非空**（有 README）→ **不触发初始化**；`items` 空（README 不进 items） | 200、`items === []`、`scene.path === 'characters/sumi/README.md'`、且 `isNookEmpty(directChildrenOf(files,'characters/sumi')) === false` |
| **N1-A17b** | `characters/old-sailor/{preset.json}` | **真空** → 触发初始化（`04` 的判定输入） | 200、`items === []`、`scene === null`（不合成 stub）、且 `isNookEmpty(...) === true` |

```js
// packages/shared/test/nook.test.mjs + apps/server/test/nook-routes.test.mjs
test('nook §N1-A17a/b: two kinds of empty are distinguished (init trigger input)', async () => {
  // ① 纯函数层（不依赖路由）——这是 04 的初始化触发输入
  const FILES = ['characters/sumi/README.md', 'characters/sumi/preset.json',
                 'characters/old-sailor/preset.json'];
  assert.equal(isNookEmpty(FILES, 'characters/sumi'), false, 'a README is content -> init NOT triggered');
  assert.equal(isNookEmpty(FILES, 'characters/old-sailor'), true, 'preset.json only -> init triggered');

  // ② 路由层：两种都 200 且 items 空；差别在 scene
  const { base, close } = await mountWorld(NOOK_FILES);
  try {
    const sumi = await (await fetch(`${base}/nook?character=sumi`)).json();
    assert.deepEqual(sumi.items, [], 'preset.json is not content (doc-11 §4.1)');
    assert.equal(sumi.scene?.path, 'characters/sumi/README.md');

    const sailor = await (await fetch(`${base}/nook?character=old-sailor`)).json();
    assert.deepEqual(sailor.items, []);
    assert.equal(sailor.scene, null, 'no README -> scene null, never a synthesised stub (contract §3.1)');
  } finally { await close(); }
});
```

> **本条不是非空性判据**（修复前 404），但它锁死「初始化触发点的判定输入」——`04-占位与回写.md` 识别出的「空目录」判定，其可观测面正是 `isNookEmpty`（`rules/emptiness.ts`，签名为 `(files, dir)` **纯函数，取 `store.listFiles` 输出**，不是 `(store, id)`）。`preset.json` **不算内容**（`doc-11 §4.1` 逐字）；`README.md` **算内容**（实测）。

### 3.7 N1-A13 / N1-A14：BLOCKER-N1 坐席唯一性（非空性判据 ③，本节是全篇最重要的断言）

**背景（契约 §2.7，主 agent 2026-09-13 实测发现）**：`seatUnplaced` 建 `occupied` 时只放**已存在的行**（`local-store.ts:599-603`），循环里 INSERT 之后**从不 push**（`:639-653` 的循环体里没有 `occupied.push`），尽管 `:598` 的注释写着 "Occupied bounds = existing rows in this batch ∪ seats already assigned here"。平行的 `reseatLayer` **有** push，且在**两处**（`:807` legacy 分支 / `:826` measured 分支）——**对照组成立，这是遗漏不是设计**。

**契约 §2.7 第 6 条边界（用户 2026-09-13 拍板，本文遵守，不扩大）**：

| 函数 | 位置 | 是否中招 | 依据 |
|---|---|---|---|
| `seatUnplaced` | `local-store.ts:580` | ✅ **中招** | 批量循环内不 push `occupied`（`:599` 与 `:639-653`） |
| `seatNear` | `local-store.ts:1157` | ❌ 不中招 | 一次只排一张卡，每次调用重新读全层占用集 |
| `reseatLayer` | `local-store.ts:690` | ❌ 不中招 | `:807`/`:826` 两处都有 push |

**修复范围因此被钉死**：只改 `seatUnplaced` 一处（`01-取数端点` 的实现范围），**MUST NOT** 顺手改另两处。验收断言只针对 `seatUnplaced`：

```ts
// local-store.ts，seatUnplaced 循环内，INSERT（:639-653）之后补一行，与 reseatLayer:807/:826 逐字同形
occupied.push({ cx: placedX + w / 2, cy: placedY + h / 2, w, h });
```

**本文作者独立复现（2026-09-13，本机 `packages/shared/dist` + 真 SQLite）**：

```
# ① 真实内容：把 holmes 真实三卡拷进 characters/ryo/ 后同一批 seat（真实 kind，非合成目录）
$ node /tmp/nook05-real.mjs
declared: [evening.md chalk 460×190, late-night.md chalk 460×190, raindrops.md note 200×168]
 seat world/baker-street/evening.md    730 445 460 190      ← 两张同尺寸 chalk 坐标逐字相同
 seat world/baker-street/late-night.md 730 445 460 190
 seat world/baker-street/raindrops.md  860 456 200 168
distinct centres: 1 / 3                                    ← 三卡中心全落在 SEAT_ANCHOR (960,540)

# ② 同尺寸批量（最诚实的信号）：4 张 note(200×168) 一次 seat
$ node /tmp/nook05-seat2.mjs
characters/ryo/{diary,letter,photo,ticket}.md → 全部 860 456
distinct centres (same-size batch): 1 / 4
```

> **两卡坐标逐字相同（`730,445`）** 与主 agent 广播里的「3 张卡全部 x=820 y=450」是同一现象、不同 fixture（他的 declared 各异，spiral first-fit 落点不同）——**结论一致：distinct positions === 1**。

**判据（冻结，写进测试）**：**distinct 中心集合的大小 === 输入卡数**。

- 用**中心**（`x + w/2, y + h/2`）而非 `(x, y)`：两颗不同尺寸的卡落在**同一格**时有相同的中心、不同的左上角——按 `(x,y)` 去重会**漏报**（`NookRoute01` 2026-09-13 观测到按 `(x,y)` 是 2/3、按中心是 1/3，差异即此）。
- 用**集合大小**而非「两两不等」：同尺寸卡在同一格中心、只 w/h 不同时坐标本就可能相同，逐对比较会写出脆弱断言。

```js
// packages/shared/test/nook.test.mjs
test('nook §N1-A13: a same-batch seat assigns N DISTINCT positions (BLOCKER-N1)', async () => {
  const { store, cleanup } = await tempStore('airp-nook-seat-');   // 见 §8.2 helper
  try {
    for (const f of SAME_SIZE_NOOK_FILES) {
      await store.writeFile(f.path, '---\ntype: note\ntitle: x\n---\n\nbody');
    }

    const seats = await store.seatUnplaced('characters/ryo', SAME_SIZE_NOOK_FILES);

    // 非空性本体：修复前 distinct centres === 1（全部叠在 SEAT_ANCHOR），此处红
    const centres = new Set(seats.map((s) => `${s.x + s.w / 2},${s.y + s.h / 2}`));
    assert.equal(centres.size, SAME_SIZE_NOOK_FILES.length,
      `every unplaced card needs its own seat; got ${centres.size}/${SAME_SIZE_NOOK_FILES.length}`);

    // 落库的行也必须唯一（防「返回对了、INSERT 错了」）
    const rows = store.getLayerCards(SAME_SIZE_NOOK_FILES.map((f) => f.path));
    const rowCentres = new Set(rows.map((r) => `${r.x + r.w / 2},${r.y + r.h / 2}`));
    assert.equal(rowCentres.size, rows.length, 'persisted rows must be distinct too');

    // 且互不重叠（含 SEAT_PAD，与 local-store.ts:1574 overlapsOccupied 同口径）
    assert.deepEqual(overlappingPairs(rows, SEAT_PAD), [], 'seated boxes must not overlap');
  } finally { await cleanup(); }
});

// —— 归因锁定（对照实验）：同一个 bug 在普通层上也能复现 ——
test('nook §N1-A14: the same batch seats distinctly in a NORMAL layer (attribution lock)', async () => {
  const { store, cleanup } = await tempStore('airp-nook-seat-normal-');
  try {
    // 真实 kind + 真实声明尺寸（与上述 ① 同源）
    const REAL = [
      { path: 'world/baker-street/evening.md',    kind: 'chalk', w: CARD_FORMS.chalk.w, h: CARD_FORMS.chalk.h },
      { path: 'world/baker-street/late-night.md', kind: 'chalk', w: CARD_FORMS.chalk.w, h: CARD_FORMS.chalk.h },
      { path: 'world/baker-street/raindrops.md',  kind: 'note',  w: CARD_FORMS.note.w,  h: CARD_FORMS.note.h  },
    ];
    for (const f of REAL) await store.writeFile(f.path, `---\ntype: ${f.kind}\ntitle: x\n---\n\nbody`);
    const seats = await store.seatUnplaced('world/baker-street', REAL);
    const centres = new Set(seats.map((s) => `${s.x + s.w / 2},${s.y + s.h / 2}`));
    assert.equal(centres.size, REAL.length, 'the bug is in seatUnplaced, not in the nook route');
  } finally { await cleanup(); }
});
```

**为什么 N1-A14 必须有**：它把「红」钉在 `seatUnplaced` 上。没有它，实现者可能误以为「小天地路由写错了」而去改 `/api/nook`，座位依旧全叠。N1-A14 与 N1-A13 **同红同绿**——任何一次只让其中一条变绿的「修复」都是错的。

**漏了会怎样（写进测试注释）**：任何「同一批插入多张无行卡」的路径都叠成一个点——`/api/layer` 首次打开一个多 md 的层（`routes/world.ts:409-417`）、`move` 批量落卡、**以及本批的 `/api/nook` 首次进入小天地**（角色目录 2–4 件陈设必然叠）。**N1 的判据「玩家走进小天地看见那里的陈设」第一眼就不成立**——这不是视觉小瑕疵，是功能不成立。用户已拍板**随 N1 一起修**（契约 §2.7 第 6 条），不是「登记为后续」。

**同一函数的第二处缺陷：`nextZ++` 也不在循环里（`01 §⑬ 冲突 2`，本文独立复现）**。契约 §2.7 与主 agent 广播只提 `occupied.push`；实测 `nextZ` 同样只算一次（`local-store.ts:596` 算，`:643-654` 的 INSERT 只读它、循环内无 `nextZ++`）。

```
$ node /tmp/nook05-z.mjs        # 3 张同尺寸 note 一次 seat
z per seat:    1,1,1
distinct z:    1 / 3
persisted z:   1,1,1
```

**后果**：只补 `occupied.push` 会让座位分散、**但 z 序全等**——`AGENTS §7.5` 第 6 条的叠放顺序由 `z_index` 决定，同 z 让「谁盖谁」变成行序偶然。故本条断言是 N1-A13 的**孪生**，MUST 一起绿：

```js
// packages/shared/test/nook.test.mjs —— N1-A27（与 N1-A13 同一 fixture、同一 pass）
test('nook §N1-A27: a same-batch seat assigns strictly increasing z (BLOCKER-N1, second half)', async () => {
  const { store, cleanup } = await tempStore('airp-nook-seat-z-');
  try {
    for (const f of SAME_SIZE_NOOK_FILES) {
      await store.writeFile(f.path, '---\ntype: note\ntitle: x\n---\n\nbody');
    }
    const seats = await store.seatUnplaced('characters/ryo', SAME_SIZE_NOOK_FILES);
    // 修复前：全部 z === 1（distinct z 1/4）
    assert.equal(new Set(seats.map((s) => s.z)).size, SAME_SIZE_NOOK_FILES.length,
      'each new seat needs its own z_index (the stacking order is z)');
    const rows = store.getLayerCards(SAME_SIZE_NOOK_FILES.map((f) => f.path));
    assert.equal(new Set(rows.map((r) => r.z)).size, rows.length, 'persisted z must be distinct too');
  } finally { await cleanup(); }
});
```

> **修复指向（`01-取数端点` 范围，05 只登记）**：`local-store.ts:643-654` 的 INSERT 之后补 `nextZ++`（与 `reseatLayer:803` 的 `nextZ++` 同形）。**两行都必须补**（`occupied.push` + `nextZ++`），少一行则 N1-A13/N1-A27 只绿一半。
> **回写登记**：这半条契约 §2.7 未写；`01 §⑬ 冲突 2` 已请主 agent 在验收阶段回写进 nook 00 §2.7（本文 W14 同步登记）。

### 3.8 N1-A21：探针与测试同口径

探针段（§8.1）复刻 N1-A13 的时序与判定：**`distinct centres === N`**，用 `throw` 体例（`probe-writer.mjs` 没有 `check()`；`check()` 是 `probe-tools.mjs:74-78` 的体例）。修复前 `pnpm probe` 在 `[Probe 1.6]` 段**失败**。

### 3.9 N1-A15 / N1-A16：`portrait` kind 的六处同步（计数断言归本篇登记）

契约 §5.3 的六处（2026-09-13 实测复核后由 5 处修正为 6 处）：

| # | 文件 | 改什么 | 05 的断言 |
|---|---|---|---|
| 1 | `packages/shared/src/schemas/components.ts:12-37` | `COMPONENT_KINDS` 加 `'portrait'` | N1-A15（计数） |
| 2 | `packages/shared/src/schemas/components.ts:218-` | 加 `PortraitKindSchema` + `COMPONENT_SCHEMAS` 映射 | N1-A16（加载不 throw） |
| 3 | `packages/shared/src/schemas/forms.ts:64-` | `CARD_FORMS` 加 `portrait` 行 | **N1-A16 的直接靶子**（漏了 `registry.ts:31/42` throw） |
| 4 | `packages/shared/src/components/packs/room.ts` | 加 `ComponentDef`（`accepts` 与 `channels.useItemTarget` 必须同真同假，否则 `registry.ts:48` throw） | N1-A16 |
| 5 | `packages/shared/src/components/registry.ts:14-21` | **无需手改**（`ALL` 从各 pack 展开，`ROOM_PACK` 已在 `:9` 导入）—— 核对点 | — |
| 6 | `packages/shared/test/components.test.mjs` | **`18` → `19`，共 5 处**：`:35`（`assert.equal(kinds.length,18)`）、`:37`（错误信息 `18 docked kinds`）、`:96`（`'18 registered'`）、`:180`（`components.length`）、`:192`（`/18 registered kinds/`） | N1-A15 本体 |

**N1-A15 的机械核验（5 处逐条定位，2026-09-13 实测）**：

```
$ grep -n "18" packages/shared/test/components.test.mjs
35:test('T2: the registry holds 18 kinds, each with a CARD_FORMS row and a full entry', () => {
37:  assert.equal(kinds.length, 18, 'doc 10 §14.2: 18 docked kinds');
96:  assert.ok(text.includes('18 registered'), 'index reports the count');
180:  assert.equal(index.details.components.length, 18);
192:    assert.match(err.message, /18 registered kinds/);
```

**修复前必红的具体路径（两种实现方式，两条都红）**：

- **只加 kind、不改测试** → `:37` `assert.equal(19, 18)` 失败（T2 红），`:180` 失败（T10 红），`:96`/`:192` 的子串/正则断言也失败。
- **只改测试、漏加 `CARD_FORMS` 行** → `import` `COMPONENT_REGISTRY` 时**模块加载期 throw**（`registry.ts:31/42`，契约 §5.3 实测），**整个 `nook.test.mjs` / `components.test.mjs` 直接报错**（不是 fail 而是 error）。

```js
// packages/shared/test/nook.test.mjs
test('nook §N1-A15/§N1-A16: portrait is a first-class kind (registry loads, form exists, count is 19)', () => {
  // N1-A16: import 成功本身就是断言（漏 CARD_FORMS 行会在这里模块加载期 throw）
  assert.ok(COMPONENT_KINDS.includes('portrait'), 'kind is registered');
  assert.equal(COMPONENT_KINDS.length, 19, 'eighteen docked kinds + portrait');
  assert.ok(CARD_FORMS.portrait, 'portrait has a CARD_FORMS row (the ONLY size source)');
  assert.equal(CARD_FORMS.portrait.chrome, 'bare', 'declared bare (contract §3.5)');
  assert.ok(COMPONENT_REGISTRY.portrait, 'registry entry present');
  assert.equal(COMPONENT_REGISTRY.portrait.pack, 'room', 'lives in the room pack');
  // 同对象（无第二张尺寸表，继承 components.test.mjs T4 的口径）
  assert.deepEqual(componentDocOf('portrait').form, CARD_FORMS.portrait);
});
```

> **尺寸值不锁死在本文**：契约 §3.5 现冻结 `CARD_FORMS.portrait = { label: 'Portrait', w: 360, h: 480, chrome: 'bare' }`（3:4 = 现有立绘 1080×1440 的比例；「宽取 360」仍留 03 篇按周围卡宽度量级复核）。上面的断言**只锁 `chrome: 'bare'` 与「存在」**，`w/h` 若被 03 复核后调整，本条**不红**（`deepEqual` 比的是同一对象，不是数值字面量）。

### 3.10 N1-A22：`portrait` 视频 URL 与 `/api/asset`（路由级，轻量断言）

- 契约 §5.4：frontmatter 的 `video:`/`poster:` 是**相世界根相对路径**，前端拼 `/api/asset?path=<encodeURIComponent(path)>`。
- `/api/asset` 走 `res.sendFile`（`routes/world.ts:879`），`mime-types` 实测已能给 `video/webm`（契约 §2.5）。
- **路由级断言**（不需要 ffmpeg，往 fixture 放一个占位 `.webm`/`.png` 即可）：`/api/asset?path=assets/characters/ryo/ryo.webm` → 200 且 `content-type` 以 `video/` 开头；缺失时 → **404**（前端降级到 poster，不崩）。
- **素材真实性（资产层）**：契约 §4.1 裁决**素材生产是本批交付物**（至少 1 个角色的 portrait 资产真落进 `templates/<world>/assets/characters/<id>/`，由 `tools/motion-clip.mjs` 从绿幕产出 `alpha_mode=1` webm）。其可机械核验判据见 **N1-A33**（§3.13），**MUST NOT** 用 `ffprobe` 的 `alpha_mode` tag 当「透明度正确」的证明（`assets/skills/motion-portrait/SKILL.md` 逐字警告）。

### 3.11 N1-A23 / N1-A24：拖卡通路 —— `arrangeCards` 的 nook 分支（契约 §3.7，非空性判据 ④）

**背景**：主 agent 曾错误广播「`/api/card/position` 无 layer 门禁 → 小天地拖卡天然可用」，**已撤回**（契约 §3.7 留痕）。事实是**动作层内部**有门禁：`packages/shared/src/actions/canvas.ts:456-464`（`hasPlace` 分支）：

```ts
await assertCardExists(store, place.path);
const layer = await store.resolveLayer(place.path);
if (layer === null) {
  fail('not_found', `"${place.path}" is not on a canvas layer; arrange can only place cards shown on a layer page.`);
}
```

`resolveLayer`（`local-store.ts:550-556`）对 `characters/**` **逐字返回 null** → nook 路径拖卡**必 404**。表现：`card_position` 帧先广播、HTTP 随后 404、前端乐观更新回滚——**「拖了又弹回去」**。

**契约 §3.7 冻结的实现要点**：(a) 在 `arrangeCards` 加 nook 分支（否决路由绕开/新端点）；判据用 §5.2 的唯一 `isValidCharacterId(id)`；`resolveLayer` 对 `characters/**` 的返回语义**不动**（nook 是并列分支）；nookId 喂给 `seatDeclaredRows`/`placeCard`（实测 `placeCard` 直接写 `cards.layer` 不反查）。`arrangeCards` 是玩家 UI（`routes/world.ts:610`）与 Agent 工具（`extensions/toolkit/arrange.ts:86`）的**唯一共同入口**，故这条验收**同时覆盖 HTTP 与工具两条路径**。

```js
test('nook §N1-A23: arrangeCards({place}) works for a nook path (drag-to-place channel)', async () => {
  const { base, store, close } = await mountWorld(NOOK_FILES);
  try {
    await fetch(`${base}/nook?character=ryo`);                       // 建行
    const res = await fetch(`${base}/card/position`, {
      method: 'POST', headers: { 'content-type': 'application/json' },
      body: JSON.stringify({ path: 'characters/ryo/diary.md', x: 123, y: 456 }),
    });
    const body = await res.json();

    // 非空性本体：修复前 404 {ok:false, code:'not_found'}（契约 §3.7 实测：resolveLayer 返回 null）
    assert.equal(res.status, 200, `expected 200, got ${res.status}: ${JSON.stringify(body)}`);
    assert.equal(body.ok, true);
    assert.equal(body.layer, 'characters/ryo', 'details carry the nook id');

    const row = store.getLayerCards(['characters/ryo/diary.md'])[0];
    assert.equal(row.x, 123); assert.equal(row.y, 456);
    assert.equal(row.layer, 'characters/ryo', 'placeCard writes cards.layer directly (no re-lookup)');
  } finally { await close(); }
});

// N1-A24: 工具路径与 HTTP 路径共用同一个 arrangeCards → 同一分支必须对工具也成立
test('nook §N1-A24: the SAME branch serves the agent tool path (one entry point, two callers)', async () => {
  const { store, cleanup } = await tempStore('airp-nook-arrange-tool-');
  try {
    // ⚠️ fixture 必须**真的落盘这张卡**：否则先撞 assertCardExists（canvas.ts:457）抛
    //    "Card not found"，nook 分支根本走不到 → 断言"通过"是假的（ReviewC C2）。
    await store.writeFile('characters/ryo/diary.md', '---\ntype: note\ntitle: x\n---\n\nbody');

    const service = createActionService(store, { type: 'writer' });   // 复刻 extensions/toolkit/arrange.ts:86 的调用面
    const r = await service.arrangeCards({ place: { path: 'characters/ryo/diary.md', x: 10, y: 20 } });
    assert.equal(r.details.layer, 'characters/ryo');
    const row = store.getLayerCards(['characters/ryo/diary.md'])[0];
    assert.equal(row.layer, 'characters/ryo');
  } finally { await cleanup(); }
});
```

**为什么 N1-A23 必须是独立的一条**（不能并入 N1-A8）：N1-A8 走的是 **footprint 写口**，N1-A23 走的是 **arrange 摆放口**——两者在**不同的文件、不同的门禁**（`routes/world.ts:648` 的 layer 门禁 vs `actions/canvas.ts:457` 的动作层门禁）。修了 N1-A8 不会让 N1-A23 变绿（反之亦然）；两条**必须各自有修复**。

**修复前的实测观测（2026-09-13，本机，真 router）**：

```
$ node /tmp/nook05-arrange.mjs   # 真 LocalWorldStore + createActionService，直呼 arrangeCards({place})
FAIL code= not_found status= 404
  msg= "characters/ryo/diary.md" is not on a canvas layer; arrange can only place cards shown on a layer page.
  # 根因行：packages/shared/src/actions/canvas.ts:457-464（resolveLayer 对 characters/** 恒 null）
  # 契约 §3.7 另由 NookView02 在浏览器里实测复现「拖了又弹回去」（card_position 帧先广播、HTTP 随后 404）
```

**漏了会怎样**：N1 的判据含「能在里面正常看卡/拖卡」（契约 §4 末条）。N1-A23 漏了，玩家拖不动陈设——**N1 只完成一半**。

### 3.12 N1-A25：布局通路 —— `arrangeCards` 的 **layout** 分支（契约 §3.7.1，非空性判据 ⑤）

**背景（契约 §3.7.1，主 agent 2026-09-13 实测补记）**：`arrangeCards` 有**两条互斥分支**，**两条都对 nook 死**，必须一起修——只修 place 会让「整理小天地布局」仍然失败。layout 分支（`packages/shared/src/actions/canvas.ts:482-533`）有**三个卡点**：

| 卡点 | 行 | 机制 | 修复前报错 |
|---|---|---|---|
| ① | `:490-495`（fail 在 `:493`） | 显式 `layer` 走 `(await store.getManifest()).layers` 白名单 | `Unknown layer "characters/ryo"` |
| ② | `:508-520`（`resolveLayer` 在 `:512`，fail 在 `:513-518`） | 不给 `layer` 时 `resolveLayer(paths[0]) === null` | `"…diary.md" is not on a canvas layer` |
| ③ | `:530-532` | 逐 path 复核 `(await store.resolveLayer(p)) !== layer` | `"…" is not on layer "…"` |

**契约 §3.7.1 冻结的同型修法**：nookId 作为**合法 layer 通过白名单**（①）；逐 path 复核**改判「path 是否属于该 nookId」**（②③），判据复用 §5.2 的 `characterIdOfPath`/`isValidCharacterId`（落点见 §4.2，`01 §2.2/§2.3` 给全签名）。

**本文作者独立复现（2026-09-13，真 `createActionService`）**：

```
$ node /tmp/nook05-layout.mjs
FAIL {"layout":{"mode":"grid","layer":"characters/ryo"}}                     code= not_found status= 404
  msg= Unknown layer "characters/ryo".
FAIL {"layout":{"mode":"grid","paths":["characters/ryo/diary.md","characters/ryo/letter.md"]}}
  code= not_found status= 404
  msg= "characters/ryo/diary.md" is not on a canvas layer; arrange can only lay out cards shown on a layer page.
```

> 复现脚本里第二张卡名作 `letter.md`；与 §2.2 规范 fixture 的 `photo.md` **同形**（同一形状，只是文件名）——故断言里出现 `letter` kind。

```js
// packages/shared/test/nook.test.mjs
test('nook §N1-A25: arrangeCards layout mode works for nook paths (both card points)', async () => {
  const { store, cleanup } = await tempStore('airp-nook-layout-');
  try {
    for (const n of ['diary', 'letter']) {
      await store.writeFile(`characters/ryo/${n}.md`, '---\ntype: note\ntitle: x\n---\n\nbody');
    }
    const service = createActionService(store, { type: 'writer' });
    const PATHS = ['characters/ryo/diary.md', 'characters/ryo/letter.md'];

    // 卡点 ①：显式 nook layer 必须穿过 manifest.layers 白名单
    const byLayer = await service.arrangeCards({ layout: { mode: 'grid', layer: 'characters/ryo' } });
    assert.equal(byLayer.details.layer, 'characters/ryo');

    // 卡点 ②③：不给 layer、逐 path 复核「是否属于该 nookId」
    const byPaths = await service.arrangeCards({ layout: { mode: 'grid', paths: PATHS } });
    assert.equal(byPaths.details.layer, 'characters/ryo');
    assert.equal(byPaths.details.cards.length, 2, 'both nook cards got laid out');

    // 复核不是「改松 resolveLayer」，也**不是**「接纳任意 characters/ 路径」：
    // 跨 nook 的 path MUST 仍被拒（已落地的 characterIdOfPath('characters/ryo/letters/unsent.md') === 'ryo'，
    // 子目录判定**不适用**——契约 §5.2 的有意设计：按 id 归属，不按目录层级）。
    await store.writeFile('characters/other/diary.md', '---\ntype: note\ntitle: x\n---\n\nbody');
    await assert.rejects(
      () => service.arrangeCards({ layout: { mode: 'grid', layer: 'characters/ryo', paths: ['characters/other/diary.md'] } }),
      (err) => { assert.equal(err.code, 'not_found'); return true; },
      'a card belonging to ANOTHER nook must still be rejected (the guard is ownership, not looseness)'
    );
  } finally { await cleanup(); }
});
```

**为什么 N1-A25 必须独立于 N1-A23**：两条分支**互斥**（`hasPlace` vs `layout`，`canvas.ts:437-440` 的 `fail('invalid_argument', …pass either…not both…)`），且**卡点不同**——place 只有一处 `resolveLayer`，layout 有**三处**（白名单 + 推断 + 逐 path 复核）。修了 place 不会让 layout 变绿：`Unknown layer`（白名单）与 `resolveLayer` 是**两条独立代码路径**。
**回归护栏（最后一段断言）**：**跨 nook 的 path MUST 仍被拒**——即修法是「加 nook 并列分支」，**不是把 `resolveLayer` 改松**（契约 §3.7 要点 2 / §3.7.1 同款）。若有人图省事让 `resolveLayer` 接纳 `characters/**`，这条断言会红（且会污染 A 方案早已否决的层语义）。
**漏了会怎样**：「整理小天地布局」这个动作（玩家 UI 的 layout 模式与 Agent 的 `arrange` 工具 layout 模式都走它）对 nook **永远失败**；而 N1 的判据含「能在里面正常看卡/拖卡」——layout 是玩家把散落陈设归拢的正常手段，漏了它，小天地里**只能一张一张拖**，无法一键整理。
> ⚠️ **不能用「子目录卡返回 null」当护栏**（本文初稿的错误，ReviewC C3 指出）：已落地的 `characterIdOfPath('characters/ryo/letters/unsent.md') === 'ryo'`（契约 §5.2 实测：按**第一段 id 归属**，不按目录层级），所以子目录卡属于**同一个 nook**、应被接纳；正确的负例是**另一个角色的** path。`nookCardPaths`（决定 `items` 显示哪些卡）与 `characterIdOfPath`（决定 path 归谁）是**两个不同问题**（`rules/characters.ts` 文件头逐字写明），不要合并。

### 3.13 N1-A28 – N1-A33：立绘判据（**契约 §4.1 新增，本批交付物**）

**背景（ReviewC 实测 + 契约 §4.1 裁决）**：N1 的判据逐字含「看见那里的陈设（**含一件会动的立绘**）」，但本文初稿把 portrait 的回退**全推给 §10.4 手测**——而手测第 8/9 条**没有任何 fixture**，构成「三处互推、无人负责」。契约 §4.1 裁决：**素材生产是本批交付物**，`05` **MUST** 有 portrait 的 DOM/回退断言（不许只写手测）。本节补齐。

**落点**：`apps/server/test/nook-portrait.test.mjs`（NEW）——**需浏览器**（复用仓库既有的 puppeteer 通道；若测试环境不启浏览器，则这些断言降级为 `03 §10.5` 的探针段，但 **MUST NOT** 消失为纯手测）。`03 §10.6` 的两条 DOM 断言（`.portrait__video` 计数 1/0）逐字保留。
**fixture（真实 webm）**：用契约 §4.1 已产出的样例 `/tmp/nook-demo/assets/characters/ryo/ryo.webm`（0.74 MB、27% 透明、256 级 alpha）——拷进测试世界的 `assets/characters/ryo/ryo.webm`，并把 `characters/ryo/portrait.md` 的 `video:` 指向它。**MUST NOT** 用空文件伪装（`<video>` 对 0 字节文件也会触发 `error`，会让 N1-A28 与 N1-A29 假绿）。

**`openNookPage(files, { reducedMotion })`（NEW，测试内 helper）**：起一个临时世界（§8.2 `mountWorld` 同款）+ 把 `files` 落盘，起浏览器加载前端，`?character=ryo` 进入 nook，返回可 `$eval/$$eval/waitForSelector` 的页面句柄；`reducedMotion: 'reduce'` 时用 CDP `Emulation.setEmulatedMedia` 注入该偏好。**不引入新生产符号**——纯测试脚手架。

```js
// apps/server/test/nook-portrait.test.mjs（NEW）—— 真实浏览器
import { chromium } from 'playwright';                    // [推断] 具体驱动以仓库既有探针为准

const PORTRAIT_MD = (video, poster) => `---
type: component
component: portrait
title: Ryo
video: ${video}
${poster ? `poster: ${poster}\n` : ''}---

She is idly turning a page.`;

test('nook §N1-A28: a real portrait card mounts exactly one <video>', async () => {
  // fixture: characters/ryo/{README.md, portrait.md(video: assets/characters/ryo/ryo.webm)} + 真实 webm
  const page = await openNookPage({ 'characters/ryo/portrait.md': PORTRAIT_MD('assets/characters/ryo/ryo.webm') });
  await page.waitForSelector('.portrait__video');           // 修复前：选择器永不出现（kind 不存在）
  assert.equal(await page.$$eval('.portrait__video', (els) => els.length), 1);
});

test('nook §N1-A29: video 404 + NO poster → a visible .portrait__missing (not a collapsed blank)', async () => {
  const page = await openNookPage({ 'characters/ryo/portrait.md': PORTRAIT_MD('assets/characters/ryo/nope.webm') });
  await page.waitForSelector('.portrait__missing');
  assert.ok((await page.$eval('.portrait__missing', (el) => el.textContent)).trim().length > 0);
  assert.ok((await page.$eval('.portrait__missing', (el) => el.offsetHeight)) > 0, '不塌陷');
});

test('nook §N1-A30: video 404 + real poster → .portrait__still img really decodes', async () => {
  const page = await openNookPage({ 'characters/ryo/portrait.md': PORTRAIT_MD('assets/characters/ryo/nope.webm', 'assets/characters/ryo/base.png') });
  await page.waitForSelector('.portrait__still img');
  // naturalWidth > 0 证明真的解码出图，不是裂图（03 §10.5 的验收口径）
  await page.waitForFunction(() => document.querySelector('.portrait__still img')?.naturalWidth > 0);
});

test('nook §N1-A31: prefers-reduced-motion → NEVER mounts <video>, shows .portrait__still', async () => {
  const page = await openNookPage({ 'characters/ryo/portrait.md': PORTRAIT_MD('assets/characters/ryo/ryo.webm', 'assets/characters/ryo/base.png') },
    { reducedMotion: 'reduce' });
  assert.equal(await page.$$eval('.portrait__video', (els) => els.length), 0, 'never mounted, not mounted-then-paused');
  assert.equal(await page.$$eval('.portrait__still', (els) => els.length), 1);
});

test('nook §N1-A32: L1 ≤1 video, L2 document.hidden unmounts it (03 §10.6, MUST keep)',
  async () => {
    const page = await openNookPage({
      'characters/ryo/a.md': PORTRAIT_MD('assets/characters/ryo/ryo.webm'),
      'characters/ryo/b.md': PORTRAIT_MD('assets/characters/ryo/ryo.webm'),
    });
    // L1：同一小天地 2 张 portrait → 至多 1 路解码（AGENTS §7.6）
    await page.waitForSelector('.portrait__video');
    assert.equal(await page.$$eval('.portrait__video', (els) => els.length), 1);
    // L2：切后台卸载
    await page.evaluate(() => { Object.defineProperty(document, 'hidden', { value: true, configurable: true }); document.dispatchEvent(new Event('visibilitychange')); });
    await page.waitForFunction(() => document.querySelectorAll('.portrait__video').length === 0);
  });
```

**N1-A33（素材真实性，资产层）**：`templates/<world>/characters/<id>/` 下存在一张 `component: portrait` 的 md，且其 `video:` 指向的文件**存在**；**且**该文件 alpha 真解码后透明像素比例 > 0：

```bash
# 修复前：文件不存在 → 非零退出（本批素材未产出时这条红）
test -f templates/<world>/assets/characters/<id>/<id>.webm
# 真解码（不是读 ffprobe 的 alpha_mode tag —— SKILL.md:27-31 逐字警告）
ffmpeg -v error -c:v libvpx-vp9 -i <id>.webm -frames:v 1 -f rawvideo -pix_fmt rgba - \
  | (统计 alpha<128 的字节比例 > 0)
```

**漏了会怎样**：N1 的判据含「含一件会动的立绘」。本节漏了，交付物可以是「走进一个只有静态卡片、没有任何会动的东西的小天地」——**判据的一半不成立**，且无人负责（ReviewC 判定「三处互推」正是这个）。
**N1-A32 是静默失效的兜网**（`03 §③-4` 逐字）：`NookView` 传给 `Canvas` 的 `still` 若因 `Canvas` 没开口而被 React 静默丢弃，L1/L3 全部失效且**没有任何测试会红**——只有 `.portrait__video` 计数断言能抓到。

---

## 4. 文件与副作用（精确到 `file:line`）

### 4.1 测试运行会触碰的东西

- **临时目录 + 真 SQLite**：每个测试用 `fs.mkdtemp(path.join(os.tmpdir(), 'airp-nook-'))` 建独立世界，**绝不碰 `templates/holmes-world`**（`probe-writer.mjs:144-146` 有「模板必须保持干净」检查）。探针段也走 tmp（`probe-writer.mjs:154-156` 的同款写法）。
- **`NOOK_FILES` 是文字 fixture**（§2.2），不是「读某个模板目录」——避免依赖模板内容漂移（`docs/footprint/05 §4.1` 同款纪律）。
- **副作用清单**（运行后应无残留）：temp 目录删除；`store.close()`；HTTP server `server.close()`（仿 `smoke-routes.mjs:218-220`）。

### 4.2 被测符号（全部真实存在，逐一 grep 核实过；`NEW` 者为修复后新增）

| 符号 | 位置 | 导出 | 用途 |
|---|---|---|---|
| `LocalWorldStore` | `packages/shared/src/store/local-store.ts` | ✅ | 全篇 |
| `SEAT_PAD`（= 22） | `local-store.ts:25` | ✅ `export const` | 重叠判定口径（§3.7） |
| `SEAT_ANCHOR`（= {x:960,y:540}） | `local-store.ts:21` | ✅ `export const` | 探针断言「全部落在锚点」的证据 |
| `seatUnplaced` | `local-store.ts:580` | ✅ | N1-A13/N1-A14 |
| `reseatLayer` | `local-store.ts:690` | ✅ | 对照组（`:807`/`:826` 有 push） |
| `seatNear` | `local-store.ts:1157` | ✅ | 边界核对（不中招，不测） |
| `writeFootprints` | `local-store.ts:855` | ✅ | N1-A8/N1-A12（经路由调用） |
| `placeCard` / `placeCards` | `local-store.ts:970` / `:979` | ✅ | N1-A23（经 `arrangeCards`） |
| `resolveLayer` | `local-store.ts:550` | ✅ | N1-A8/N1-A23 根因（对 `characters/**` 恒 null） |
| `cardKindOf` / `cardFormOf` | `packages/shared/src/schemas/forms.ts:108` / `:123` | ✅（barrel 已 re-export） | 真实内容 fixture 的 declared 值 |
| `CARD_FORMS` / `COMPONENT_KINDS` / `COMPONENT_REGISTRY` / `componentDocOf` | `schemas/forms.ts` / `schemas/components.ts` / `components/registry.ts` | ✅ | N1-A15/N1-A16 |
| `createActionService` | `packages/shared/src/actions/service.ts` | ✅ | N1-A24 |
| `createWorldRouter` | `apps/server/src/routes/world.ts:289` | ✅ | 路由测试 |
| `EventBridge` | `apps/server/src/engine/event-bridge.ts` | ✅ | 桩（smoke 同款） |
| `isValidCharacterId` / `nookIdOf` / `characterIdOfPath` / `nookCardPaths` | `packages/shared/src/rules/characters.ts`（**✅ 已落地**，契约 §5.2 冻结落点；`index.ts` 已 export） | ✅ | N1-A4/N1-A5/N1-A9/N1-A23/N1-A25 |
| `isNookEmpty` / `directChildrenOf` / `hasInitProduct` | `packages/shared/src/rules/emptiness.ts`（**✅ 已落地**，B 档 commit；签名 `(files, dir)` 取 `store.listFiles` 输出，**不是** `(store, id)`） | ✅ | N1-A17a/N1-A17b |
| `CardRecord`（顶层 `measuredAt`/`seatW`/`seatH`，**无嵌套 `metadata`**） | `packages/shared/src/store/world-store.ts:13-44` | ✅ `interface` | N1-A12（防假红的字段口径） |

> **三个纯函数的 import 路径（契约 §5.2 冻结，已落地）**：落在 **`packages/shared/src/rules/characters.ts`**（`rules/` 是「零依赖纯规则」的既有归属地，先例 `rules/dice.ts` / `rules/interactive.ts`）、**从 shared barrel 导出**；测试写 `import { isValidCharacterId, characterIdOfPath, nookCardPaths } from '../dist/index.js'`。**不是** `apps/server/dist/routes/world.js`——因为契约 §3.7/§3.7.1 让 `arrangeCards`（shared 的 `actions/canvas.ts`）也要调它，而 **shared 不能 import server**。三者是**同一处实现**，三处调用点（§3.7/§3.7.1 arrangeCards 两分支、§3.3 footprint 分支、`GET /api/nook`）MUST 共用它（契约 §6 反模式 2）。`nookCardPaths`（决定 `items` 显示哪些卡，直接子级 `.md` 去 README）与 `characterIdOfPath`（决定 path 归谁，**含子目录**）是**两个不同问题**（`rules/characters.ts` 文件头逐字写明），不要合并。


---

## 5. 落账（遵守契约 §3.6）

- **N1-A1–N1-A12、N1-A17、N1-A23、N1-A25** 只读写 `canvas.db`；写操作只有 N1-A8 的 `POST /api/card/footprint`（只改 `width/height` + `metadata.measuredAt`）与 N1-A23 的 `POST /api/card/position`（只改 `x/y`）——两者**都不落 `history.db`**（footprint §3.6 / `doc-21`「画布状态层不落事件」）。N1-A25 走动作层 `arrangeCards`，同样只动 `canvas.db`。
- **N1-A7 是唯一**对「`/api/nook` 不落事件」的显式断言——同时守契约 §3.6 与 `doc-21`。
- **N1-A12** 断言 footprint **不动 `seatW`/`seatH`**（footprint §5.2 反模式 9）。
- **N1-A23 后补一条**（并入同测试）：`card_position` 帧**允许**广播（`routes/world.ts:611` 的既有行为），但 `history.db` **不得**多出事件——契约 §3.7 未新增任何落账。
- **探针不打事件**：§8.1 只读写 `canvas.db`。
- **本批 MUST NOT 新增事件 `type`**（契约 §6 反模式 7）。

---

## 6. WS / 前端反应

- **本批不新增帧名**（契约 §3.6）：`GET /api/nook` 与 `POST /api/card/footprint` **都不广播**；`POST /api/card/position` 沿用既有 `card_position` 帧（`routes/world.ts:611` 不变）。
- N1-A18 断言 `node tools/check-ws-contract.mjs` 的 findings 集合**相对实现时基线无新增项**（**不硬编码数**，见 §8.3），即「有人私加帧」会被这条抓住。
- 前端侧（**02/03/04 owner**）的验收面**不在本文件**：`NookView` 渲染、`nookChar` 状态、`ResizeObserver` 防抖——05 在 §10.4 给**手测 checklist**（标 `[手测]`）。**例外：`portrait` 的 `<video>` DOM/回退/性能已升级为自动断言**（N1-A28–N1-A32，§3.13），不再只是手测。
- **依赖关系（主 agent 广播点名）**：`NookView` 的首次渲染**依赖 §2.7 的修复**（N1-A13）——本批若不修 `seatUnplaced`，小天地首屏必然是叠成一团的陈设；拖卡**依赖 §3.7 的修复**（N1-A23）——不修则「拖了又弹回去」。**02/03 的前端验收项 MUST 注明这两条前置**。

---

## 7. 错误边界（含「不静默降级」）

| 场景 | 期望 | 断言 |
|---|---|---|
| `?character` 非法（含穿越串） | **400 `invalid_argument`**（正则闸），不是 404、不是 reshape | N1-A4/N1-A5 |
| 合法 id 但目录不存在 | **404 `not_found`** | N1-A6 |
| footprint 的 `layer` 非法 id | **400 `invalid_argument`** | N1-A9 |
| footprint 的 `layer` 目录不存在 | **404 `not_found`** | N1-A10 |
| nook 路径拖卡（`arrangeCards` place） | 修复前 `not_found` → 修复后 200；**MUST NOT** 静默吞掉 | N1-A23 |
| nook 路径 arrange layout（`arrangeCards` layout） | 修复前 `not_found` → 修复后 200；**跨 nook 的 path 仍 MUST 404** | N1-A25 |
| 未加载世界（`getActiveStore()` null） | `400 {error:'No active world'}`（与 `/api/layer` 同款，`routes/world.ts:376`） | N1-A26（并入 N1-A1 文件） |
| `/api/nook` 内某卡的 md 读失败 | 不静默：该卡跳过或 500 可见，**MUST NOT** 假装空 items | `[待 01 定]`（§12 待拍板 3） |
| 前端取数失败 | UI 空态 + `details` **可见**，**MUST NOT** `console.warn` 当「失败可见」（契约 §6 反模式 4） | `[手测]`（02 范围） |
| `seatUnplaced` 600 候选耗尽 | `console.warn`「no free cell…」（`local-store.ts:630-632`），**不静默** | N1-A13 若出现该 warn 说明 fixture/锚点异常，测试须断言**无**该 warn |

**不静默降级纪律**（`skill://design-first-feature-workflow` §6 第 6 条）：测试文件内**禁止** `test.skip`；断言前的 fixture 写盘失败、`store.close()` 失败 MUST 让测试红。

---

## 8. 代码落点（精确到文件与函数）

| 文件 | 动作 | 具体 |
|---|---|---|
| `packages/shared/test/nook.test.mjs` | NEW | §2.2 `SAME_SIZE_NOOK_FILES` + §3.7 N1-A13/N1-A14/**N1-A27** + §3.9 N1-A15/N1-A16 + §3.11 N1-A24 + §3.12 N1-A25 |
| `apps/server/test/nook-routes.test.mjs` | NEW | §2.2 `NOOK_FILES` + §8.2 `mountWorld` helper + N1-A1–N1-A12、N1-A17、N1-A23、N1-A26 |
| `tools/probe-writer.mjs` `runProbe()` | 改 | `:8` import 增补；在 `:134`（Probe 1 结束）与 `:137`（Probe 2 开始）之间插 `await runNookSeat()`；新增函数 `runNookSeat()`（§8.1） |
| `packages/shared/test/components.test.mjs` | 改（**03 owner**） | 5 处 `18`→`19`（§3.9 表第 6 行）；**05 只登记，不改** |

### 8.1 探针段 `runNookSeat()`（`tools/probe-writer.mjs`，`throw` 体例）

```js
// tools/probe-writer.mjs —— 插在 runProbe() 的 Probe 1 之后、checkSpawn(Probe 2) 之前
async function runNookSeat() {
  console.log('\n[Probe 1.6] Nook seating: a same-batch seat must assign distinct positions...');
  const tmp = fs.mkdtempSync(path.join(os.tmpdir(), 'airp-nook-seat-'));
  try {
    const store = new LocalWorldStore(tmp);
    await store.writeFile('world.json', JSON.stringify({
      id: 'probe-nook', name: 'Nook', description: '', author: '', genre: 'test',
      createdAt: '2026-01-01T00:00:00.000Z', updatedAt: '2026-01-01T00:00:00.000Z',
    }));
    // 同声明尺寸的一批无行卡（note = 200×168）—— 与 §2.2 fixture 同型
    const NAMES = ['diary', 'letter', 'photo', 'ticket'];
    const files = NAMES.map((n) => ({ path: `characters/ryo/${n}.md`, kind: 'note', w: 200, h: 168 }));
    for (const f of files) await store.writeFile(f.path, '---\ntype: note\ntitle: x\n---\n\nbody');

    const seats = await store.seatUnplaced('characters/ryo', files);
    const centres = new Set(seats.map((s) => `${s.x + s.w / 2},${s.y + s.h / 2}`));
    if (centres.size !== files.length) {
      // 修复前：全部落在 SEAT_ANCHOR，centres.size === 1（契约 §2.7 BLOCKER-N1）
      throw new Error(
        `nook seating stacked: ${centres.size}/${files.length} distinct centres ` +
        `(SEAT_ANCHOR is ${SEAT_ANCHOR.x},${SEAT_ANCHOR.y}); seatUnplaced must push each new seat into \`occupied\``
      );
    }
    // 孪生缺陷（01 §⑬ 冲突 2）：nextZ 也不自增 → z 全等（修复前 1/N）
    const zs = new Set(seats.map((s) => s.z));
    if (zs.size !== files.length) {
      throw new Error(
        `nook stacking order flat: ${zs.size}/${files.length} distinct z_index; ` +
        `seatUnplaced must \`nextZ++\` per insertion (stacking order is z, AGENTS §7.5 第 6 条)`
      );
    }

    // 互不重叠（含 SEAT_PAD，与 local-store.ts:1574 overlapsOccupied 同口径）——
    // 真正参与断言：算出 ox/oy 后 MUST 判定，而不是只算不用（ReviewC C7：初稿的
    // `ox/oy` 是死代码，`none overlapping` 在任何实现下都打印）。
    const rows = store.getLayerCards(files.map((f) => f.path));
    const overlaps = [];
    for (let i = 0; i < rows.length; i++) for (let j = i + 1; j < rows.length; j++) {
      const a = rows[i], b = rows[j];
      const ox = (a.w + b.w) / 2 + SEAT_PAD - Math.abs((a.x + a.w / 2) - (b.x + b.w / 2));
      const oy = (a.h + b.h) / 2 + SEAT_PAD - Math.abs((a.y + a.h / 2) - (b.y + b.h / 2));
      if (ox > 0 && oy > 0) overlaps.push(`${a.id} ∩ ${b.id} (ox=${ox.toFixed(0)} oy=${oy.toFixed(0)})`);
    }
    if (overlaps.length > 0) {
      throw new Error(`seated nook cards overlap (SEAT_PAD=${SEAT_PAD}):\n  ${overlaps.join('\n  ')}`);
    }
    console.log(
      `✓ ${files.length} unplaced nook cards got ${centres.size} distinct seats ` +
      `(distinct z: ${zs.size}); no overlapping boxes at SEAT_PAD=${SEAT_PAD}.`
    );
    store.close();
  } finally { fs.rmSync(tmp, { recursive: true, force: true }); }
}
```

> **探针不测**（防 scope 蔓延）：不 spawn 引擎（不是 Probe 2/3 那类重型检查）、不启浏览器、不写 `history.db`、不打帧。
> **`SEAT_ANCHOR` / `SEAT_PAD` 的 barrel 导出已核实**（`index.ts` re-export `local-store`）；`cardKindOf`/`cardFormOf` 同理（barrel re-export `forms`）。若实现阶段发现任一未导出，MUST 报主 agent（属 shared 导出面变更，不在 05 范围）。

### 8.2 测试 helper（仅测试文件内，`NEW`）

```js
// apps/server/test/nook-routes.test.mjs
import fs from 'node:fs/promises';
import os from 'node:os';
import path from 'node:path';
import http from 'node:http';
import express from 'express';                                     // 解析走 apps/server/node_modules/express（已核实）
import { LocalWorldStore, CARD_FORMS } from '@airp/shared';        // 与 smoke-routes.mjs:18 同款
import { createWorldRouter } from '../dist/routes/world.js';       // 必须先 build（AGENTS §6.5）
import { EventBridge } from '../dist/engine/event-bridge.js';

/** NEW: 挂一个真 router + 真 store + 桩 lifecycle（仿 smoke-routes.mjs:47-62）。 */
async function mountWorld(files) {
  const root = await fs.mkdtemp(path.join(os.tmpdir(), 'airp-nook-'));
  const store = new LocalWorldStore(root);
  for (const [rel, content] of Object.entries(files)) await store.writeFile(rel, content);
  const lifecycle = { stopCharacters: async () => {}, startWriter: async () => {} }; // 桩：路由 MUST NOT 触达 agent
  const app = express();
  app.use(express.json());
  app.use('/api', createWorldRouter(process.cwd(), lifecycle, new EventBridge(), () => store, () => {}));
  const server = http.createServer(app);
  await new Promise((r) => server.listen(0, '127.0.0.1', r));
  return {
    store,
    base: `http://127.0.0.1:${server.address().port}/api`,
    close: async () => { server.close(); store.close(); await fs.rm(root, { recursive: true, force: true }); },
  };
}
```

```js
// packages/shared/test/nook.test.mjs
import { LocalWorldStore, CARD_FORMS, COMPONENT_KINDS, COMPONENT_REGISTRY, SEAT_PAD }
  from '../dist/index.js';

/** NEW: 临时世界 + 收尾。体例同 footprint.test.mjs:31-74。 */
async function tempStore(prefix) {
  const root = await fs.mkdtemp(path.join(os.tmpdir(), prefix));
  const store = new LocalWorldStore(root);
  await store.writeFile('world.json', JSON.stringify({ id: 'nook-test', name: 'N', description: '',
    author: '', genre: 'test', createdAt: '2026-01-01T00:00:00.000Z', updatedAt: '2026-01-01T00:00:00.000Z' }));
  return { store, root, cleanup: async () => { store.close(); await fs.rm(root, { recursive: true, force: true }); } };
}

/** NEW: 复刻 local-store.ts:1574 overlapsOccupied 的语义（含 SEAT_PAD）。 */
function overlappingPairs(rows, pad) {
  const out = [];
  for (let i = 0; i < rows.length; i++) for (let j = i + 1; j < rows.length; j++) {
    const a = rows[i], b = rows[j];
    const ox = (a.w + b.w) / 2 + pad - Math.abs((a.x + a.w / 2) - (b.x + b.w / 2));
    const oy = (a.h + b.h) / 2 + pad - Math.abs((a.y + a.h / 2) - (b.y + b.h / 2));
    if (ox > 0 && oy > 0) out.push([a.id, b.id, ox, oy]);
  }
  return out;
}
```

> **为什么 helper 复刻而不 import `overlapsOccupied`**：它是 `local-store.ts` 的**模块私有函数**（无 `export`），测试拿不到。`SEAT_PAD`（`local-store.ts:25`）**有** export，从 `../dist/index.js` import（与 `docs/footprint/05 §2.2` 同一处纪律）。

### 8.3 门禁命令（本节回答任务点 4：`package.json` 的 scripts 实况）

**先修正契约 §7 的一处过期表述**（见 §11 冲突 1，已由主 agent 采纳）：**root `package.json` 没有 `test` script**（`package.json` scripts 实测无 `test`）。故 `pnpm test` **不存在**，改用 `node --test <file>`。同理 `pnpm check:ws` 只作快捷方式，写文档时一律给 `node tools/check-ws-contract.mjs`（可读、可复制）。

| # | 命令 | 期望输出 | 说明 |
|---|---|---|---|
| C1 | `pnpm build` | 退出码 0 | `packages/shared` 改了 schema/forms → 必须 build（`AGENTS §6.5`） |
| C2 | `node --test packages/shared/test/nook.test.mjs` | 全 pass（N1-A13/N1-A14/**N1-A27**/N1-A15/N1-A16/N1-A24/N1-A25） | 坐席唯一性 + z 序 + kind + 两条 arrange 分支 |
| C3 | `node --test apps/server/test/nook-routes.test.mjs` | 全 pass（N1-A1–N1-A12/N1-A17a/N1-A17b/N1-A23/N1-A26） | 路由契约 |
| C4 | `node --test packages/shared/test/ apps/server/test/` | 全 pass（无新增 fail） | 全量回归（**含** 18→19 后的 `components.test.mjs`） |
| C5 | `pnpm probe` | `=== [AIRP Gate Probe] ALL CHECKS PASSED ===`（含 `[Probe 1.6]` 的座位 + z 序 + **真不重叠**三段） | 坐席几何探针 |
| C6 | `node tools/check-ws-contract.mjs --json` | **findings 集合 ∖ 实现时基线 = ∅**（**不硬编码数**） | 见下「基线捕获」；本批不加帧 |
| C7 | `pnpm check:docs` | `check-hooks-docs: clean…`**【实测】** | 文档一致性（注：它只扫 `docs/hooks/`，见 §11 冲突 2） |
| C8 | `pnpm probe:tools` | `ALL TOOL-FACE PROBE ASSERTIONS PASSED` | 不改工具面；防回归 |
| C9 | `node --test apps/server/test/nook-portrait.test.mjs` | 全 pass（N1-A28–N1-A32，**需浏览器**） | 契约 §4.1 立绘判据（§3.13） |
| C10 | 浏览器手测 `[手测]` | 见 §10.4 | 仅剩无法自动化的观感项 |

**基线捕获（N1-A18 的正确判据，替换「findings === 14」）**：**实现开始前**先跑一次并把完整 findings 集合存档：

```bash
node tools/check-ws-contract.mjs --json > /tmp/nook-ws-baseline.json   # 动手改代码之前
# 实现完成后再跑一次，断言「新增 findings」为空（集合差，不是计数）
node tools/check-ws-contract.mjs --json | node -e "
  const base = new Set(JSON.parse(require('fs').readFileSync('/tmp/nook-ws-baseline.json')).findings.map(f=>f.check+'|'+f.message));
  const now = JSON.parse(require('fs').readFileSync(0)).findings;
  const added = now.filter(f => !base.has(f.check+'|'+f.message));
  if (added.length) { console.error('新增 findings:', added); process.exit(1); }
  console.log('no NEW findings vs baseline');"
```

> ⚠️ **为什么不能写死 `findings === 14`**（本文初稿的假红，ReviewC C4）：`consumed` 随前端接线漂移——实测**同一分钟**本文观测 8→11、`findings` 14→9（wiring 批次刚落）。硬编码任何数都会在别的 checkout 上假红/假绿。**判据必须是「相对实现时基线的集合差」**。

**修复前/后的门禁基线（2026-09-13 本机实测，仅作参考，不入断言）**：

```
$ node tools/check-ws-contract.mjs --json | jq .summary
{ "contract": 24, "emitted": 24, "consumed": 11, "findings": 9 }   # 与初稿的 8/14 不同——正是「不能硬编码」的实证
$ node tools/check-hooks-docs.mjs
check-hooks-docs: clean (7 docs, 15 owned symbols, 55 cited paths)
```

**测试先红后绿是验收的一部分**：实现者须在 commit/PR 里附 N1-A1/N1-A8/N1-A13/N1-A27/N1-A23/N1-A25（+ N1-A28–N1-A32 若本批产出素材）的**修复前 FAIL 输出 + 修复后 PASS 输出**（修复前输出已在 §3.1/§3.4/§3.7/§3.11/§3.12/§3.13 存档）。N1-A14/N1-A24 只需修复后 PASS。

---

## 9. 与现状差异（今天是什么 → 变成什么，带 `file:line`）

| # | 今天 | 变成 | 证据 / 守的不变量 |
|---|---|---|---|
| 1 | `/api/nook` **不存在**（`node /tmp/nook05-route.mjs` → 404 HTML） | 200 + `items` 非空（N1-A1） | 契约 §3.1；`routes/world.ts:374` 旁的并列路由 |
| 2 | `POST /api/card/footprint` 对 nook **必 404**（`routes/world.ts:648` 的 `resolveLayer` 门禁） | nook 分支 → 200（N1-A8） | 契约 §3.3；`resolveLayer` 对 `characters/**` 恒 null（`local-store.ts:550-556`） |
| 3 | nook 路径对 `arrangeCards` **两条分支都死**：place（`actions/canvas.ts:457-464`）与 layout（白名单 `:490-495`、推断 `:508-520`、逐 path 复核 `:530-532`） | 两条分支各加 nook 判据 → 200（N1-A23 place / N1-A25 layout） | 契约 §3.7 + §3.7.1（主 agent 错误广播已撤回） |
| 4 | `components.test.mjs` 断言 18（`:35,37,96,180,192`） | 19 | 契约 §5.3 六处同步 |
| 5 | 「`pnpm test` 是门禁」（契约 §7 写法） | **`pnpm test` 不存在**；改 `node --test <file>` | §11 冲突 1（已闭合） |
| 6 | `probe-writer.mjs` 不测坐席几何 | `[Probe 1.6] Nook seating`（N1-A21），含 z 序（N1-A27）+ **真不重叠判定** | §8.1 |
| 7 | `seatUnplaced` 批内 `nextZ` 不自增（`local-store.ts:643-654` 无 `nextZ++`）→ 同批 z 全等（实测 3 卡全 `1`） | 同批 distinct z === N（N1-A27） | `AGENTS §7.5` 第 6 条；`01 §⑬ 冲突 2` |
| 8 | `templates/` 全树无 `component: portrait`、无 `*.webm`/无 `base*.png`（真资产只在 gitignored 的 `assets/` 车间） | 至少 1 个角色的 portrait 资产**真落进模板目录**（N1-A33），且 `NookView` 渲染 `<video>`（N1-A28–N1-A32） | 契约 §4.1；`03 §10.5/§10.6` |
| 9 | `05` 无 portrait DOM/回退断言（初稿推给手测，手测又无 fixture） | §3.13 的 N1-A28–N1-A32（**含 `.portrait__video` 计数 1/0 的静默失效兜网**） | 契约 §4.1 裁决 |

---

## 10. 验收测试（可机械核验）

### 10.1 五条非空性判据（实现完成时必须绿）

```bash
# 前置：改过 packages/shared/src 或 apps/server/src 必须 build（AGENTS §6.5）
pnpm build

node --test packages/shared/test/nook.test.mjs
node --test apps/server/test/nook-routes.test.mjs
```

期望输出（修复完成后）：

```
ok - nook §N1-A13: a same-batch seat assigns N DISTINCT positions (x/y only — see N1-A27)
ok - nook §N1-A27: a same-batch seat assigns strictly increasing z (BLOCKER-N1, second half)
ok - nook §N1-A14: the same batch seats distinctly in a NORMAL layer (attribution lock)
ok - nook §N1-A15/§N1-A16: portrait is a first-class kind (registry loads, form exists, count is 19)
ok - nook §N1-A24: the SAME branch serves the agent tool path (one entry point, two callers)
ok - nook §N1-A25: arrangeCards layout mode works for nook paths (both card points)
ok - nook §N1-A1: GET /api/nook returns the character nook, items non-empty
ok - nook §N1-A8: footprint writes land for a nook layer (the write channel exists)
ok - nook §N1-A23: arrangeCards({place}) works for a nook path (drag-to-place channel)
ok - nook §N1-A17a/b: two kinds of empty are distinguished (init trigger input)
# tests N / pass N / fail 0
```

### 10.2 探针

```bash
pnpm probe
```

期望：`=== [AIRP Gate Probe] ALL CHECKS PASSED ===`，且 `[Probe 1.6] Nook seating` 段打印 `✓ 4 unplaced nook cards got 4 distinct seats (distinct z: 4); no overlapping boxes at SEAT_PAD=22.`。

> ⚠️ **N1-A13 只锁 x/y，抓不到 `nextZ`**（ReviewC 专项实测：只补 `occupied.push`、不补 `nextZ++` 时，中心 4/4 绿、(x,y) 4/4 绿、**z 1/4 红**——z 不参与坐席几何）。**N1-A13 与 N1-A27 必须同时绿**才算坐席修完；标题里的 `DISTINCT positions` 仅指 x/y。

### 10.3 「怎么证明『没有这个修复就会失败』」——逐条修复前的观测

这是本任务点名要回答的**核心问题**。判据证明方式各不相同，逐条列出**具体 fixture** 与其**修复前的观测结果**（N1-A27 与 N1-A13 共用 fixture）：

| 判据 | fixture | 修复前的观测（怎么被观测到） |
|---|---|---|
| **N1-A1** | `characters/ryo/{README.md, diary.md, photo.md}` 写进 tmp 世界，挂真 router，`GET /api/nook?character=ryo` | **`node /tmp/nook05-route.mjs` → `404 <!DOCTYPE html>…Cannot GET /api/nook`**；体是 HTML 不是 JSON，`res.json()` 抛错。对照：同脚本 `/layer?layer=characters/ryo → 200`，证明确有内容可返回、只是没有端点 |
| **N1-A8** | 同上世界，`POST /api/card/footprint {layer:'characters/ryo', boxes:[…]}` | **`404 {"ok":false,"code":"not_found","error":"layer must be an existing layer id"}`**（同一脚本实测）；根因行 `routes/world.ts:648` |
| **N1-A13** | ① `characters/ryo/` 下 4 张同尺寸 `note`(200×168) 无行卡；② 把 holmes 真实三卡（chalk/chalk/note）拷进任意层 | ① **`distinct centres: 1 / 4`**（`node /tmp/nook05-seat2.mjs`，四卡全部 `860 456`）；② **`distinct centres: 1 / 3`**，`evening.md` 与 `late-night.md` 坐标**逐字相同 `730 445`**（`node /tmp/nook05-real.mjs`） |
| **N1-A27** | 同 N1-A13 的 4 张同尺寸 `note`（`characters/ryo/{diary,photo,sketch,ticket}.md`） | **`node /tmp/nook05-z.mjs` → `z per seat: 1,1,1` / `distinct z: 1 / 4`**（只 `occupied.push` 而不 `nextZ++` 的现状） |
| **N1-A23** | `characters/ryo/diary.md` 存在；直呼 `service.arrangeCards({place:{path, x, y}})`（= `extensions/toolkit/arrange.ts:86` 与 `routes/world.ts:610` 的共同底层） | **`node /tmp/nook05-arrange.mjs` → `FAIL code= not_found status= 404`，msg `"characters/ryo/diary.md" is not on a canvas layer; …`**；根因行 `actions/canvas.ts:457-464`。契约 §3.7 另由 `NookView02` 在浏览器复现「拖了又弹回去」 |
| **N1-A25** | `characters/ryo/{diary,photo}.md` 存在；`service.arrangeCards({layout:{mode:'grid', layer:'characters/ryo'}})` 与 `{layout:{mode:'grid', paths:[…]}}` | **`node /tmp/nook05-layout.mjs` → 两次 `FAIL code= not_found status= 404`**；msg 分别 `Unknown layer "characters/ryo"`（卡点 ① `actions/canvas.ts:490-495`）与 `"…diary.md" is not on a canvas layer`（卡点 ② `:508-520`） |
| **N1-A17a/b** | `characters/sumi/{README.md,preset.json}` 与 `characters/old-sailor/{preset.json}` | 直跑纯函数：`isNookEmpty(['characters/sumi/README.md','characters/sumi/preset.json'],'characters/sumi') === false`（README 是内容）；`isNookEmpty(['characters/old-sailor/preset.json'],'characters/old-sailor') === true`（只有 json）。两者都**不依赖本批改动** |
| **N1-A28–N1-A32** | 一张/两张 `component: portrait` 的 md + **真实** `ryo.webm`（`/tmp/nook-demo/assets/characters/ryo/ryo.webm`） | 修复前：`grep -rl "component: portrait" templates/` **零命中** → DOM 里 `.portrait__video` 永不出现，五条全红（**红因是「组件不存在」，与「prop 通路断」是同一处兜网**） |
| **N1-A33** | `templates/<world>/characters/<id>/` 的 portrait md + 其 `video:` 指向的文件 | 修复前：`test -f …/<id>.webm` 非零退出（真资产只在 gitignored 的 `assets/` 车间，实测全树 `*.webm` 零命中） |

> **复现脚本原地保留的证据**：N1-A1/N1-A8 来自 `/tmp/nook05-route.mjs`（真 `createWorldRouter(dist)` + 真 `LocalWorldStore` + express + fetch），N1-A13 来自 `/tmp/nook05-real.mjs`（真 `templates/holmes-world` 拷贝）与 `/tmp/nook05-seat2.mjs`（同尺寸批量），N1-A27 来自 `/tmp/nook05-z.mjs`（z 序），N1-A23 来自 `/tmp/nook05-arrange.mjs`（真 `createActionService`），N1-A25 来自 `/tmp/nook05-layout.mjs`（同理）。五者都**不依赖本批任何改动**，即「今天跑就是红的」。

### 10.4 手测 checklist（`[手测]`，02/03/04 范围，列此备评审）

1. `firstsnow`（或 holmes）打开角色 tab → 每行第 4 个按钮「Nook」可见、可点（02）。
2. 点进去 → **看到陈设各自散开、不叠成一摞**（**依赖 N1-A13 修复**；未修则全叠在屏幕中心）。
3. 里面拖一张卡 → **松手后停在拖到的位置**（**依赖 N1-A23 修复**；未修则弹回原位）。
4. 两张卡重叠时，**后放的盖住先放的**（**依赖 N1-A27 修复**；未修则 z 全等、谁盖谁由行序偶然决定）。
5. 「整理布局」类动作（layout 模式）→ **陈设被重新排开**，不报 `Unknown layer`（**依赖 N1-A25 修复**）。
6. 拖完刷新页面 → 位置**保持**（`cards.x/y` 已落库）。
7. 空角色（只有 README + preset.json）→ 一片空房间的提示，**不报错、不 blank**（N1-A17a 的可视面）。
8. **真空**角色（只有 preset.json，如 `old-sailor`）→ 同上但 `scene` 为 null（N1-A17b 的可视面）。
9. DevTools Network：`POST /api/card/footprint` 在渲染稳定后**只发一次**（尾防抖 200ms，footprint §3.3）。

> **portrait 的 DOM/回退/性能已升级为自动断言**（N1-A28–N1-A32，§3.13）——**不在手测清单里**（契约 §4.1 裁决：不许只写手测）。这里只留「观感」项。

---

## 11. 发现的冲突 / 需要修订的上位文档（**回写清单**）

### 11.1 任务点 5：本批改了哪些上游文档的断言

> 纪律：05 **只登记「改成什么」**，不改别人的文件（`docs/nook/00 §5.6`）。回写发生在实现 commit，由各 owner 落笔；主 agent 在验收阶段统一核对。

| # | 文件 | 位置（2026-09-13） | 今天原文（节选） | 改成 | owner |
|---|---|---|---|---|---|
| W1 | `AGENTS.md §2` 目录树 | `AGENTS.md:48` | 「`routes/world.ts # 玩家 UI 路由 → 动作服务（/move, /dice, /use-item, /choice, /enter-layer, /god-action, …）」 | 补 `/nook`（本批新增，01）与 `/card/footprint`（F1 已有，目录树漏列）到清单——漏列的两条写口半年后没人知道它们存在 | 01/04 |
| W2 | `AGENTS.md §2` 目录树 | `AGENTS.md:53` | 「`brief-builder.ts # buildSceneInitBrief / buildNookInitBrief（动态 brief）」 | 若 04 识别出「空目录判定」落在别处，在此注明；`buildNookInitBrief` 仍**无调用点**（本批不实现 nook-init） | 04 |
| W3 | `AGENTS.md §2` 目录树 | `AGENTS.md:63-70` | `packages/shared/src/` 各子目录说明 | 新增 **`rules/characters.ts`**（契约 §5.2 冻结落点：`isValidCharacterId`/`nookIdOf`/`characterIdOfPath`）后补一行——`rules/` 已有「零依赖纯规则」条目（`rules/dice.ts`/`rules/interactive.ts`），只需并进去；同时 `index.ts` 的手工 barrel 补行属「新增模块 MUST 进 barrel」纪律，不入目录树 | 01 |
| W4 | `AGENTS.md §3.3` preset 即 Agent 人格 | `AGENTS.md:140` | 「初始化只有一个可委托 profile（`scene-init`），作家委托（R1）与引擎直唤（R2）共用它」 | **今天已过期**：`presets/nook-init.json` 在盘上，`doc-11 §1` 也已把 profile 说成两个（`scene-init` / `nook-init`）。改成「两个 profile（`scene-init` / `nook-init`）」——与本批无关但同属 §3 断言过期，一并登记 | 主 agent |
| W5 | `AGENTS.md §2` 目录树 | `AGENTS.md:83` | 「`tools/motion-clip.mjs # 微动立绘 / 背景视频生产（`pnpm motion`）」 | **无需改**（本批复用它，路径/命令不变）；仅登记为「本批的立绘视频唯一生产入口」 | 03 |
| W6 | `AGENTS.md §7.5` 第 7 条 | `AGENTS.md:379` | 「前端实测经 `POST /api/card/footprint` 回写 `width/height` + `metadata.measuredAt`」 | 补一句：「`layer` 门禁对 nook（`characters/<id>`）走**并列分支**，不经过 `resolveLayer`（它恒返回 null）；见 `docs/nook/00 §3.3`」 | 04 |
| W7 | `docs/init/doc-11 §4` 角色小天地初始化 | `docs/init/doc-11-场景与小天地初始化协议.md:232-292` | 整节（触发/呈现/产出规范/初始化后） | **本批已落地的是「入口与空态可观测」，不是初始化执行**：§4.1 的「进入空的小天地」触发点现在**有了可观测面**（`GET /api/nook` 返回 `items: []`，N1-A17）——但**spawn/brief/超时/W2 兜底本批不做**（契约 §4 边界）。故 §4 的「呈现」一段**尚未落地**，MUST NOT 标「已落地」；应标注：「§4.1 的判定输入已可观测（`items.length === 0` 且目录无 md；`preset.json` 不算内容）；初始化执行归后续批次」 | 04（识别）+ 主 agent（裁决） |
| W8 | `docs/init/doc-11 §4.3` 产出规范 | `docs/init/doc-11-场景与小天地初始化协议.md:266-292` | 「`README.md # 若缺则补：type: readme，角色简介（小天地门面）」 | 与契约 §3.1 对齐：「`scene` 取该角色 README；**缺 README 时 `/api/nook` 返回 `scene: null`，不合成 stub**」——注意 `doc-11 §4.3` 的「若缺则补」是**初始化产物**（本批不做），不是取数行为 | 04 |
| W9 | `docs/footprint/00-共同上下文.md §3.3` | `docs/footprint/00-共同上下文.md:126-142` | `POST /api/card/footprint` 的 layer 门禁 = `resolveLayer`（要求 `resolveLayer(dirOfLayer(layer)) === layer`） | 补勘误：「对 `characters/<id>`（nook），门禁走**并列分支**——用 `isValidCharacterId` 校验 id，**不走 `resolveLayer`**（它恒返回 null）。`docs/nook/00 §3.3` 是本批的上位，只加分支、不改本契约的形状。」**勘误由主 agent 在验收阶段统一回写**（契约 §2.7 广播：04 只需引用 §2.7，不改此文件） | 主 agent |
| W10 | `docs/nook/00-共同上下文.md §7` | `docs/nook/00-共同上下文.md:297` | 「**`pnpm test`**：`components.test.mjs` 的 kind 计数（§5.3）与 nook 验收（`05` 篇）」 | **修正**：root `package.json` **无 `test` script**（`package.json:6-22`）。改成「`node --test packages/shared/test/*.test.mjs`」（主 agent 已确认采纳本文实测） | 主 agent ✓已修 |
| W11 | `docs/footprint/00-共同上下文.md §2.2`（连带） | `docs/footprint/00-共同上下文.md:52` | 「另有**完全未使用**的 `metadata TEXT` 列」 | **今天已过期**：`metadata` 已被 `formVersion`/`measuredAt`/`seatW`/`seatH` 使用。与本批无关，但同属过期断言，登记备裁 | 主 agent |
| W12 | `docs/nook/00-共同上下文.md` §3.7 正文 | 契约 §3.7 | 只点了 **place** 分支（`canvas.ts:457-464`） | §3.7.1 已补 layout 分支（`:490-495`/`:508-520`/`:530-532`）——**两篇必须一起读**，否则「只修 place」会让「整理布局」仍失败（N1-A25 直接目标） | 主 agent |
| W13 | `docs/nook/00-共同上下文.md` §3.7 要点 1 | 契约 §3.7 | 「从 path 提取 `characters/<id>/` 前缀 → 调用 `isValidCharacterId(id)`」 | **补**：契约 §5.2 已冻结第三个纯函数 **`characterIdOfPath(path)`**（`characters/<id>/…` → id；段数必须正好 3）——布局分支的**逐 path 复核**（`:530-532`）正需要它；只提 `isValidCharacterId` 会让实现者各写一份解析（违反 §5.2「禁止两处各写一份」）。<br>⚠️ **待裁决**：`01 §2.2` 把同一函数命名 `nookIdOfPath`（返回 nookId），契约 §5.2 命名 `characterIdOfPath`（返回 id）——**两篇命名不一致**，但落点（`rules/characters.ts`）与语义一致。契约是上位（§0 权威层级），落地时以契约的名字为准；本文不私改，登记请主 agent 在验收阶段统一 | 主 agent |
| W14 | `docs/nook/00-共同上下文.md §2.7` | 契约 §2.7 | 只写了 `occupied.push` 一行修法 | **补第二行**：`nextZ++` 同样不在 `seatUnplaced` 循环内（`local-store.ts:596` 算一次、`:643-654` 只读），实测三卡 `z` 全 `1`（`/tmp/nook05-z.mjs`）——只补 push 会让座位分散但 z 序全等，违反 `AGENTS §7.5` 第 6 条的叠放语义。`01 §⑬ 冲突 2` 独立发现同一半条；**两行都必须补** | 主 agent |
| W15 | `docs/footprint/00-共同上下文.md §3.3`（连带） | — | 行匹配键 `cards.id = path` ONLY | **无需改**（`writeFootprints` 实测 `SELECT ... WHERE id = ?`，layerId 只进 warn 文案——主 agent 已实测确认，与本批一致） | 主 agent |
| W16 | `AGENTS.md §2` 目录树 | `AGENTS.md:63-70` | `packages/shared/src/` 各子目录说明 | 补 **`rules/emptiness.ts`**（`isNookEmpty`/`isLayerEmpty`/`directChildrenOf`/`hasInitProduct`，已落地）与 **`rules/characters.ts`**（本批 §5.2 落点）两行——`rules/` 的条目已存在，把这两个模块名并进去 | 01/主 agent |
| W17 | `docs/nook/00-共同上下文.md §3.5` / §2.7 | 契约 | §3.5「`CARD_FORMS.portrait` w 留 03 复核」/ §2.7 只写 `occupied.push` | §3.5 已冻结 `360×480`，本文跟随（N1-A15/N1-A16 只锁 `chrome:'bare'`）；§2.7 需补 `nextZ++`（W14）。两处已由主 agent 在契约侧同步 | 主 agent |

### 11.2 冲突 1（任务点 4 直接触发）：**`pnpm test` 不存在**

契约 §7 与 `docs/footprint/05 §10.3 C6` 都引用 `pnpm test`，但 root `package.json`（`package.json:6-22`）**没有 `test` script**。实测脚本清单：`dev`/`dev:server`/`dev:web`/`build`/`scaffold`/`motion`/`probe`/`probe:tools`/`probe:inject`/`probe:prompt`/`check:skills`/`typecheck:extensions`/`check:docs`/`check:ws`/`pi`——**无 `test`**。

**结论**：验收一律用 **`node --test <file|dir>`**（Node 内置 test runner，`node:test` + `node:assert/strict`，各测试文件已按此写）。`NookRoute01` 2026-09-13 已在其 01 篇登记同一冲突——两处一致，无分歧。**主 agent 已确认采纳本文实测并修正契约 §7**（本行由「冲突」降级为「已闭合」）。**可选改进**（不属本批）：加一条 `"test": "node --test packages/shared/test/ apps/server/test/"` 到 root `package.json`。

### 11.3 冲突 2：`pnpm check:docs` **不扫 `docs/nook/`**

`tools/check-hooks-docs.mjs:28` 的 `HOOKS = path.join(REPO, 'docs/hooks')`——它**只扫 `docs/hooks/`**（实测输出 `clean (7 docs, 15 owned symbols, 55 cited paths)`）。故：

- `pnpm check:docs` **不会**校验 `docs/nook/` 里的 `file:line` 引用与符号归属。
- **本批的「文档一致性门禁」因此实际上只覆盖 hooks 批次**。`docs/nook/` 的 `file:line` 纪律（契约 §5.6）**没有机械门禁**，靠评审人工核。
- **登记为后续**（不属本批范围）：若要为 `docs/nook/` 建同类门禁，需扩 `check-hooks-docs.mjs` 的扫描根（或参数化），建议由主 agent 决定。

### 11.4 冲突 3：`overlapsOccupied` 未 export（继承 footprint 冲突 3）

`overlapsOccupied`（`local-store.ts:1574`，无 `export`）是测试复刻的对象；`SEAT_PAD`（`local-store.ts:25`）有 export。**风险**：若 01 改重叠判定（如 `SEAT_PAD` 改 0），复刻会脱节、出现「测试绿而排座实际重叠」。**建议**（评审裁）：01 把 `overlapsOccupied` 一并 `export`（或抽成纯函数），测试直接 import。**本文件不私改**，只登记。

### 11.5 冲突 4：N1-A23 的归属在 01/02 之间未定（契约 §3.7 第 4 点）

**契约已裁决（2026-09-13，采纳 ReviewA R2）：这条摆放通路归 `01-取数端点`**（`arrangeCards` 是服务端动作层，与 01 own 的 `isValidCharacterId`、`GET /api/nook` 同属服务端；`02` 只 own 前端入口 `NookView`）。**故本文的 N1-A23/N1-A24/N1-A25 全部由 01 的测试文件承载**：N1-A23/N1-A26（HTTP）放 `nook-routes.test.mjs`、N1-A24/N1-A25（动作层两条分支）放 `nook.test.mjs`——两个文件都属 01 的实现范围。**冲突 4 关闭。**

### 11.6 冲突 5（**新发现，本文独立复现**）：`nookIdOfPath` 与 `characterIdOfPath` **命名不一致**

契约 §5.2 冻结三个纯函数（`isValidCharacterId` / `nookIdOf` / **`characterIdOfPath`**，返回**角色 id**）；`01 §2.2` 定义的是 `isValidCharacterId` / **`nookIdOfPath`**（返回 **nookId**）。**同一职责、同一落点（`rules/characters.ts`）、不同名字与不同返回形状**（id vs `characters/<id>`）。契约 §0 是权威层级最高者，落地时以 **`characterIdOfPath` + `nookIdOf(id)`** 为准（组合即得 nookId）；`01` 的实现只需把 `nookIdOfPath(path)` 改写为 `nookIdOf(characterIdOfPath(path)! ?? '')` 式的组合，或直接改名。**本文不私改契约/01**，登记请主 agent 在验收阶段统一（W13 已含此点）。

### 11.7 冲突 6（**新发现**）：契约 §2.7 的修法漏了 `nextZ++`

见 §3.7 末与 W14。契约 §2.7 与广播只写 `occupied.push`；实测 `nextZ` 同样在循环内不自增（三卡 `z` 全 `1`）。**只补一行会让 N1-A27 保持红**——z 序全等，违反 `AGENTS §7.5` 第 6 条的叠放语义。`01 §⑬ 冲突 2` 独立发现同一半条，两处结论一致。

---

## 12. 仍未知待拍板

1. ~~**`pnpm test` 的处置**~~ → **已闭合（2026-09-13）**：主 agent 确认 root `package.json` 无 `test` script，契约 §7 已修正为 `node --test packages/shared/test/*.test.mjs`。本文一律 `node --test <file>`。
2. **`docs/nook/` 是否建文档门禁**（冲突 2）：`pnpm check:docs` 只扫 `docs/hooks/`。是否扩扫描根？
3. **`/api/nook` 内单卡 md 读失败的语义**（错误边界表）：跳过该卡（`items` 少一个，静默）还是整体 500（fail-loud）？本文倾向 **500 + console 可见**（契约 §6 反模式 4 的「失败可见」精神），但这是 **01 的实现选择**，需 01 定稿后回填本文 §7。
4. **`overlapsOccupied` 是否 export**（冲突 3）：决定测试复刻还是 import。
5. ~~**N1-A23/N1-A25 的实现归属**~~ → **已闭合**：契约裁定归 **01**（ReviewA R2）。见 §11.5。
6. **`CARD_FORMS.portrait` 的 `w`**：契约 §3.5 现冻结 `360×480`（3:4），但注明「宽取 360」仍留 03 篇按周围卡宽度量级复核。若 03 改成别的值，本文 §3.9 的断言**不红**（只锁 `chrome: 'bare'`），但 §2.2 fixture 若用到 `CARD_FORMS.portrait` 需同步——**03 定稿后回填**。
7. **`characterIdOfPath` vs `nookIdOfPath` 的命名**（冲突 5）：以契约 §5.2 的 `characterIdOfPath`（返回 id）+ `nookIdOf`（拼 `characters/<id>`）为准落地，`01 §2.2` 跟随改名或组合。**需主 agent 在验收阶段统一**（否则两篇文档的符号名对不上，`check:docs` 类门禁也抓不到——它不扫 `docs/nook/`）。
8. **越界点名（本文件不改别人的文件，仅此登记）**：
   - W1/W3/W4（`AGENTS.md` 目录树与 §3.3）——建议 **01/04/主 agent** 处理。
   - W7/W8（`doc-11 §4`）——建议 **04** 识别、**主 agent** 标注是否「已落地」（**本批只落地「判定输入可观测」，初始化执行未落地**，勿误标）。
   - W9/W10/W11/W12/W13/W14/W15/W16/W17（`docs/footprint/00`、`docs/nook/00 §7`/§3.7/§2.7/§3.5、`AGENTS.md` 目录树）——**主 agent** 统一回写。其中 **W14（`nextZ++`）是本文新发现的半条 bug**，请一并回写。
9. **`portrait` DOM 断言的测试环境**（§3.13）：`nook-portrait.test.mjs` **需真实浏览器**（仓库既有探针用 puppeteer；本文代码片段按 `openNookPage` 抽象写，具体驱动由 03/主 agent 定）。若本批不产出 webm 素材（N1-A33 红），N1-A28–N1-A32 **应保持红而不是 skip**——那正是「判据无人负责」的信号。

---

## 附：本文件自检（契约 §5.6「示例当代码自检」）
- 本文所有 `file:line` 于 2026-09-13 在本机复核；**行号有保质期**，漂移以符号名重定位并回写本文。
- 代码片段只引用**真实存在**的符号（§4.2 逐条 grep 核实）；`NEW` 符号（`runNookSeat`、`mountWorld`、`tempStore`、`overlappingPairs`、`openNookPage`）已标 `NEW`；已落地的 shared 符号（`isValidCharacterId`/`nookIdOf`/`characterIdOfPath`/`nookCardPaths`/`isNookEmpty`）标 **✅ 已落地**并从 barrel import。
- 未使用任何推断值冒充已定：N1-A1/N1-A8/N1-A13/N1-A14/N1-A23/N1-A25/N1-A27/N1-A17a-b 的「修复前观测」均为**真跑输出**（脚本路径随文标注；N1-A27 来自 `/tmp/nook05-z.mjs`；N1-A17 来自 dist 直跑 `isNookEmpty`）；`CARD_FORMS.portrait` 的 `w/h` 明确标为「由 03 定稿」，断言只锁 `chrome: 'bare'`。
- **假红自查**（评审门教训）：凡「修复后仍会红」的断言 MUST 在本文被标出并消除——N1-A12（`CardRecord` 顶层字段，非嵌套 `metadata`）、N1-A24（fixture 必须落盘卡片）、N1-A25（负例改跨 nook）、N1-A18（集合差而非计数）、§8.1 探针（`ox/oy` 真参与判定）、§3.13（`nextZ` 不参与坐席几何 → N1-A13 与 N1-A27 必须同时绿）。
- **N1-A26 已定义**（§3.0 表 + §8.3 C3），不再是悬空编号；**N1-A17① 已拆为 N1-A17a/b**（sumi 非空 / old-sailor 真空）；N1-A28–N1-A33 为契约 §4.1 新增的立绘判据。
