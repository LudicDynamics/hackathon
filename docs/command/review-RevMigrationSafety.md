# 评审报告 · RevMigrationSafety（第五份评审 · 独立评审）

> 评审范围：`docs/command/08-迁移与收敛.md`（1109 行）、`docs/command/09-安全与边界.md`（1051 行）。
> 依据：`docs/command/00-共同上下文.md`（读时 575 行，写报告时已增至 606 行，含新增 §10.14 效果名裁定）。
> 立场：**独立评审，不是设计者**。本报告不复述设计，只做核验与判据。
> 已生效的两条冻结裁定（`Main`，2026-09-14）：效果名以 `04` 的集合为准（`take`/`put`/`place` 不是效果名）；`unlink` MUST 移除 → 集合 **7 名**。
> 抽查口径：本报告的每条 `file:line` 都实测过（`git grep` / `sed -n` / `node` 独立复现 zod 行为 / `git rev-list` / 逐文件 YAML 解析）。不是抽样——`08` §2.3 与 `09` §2.3 的**每一个数字**我都重算了一遍。检出的偏差与缺陷分别在 §2、§6.3、§7 逐条给出。

---

## 1. 评审判定

| 文档 | 判定 | 一句话理由 |
|---|---|---|
| `08-迁移与收敛.md` | **有条件通过** | 存货盘点的证据质量在本批次最高（144/144、3/36、6/30 等统计我逐项复算全部命中），路线 D 的论证站得住；但 **7 处事实/内部一致性缺陷**（含两处 `git rev-list` 数字过期、一处自相矛盾的定量表、一处**永真**的验收断言），以及一处违反冻结效果名裁定的残留。修完即通过。 |
| `09-安全与边界.md` | **有条件通过** | 本篇是全批次**最硬**的一篇：fail-open 复现我独立重跑并逐字一致，世界包代码执行发现属实且定性恰当，R1–R11 的分析有依据，四条硬前提**可机械核验**。但有三处**内部计数自相矛盾**、三处**违反冻结效果名裁定**（其中 `:487` 的联合类型被自己用于穷尽 switch，是真实的类型安全损失），以及交叉评审已认定的 **B1/B2/B3 三条接缝阻断未闭合**。修完即通过。 |

**两份都不到「不通过」**：没有发现设计方向性错误，也没有发现伪 helper/假字段（`NEW` 符号的签名都给了，`estimateWorldCommandCost` / `expandDiceOutcomes` / `worldCommandsEnabled` 的签名与依赖都是可编译的真实形状）。

---

## 2. 跨文档一致性核验

### 2.1 效果名（冻结裁定 §10.14）——**发现 `09` 3 处未同步，其中 1 处是任务书清单外的**

| 位置 | 写法 | 应写 | 性质 |
|---|---|---|---|
| `09:435` | `Available: give, take, put, edit, set_status, consume, enter, link, place.` | `give, move, edit, set_status, consume, enter, link`（7 名） | 违反裁定；**错误文案面向模型**，会教 Agent 写不存在的效果名 |
| `09:487` | `WorldCommandEffectName = 'give' \| 'take' \| 'put' \| … \| 'place'` | 7 名联合 | 违反裁定；**`09` 自己注释「re-exported here so the cost estimator can switch exhaustively」**——集合错 2 个名字会让穷尽 switch 漏分支，是真实的类型安全损失 |
| `09:156`、`09:617` | 「`04` 的 **9 效果**注册表 / 9 效果封闭表」 | 7 | 违反裁定；数量过期 |
| `08:550` | 「命令侧用 `04` 的 **`take`**（→`moveEntity`）表达同一能力」 | `move` | 违反裁定。注意 `08:513`/`:554` 用 `take` 是在描述**上游 `Recipe`/`choice_actions`**，**合法**（裁定明文许可）；`:550` 是命令效果语境，不合法 |

> **任务书清单外补报（按 `Main` 要求）**：`08:550` 不在 `Main` 的修订清单里（清单只列了 `01/02/03/05/06/10`，`08` 未被点名），但它确实把 `take` 用作命令效果名。**`08` 需一并修**。
> **`unlink` 补报**：`09` 全文零命中 `unlink`，`08` 全文零命中 `unlink`——两份都不受影响，`Main` 的第二条裁定对它们无动作。

### 2.2 `dry-run` × `command_log`（交叉评审 B1，未闭合）

- `09:249`：`dry-run`「**计划写进 `command_log` / `details`**」；
- 而 `05` 把 `command_log` 定为**唯一幂等判据**，`status` 是闭枚举 `[partial|done]`，无法表达"未执行的计划"。
- → 后果链成立：dry-run 落一条 `command_log` → 真实触发按 key 查到"跑过了" → 判 `reused` → **奖励永不落地**。
- **`09` 对此未作任何交叉引用或保留标记。** 这是必须修的口径冲突，且 `09` 是相干方之一（`Main` 已指派 `09` 写进 §10.1）。

### 2.3 `detail` 可选键集合（B2，未闭合）

- `09:377` 逐字称 `detail.command` 是「**唯一**的可选附加键」；
- `09` 全文 **零命中 `commandKey`**（我 grep 确认）；
- 而 `05:303` 追加 `detail.commandKey`。
- → `09:377` 的「唯一」是**可证伪的过强表述**（与 `09` 自己 §11.7 批评契约候选 A「措辞过强」的错误形态同型）。必须二改一。

### 2.4 R6 未覆盖幂等 `fact` 的读取（B3，未闭合）

- `05:144` 的 `use_item` 幂等 `fact` = 读物品文件内容做哈希；
- `09:123-124`（R6）与 `09:571-575`（攻击面第 2 条）的读取上限模型**只覆盖命令文件与实体 frontmatter 的读取**，**不覆盖这次读**；
- `09` 未登记它，也未在 §12.3「仍未知」里提到。
- → 这正是 **`09` §2.3 的静态上界模型会不会被"实现细节"蛀空**的第一道裂缝，必须补格或补字节门。

### 2.5 与 `04` 的 `materialize_outcome` 分歧（任务书第 7 条）——**登记恰当 ✅**

`08` 倾向 D-1（`08:437-443`），明写「**裁决权仍归 `04`**（效果集是它的范围）」，且 `[08-d]`（`08:1049`）保留未拍板标记、给出理由链（方向三之后"需要新语法面"这条反对理由失效）。`08` 还诚实标出 D-1 的代价（3 效果 vs 1 效果）。**分歧没有被藏起来，且裁决权归属正确。这一条通过。**

---

## 3. 契约冻结遵守核验

| 冻结项 | `08` | `09` |
|---|---|---|
| 6 个顶层 key（`name`/`desc`/`params`/`do` + 实体侧 `on`/`command_log`/`command_error`） | ✅ `08:78` 逐字「顶层四键」+ 实体侧 `on` | ✅ `09:238` 用 `.strict()`，不含第五个顶层 key |
| `detail.command`（`by` 已删） | ✅ `08:811-815` 明文纠正自己此前的 `detail.by` 写法 | ✅ `09:379-385` 三条理由完整，且**给出了任务书没写的精确边界**（非 strict 的 `z.object` 下 `by` 只在撞 `layer_initialized` 时炸） |
| actor = 触发者 | ✅ `08:823` 与契约口径一致 | ✅ `09:351` 写成硬约束「命令文件里 MUST NOT 有任何表达 actor 的字段」 |
| 不新增事件类型 | ✅ `08:809` | ✅ `09:371-377`，且补了一条**本文视角的独立理由**（新增类型是一条既有审计工具看不见的通路） |
| 执行位置 = 动作层 | ✅ `08:647`、§12 `:873` | ✅ `09:331`（G6「命令 MUST NOT 直接调 `store`」） |
| 能力集 ⊆ 动作层 | ✅ `08:539` | ✅ `09:16`、§11.6 表格 |
| `action-feedback` 四态不发明第五态 | 不适用（`06`） | ✅ `09:449`「禁用不是错误，MUST NOT 用报错样式」 |

**否决项检查：无。** 两份都没有私改契约的冻结形状。`09:238` 的 `.strict()` 与契约 §3.1「未在 schema 中声明的顶层 key = 写入时报错」一致。

> **一处"半冻结"**：`09:232-243` 把 `worldCommands` 加进 `WorldSettingsSchema` 并保持 `.strict()`，没有引入契约外的顶层形状。这是 `[C-4]` 的方案 A，**保留了未拍板标记**（`09:222`、`:1024`），合格。

---

## 4. 语义兑现 / 需求强度

### 4.1 `09` 的表达力天花板：**给了具体场景，不是抽象谈 —— 通过，但有 2 处场景需修正**

任务书要求「逐类举出」。`09:44-51` 的 F1–F5 表**每一类都绑了一个真实 `templates/` 场景**，且都给了机制解释（不是"难"）。逐条核验：

| # | 场景 | 核验 |
|---|---|---|
| F1 | `wuwu` 灯塔、背包里所有煤油灯 | ✅ `templates/wuwu/world/harbor-chart/old-lighthouse/lamp-oil.md` 存在；`backpack.ts:32-34` 逐字 `store.listFiles('player')` 枚举，长度无 schema 上限。**场景成立** |
| F1b | `rewards` 长度可变、schema 允许 0–3 | ✅ 实测 `origin/niko:declared-actions.ts:146-147` 逐字 `x.rewards === undefined ? [] : x.rewards` + `rewards.length > 3`。**成立**，且 `144/144 恰好 1 项` 我复算命中 |
| F2 | `moonlit-contract` 蓝丝带三次承诺 | ⚠️ `templates/moonlit-contract/world/blue-ribbon.md:5-9` 的 `status.data.contract` 实测只有 `contract: Not Contracted`（**一个字符串键，不是计数器**）。`09:48` 说「能存数字」——**属实**（`InteractiveValueSchema` 含 `z.number()`，`frontmatter.ts:13-14`），但**该文件今天并没有存数字**。`09` 用"这是一个计数器"的语气引用它，**把"能建模成计数器"说成了"内容是计数器"**。属可容忍的修辞，但建议改成"该契约状态若要计数需建模为 `status.data.contract_n`" |
| F3 | `whitechapel-jp` 跨实体聚合 `clue_found ≥ 3` | ❌ **`grep -rln 'clue_found' templates/` → 零命中。** `09:49` 说的「带 `status.data.clue_found` 的 chalk」**在本仓不存在**。这是 `09` 五条 F 里唯一**场景是凭空造的**。机制论证（命令只能读触发实体）本身对，但场景是编的 —— 违反契约 §0「没有证据的判断一律标 `[推断]`」 |
| F4 | `divergence` time-map 三十年后开店 | ✅ `templates/divergence/world/time-map/thirty-years-later/tokiwa-electronics/shop-record.md:7-8` 逐字 `timeline: original` / `shop_open: false`。**成立** |
| F5 | 真实日期/天气/自由文本理解 | — 抽象类，合理 |

**→ `09` 没有滥用也没有回避"质疑整个方案"的授权**：它明确写「**我不主张 reopen 任何候选**」（`09:932`），同时对 §9.2 决策记录提出四处修订。四处修订我逐条对照契约核验，**三处已在契约中落地、一处措辞不精确**：

| 候选 | `09` 的修订主张 | 契约现状 | 核验 |
|---|---|---|---|
| A 措辞过强 | 改「挡住一切确定性玩法」→「挡住可靠性」 | 契约 `:347` 逐字已改为「**挡住的是可靠性，失败按概率累积**」 | ✅ **已落地**，且修订记录见 `:355` |
| C 可逆性不准 | 「删掉 `command/` 即回退到 A」不准 | 契约 `:349` 已改为「**删掉 `command/` 不等于回退到 A**——`on` 会变成悬空绑定」 | ✅ **已落地** |
| B 漏记优点 | 补「不引入任何不受信输入面」 | 契约 `:348` 逐字已补「**优点（原表漏记）**：不引入任何不受信输入面」 | ✅ **已落地** |
| D 升级为事实性排除 | 用 Worker 冷启 39.94ms / QuickJS 11.86–14.14ms 排除 | 契约 `:350` 已写「**从偏好升级为事实的排除**…执行模型上不相容」 | ✅ **已落地** |

**这四处修订全部成立**，且是**高质量的**：A 的修订尤其正确（"挡住一切"可被一次成功反例推翻；"挡住可靠性"是结构性成立）。这一节兑现了契约留给 `09` 的授权，**没有滥用**（没有为 reopen 而 reopen），**也没有回避**（提出的是能改变文本的实质修订，不是空话）。

### 4.2 `09` 的世界包代码执行发现：**属实，定性恰当**

`09:877-888`（冲突 3）三条证据我逐条实测：

1. `launch.ts:55-56` 逐字「`--no-extensions`: `<cwd>/.airpworld/extensions/*.ts`」——✅ 相符（我读的是 `:55-56`）；
2. `presets.ts:130-152` 的 `extensionArgs` 扫描 `<worldRoot>/extensions/` 并**逐文件** `args.push('--extension', …)`——✅ 相符（`09` 写 `:129-149`，函数体实际 `:130-152`，**起止各差 1–3 行**，小偏）；`launch.ts:147`（writer）与 `:204`（character）都调它——✅ **逐字命中**；
3. `launch.ts:63-65` 逐字「Explicit `--extension` / `--skill` flags still load under `--no-*`」——✅ **逐字命中**。

**定性「既有 ≠ 不重要」恰当**：`09:577` 写「**零**（本模块不碰），**但这是本仓当前最严重的 UGC 问题**」；`09:887` 逐字要求「**把 doc-17 的优先级翻过来**——『扩展市场的沙箱问题』比『世界命令的沙箱问题』急一个数量级」；`09:918` 又诚实补了命令的坏处（「命令更容易被写出来 ⇒ 绝对数量会远多于扩展 ⇒ 总数上的事故面未必更低」）。**既没有拿"既有"当免责，也没有拿它 overshadow 本模块自己的风险。这是本批次最诚实的一段。**

### 4.3 `09` 的 fail-open 实测：**独立复现，逐字一致**

我在 `packages/shared/node_modules` 下用 zod 独立重跑了 `09:291-303` 的复现，得到**逐字相同**的输出：

```
after player sets off, file: {"autoWrite":"scenes","worldCommands":"off"}
.optional()  | POST body {autoWrite} -> written: {"autoWrite":"scenes"}              | next read: {...worldCommands:"on"} *** SILENTLY RE-ENABLED ***
.default(on)| POST body {autoWrite} -> written: {"autoWrite":"scenes","worldCommands":"on"} | next read: {...worldCommands:"on"} *** SILENTLY RE-ENABLED ***
```

两种 schema 形状都会静默重置。触发链的三个 `file:line` 也全部相符：
- `world.ts:450-459` POST 整体覆写（**我确认 `:457` 是 `writeWorldSettings(store.worldRoot, parsed.data)`，无 merge**）；
- `useWorld.ts:777,787` 两处 `settingsRef.current = { autoWrite: … }`（**`09` 写 `:730-742`，实际在 `:777`/`:787`，偏差 40+ 行**——但代码形状逐字对）；
- `world-settings.ts:30-35` 原子写、`:17-28` 读路径 `.partial()` + 默认展开——✅ **逐字命中**，`09:315`「读路径不需要改」的判断正确。

**`[C-4]` 的前提成立，且"只加字段不等于开关能用"这个洞察是全批次最有价值的产品级发现之一**：它把硬门 7（关掉仍可玩）从"加个字段"升级为"必须同时修一个既有 fail-open 缺口"。

> **一处行号偏差值得单列**：`09` 三次引用 `useWorld.ts:730-742`（`:263`、`:286`、`:311`、`:550`），实际 `settingsRef` 赋值在 `:777`/`:787`。这个偏差不是孤例——`useWorld.ts` 总计 813 行，`:730-742` 是 WebSocket 重连代码。**若评审据此定位会找错地方。** 建议全局改为 `:777`/`:787`。

---

## 5. 闭环性

### 5.1 `08` 的符号闭环：**全部有唯一定义处 ✅**

| 新符号 | 定义处 | 核验 |
|---|---|---|
| `expandDiceOutcomes` | `08:862` 给完整签名 + 返回类型 | ✅ 无伪 helper |
| `legacy-dice-outcomes.ts` | `08:862` 路径明确 | ✅ |
| `tools/migrate-dice-outcomes.mjs` | `08:865` 给职责 + 幂等要求 + 拒绝 `archive/`/`worlds/` | ✅ |
| `legacy_dice_outcomes_unexpandable` / `legacy_and_modern_conflict` / `legacy_band_mismatch` / `command_dir_is_not_a_layer` | `08:847-850` 四条错误码，归 `01` | ✅ 均标了归属 |
| `overlapping_command_when` | `08:1099` 明文建议与 `legacy_band_mismatch` **合并成一个** | ✅ 归属清楚 |
| `commandKey` | `08:1071` 引 `05 §3.1` 的完整公式 | ✅ 归 `05`，`08` 只消费 |

### 5.2 `09` 的符号闭环：**全部有定义处 ✅**，两处需注意

| 新符号 | 定义处 | 核验 |
|---|---|---|
| `WORLD_COMMAND_LIMITS` | `09:467-483` 完整常量表 | ✅ 无伪常量 |
| `WorldCommandEffectName` | `09:486-487` | ⚠️ **内容违反冻结裁定**（见 §2.1），但符号本身有定义 |
| `ExpandedWorldCommandEffect` | `09:490-502` 逐字段注释 | ✅ **这是全批次最扎实的类型定义**——它把"承诺给 `01`/`02` 的接口"具体化了 |
| `WorldCommandCost` / `estimateWorldCommandCost` | `09:505-523`，标注「Pure: no I/O」 | ✅ 与 §2.3 的"parse 期可算"一致 |
| `worldCommandsEnabled` / `worldCommandsDryRun` | `09:532-539` 两个完整实现 | ✅ 与 `docs/settings/00:79`「MUST NOT 在别处再写一份档位判断」一致 |
| `command_*` 十条错误码 | `09:434-443` | ✅ 均给模型文案 + 玩家文案（契约硬门 4 双通路） |

> **一条闭环缺口（与 B3 同源）**：`09:522` 的 `WorldCommandTriggerContext` 被 `estimateWorldCommandCost` 引用，但它**归 `02` 定义，`09` 未给形状**。这本身不是错误（跨模块引用），但 `09` 自己要求"每个被引用的符号有唯一定义处"——**这里 `09` 该写一句"形状归 `02`，见其 §X"，否则它是本报告范围内唯一悬空的类型**。

### 5.3 `08` 声明的 `NEW` 符号都给了签名 ✅

`08` 的 §12 表格（`:860-871`）逐文件标了 `NEW`/`有条件改动`/`迁`，其中三处 `NEW` 都给了函数签名或明确职责。**无"伪 helper"**。

---

## 6. 诚实性

### 6.1 `[推断]` 标记与 E 级声称：**两份都合规 ✅**

- 契约 §9.4 要求本批次 **MUST NOT 声称 E3**。`08:3` 标「E1（设计）+ E2（源码与内容已逐行读过）」；`09:8` 标「设计（E1）…不声称 E3」。**两份都合规。**
- `08:331` 的「正面确认」段落显式收窄证据等级：「**注意证据等级**：实测只覆盖…现有内容，**不覆盖 Agent 当场写的新命令**」，并据此推出 `fold-args` MUST 含键冲突检测。**这是本批次 E2 不越权为 E3 的最佳范例。**
- `09:1046-1048` 的 §12.3「仍未知」诚实列了 6 条，包括「R8 的真实数量级没有实测…**没有给出任何时间数字**，这个推断**没有证据**」。**合规。**

### 6.2 未知是否被伪装成已定：**基本没有，`09` 有一处**

- `09:184` 写「判定挂**三个**前提」，紧接着列了**四条**；而 `09:23`（§1 表）与 `09:1035`（§12.2）都写「**四条**硬前提」。→ **`09` 自己对自己的判定条件数说不一致。** 属笔误级，但在一个"本判定决定是否降级为仅本地自部署"的位置上，**四 ≠ 三会被读者读成"哪条是软的"**。必须改成四处统一为"四条"。
- `09` 其余所有 `[C-n]` 标记（`[C-1]`/`[C-4]`/`[C-6]`）都在 §12.2 保留「**未拍板**」，未被写成既定事实。✅

### 6.3 任务书指定的三条「设计文档与代码事实不符 / 内部不一致」——**实际找到 4 条，超出要求**

1. **`08` 内部自相矛盾（定量表 vs 迁移量表）**：`08:352` 逐字「每次触发的效果条数…**3 个**（`give` ×1…、`edit` ×2）」，而 `08:715` 的 §9.2 表逐字「单次触发的效果条数 | — | **1**」。**同一份文档、两处相反。** 后者显然是 D-2 假设的残留（`materialize_outcome` 是 1 个效果），未随 `08` 改立场到 D-1（`:437`）而更新。→ **`08` MUST 统一。**
2. **`08` 的 `git rev-list` 数字过期**：`08:629` 逐字「`git rev-list --left-right --count origin/main...origin/niko` → **`18  8`**」，实测为 **`22  8`**（`18` 错，`8` 对）；同处「`origin/main...origin/merge/niko` → **`49  0`**」，实测 **`53  0`**。**这两个数字是"分叉规模"论据的骨架**（`08` 用它论证"不能不合"）。同段的 `git diff --shortstat`（`2049 文件 / +25687 −4107`）**我实测逐字命中**——说明作者跑过 git，只是 `rev-list` 两次没同步更新。
3. **`09` 内部计数矛盾（R 项分类）**：`09:23` 逐字「**11 项成本里 9 项可静态界定、2 项需补规则（R5/R6）、1 项结构性不可界（R8）**」——9+2+1 = **12 ≠ 11**，且**完全漏掉 R9**（R9 在 `09:127` 明标 ❌ 不能）。而 `09:182` 的判定依据又把 R6 归入"可静态界定"（与 `:124` 表格里 R6 标 ⚠️ 矛盾）。**同一节的三个地方给了三种分类。**
4. **`08` 的 M-7 验收断言是"永真"的（方法性缺陷，比数字错更值得警惕）**：`08:934` 逐字 `grep -rl 'on:' --include='04-investigation-dice.md' templates | wc -l` = **24**。但 `'on:'` 是 `'action:'` 的子串（`acti` + **`on:`**）——**每个骰卡都有 8 处 `action:`**（我实测：单文件 `grep -c 'on:'` = 8）。所以这条断言在**迁移前就已经返回 24**，迁移后仍是 24。**它测不出任何东西**，却会被读成"`on` 块已落地 24 处"。同一条断言的另一半 `grep -rl dice_outcomes templates | wc -l` = **24** 期望值也错——**实测 28**（24 骰卡 + 4 份 SKILL.md，我逐个列出确认）。**这条断言照写会必然失败。**

### 6.4 任务书第 8 条：`08` 的翻译验收抽查（**我自己 grep 数了一遍**）

| `08` 的断言 | 我的复算 | 结论 |
|---|---|---|
| `templates/whitechapel-jp/.../04-investigation-dice.md` 逐字可翻 | 实读 `:1-37`：`1d100` / `expect: <=60` / 四档 `1..12 13..60 61..95 96..100` / 每档 `min+max+text+options+rewards` / `options` 2 项 / `rewards` 1 项。`08:226-265` 的「旧」块与真实文件**逐字一致** | ✅ **抽查通过** |
| `templates/wuwu/.../04-investigation-dice.md`（2d10） | 实读 `:1-35`：`2d10` / `expect: ">=11"` / 四档 `2..4 5..10 11..17 18..20`。`08:368-370` 的三处差异描述**逐字命中** | ✅ **抽查通过**（最复杂一档亦通过） |
| 36 文件 × 4 档 = 144 全部可翻 | 我逐文件解析 36 个：档数分布 `{4: 36}`、`rewards` 长度分布 `{1: 144}`、`options` 长度分布 `{2: 144}`、每档键集**唯一** `(min,max,options,rewards,text)`、顶层键集**唯一** `(type,title,roll_dice,choice,choice_actions,dice_outcomes)`、`options.id` 全集 `{back, play-result, read-clue}`（**3 个**）、`action.kind` 全集 `{enter, read, writer}`（**3 个**）、枢纽卡 6 / 地点卡 30 | ✅ **`08` §2.3 的每一行统计全部命中** |
| 「翻译不了的语义单列」 | `08:388-499`（§4.5）单列了 4 条缺口并逐条上报；缺口 1/2/3 标为"已解决"，缺口 4（`from` 跨字段）标为"归 `07`，本文登记" | ✅ **单列了，且没有伪装成已解** |

**`08` 的翻译验收是本批次证据质量最高的部分。** 唯一瑕疵在 §6.3 第 4 条（M-7 断言的期望值本身写错）。

### 6.5 诚实性的一处亮点（值得记入评审纪要）

`08:717` 主动写出路线 D 的**唯一退步**：「路线 D 下，区间在内容里**被写了两次**…这是路线 D 的**已知代价**，也是它相对路线 A（只写一次）的唯一退步」，并给出两点缓解（同文件相邻、可写入期校验）。`08:1087` 又把这条代价推到一个**真实的运行时危害**（区间重叠 → 同 key → 静默 `reused`），并给出静态校验的对策与错误码文案。**自己找出自己方案的代价并推导到后果链，这是设计文档的合格线以上。**

---

## 7. 四份/两份汇总表

| 文档 | 判定 | 阻断/条件（按严重度） |
|---|---|---|
| `08-迁移与收敛.md` | **有条件通过** | ① `08:715` vs `:352` 效果条数自相矛盾（1 vs 3）——**必须统一**；② `08:934` M-7 的两条 grep 断言期望值错误且 `'on:'` 匹配 `'action:'`（**永真**）——必须重写；③ `08:629` 两处 `git rev-list` 数字过期（18→22、49→53）；④ `08:550` 命令效果名用 `take`，违反冻结裁定（任务书清单外补报）；⑤ `08:681`「2983 行 YAML」口径未标明含 `archive/`（实测 templates+archive 合计 2983，**数字本身对**，但读作"24 个活模板"会误导：24 个活模板仅 2317 行） |
| `09-安全与边界.md` | **有条件通过** | ① **B1**（`09:249` dry-run 写 `command_log`）与 `05` 的幂等判据冲突 → 必须与 `05` 协商统一口径；② **B2**（`09:377`「唯一可选键」）与 `05:303` 的 `commandKey` 冲突 → 二改一；③ **B3**（R6 不覆盖幂等 `fact` 的读取）→ 补格或补字节门；④ `09:435`/`:487`/`:156`/`:617` 四处效果名/数量过期（`:487` 是穷尽 switch 的联合类型，**类型安全损失**）；⑤ `09:23`/`:182`/`:124` 的 R 项分类三处不一致（9+2+1=12≠11 且漏 R9）；⑥ `09:184`「三个前提」应为「四个」（与 `:23`/`:1035` 冲突）；⑦ `09:49` F3 场景 `clue_found` 在仓内**不存在**（零命中，未标 `[推断]`）；⑧ `useWorld.ts:730-742` 应为 `:777`/`:787` |

**两份共同的系统性偏差**：引用 `origin/niko` 独有符号时行号普遍偏乐观（`presets.ts:129-149` 实际 `:130-152`；`09` 的 `useWorld` 偏差 40+ 行）。**证据质量总体可靠，但"引用了 `origin/niko` 的东西却没重新核行号"是本批次的系统性弱点**——与交叉评审给的"11 条里 9 条相符"比例一致。

---

## 8. 对评审门的裁决输入（任务书第 2 条）

**契约 §9.5 reopen 条件 3（是否降级为"仅本地自部署"）：**

> **我的判断：同意 `09` 的「不触发降级」。**

依据：`09:184-188` 的四条硬前提**全部可机械核验**（不是形容词）：

| 前提 | 机械判据 | 可核验 |
|---|---|---|
| 1. `04` 排除 `generateImage` | `WORLD_COMMAND_EFFECTS` 里无该项 | ✅ `04:40` 逐字 8 名（应收 7），**确无** `generateImage` |
| 2. 链式深度是代码常量 | `commandDepth` 定义在 `limits.ts`，`grep` 于 `world-settings.ts` 应零命中 | ✅ 可核验 |
| 3. `01`/`02` 强制长度上限 | `WORLD_COMMAND_LIMITS.maxParamLength` 等常量存在 + `09:816` 的断言存在 | ✅ 可核验 |
| 4. 数组型 key 上限在 zod 里 | `rewards.max(3)` / `options.max(2)` 出现在 schema | ⚠️ **可核验，但今天无落点**（详见下方采纳结果）——我实测 `packages/shared/src/schemas/` 里 `dice_outcomes` 与 `rewards` **零命中**；而实体侧走 `passthrough`（`frontmatter.ts:138-141`），`dice_outcomes` 根本不被任何 schema 校验。**"MUST 在 zod 里"意味着要给一个今天完全不受校验的 key 新写 schema——这是 `01`/`02` 的新增工作量，`09` 未标注它，也未指派归属。** 这一条从"验收项"变成"待立项" |

**结论**：前提 1–3 可机械核验且方向正确；**前提 4 是一个未指派归属的新要求**，建议评审门把它显式指派给 `01`/`02`，否则 §2.3 的 R10/R11（"声明式路线最有利的一条论据"）在落地时会静默失效——**这正是 `09` 自己在 `:142` 警告的那种失败**（"若上限只写在 `04` 的文档里而没进 schema…R10 当场失效"）。

> **采纳结果（评审门已裁，2026-09-14）**：本报告的前提 4 意见被采纳并升级为正式指派——`rewards`/`options` 的长度上限**由 `01` 的 `parseOnBindings`（`07` 写入时调用）承担**，不再指望 zod 侧的 `EntityFrontmatter`（实体走 passthrough，`frontmatter.ts:138-141`，指望它是无效的）。`04` 的 `≤3` 与 `01` 共用同一常量来源；`09` 须在 §12.3 写出这条前提的**落点**而非只写"由 zod 强制"。→ **§8 表格第 4 行的「待立项」闭合。**

**`[C-6]`（`{{ trigger.fm.* }}` 读取面）与 `[C-5]` 路线 ④ 的依赖方向：** `09:1051` 已显式登记。**属实且必须同次评审一起定**——路线 ④（`08` 的 `[C-5]` 首选）依赖 `[C-6]` 选 (a)，而 `09:605-609` 倾向 (a) 且给出了理由（读取面不构成越权）。**两份的待拍板项互锁，评审门 MUST 同时裁。**
