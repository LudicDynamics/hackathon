# docs/gateway/04 — 契约回写与验收

> 状态：**设计稿（2026-09-14）**，待评审。属 B6「网关与会话域」（见 `docs/gateway/00-共同上下文.md`，下称 `00`）。
> 边界：本文只改**文档与测试**——`docs/tools/12-工具注册与路由统一.md`、`docs/development/后端实现计划.md`（`00 §8` 登记、§9 表逐行列出的 **10 行落点**）、新增 `apps/server/test/gateway-replay.test.mjs`。**不改源码**。
> 依据：`00 §4`（跨端同批矩阵）、`AGENTS.md:53`、`tools/check-ws-contract.mjs`、`skill://design-first-feature-workflow`（冻结契约须可机械核验）。

---

## 1. 一句话定位

**把 B6 的三处改动锁进机器能验的容器：一帧进 `docs/tools/12 §6.2`、一条断言进单测、一条 `pnpm check:ws` 的基线数字——并顺手把 `00 §8` 登记的上位文档过时断言改对（§9 表逐行给出 10 处落点）。**

---

## 2. 签名与参数

### 2.1 `docs/tools/12 §6.2` 新增一行（NEW）

插在 `agent_activity` 行之后、`要删的两个帧` 子表之前（即 `docs/tools/12-工具注册与路由统一.md` 的 `:596` 与 `:598` 之间）：

```markdown
| `replay_done` | `{ turns, timestamp }` | `event-bridge` 的连接回放路径（`subscribe`/`replayTo`；**网关合成**，非引擎透传） | 解除「回放中」输入锁（`docs/gateway/02 §6.2`）；**它不是 `replay_entry`**——后者是 `lifecycle.scheduleWarmup` 的 custom entry 回放 |
```

**为什么必须在这张表**：`tools/check-ws-contract.mjs` 的 `contractFrom`（`:151-182`）**只认 §6.2 的第一张表**（表开始后遇到首个空行即结束，`:162`）；放错表 = 契约侧看不见 = 门禁把发射点报成 `undeclared`（`:218-222`）。

> **与 `00 §3.2` 一致（裁决 H）**：本行载荷 `{ turns, timestamp }` 与 `00 §3.2` 的新冻结形状**现已一致**——两处均为 `{ type, turns, timestamp }`（`type` 即帧名 `replay_done`；`turns` = 实际回放的**完整轮数**，可 < 5）。原 M3 分歧（`00` 无 `turns` vs 本文有 `turns`）已由裁决 H 消解。

### 2.2 `apps/server/test/gateway-replay.test.mjs`（NEW）

单文件承载三篇的纯函数验收（`01 §10`、`02 §10`、`03 §10`），避免三个测试文件共享一套假件。约定：

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
// Runtime code: dist (repo convention — apps/server/test/*.test.mjs import '../dist/...',
// see connection-recovery.test.mjs:7-8). Pure protocol constants may come from the
// source barrel only if @airp/shared is built; the existing convention is dist, keep it.
import { replayWindow } from '../dist/engine/event-bridge.js';
import { PROTOCOL_VERSION, WS_CLOSE, WS_COMMANDS, GATEWAY_EVENTS, REPLAY_TURNS, closeKind } from '@airp/shared';
import { shouldTerminate, startHeartbeat } from '../dist/gateway/heartbeat.js';
```

---

## 3. 行为契约逐步

1. **`replay_done` 只在 §6.2 第一张表出现一次。**
   漏了会怎样：出现两次 → `contractFrom` 的 `Map` 后写覆盖前写（`check-ws-contract.mjs:186`），行号指向后一行，验收表核对时找不到。
2. **改完后 `pnpm check:ws` 的 `summary` 变成 `{contract: 28, emitted: 28, consumed: 25, findings: 0}`。**
   漏了会怎样：`contract` 涨了 `emitted` 没涨（或反之）→ 门禁红，且红的是 `missing`/`dark` 两类之一，必须按类别核对是**哪一处**漏改（`00 §4` 的矩阵）。
3. **三个集合的消费者名单（`EMITTERS`/`CONSUMERS`，`check-ws-contract.mjs:40-58`）不改。**
   漏了会怎样：`replayTo` 落在 `event-bridge.ts`（已在 `EMITTERS`）、`case 'replay_done'` 落在 `useWorld.ts`（已在 `CONSUMERS`）——若实现时把回放逻辑搬进一个新文件，**必须同批把该文件加进 `EMITTERS`**，否则它逃出门禁。
4. **`docs/development/后端实现计划.md` 的回写按 §9 表（10 行落点）逐条落。**
   漏了会怎样：文档继续说「WS 断线重连未做」（实测已做）、说 `worldFrozen` 是模块级变量（实际是 router 闭包），下一个读者据此做错决定。
5. **§13 的两条 checkbox 按验收结果勾。**
   漏了会怎样：勾早了 = 假绿；`04 §11` 的手工验收是它们唯一判据。

---

## 4. 文件与副作用

| 文件 | 改动 | 量 |
|---|---|---|
| `docs/tools/12-工具注册与路由统一.md` | §6.2 第一张表加 `replay_done` 一行 | +1 行 |
| `docs/development/后端实现计划.md` | 见 §9 表的 10 行落点 | ~10 行 |
| `apps/server/test/gateway-replay.test.mjs` | **NEW** | ~180 行 |
| `docs/gateway/*.md` | 本批（`00`–`04`） | — |

**无源码副作用。** 测试文件是新文件，不触发既有测试的加载路径（`package.json` 的 `test` 脚本按目录收集：`node --test packages/shared/test/ apps/server/test/ …`）。

---

## 5. 落账

**不落账。** 文档与测试不写世界、不写 `history.db`。

---

## 6. WS / 前端反应

本文不改任何 WS 行为；它保证**实现之后**三处同批这件事可被机器证明。跨端矩阵见 `00 §4`。

---

## 7. 错误边界（不静默降级）

| 情形 | 行为 | 判据 |
|---|---|---|
| 实现只改了服务端、忘了前端 | `pnpm check:ws` 报 `dark`（`check-ws-contract.mjs:193+`） | 这是门禁存在的原因 |
| 实现只改了前端、忘了服务端 | 报 `ghost` | 同上 |
| 忘了 §6.2 加行 | 报 `undeclared` | 同上 |
| 有人把 `replay_done` 加进 `KNOWN_DARK`（`:81-94`）以「洗白」 | **评审拒绝**：`KNOWN_DARK` 的注释（`:78-80`）明写「the live defect, reported one by one」——它记录**待办**，不是豁免工具 | 本批 MUST NOT 修改该集合 |
| `docs/tools/12-工具注册与路由统一.md:596` 之后、`:598` 之前 | `replay_done` 行（§2.1） |

---

## 8. 代码落点

| 落点 | 内容 |
|---|---|
| `docs/tools/12-工具注册与路由统一.md:596` 之后 | `replay_done` 行（§2.1） |
| `docs/development/后端实现计划.md` | §9:519/539/544-548、§2.1:126、§13:606、§0:78 —— 见 §9 表 |
| `apps/server/test/gateway-replay.test.mjs` | 全文件 |

---

## 9. 上位文档回写清单（`00 §8` 的落点）

| # | 文件:行 | 现状文字 | 改成 |
|---|---|---|---|
| 1 | `docs/development/doc-07-AIRP黑客松作战计划.md:222` | 「进程守护…**WS 断线重连**、世界重载…（**进程守护已落地**；**WS 断线重连与世界重载未做**）」 | 「**WS 断线重连已落地**（`useWorld.ts:969-995` 的 1200ms 重连 + `socket_close` 解锁）；世界重载中**会话侧已落地**（`routes/world.ts:614,640-641`）、B6 的回放心跳见 `docs/gateway/`」 |
| 2 | `docs/development/后端实现计划.md:544-548`（T6.4） | 「现在只 `store.close()`：不停旧作家进程、`worldFrozen` 是 router 闭包里的模块级变量、切世界不重置」 | 三条已落地（`world.ts:614` 停角色、`:556/:610` 闭包且加载重置、`schema.ts:5,119` 双库 WAL）；**保留**「fail-loud」一条 |
| 3 | 同上 `:519` | 合成清单含 `layer_initializing` | 删 `layer_initializing`（同文档 `:490` 已明写不得新增；`docs/init/00:289` 只承认 `layer_initialized`/`layer_init_failed`） |
| 4 | 同上 `:519` | 合成清单含 `process_exit` | 删 `process_exit`（全仓零实现零消费；本批不做，见 `00 §8-5`） |
| 5 | 同上 `:539` | 「路由 `/ws?v=1`（或 `/api/sessions/:id/events?v=1`，若走多世界）」 | 取前者；删括号（AIRP 单世界单会话，无 session id 概念） |
| 6 | 同上 `:126`（§2.1 #3） | 「回放按 `agent_end` 切轮次」 | 「按 **`writer_idle` 帧**切轮次（`agent_end` 不到达帧面，见 `docs/gateway/00 §7` 裁决 C）」 |
| 7 | 同上 `:606` | 行锚 `lifecycle.ts:417,435` | `lifecycle.ts:423-440`（`scheduleWarmup`；`replay_entry` 发射在 `:435`） |
| 8 | 同上 `:607` | 未勾 + 「未做端到端手工验」 | 保持未勾，**加注**「并发复用已由 `lifecycle.startWriter:175-187` 保证；端到端手工验见 `docs/gateway/04 §11.2`」 |
| 9 | 同上 `:606` | 未勾（回放） | 若 `04 §11.1` 通过 → 勾选并改注「已落地：`event-bridge.replayWindow` + `replayTo` + `replay_done`，见 `docs/gateway/02`」 |
| 10 | 同上 `:78`（§0.3 表 `index.ts` 行） | 「仍缺**请求 id 关联**、断线恢复」 | 断线恢复已落地（`useWorld.ts:969-995`）；**请求 id 关联仍缺**（本批**不做**：命令回执链路的现状是「传输接受即 seam」，`useWorld.ts:538` 的原因注释；要做得同时改三处，收益低）→ 行尾加注并保留该缺项 |

> **注（`plan:517` — 无需回写）**：`docs/development/后端实现计划.md:517`（T6.1 表）**逐字已含**「（**未实现，T6.1 目标态**）」（锚点 `45269cf` 与当前 HEAD 双证），故**不回写**；真实冲突只是「目标名（`input`/`choice`/`character_open`/…）vs 现状名（`writer_prompt`/…）」——本批已冻结现状名（`00 §3.3`）。该行不再作为本表的回写项。

> **注意**：`docs/development/后端实现计划.md` 在本批写作期间**正被合并收口批次改动**（HEAD 从 `9af9c9b` 走到 `45269cf`）。实现本批时**先 `git status` 复核**该文件是否仍干净，行锚若已漂**按文字定位**（每行都以引文给出）。

---

## 10. 验收测试（可机械核验）

### 10.1 门禁

```bash
pnpm check:ws          # 期望：exit 0；summary {contract:28, emitted:28, consumed:25, findings:0}
pnpm check:docs        # 期望：clean（本批文档不在扫描面，但不得引入坏引用）
node --test apps/server/test/gateway-replay.test.mjs
```

**基线（实测，HEAD `45269cf`）**：`node tools/check-ws-contract.mjs --json` → `{contract: 27, emitted: 27, consumed: 24, findings: 0}`，exit 0。

### 10.2 源码扫描断言（补门禁盲区）

`gateway-replay.test.mjs` 里**四条读源码**断言（1–4）加**一条行为**断言（5，回放环过滤）（门禁是显式清单，新文件会逃逸，见 §7 最后一行）：

1. **`WS_COMMANDS` 与实现相等**：读 `apps/server/src/index.ts`，抽出所有 `data.type === '…'` 字面量（含 `data.type === 'writer_abort' || data.type === 'abort'` 的两个），断言其集合 === `new Set(WS_COMMANDS)`。
2. **前端拼了版本**：读 `apps/web/src/lib/airp-gateway.ts`，断言 `new WebSocket(` 的模板串里出现 `PROTOCOL_VERSION`（或字面 `?v=`）。
3. **协议锚无 Node 依赖**：读**新建**的 `packages/shared/src/protocol/ws.ts`，断言不含 `from 'node:` / `from "node:` / `from 'ws'`。
4. **硬禁令**：断言 `WS_COMMANDS` 与**新建**的 `packages/shared/src/protocol/ws.ts` 源码均不含 `get_state`/`set_state`/`state_update`/`watch_state`（`AGENTS.md:29`）。
5. **回放环过滤**：验证 `broadcast` 入环按 `REPLAY_FRAME_ALLOWLIST` 过滤（`02 §10` 第 6 条）。

### 10.3 非空性证明（防「假绿」）

每条断言必须能**证明自己会红**：

| 断言 | 反向实验 | 期望 |
|---|---|---|
| `pnpm check:ws` findings 0 | 临时删掉 `useWorld.ts` 的 `case 'replay_done'` | 报 `dark`，exit 1 |
| 同上 | 临时删掉 `docs/tools/12 §6.2` 的 `replay_done` 行 | 报 `undeclared`，exit 1 |
| 源码扫描 1 | 在 `index.ts` 临时加 `data.type === 'bogus_command'` | 测试红 |
| `replayWindow` 切轮（`02 §10.1`） | `found === turns + 1` 改 `found === turns` | 测试红 |
| `replayWindow` 尾部裁剪（裁决 I，`02 §10.1`） | fixture = 5 完整轮 + 尾部半轮；去掉「从尾回退到最后一个 `writer_idle`」那一步 | 输出末帧必须是 `writer_idle`；去掉裁剪 → 测试红 |
| `shouldTerminate`（`03 §10.2`） | sweep 先置 `false` 再判 | 测试红 |

`tools/check-ws-contract.test.mjs` 已有同类先例（该文件的 `compare()` 直接驱动、含四类检测的非空性用例），本批沿用其写法。

---

## 11. 手工验收（端到端，`docs/development/后端实现计划.md §13` 的两条判据）

### 11.1 「刷新浏览器不丢叙事」（`:606`）

```
1. pnpm dev
2. 浏览器进世界，跟作家交互 6 轮（每轮都写 chalk）
3. 观察：第 1 轮的湿墨/笔尖帧已不在屏幕上
4. F5 刷新
5. 断言：控制台（或临时日志）看到最近 5 轮的演出帧被重发，最后一帧是 replay_done
6. 断言：刷新后 3 秒内输入条可用（replaying 归位）
7. 断言：服务端日志中 `replay_done` 恰好出现一次
```

**降级判据**（真模型不可用时）：用 `pnpm probe:*` 的 mock provider 或跳过并按 `doc-11 §2.3` 纪律**显式打印 SKIPPED**，不得静默通过。

### 11.2 「连点两次加载世界只起一个 pi 进程」（`:607`）

```
1. 开两个浏览器标签（或连点两次「加载世界」）
2. 断言：`pgrep -fc "pi-rp.*cli.js"` 在同一 world 下 = 1
3. 断言：`<world>/.airpworld/sessions/*.jsonl` 里没有多出来的 preset_change 条目
```

> 这条**不需要本批改动**（`lifecycle.startWriter:175-187` 已保证复用，`00 §1.2` 证据 B 的 #8）。本批只是把它**验掉**并勾选。

### 11.3 版本协商（本批独有的高危验证）

```
1. 启动服务端（含门禁）
2. curl 一个不带 ?v= 的 WS 握手（或用 node -e + ws）：
   → 断言 close code === 4400，reason === 'protocol version mismatch'
3. 浏览器正常打开前端：
   → 断言连上（无 4400），且 pnpm dev 控制台无 'protocol version mismatch'
4. 故意把 airp-gateway.ts 的 ?v= 去掉 → 断言 closeKind === 'protocol' 且【不再重连】
5. 故意把服务端 intervalMs 设成 1000 并 suspend 浏览器标签（不关）→ 断言客户端在 2 个 interval 内
   被 terminate，且前端按 1006（closeKind null）重连
```

### 11.4 STT 自身回归 + `noServer` 连带面（`03 §8`）

**STT 自身回归**（只证明 STT 未被动坏，**不**证明 `noServer` 改造）：

```
node --test apps/server/test/stt-stream.test.mjs   # 必须仍绿
```

> 该测试**不经** `index.ts` 的 upgrade 钩子——它自建 server 直调 `handleSttStream`（`stt-stream.test.mjs:54-55`），故它**证明不了** `noServer` 改造的连带面（评审 M6：不可能变红的测试当了最高风险改动的安全网）。

**真能红的端到端验收**（覆盖 upgrade 钩子 + STT 归属）：

```
1. pnpm dev（`index.ts` 已切 `{noServer:true}` + upgrade 钩子）
2. 打开任意世界的语音输入
3. 断言：连上真实 `/ws/stt`（**不带** `?v=`），首帧为 {type:'ready'}
4. 反向实验：把 upgrade 钩子的 STT 归属判断写错（把 `/ws/stt` 也交给版本门禁）→ 断言步骤 3 变红
   （被 4400 关闭，收不到 ready）
```

---

## 12. 发现的冲突 / 需要修订的上位文档

见 §9 表（**10 行落点**）、对应 `00 §8` 的登记。**本文不自己发明新的冲突**——若实现时发现新的，写进本文件的下一版并 `hub send` 给主 agent。

---

## 13. 仍未知待拍板

1. **（已裁定）`replay_done` 载荷带 `turns`**——见裁决 H（`00 §7`）：冻结为 `{ type, turns, timestamp }`，`00 §3.2` 与本文 §2.1 已同批统一。本文按「带」写（可核验）。
2. **`pnpm check:ws` 的基线数字要不要写进门禁源码**（例如断言 `contract === 28`）？倾向**不要**：契约表会随批次增行，写死数字会让每个后续批次都改门禁。基线只写在本批验收里。
3. **`gateway-replay.test.mjs` 是否该拆成 `gateway-protocol.test.mjs` + `gateway-lifecycle.test.mjs`**？倾向**不拆**：三篇的纯函数都不大，一个文件共享一套假件（假 timer、假 sink）更省；若评审要求按篇拆，按 `01`/`02`/`03` 三个 `describe` 即可。
4. **手工验收 11.1 的自动化边界**：真模型轮次贵且不稳（`docs/agent/Agent前端接线.md` 的超时诊断记录过一次 65s 未 settled）。是否值得做成 `pnpm probe:replay`（mock provider + 假 WS 客户端）？倾向**登记为后续**——本批单测已覆盖纯函数，端到端在演示前跑一次即可。**待拍板。**
5. **`docs/gateway/` 加进 `tools/check-hooks-docs.mjs` 的 `CITE_DIRS`** —— **已完成**（主 agent，修订 1）：check 4 现在扫描本目录的 `file:line` 引用。已为「本批新建」的路径加 `新建`/`NEW` 豁免标记，门禁为 0 findings。**本项已落地。**
