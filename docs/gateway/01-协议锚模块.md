# docs/gateway/01 — 协议锚模块（`packages/shared/src/protocol/ws.ts`）

> 状态：**设计稿（2026-09-14）**，待评审。属 B6「网关与会话域」（见 `docs/gateway/00-共同上下文.md`，下称 `00`）。
> 边界：新建 `packages/shared/src/protocol/ws.ts` 一个文件 + `packages/shared/src/index.ts` 一行 barrel 追加。**不碰** `apps/server/`、`apps/web/`。
> 依据：`00 §3.1`（冻结常量）、`AGENTS.md:53`（新增帧三处同批）、`AGENTS.md:29`（状态命名空间硬禁令）。

---

## 1. 一句话定位

**把今天散在 `index.ts` 的 `if` 链字面量、`event-bridge` 的帧名、以及未来要加的关闭码收成一份「只有形状与词汇」的常量模块——它不认识 WebSocket、不做 I/O、不持状态，前端与服务端都能安全 import。**

它是 `AGENTS.md:53`「新增帧必须同时更新三处」里那句「契约文档」的**代码侧锚点**：门禁（`tools/check-ws-contract.mjs`）比对的是文档与源码，而本模块是源码侧「这些名字是被声明过的」的唯一出处。

---

## 2. 签名与参数

### 2.1 文件头（`packages/shared/src/protocol/ws.ts`，NEW）

```ts
/**
 * AIRP WebSocket vocabulary — the ONE place frame names, close codes and the
 * protocol version are written down.
 *
 * Boundaries (docs/gateway/00 §1, decision R0):
 *   - No I/O, no state, no `ws` import, no `node:*` import. The browser imports
 *     this file too; anything Node-only here is a build break in `@airp/web`.
 *   - Pure shape only. The WS shell lives in `apps/server/src/index.ts`; the
 *     session domain is `apps/server/src/engine/lifecycle.ts`.
 *   - Frame names and event `type`s are DIFFERENT namespaces (docs/tools/00
 *     §5.3). Nothing here may be added to `schemas/events.ts`'s closed fifteen.
 */
```

### 2.2 导出（全部 `NEW`）

```ts
/** WS query parameter `?v=` — a mismatch closes with 4400 (docs/gateway/03 §3). */
export const PROTOCOL_VERSION = '1' as const;

/**
 * One code, one meaning. `UNAUTHORIZED` is a PLACEHOLDER: AIRP has no auth
 * today, but the code's slot is frozen so adding auth never changes the
 * protocol (docs/development/后端实现计划.md §2.3).
 */
export const WS_CLOSE = Object.freeze({
  PROTOCOL_MISMATCH: 4400,
  UNAUTHORIZED: 4401, // placeholder — no auth implementation in this batch
  NOT_FOUND: 4404,
  GOING_AWAY: 1001,
} as const);

export type CloseKind = 'protocol' | 'auth' | 'session';

/**
 * The ONLY classifier for a close code. `null` means "not a protocol-level
 * verdict" — the caller treats it as a network blip and may retry. The browser
 * clamps illegal codes to 1005/1006, which must land here as `null` too:
 * sniffing `reason` strings is forbidden (docs/development/后端实现计划.md §2.1 #4).
 */
export function closeKind(code: number | undefined | null): CloseKind | null;

/** Client → server command names. TODAY'S names, not a future rename
 *  (docs/gateway/00 §3.3). No `get_state`/`set_state`/`watch_state` — banned by
 *  AGENTS.md §2. */
export const WS_COMMANDS = Object.freeze([
  'writer_prompt', 'airp_init', 'writer_abort', 'abort',
  'character_start', 'character_prompt', 'character_abort', 'character_stop',
] as const);

/** Frames the GATEWAY synthesises (not passthrough from the engine). Only
 *  `replay_done` in this batch (docs/gateway/00 §3.2); everything else in
 *  docs/tools/12 §6.2 is emitted by `event-bridge` from engine events or by
 *  the watcher/routes. */
export const GATEWAY_EVENTS = Object.freeze(['replay_done'] as const);

/** Complete turns replayed on connect (docs/development/后端实现计划.md §9:126/:606).
 *  At most N COMPLETE turns — the trailing half turn is trimmed (00 §7 裁决 I). */
export const REPLAY_TURNS = 5;

/** Frames allowed into the replay ring (00 §3.1, 裁决 D). An ALLOWLIST, not a
 *  denylist: `*_delta`, ritual frames and meta frames never enter. */
export const REPLAY_FRAME_ALLOWLIST = Object.freeze([
  'chalk_writing', 'chalk_landed', 'card_writing', 'writer_message', 'writer_idle',
] as const);

/** Upper bound of the replay ring; entries are filtered by the allowlist above
 *  BEFORE they are pushed (00 §3.1). */
export const REPLAY_BUFFER_KEEP = 4000;
```

> `REPLAY_ID` **不导出**（`00 §9-6`，为避免空壳常量）。`REPLAY_FRAME_ALLOWLIST` / `REPLAY_BUFFER_KEEP` 是 `00 §3.1` 修订 1 新增的协议锚导出（裁决 D / `02 §3`），本表已一并覆盖。

---

## 3. 行为契约逐步

每步写「漏了会怎样」。

1. **`closeKind(4400) === 'protocol'`**，`closeKind(4401) === 'auth'`，`closeKind(4404) === 'session'`，`closeKind(1001) === 'session'`。
   漏了会怎样：前端只能嗅探 `CloseEvent.reason` 字符串，服务端改一个字（翻译、大小写）就静默改变重连策略。rivet 踩过 4400/4401 共用一码的坑（`ws.mjs:60-123`）。
2. **`closeKind(undefined) === null`、`closeKind(1006) === null`、`closeKind(9999) === null`、`closeKind(-1) === null`**。
   漏了会怎样：网络掉线（浏览器给 1006）被误分类成 `'session'` → 前端以为「这个会话没了」而**不重连**。
3. **`closeKind` 是纯函数、无副作用、不读全局**。
   漏了会怎样：它会在浏览器与服务端各被调用一次却得到不同结果（若读 env）。
4. **`WS_COMMANDS` 恰好是 8 项，且与 `apps/server/src/index.ts` 的 `data.type === '…'` 分支集合**（`:206/227/253/258/279/297/302`；`abort` 与 `writer_abort` 同分支，`:253`）**逐项相等**。
   漏了会怎样：常量声明与实现漂移，常量变成谎言。**核验方式**见 `04 §10.2`（一条扫描测试）。
5. **`GATEWAY_EVENTS` 只含 `replay_done`**。
   漏了会怎样：把 `chalk_landed` 之类也列进来，会让「谁该保证顺序」这个问题重新变得不可判定（§2.1 #2 的原始动机）。
6. **模块顶层零 `import`**（除类型）。
   漏了会怎样：`@airp/web` 的 Vite 构建去解析 `node:fs` 而失败——构建期才发现，且报错点在 `index.ts` barrel，不指向本文件。

---

## 4. 文件与副作用

| 文件 | 改动 |
|---|---|
| `packages/shared/src/protocol/ws.ts` | **NEW**，约 85 行（含注释；含 `REPLAY_FRAME_ALLOWLIST` / `REPLAY_BUFFER_KEEP`） |
| `packages/shared/src/index.ts` | 追加一行 barrel 导出（与 `:1-14` 现有的 `export * from './schemas/…'` 同族） |

**无其它副作用**：不写文件、不读 env、不注册监听、不改 `package.json`（`@airp/shared` 的 exports map 已有 `.` 入口，`packages/shared/package.json:7-10`，新文件从根 barrel 出去即可；**不新增子路径导出**——`./protocol` 子路径会多一条需要维护的公开面，而唯一的消费者是仓库内两个 app，走根 barrel 足够）。

---

## 5. 落账

**不落账。** 本模块是常量与纯函数，不产生事件、不写 `history.db`/`canvas.db`。理由（`00 §3.6` 第 4 条）：`AGENTS.md:13` 区定「写世界的动作必须经过动作层并落事件」——本模块不写世界。

---

## 6. WS / 前端反应

- 服务端：`apps/server/src/index.ts` 的 upgrade 门禁（`03 §3`）用 `PROTOCOL_VERSION` + `WS_CLOSE.PROTOCOL_MISMATCH`；心跳用 `WS_CLOSE.GOING_AWAY`（`03 §4`）。
- 前端：`apps/web/src/lib/airp-gateway.ts` 的 `openAirpSocket` 用 `PROTOCOL_VERSION` 拼 `/ws?v=1`（`00 §3.4`；当前裸 `/ws` 在 `:156`）；`useWorld.ts` 的 `ws.onclose`（`:986`）用 `closeKind(ev.code)` 区分「重连」与「不重连」。
- **本文件本身不发帧。** 新帧 `replay_done` 的发射在 `02`。

---

## 7. 错误边界（不静默降级）

| 情形 | 行为 |
|---|---|
| 常量被误改（例如 `PROTOCOL_VERSION` 改成 `'2'` 而前端未跟） | **门禁红**：`04 §10.2` 的测试断言前端拼出的 query 与服务端期望相等 |
| 有人往 `WS_COMMANDS` 加 `get_state` | **测试红**：`04 §10.2` 断言该数组不含硬禁令的四个名字（`AGENTS.md:29`） |
| 有人往 `GATEWAY_EVENTS` 加未在 `docs/tools/12 §6.2` 声明的帧 | **门禁红**：`pnpm check:ws` 的 `check 4 missing/3 undeclared`（`check-ws-contract.mjs:193+`）只有在**发射点**出现该字面量时才报——本模块**不是发射点**（`EMITTERS` 不含它，`:40-46`），故此处需靠 `04 §10.2` 的测试补位 |
| 浏览器 bundle 里出现本模块 | **这是期望行为**，不是错误 |

---

## 8. 代码落点

| 落点 | 内容 |
|---|---|
| `packages/shared/src/protocol/ws.ts`（NEW） | 上述全部导出 |
| `packages/shared/src/index.ts:1-14` 一带 | `export * from './protocol/ws.js';` |

`protocol/` 目录本身也是 `docs/hooks/AUDIT-doc-22体检.md:94` 记录的那个「当前是空目录」的目标（`00 §8-6`）。

---

## 9. 与现状差异

| 今天 | 变成 |
|---|---|
| 协议版本无处声明（`/ws` 无 `?v=`，`airp-gateway.ts:156`） | `PROTOCOL_VERSION` + 协商（`03`） |
| 关闭码无处声明；浏览器 `onclose` 只看 `stopped`（`useWorld.ts:986-995`） | `WS_CLOSE` + `closeKind` 驱动重连决策 |
| 命令名是 `index.ts` 的裸字面量（`:206/227/253/258/279/297/302`） | 同一份常量**声明**它们（**不改名**，`00 §3.3`） |
| `chalk_landed`/`world_frozen` 之类「网关合成」与「引擎透传」只靠文档区分 | `GATEWAY_EVENTS` 显式列出合成面（本批只有 `replay_done`） |
| `packages/shared/src/protocol/` 不存在 | 存在 |

---

## 10. 验收测试（可机械核验）

**新建**的 `apps/server/test/gateway-replay.test.mjs`（与 `02`/`03` 共用，见 `04`）：

1. `closeKind` 全值域表驱动：`4400→'protocol'`、`4401→'auth'`、`4404→'session'`、`1001→'session'`、`undefined/0/1005/1006/9999→null`。
2. `WS_COMMANDS` 与 `index.ts` 的分支集合相等（**扫描源码**，见 `04 §10.2`）。
3. `WS_COMMANDS` 不含 `get_state`/`set_state`/`state_update`/`watch_state`。
4. `packages/shared/src/protocol/ws.ts` 的源码**不含** `from 'node:`、`from "node:`、`from 'ws'`。
5. `PROTOCOL_VERSION === '1'`。

**非空性**：把 `closeKind` 的未知分支改成 `'session'` → 测试 1 必须红（`04 §10.3`）。

---
## 11. 发现的冲突 / 需要修订的上位文档

| # | 位置 | 冲突 | 处置 |
|---|---|---|---|
| 1 | `docs/development/后端实现计划.md:517` | `WS_COMMANDS` 列未来名（`input/choice/character_open/…`），与现状实现不同 | **无需回写**：该行**逐字已含**「（**未实现，T6.1 目标态**）」；真实冲突只是「目标名 vs 现状名」。见 `00 §8-3`，取消本批的改名 |
| 2 | `docs/development/后端实现计划.md:519` | 「网关合成」清单含 `layer_initializing`（同文档 `:490` 明写不得新增）与 `process_exit`（零实现） | 见 `00 §8-4/§8-5` |
| 3 | `docs/hooks/AUDIT-doc-22体检.md:94` | 「`packages/shared/src/protocol/` 当前是空目录」 | R0 落地后自然失效；其结论（B2 状态块不属于协议层）不回改 |
| 4 | `docs/tools/12-工具注册与路由统一.md` §6.2 表 | 表里没有「哪些是网关合成」的列，只有「谁发」的文字 | **不回改**（本批只加 `replay_done` 一行，`04 §3`）；「合成 vs 透传」的权威在 `GATEWAY_EVENTS` 常量 |

---

## 12. 仍未知待拍板

1. **`REPLAY_ID` 是否整个不导出**（`00 §9-6`）：倾向不导出；若将来 warmup 需要 `get_entries` 的魔法 id（`02 §12`），再导出。
2. **`WS_COMMANDS` 要不要也覆盖前端内部广播的 `airp:*` DOM 事件名**（`airp:world-event` 等）？倾向**不要**——那些不是 WS 帧，`docs/tools/12 §6.6` 已定它们属前端内部契约。
3. **`abort`（旧名）的下线时机**：`index.ts:253` 同时收 `writer_abort` 与 `abort`，前端 `useWorld.ts:264-265` 用一个拼接技巧同时支持两者（`WRITER_ABORT_TYPE`/`LEGACY_ABORT_TYPE`）。本批**都保留**；何时删旧名待拍板（删它要跨端同批 + 契约回写）。
