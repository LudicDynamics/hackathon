import React from "react";
import { LauncherShot } from "./Launcher";

/** Where the world launcher appears: the port of the app's glass-brick WorldLauncher, landing on Fogwharf. */
export const LauncherSlot: React.FC = () => <LauncherShot target="wuwu" />;
